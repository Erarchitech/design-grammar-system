---
phase: 32.1-cross-platform-identity-and-mapping-dg-id
plan: 07
type: execute
wave: 3
depends_on: [32.1-02, 32.1-03, 32.1-04, 32.1-05, 32.1-06]
files_modified:
  - spec/DATABASE.md
  - cypher_template.txt
  - training/dataset_schema.json
  - CLAUDE.md
  - .github/copilot-instructions.md
  - README.md
  - ontology/dg-shapes.ttl
  - data-service/dg_context.py
autonomous: true
requirements: [DGID-01, DGID-02, DGID-04]

must_haves:
  truths:
    - "spec/DATABASE.md documents the identity registry: the dgId property on Computgraph nodes, the Representation and SharedProperty node shapes, and the HAS_REPRESENTATION / HAS_SHARED_PROPERTY relationships"
    - "CLAUDE.md's schema tables include Representation and SharedProperty rows and the two new relationships — the standing schema-change propagation rule is satisfied, not skipped"
    - "ontology/dg-shapes.ttl carries SHACL node shapes for Representation and SharedProperty consistent with the existing shape style"
    - "dg_context.py's Cypher validator allow-lists accept the new labels/relationships/properties so generated Cypher touching the identity registry is not falsely rejected"
  artifacts:
    - "spec/DATABASE.md — new identity-registry section cross-referencing spec/DG-ID.md"
    - "CLAUDE.md, .github/copilot-instructions.md, README.md — schema tables/notes updated"
    - "cypher_template.txt + training/dataset_schema.json — identity labels documented per the Phase 14 document-only precedent"
    - "ontology/dg-shapes.ttl — RepresentationShape + SharedPropertyShape"
    - "data-service/dg_context.py — additive allow-list entries"
  key_links:
    - "every surface documents the SAME shape Plans 03/04 implemented (read their SUMMARYs first) — property names, label strings, and relationship types must match the live Cypher byte-for-byte"
    - "dg_context.py changes are strictly additive appends to existing allow-list constants — Phase 29 gap-closure plans (29-06..08) touch the same file, so no restructuring"
---

<objective>
Execute the schema-change propagation checklist for the Phase 32.1 graph schema additions: the `dgId` property on Computgraph nodes, the `Representation` and `SharedProperty` node labels, and the `HAS_REPRESENTATION` / `HAS_SHARED_PROPERTY` relationships. Per the standing CLAUDE.md rule, adding these IS a schema change and every propagation surface must be updated: `spec/DATABASE.md`, `cypher_template.txt`, `training/dataset_schema.json`, CLAUDE.md schema tables, `.github/copilot-instructions.md`, `README.md`, `ontology/dg-shapes.ttl` (SHACL), and the Phase 29 catalog surface (`dg_context.py` validator allow-lists).

Purpose: make the registry shape (DGID-02), the per-entity dgId (DGID-01), and the shared-property records (DGID-04) visible to every schema consumer — LLM prompts, SHACL validation, documentation, and future connectors — so the identity spine cannot silently drift from its documentation.
Output: 8 propagation-surface updates, grep-gated and regression-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-CONTEXT.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-RESEARCH.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-03-SUMMARY.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-04-SUMMARY.md
@spec/DG-ID.md
</context>

## Artifacts this phase produces (created by THIS plan)

New documentation/schema symbols (no runtime code symbols):
- `spec/DATABASE.md` section "Identity registry (Phase 32.1)" — documents node labels `Representation`, `SharedProperty`; relationships `HAS_REPRESENTATION`, `HAS_SHARED_PROPERTY`; the `dgId` property on Computgraph entity nodes
- SHACL shapes `dgs:RepresentationShape` and `dgs:SharedPropertyShape` in `ontology/dg-shapes.ttl` (prefix per the file's existing convention)
- New allow-list entries in `data-service/dg_context.py` validator constants: labels `Representation`, `SharedProperty`; relationships `HAS_REPRESENTATION`, `HAS_SHARED_PROPERTY`; properties `dgId`, `nativeId`, `nativeIdKind`, `platform`, `connector`, `boundAt`, `propertyName`, `writtenAt`
- New rows in the CLAUDE.md / copilot-instructions schema tables for the two labels and two relationships
- The recorded decision that `dgId` gets NO OWL annotation property in V7 for this phase (runtime/persistence identity, not ontological vocabulary — rationale in spec/DATABASE.md + cross-ref to the ADR)

The labels/relationships/properties themselves were created at runtime by Plans 03/04/05 — this plan makes them visible on every schema surface.

<tasks>

<task type="auto">
  <name>Task 1: spec/DATABASE.md + cypher_template.txt + training/dataset_schema.json</name>
  <files>spec/DATABASE.md, cypher_template.txt, training/dataset_schema.json</files>
  <read_first>
    - .planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-03-SUMMARY.md and 32.1-04-SUMMARY.md (the FINAL Representation/SharedProperty Cypher shapes as implemented — property names and MERGE keys must be transcribed exactly, not from memory)
    - spec/DATABASE.md (section structure, how ValidGraph/DesignState document-only nodes are described, the v3→v4 historical-note style — match the register; find where Computgraph will slot)
    - cypher_template.txt (the v4 template shape — note how DesignState was removed/documented as document-only per D-03/D-14; the identity note follows that precedent)
    - training/dataset_schema.json (the JSON structure — where labels/relationships are enumerated; how document-only nodes were handled in Phase 14)
    - spec/DG-ID.md (Plan 02 — the normative source to cross-reference, never restate divergently)
  </read_first>
  <action>
    **spec/DATABASE.md:** add an "Identity registry (Phase 32.1)" section documenting: (a) the `dgId` property (format `dg:` + 16 uppercase hex, deterministic from `project|definitionId|cgId`) carried by Computgraph entity nodes; (b) a `Representation` node table — `graph:'Computgraph'`, `project`, `platform` (Grasshopper|Revit|IFC|Speckle), `nativeIdKind` (InstanceGuid|UniqueId|GlobalId|ApplicationId), `nativeId`, `connector`, `boundAt` — merge key `(nativeId, platform, project)`; (c) a `SharedProperty` node table — `graph:'Computgraph'`, `project`, `dgId`, `propertyName`, `value`, `platform`, `connector`, `writtenAt` — merge key `(dgId, propertyName, project)`; (d) relationships `HAS_REPRESENTATION` (entity→Representation) and `HAS_SHARED_PROPERTY` (entity→SharedProperty); (e) a one-line recorded decision: no OWL annotation property for `dgId` in V7 — it is runtime/persistence identity, not ontological vocabulary; rationale in the ADR (`DG_OBSIDIAN/knowledge/decisions/DG ID cross-platform identity scheme.md`); (f) cross-reference `spec/DG-ID.md` as the normative identity spec.

    **cypher_template.txt:** append a brief comment block (following the document-only DesignState precedent, STATE.md D-03/D-14) stating that identity-registry nodes (`Representation`, `SharedProperty`, the `dgId` property, `HAS_REPRESENTATION`/`HAS_SHARED_PROPERTY`) live under `graph:'Computgraph'`, are written ONLY by the data-service identity API, and are NOT part of rule ingestion — the template's MERGE blocks must not create them. Do not alter any existing MERGE block.

    **training/dataset_schema.json:** following the Phase 14 document-only precedent, add the two labels, two relationships, and the `dgId` property to the schema's enumeration/documentation structures (match the existing JSON shape exactly — read first; if the file enumerates only ingestion-scope labels, add them to whatever "documented but not ingested" structure Phase 14 used for DesignState/Run, or the closest equivalent). The file must remain valid JSON.
  </action>
  <verify>
    <automated>python -c "import json; json.load(open('training/dataset_schema.json')); print('JSON_OK')" && grep -q "HAS_REPRESENTATION" spec/DATABASE.md && grep -q "SharedProperty" spec/DATABASE.md && grep -qi "Representation" cypher_template.txt && echo PROP1_OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `JSON_OK` and `PROP1_OK`
    - `spec/DATABASE.md` documents both node tables with the exact property names from the Plan 03/04 SUMMARYs, both relationships, the dgId format, and the no-OWL-annotation decision with ADR cross-reference
    - `cypher_template.txt` gains only an appended comment block — `git diff` shows no changes to existing MERGE blocks
    - `training/dataset_schema.json` parses as valid JSON and mentions `Representation`, `SharedProperty`, and `dgId`
    - Property names in all three files match the implemented Cypher byte-for-byte (`nativeIdKind` not `native_id_kind` — Neo4j properties are camelCase)
  </acceptance_criteria>
  <done>The three core schema surfaces document the identity registry exactly as implemented, with cypher_template.txt guarding rule-ingestion scope per the document-only precedent.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md + .github/copilot-instructions.md + README.md schema tables</name>
  <files>CLAUDE.md, .github/copilot-instructions.md, README.md</files>
  <read_first>
    - CLAUDE.md (the "Graph Schema v4" section: Node Labels table, ValidGraph Labels table, Relationships list — the row format to extend; also the Schema Change Propagation paragraph which this plan is executing)
    - .github/copilot-instructions.md (its schema section structure — Phase 20 rewrote it as a full document; find the equivalent tables/lists)
    - README.md (the "Data model conventions" area noted in its structure — where a brief identity note belongs)
    - .planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-03-SUMMARY.md + 32.1-04-SUMMARY.md (the implemented shapes)
  </read_first>
  <action>
    **CLAUDE.md:** in the Graph Schema v4 section add two rows to the node-label tables (place them in a small new "Identity registry (Computgraph)" subsection or extend the existing tables — match local structure): `Representation` | Computgraph | `nativeId`+`platform`+`project` (merge key) | display `nativeId`; `SharedProperty` | Computgraph | `dgId`+`propertyName`+`project` | display `propertyName`. Extend the Relationships line with `HAS_REPRESENTATION` (entity→Representation), `HAS_SHARED_PROPERTY` (entity→SharedProperty). Add one sentence noting every tagged Computgraph entity carries a `dgId` property (see `spec/DG-ID.md`). Use scoped edits only — do not rewrite unrelated sections.

    **.github/copilot-instructions.md:** mirror the same additions in its schema section, matching its local format.

    **README.md:** add a brief identity note to the data-model conventions area: Computgraph entities carry a deterministic platform-neutral `dgId`; platform representations and cross-platform shared properties are registry nodes managed by the data-service `/identity/*` API; normative spec at `spec/DG-ID.md`.
  </action>
  <verify>
    <automated>grep -q "Representation" CLAUDE.md && grep -q "SharedProperty" CLAUDE.md && grep -q "HAS_REPRESENTATION" CLAUDE.md && grep -q "Representation" .github/copilot-instructions.md && grep -qi "dgId" README.md && echo PROP2_OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `PROP2_OK`
    - CLAUDE.md's schema section lists both labels with graph partition `Computgraph`, their merge-key properties, and both relationships
    - copilot-instructions.md mirrors the additions in its own format
    - README.md references `spec/DG-ID.md` and the `/identity/` API
    - `git diff` on all three files shows only additive scoped hunks (no unrelated section rewrites)
  </acceptance_criteria>
  <done>The three assistant/developer-facing documentation surfaces carry the identity-registry schema rows, satisfying the CLAUDE.md standing propagation rule for this phase's schema change.</done>
</task>

<task type="auto">
  <name>Task 3: SHACL shapes + dg_context.py validator allow-lists</name>
  <files>ontology/dg-shapes.ttl, data-service/dg_context.py</files>
  <read_first>
    - spec/RULE-PARTITION-POLICY.md (the standing rule: consult before adding a new SHACL shape — confirm data-integrity shapes for registry nodes are SHACL-side, and note if the partition line needs an update)
    - ontology/dg-shapes.ttl (existing shape style: prefixes, sh:NodeShape structure, sh:targetClass/targetNode conventions, how enums are constrained via sh:in, severity conventions — replicate exactly)
    - data-service/dg_context.py (the `validate_cypher()` allow-list constants — labels, relationship types, property names; note Phase 29-06 gap-closure also touches this file, so changes must be strictly additive appends, no restructuring)
    - data-service/tests/test_dg_context.py (the tests guarding the validator — must stay green)
    - .planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-03-SUMMARY.md + 32.1-04-SUMMARY.md (the implemented property names)
  </read_first>
  <action>
    **ontology/dg-shapes.ttl:** after consulting RULE-PARTITION-POLICY.md, add two node shapes following the file's existing style: `RepresentationShape` — constrains `platform` to the four-value set (Grasshopper, Revit, IFC, Speckle) via `sh:in`, `nativeIdKind` to the four-value set (InstanceGuid, UniqueId, GlobalId, ApplicationId), requires `nativeId`, `project`, `connector` (min count 1, string datatype); `SharedPropertyShape` — requires `dgId` matching the pattern for `dg:` + 16 uppercase hex (`sh:pattern`), `propertyName`, `project`, `platform`, `writtenAt` present. Match the file's prefix/naming/severity conventions exactly. If RULE-PARTITION-POLICY.md indicates the partition table should note the new shapes, update it per its own instructions (report in SUMMARY if so).

    **data-service/dg_context.py:** append to the existing validator allow-list constants (labels, relationship types, property names — locate the exact constant names by reading the file): labels `Representation`, `SharedProperty`; relationships `HAS_REPRESENTATION`, `HAS_SHARED_PROPERTY`; properties `dgId`, `nativeId`, `nativeIdKind`, `platform`, `connector`, `boundAt`, `propertyName`, `writtenAt`. Strictly additive — append entries, do not reorder, rename, or restructure any constant (Phase 29-06 gap closure owns convergence work in this file). Run the dg_context test suite to confirm zero regression.
  </action>
  <verify>
    <automated>cd data-service && LLM_MASTER_SECRET=test-secret python -m pytest tests/test_dg_context.py -q && grep -q "RepresentationShape" ../ontology/dg-shapes.ttl && grep -q "SharedPropertyShape" ../ontology/dg-shapes.ttl && grep -q "HAS_REPRESENTATION" dg_context.py && echo PROP3_OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints `PROP3_OK`
    - `ontology/dg-shapes.ttl` contains `RepresentationShape` with an `sh:in` platform constraint listing Grasshopper, Revit, IFC, Speckle, and `SharedPropertyShape` with an `sh:pattern` for the dgId format
    - The ttl file still parses as Turtle (best-effort: `python -c "import rdflib; rdflib.Graph().parse('ontology/dg-shapes.ttl', format='turtle')"` if rdflib is available locally, otherwise run the parse inside the dg-reasoner container or fall back to the grep gates)
    - `dg_context.py` allow-lists include both labels, both relationships, and `dgId` — as additive appends only (`git diff` shows no removed/reordered lines in the constants)
    - `python -m pytest data-service/tests/test_dg_context.py -q` exits 0 (validator regression gate)
  </acceptance_criteria>
  <done>SHACL shapes mirror the registry's data-integrity constraints per the partition policy, and the Phase 29 Cypher-validator catalog accepts the identity labels — the last two propagation surfaces are in sync with the implemented schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-generated Cypher → validate_cypher allow-lists | Widening the allow-lists lets generated Cypher reference identity labels; the validator remains the gate before execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-07a | Tampering | schema/documentation drift | medium | mitigate | This plan IS the mitigation: all 8 surfaces transcribe the implemented shape from the Plan 03/04 SUMMARYs, grep-gated per surface, so no consumer works from a stale or divergent schema. |
| T-32.1-07b | Tampering | over-widened Cypher validator | low | mitigate | Allow-list additions are the exact label/relationship/property set Plans 03/04 implemented — nothing speculative; the existing test_dg_context.py suite gates regressions, and parameterization rules in the validator are unchanged. |
| T-32.1-07c | Elevation of Privilege | SHACL shapes constraining registry integrity | low | mitigate | RepresentationShape/SharedPropertyShape encode the enum and format constraints so a future SHACL pass over the Computgraph partition detects malformed registry rows (wrong platform value, malformed dgId) rather than trusting them. |

No package-manager installs in this plan — no T-32.1-SC legitimacy gate required.
</threat_model>

<verification>
- All three task verify commands print their OK markers and exit 0
- `python -m pytest data-service/tests/test_dg_context.py -q` exits 0
- `python -c "import json; json.load(open('training/dataset_schema.json'))"` exits 0
- Property/label/relationship strings identical across all 8 surfaces and the Plan 03/04 implementations
- Git diffs on cypher_template.txt and dg_context.py are strictly additive
</verification>

<success_criteria>
Every schema-propagation surface named by the standing CLAUDE.md rule — spec/DATABASE.md, cypher_template.txt, training/dataset_schema.json, CLAUDE.md tables, copilot-instructions, README, dg-shapes.ttl, and the Phase 29 validator catalog — documents the dgId property, Representation/SharedProperty labels, and HAS_REPRESENTATION/HAS_SHARED_PROPERTY relationships exactly as implemented, with the no-OWL-annotation decision recorded — closing the schema-change obligation for DGID-01/02/04.
</success_criteria>

<output>
Create `.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-07-SUMMARY.md` when done. Note whether RULE-PARTITION-POLICY.md needed a partition-table update.
</output>
