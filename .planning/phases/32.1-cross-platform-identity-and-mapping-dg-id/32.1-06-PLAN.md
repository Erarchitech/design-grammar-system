---
phase: 32.1-cross-platform-identity-and-mapping-dg-id
plan: 06
type: execute
wave: 2
depends_on: [32.1-01, 32.1-02]
files_modified:
  - DG/src/DG.Core/Models/ObjState.cs
  - DG/src/DG.Core/Serialization/DesignStatePayloadV2Serializer.cs
  - DG/tests/DG.Tests/Identity/ObjStateDgIdTests.cs
autonomous: true
requirements: [DGID-03]

must_haves:
  truths:
    - "An ObjState carrying a DgId serializes into statePayloadJson v2 with a camelCase 'dgId' key and deserializes back with the same value — the DesignState references its member object by dgId"
    - "A pre-32.1 statePayloadJson v2 payload with NO dgId key still deserializes without error, leaving DgId null (v1/v2 reader backward compatibility)"
    - "The dgId set on an ObjState equals the dgId minted by DgIdMintingService for the same (project, definitionId, entityKey) — the identity in the Design State IS the Computgraph identity, so a GH wall and its Revit counterpart bound to that dgId resolve to one identity within the Design State"
    - "Every pre-existing DesignStatePayloadV2SerializerTests fact stays green with zero changes to existing test methods"
  artifacts:
    - "DG/src/DG.Core/Models/ObjState.cs — additive nullable string? DgId property"
    - "DG/src/DG.Core/Serialization/DesignStatePayloadV2Serializer.cs — additive DgId on ObjStateDto, mapped in ToDto/FromDto only"
    - "DG/tests/DG.Tests/Identity/ObjStateDgIdTests.cs — round-trip, backward-compat, and minting-parity facts"
  key_links:
    - "ObjStateDto gains ONE nullable property; ValidateDesignState/ValidateDeserialized/ValidateParameter are NOT restructured (Pitfall 3 — restructuring breaks the v1/v2 heuristic and data-service statePayloadJson readers)"
    - "System.Text.Json's default missing-property behavior maps an absent dgId key to null — that default IS the backward-compat mechanism; no version bump, no heuristic change"
---

<objective>
Wire the DG ID into the v7.0 Design State pipeline: `ObjState` gains an additive nullable `DgId`, and `DesignStatePayloadV2Serializer` round-trips it through `statePayloadJson v2` — strictly additive, so every existing v1/v2 payload reader (C# and the data-service `statePayloadJson` projection) keeps working unchanged. This is the ObjState half of DGID-03 (Plan 05 delivered the Computgraph half): "within one Design State" means the DesignState's ObjState payloads reference member objects by the same dgId the identity registry binds platforms to.

Purpose: DGID-03 — counterpart objects resolve to one identity within a Design State; DesignState/ObjState payloads reference member objects by `dgId`.
Output: additive model + serializer edits and a test class proving round-trip, backward compatibility, and minting parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-CONTEXT.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-RESEARCH.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-PATTERNS.md
@spec/DG-ID.md
@DG/src/DG.Core/Serialization/DesignStatePayloadV2Serializer.cs
</context>

## Artifacts this phase produces (created by THIS plan)

New symbols:
- Additive property `string? DgId` on `ObjState` (namespace `DG.Core.Models`)
- Additive property `string? DgId` on the private `ObjStateDto` inside `DesignStatePayloadV2Serializer` (emitted as camelCase JSON key `dgId` in `statePayloadJson v2`)
- Test class `ObjStateDgIdTests` (namespace `DG.Tests`)

Consumes (read-first, existing): `ObjState`, `DesignState`, `DesignStatePayloadV2Serializer` (v7.0); `DgIdMintingService` (Plan 01); the additive-field rule in `spec/DG-ID.md` (Plan 02).

The `dgId` key inside `statePayloadJson` v2 envelopes is a NET-NEW payload field this plan introduces (additive; absent on all pre-32.1 payloads).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive DgId on ObjState + ObjStateDto round-trip mapping</name>
  <files>DG/src/DG.Core/Models/ObjState.cs, DG/src/DG.Core/Serialization/DesignStatePayloadV2Serializer.cs</files>
  <read_first>
    - DG/src/DG.Core/Models/ObjState.cs (the existing model shape — property style, which fields are nullable, the Geometry object? in-process-handle precedent for fields excluded from validation)
    - DG/src/DG.Core/Serialization/DesignStatePayloadV2Serializer.cs (ObjStateDto at ~L470; ToDto(ObjState) at ~L251; FromDto at ~L316; ValidateDesignState/ValidateDeserialized/ValidateParameter — the methods that must NOT be restructured; the camelCase JsonSerializerOptions)
    - .planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-RESEARCH.md (Pattern 3 — additive-optional dgId on ObjStateDto; Pitfall 3 — the exact breakage mode to avoid: any diff to Validate* beyond the new field's own permissive handling)
    - spec/DG-ID.md (Plan 02 — the normative additive-nullable rule and the "within one Design State" contract)
  </read_first>
  <behavior>
    - Serialize a DesignState whose ObjState has DgId set → the statePayloadJson v2 string contains a camelCase "dgId" key inside the objStates entry
    - Deserialize that payload → the ObjState's DgId equals the original value
    - Serialize a DesignState whose ObjState has DgId null → the payload serializes without error (null or absent dgId key, matching how the serializer handles other nullable fields)
    - Deserialize a v2 payload string that contains NO dgId key at all → succeeds, ObjState.DgId is null
  </behavior>
  <action>
    Add `public string? DgId { get; set; }` to `ObjState` (match the file's existing property style; nullable, defaults null, no serialization attributes — the serializer owns its DTO). Null means "not minted / pre-32.1 state".

    In `DesignStatePayloadV2Serializer`: add `public string? DgId { get; set; }` to the private `ObjStateDto`; copy it in the ObjState `ToDto` method (`DgId = objState.DgId`) and back in the ObjState `FromDto` method. That is the ENTIRE serializer diff — do not touch `ValidateDesignState`, `ValidateDeserialized`, `ValidateParameter`, the version literal, the ordering logic, or any other DTO. No validation is added for the new field (permissive optional, per the RESEARCH Pattern 3 rule). The camelCase naming policy emits the key as `dgId` automatically.

    Build DG.Core and run the existing v2 serializer suite to confirm zero regression before writing new tests.
  </action>
  <verify>
    <automated>dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~DesignStatePayloadV2SerializerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~DesignStatePayloadV2SerializerTests"` exits 0 with the SAME set of passing facts as before this plan (git diff shows zero changes to DG/tests/DG.Tests/DesignStatePayloadV2SerializerTests.cs)
    - `DG/src/DG.Core/Models/ObjState.cs` declares `public string? DgId`
    - `DesignStatePayloadV2Serializer.cs` git diff shows ONLY: the ObjStateDto property addition and the two mapping lines (ToDto/FromDto) — no diff hunks inside ValidateDesignState, ValidateDeserialized, or ValidateParameter
    - `dotnet build ./DG/src/DG.Core/DG.Core.csproj -c Debug` exits 0
  </acceptance_criteria>
  <done>ObjState and its DTO carry an additive nullable DgId mapped through ToDto/FromDto only; the existing v2 serializer suite is green with zero test-file changes and zero Validate* restructuring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ObjStateDgIdTests — round-trip, backward compat, minting parity</name>
  <files>DG/tests/DG.Tests/Identity/ObjStateDgIdTests.cs</files>
  <read_first>
    - DG/tests/DG.Tests/DesignStatePayloadV2SerializerTests.cs (the builder helpers that construct a valid DesignState/ObjState for serialization — reuse their construction shape so new tests build payloads the same way; xUnit naming convention)
    - DG/src/DG.Core/Models/Identity/DgIdMintingService.cs (Plan 01 — Mint signature; the .Value string to place on ObjState.DgId)
    - DG/src/DG.Core/Serialization/DesignStatePayloadV2Serializer.cs (Task 1 output — the mapping under test)
    - .planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-CONTEXT.md (the locked GH-wall↔Revit-wall same-dgId-within-one-Design-State contract the parity fact encodes)
  </read_first>
  <behavior>
    - Round-trip: ObjState.DgId = "dg:..." survives Serialize → Deserialize byte-identically
    - Backward compat: a hand-built v2 payload JSON string with no dgId key deserializes; ObjState.DgId is null
    - Minting parity: DgIdMintingService.Mint("p1", "frame.gh", "obj:Wall").Value placed on an ObjState round-trips as the identical string — the Design State's member reference IS the Computgraph/registry identity for the wall (the same dgId a Revit UniqueId binds to via /identity/bind)
    - Null DgId: an ObjState with DgId null serializes and round-trips without error
  </behavior>
  <action>
    Create `ObjStateDgIdTests` in namespace `DG.Tests` under `DG/tests/DG.Tests/Identity/`. Reuse the DesignState/ObjState construction helpers pattern from `DesignStatePayloadV2SerializerTests` (build a minimal valid DesignState with one ObjState). Add one `[Fact]` per `<behavior>` line:
    - `ObjStateDgId_RoundTrip_PreservesValue` — set a dg-prefixed literal, Serialize + Deserialize, Assert.Equal.
    - `ObjStateDgId_PayloadWithoutDgIdKey_DeserializesWithNull` — take a serialized payload from a DgId-null state (or a hand-built v2 JSON string with no dgId key), Deserialize, assert `Assert.Null(objState.DgId)`. Add a comment naming this the pre-32.1 backward-compat guarantee.
    - `ObjStateDgId_MintedWallIdentity_RoundTripsIdentically` — `var wallDgId = DgIdMintingService.Mint("p1", "frame.gh", "obj:Wall").Value;` set it on the ObjState, round-trip, assert the deserialized value equals `wallDgId`. Add a comment tying this to the CONTEXT locked contract: the Revit BIM wall binds to this same dgId via the identity registry, so both representations resolve to one identity within the Design State.
    - `ObjStateDgId_Null_SerializesWithoutError` — DgId left null, Serialize/Deserialize succeeds, DgId stays null.
  </action>
  <verify>
    <automated>dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~ObjStateDgIdTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~ObjStateDgIdTests"` exits 0 with 4 passing facts
    - A fact asserts a payload with no dgId key deserializes with `DgId == null` (backward compat)
    - A fact mints via `DgIdMintingService.Mint` and asserts the ObjState round-trips that exact value (minting parity / DGID-03)
    - `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj` (full suite) exits 0 — no regression anywhere in DG.Tests
  </acceptance_criteria>
  <done>Four facts prove dgId round-trip in statePayloadJson v2, pre-32.1 backward compatibility, minting parity with the Computgraph identity, and null tolerance — the ObjState half of DGID-03, with the full DG.Tests suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statePayloadJson string → Deserialize | v2 payloads arrive from Neo4j / data-service round-trips; the new dgId key widens the surface the existing readers must tolerate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-06a | Tampering | v1/v2 payload reader breakage | medium | mitigate | Strictly additive nullable DTO field with zero Validate* changes (Pitfall 3); regression gate = the untouched DesignStatePayloadV2SerializerTests suite must stay green, plus an explicit no-dgId-key backward-compat fact. |
| T-32.1-06b | Spoofing | forged dgId inside a payload | low | accept | ObjState.DgId is a reference, not an authorization token; the registry (Plan 03) is the authority — resolving/binding against a dgId that was never minted returns DGID_NOT_FOUND at the API layer. |

No package-manager installs in this plan — no T-32.1-SC legitimacy gate required.
</threat_model>

<verification>
- `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~DesignStatePayloadV2SerializerTests"` exits 0 with zero changes to that test file
- `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~ObjStateDgIdTests"` exits 0 (4 facts)
- `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj` full suite exits 0
- Git diff on `DesignStatePayloadV2Serializer.cs` confined to the ObjStateDto property + two mapping lines
- If net9.0 runtime is reported absent, prefix with `DOTNET_ROLL_FORWARD=LatestMajor`
</verification>

<success_criteria>
DesignState/ObjState payloads reference member objects by `dgId`: the field round-trips through `statePayloadJson v2`, equals the deterministically minted Computgraph identity for the same object, and every pre-32.1 payload and reader keeps working untouched — completing DGID-03's ObjState half without breaking the v1/v2 contract.
</success_criteria>

<output>
Create `.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-06-SUMMARY.md` when done.
</output>
