---
phase: 32.1-cross-platform-identity-and-mapping-dg-id
plan: 05
type: execute
wave: 2
depends_on: [32.1-01, 32.1-02]
files_modified:
  - DG/src/DG.Core/Models/Computgraph/CgObject.cs
  - DG/src/DG.Core/Models/Computgraph/CgProcedure.cs
  - DG/src/DG.Core/Models/Computgraph/CgPattern.cs
  - DG/src/DG.Core/Models/Computgraph/CgParameter.cs
  - DG/src/DG.Core/Models/Computgraph/CgInterface.cs
  - DG/src/DG.Core/Services/CgContextDgIdAssigner.cs
  - DG/src/DG.Core/Serialization/ComputgraphContextSerializer.cs
  - DG/tests/DG.Tests/Identity/CgContextDgIdAssignerTests.cs
  - DG/tests/DG.Tests/ComputgraphContextSerializerTests.cs
autonomous: true
requirements: [DGID-01, DGID-03]

must_haves:
  truths:
    - "After the assigner runs over a parsed Frame context, every tagged entity (Object, each Procedure, each Pattern, each Parameter, each Interface) has a non-empty dg-prefixed DgId"
    - "Running the assigner twice on the same (project, definition) context yields byte-identical DgIds per entity — deterministic across re-extractions (DGID-01)"
    - "The cgContextJson envelope serializes and round-trips the per-entity dgId as a camelCase 'dgId' key"
    - "A pre-32.1 cgContextJson payload with no dgId keys still deserializes without error (additive-field backward compatibility)"
  artifacts:
    - "5 Computgraph entity files each gaining an additive nullable string? DgId field"
    - "DG/src/DG.Core/Services/CgContextDgIdAssigner.cs — walks a CgContext, mints and sets DgId on every tagged entity"
    - "DG/tests/DG.Tests/Identity/CgContextDgIdAssignerTests.cs + additions to ComputgraphContextSerializerTests.cs"
  key_links:
    - "CgContextDgIdAssigner mints via DgIdMintingService.Mint(project, definition.DocumentId, entityKey) where entityKey = entity.Id for typed entities and 'obj:'+Name for the Object — the cross-phase contract Phase 36 publish must reuse so pre-minted and API-minted dgIds match"
    - "the serializer dgId field is additive-nullable and the schemaVersion stays 'cg-context-1' (non-breaking additive change, no version bump — matches the Phase 14/17 additive-field precedent)"
---

<objective>
Wire the DG ID into Phase 32's Computgraph serialization surface: add an additive nullable `DgId` to each tagged entity model, add a GH-free `CgContextDgIdAssigner` that mints and stamps a deterministic `dgId` onto every tagged entity of a parsed `CgContext`, and extend the `cgContextJson v1` serializer so `dgId` round-trips — without breaking any pre-32.1 payload reader. Phase 32's five plans are treated as read-first context and must NOT be re-planned; this plan layers on top of the model and serializer they created.

Purpose: DGID-01 (every extracted Computgraph entity carries a deterministic platform-neutral dgId) and DGID-03 (DesignState/ObjState reference members by dgId — the Computgraph half; ObjState half is Plan 06).
Output: 5 additive model edits, the assigner + its tests, and additive serializer support + tests, all GH-free.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-CONTEXT.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-RESEARCH.md
@.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-PATTERNS.md
@spec/DG-ID.md
@.planning/phases/32-computgraph-serialization-core/32-CONTEXT.md
</context>

## Artifacts this phase produces (created by THIS plan)

New symbols:
- Additive property `string? DgId` on `CgObject`, `CgProcedure`, `CgPattern`, `CgParameter`, `CgInterface` (namespace `DG.Core.Models.Computgraph`)
- Class `CgContextDgIdAssigner` (static, namespace `DG.Core.Services`) — `AssignDgIds(CgContext context, string project) : void` (or returns the mutated context); reuses `DgIdMintingService`
- New serializer DTO field `dgId` on the entity DTOs inside `ComputgraphContextSerializer`
- Test class `CgContextDgIdAssignerTests` (namespace `DG.Tests`) + new facts in `ComputgraphContextSerializerTests`

Consumes (read-first, do NOT re-plan): Phase 32's `CgObject/CgAlgorithm/CgProcedure/CgPattern/CgParameter/CgInterface/CgContext` model and `ComputgraphContextSerializer`; Plan 01's `DgIdMintingService`/`DgId`.

Cross-phase contract this plan LOCKS (record in SUMMARY for Phase 36): the minting input is `(project, definition.DocumentId, entityKey)` where `entityKey = entity.Id` for typed entities and `"obj:" + objectName` for the Object. Phase 36's publish path must mint/carry dgIds with the identical inputs so a pre-minted dgId equals an /identity/mint dgId for the same entity.

<tasks>

<task type="auto">
  <name>Task 1: Additive DgId field on the five tagged Computgraph entity models</name>
  <files>DG/src/DG.Core/Models/Computgraph/CgObject.cs, DG/src/DG.Core/Models/Computgraph/CgProcedure.cs, DG/src/DG.Core/Models/Computgraph/CgPattern.cs, DG/src/DG.Core/Models/Computgraph/CgParameter.cs, DG/src/DG.Core/Models/Computgraph/CgInterface.cs</files>
  <read_first>
    - DG/src/DG.Core/Models/Computgraph/CgProcedure.cs (Phase 32 output — the existing POCO shape: init-only auto-props, `Id`, `Name`, `Source = "tagged"`, non-null collection defaults; add the new field consistently)
    - DG/src/DG.Core/Models/Computgraph/CgObject.cs (Phase 32 output — note it carries `Name`/`ClassIri`/`Source` but NO `Id` — the Object's minting key derives from Name, see Task 2)
    - spec/DG-ID.md (Plan 02 — the normative statement that every tagged Computgraph entity carries a dgId; the additive-nullable rule)
  </read_first>
  <action>
    Add a single additive property `public string? DgId { get; set; }` (nullable, defaults null) to each of the five tagged entity models — `CgObject`, `CgProcedure`, `CgPattern`, `CgParameter`, `CgInterface` — following the existing property style in each file (init-only or get/set to match the file's convention). This is strictly additive: do not rename, reorder, or retype any existing member, and do not add serialization attributes (the serializer owns its DTO tree). Null means "not yet minted" (e.g. untagged entities or pre-assign state). Build DG.Core.
  </action>
  <verify>
    <automated>dotnet build ./DG/src/DG.Core/DG.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build ./DG/src/DG.Core/DG.Core.csproj -c Debug` exits 0
    - Each of `CgObject.cs`, `CgProcedure.cs`, `CgPattern.cs`, `CgParameter.cs`, `CgInterface.cs` declares a `DgId` property of type `string?`
    - No existing property in those files changed type or name (git diff shows only additions)
    - `grep -rl "using Grasshopper" DG/src/DG.Core/Models/Computgraph/` returns no matches
  </acceptance_criteria>
  <done>All five tagged Computgraph entity models carry an additive nullable DgId field, DG.Core builds, zero Grasshopper references, no existing member disturbed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CgContextDgIdAssigner — deterministic dgId assignment over a CgContext</name>
  <files>DG/src/DG.Core/Services/CgContextDgIdAssigner.cs, DG/tests/DG.Tests/Identity/CgContextDgIdAssignerTests.cs</files>
  <read_first>
    - DG/src/DG.Core/Models/Identity/DgIdMintingService.cs (Plan 01 — `Mint(project, definitionId, cgId)` signature and the `DgId.Value` string)
    - DG/src/DG.Core/Models/Computgraph/CgContext.cs (Phase 32 — the aggregate to walk: `Object`, `Algorithms` → each `Procedures` → each `Patterns`/`Parameters`/`Interfaces`; `Definition.DocumentId`)
    - DG/src/DG.Core/Services/DesignStateIdGenerator.cs (the Services-namespace static-service convention to match)
    - spec/DG-ID.md (Plan 02 — the entityKey derivation: entity.Id for typed entities, name-derived for the Object)
  </read_first>
  <behavior>
    - After AssignDgIds(context, "p1"), context.Object.DgId, every Procedure.DgId, every Pattern.DgId, every Parameter.DgId, and every Interface.DgId are non-null and start with "dg:"
    - Two separate AssignDgIds calls on two structurally-identical contexts with the same project produce identical per-entity DgIds (determinism)
    - The Object's DgId derives from its Name ("obj:"+Name) since CgObject carries no Id; typed entities derive from their Id
    - Changing the project changes every assigned DgId (project folded into the mint input)
  </behavior>
  <action>
    Create `CgContextDgIdAssigner` as a `public static class` in namespace `DG.Core.Services`. Add `public static void AssignDgIds(CgContext context, string project)` (guard nulls). Walk the aggregate and set `.DgId` on each tagged entity via `DgIdMintingService.Mint(project, context.Definition.DocumentId, entityKey).Value`:
    - Object (if present): `entityKey = "obj:" + context.Object.Name`.
    - For each Algorithm, for each Procedure: `entityKey = procedure.Id`.
    - For each Procedure's Patterns: `entityKey = pattern.Id`; Parameters: `entityKey = parameter.Id`; Interfaces: `entityKey = interface.Id`.
    Do NOT assign dgIds to untagged nodes or the raw `Nodes`/`Wires` substrate (only ontological tagged entities carry identity, per DGID-01). Use `context.Definition.DocumentId` as the definitionId (the stable document GUID — this is the cross-phase contract; record it in the SUMMARY).

    Create `CgContextDgIdAssignerTests` in namespace `DG.Tests` under `DG/tests/DG.Tests/Identity/` with an in-code `CreateContext()` builder (a minimal Frame-like context: 1 Object, 1 Algorithm, 1 Procedure with 1 Pattern + 1 Parameter + 1 Interface). Add `[Fact]`s per `<behavior>` line: all-entities-assigned (non-null, dg-prefixed), determinism (two contexts → equal per-entity dgIds), object-key-from-name, project-changes-all.

    <!-- planner-discipline-allow: dg: -->
  </action>
  <verify>
    <automated>dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~CgContextDgIdAssignerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~CgContextDgIdAssignerTests"` exits 0
    - A fact asserts every tagged entity (Object + Procedure + Pattern + Parameter + Interface) has a non-null `DgId` starting with `dg:` after assignment
    - A fact asserts determinism (two structurally-identical contexts, same project → equal per-entity DgIds)
    - A fact asserts the Object DgId derives from its Name and typed-entity DgIds from their Id
    - `CgContextDgIdAssigner.cs` calls `DgIdMintingService.Mint(` with `context.Definition.DocumentId` as the definitionId argument
    - `grep -rl "using Grasshopper" DG/src/DG.Core/Services/CgContextDgIdAssigner.cs` returns no matches
  </acceptance_criteria>
  <done>CgContextDgIdAssigner deterministically stamps a dg-prefixed DgId onto every tagged entity of a CgContext (Object from Name, typed entities from Id, project folded in), proven by facts, GH-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cgContextJson dgId round-trip + backward-compat (additive, no version bump)</name>
  <files>DG/src/DG.Core/Serialization/ComputgraphContextSerializer.cs, DG/tests/DG.Tests/ComputgraphContextSerializerTests.cs</files>
  <read_first>
    - DG/src/DG.Core/Serialization/ComputgraphContextSerializer.cs (Phase 32 output — the private DTO tree: `CgObjectDto`, `CgProcedureDto`, `CgPatternDto`, `CgParameterDto`, `CgInterfaceDto`; the `ToDto`/`FromDto` mapping; the camelCase Options; the version-check-first Deserialize)
    - DG/tests/DG.Tests/ComputgraphContextSerializerTests.cs (Phase 32 output — the existing round-trip/idempotency/version-guard facts that MUST stay green; the `CreateContext()` builder to extend)
    - .planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-RESEARCH.md (Pattern 3 additive-optional field; the additive-field precedent — new nullable key, old readers ignore it, no version bump)
    - spec/DG-ID.md (Plan 02 — the decision that dgId is additive and schemaVersion stays cg-context-1)
  </read_first>
  <behavior>
    - Serializing a context whose entities have DgId set emits a camelCase "dgId" key on each entity object in the JSON
    - Deserializing that JSON restores each entity's DgId
    - Serialize(Deserialize(s)) stays idempotent with dgIds present (extends the Phase 32 idempotency fact)
    - Deserializing a valid cg-context-1 payload that has NO dgId keys succeeds, leaving DgId null on every entity (pre-32.1 backward compatibility)
    - schemaVersion remains "cg-context-1" — the additive field does not force a version bump
  </behavior>
  <action>
    Extend the private DTO tree in `ComputgraphContextSerializer`: add a nullable `string? DgId` to `CgObjectDto`, `CgProcedureDto`, `CgPatternDto`, `CgParameterDto`, `CgInterfaceDto`. Map it through in the corresponding `ToDto` (copy `entity.DgId`) and `FromDto` (copy back to `.DgId`) methods — additive only, no restructuring of existing mappings. The camelCase Options already yield the JSON key `dgId`. Keep `SchemaVersion` at `"cg-context-1"`; do NOT bump the version (the field is nullable/additive — the Deserialize version guard still accepts existing payloads, and a payload lacking `dgId` maps to null via System.Text.Json's default missing-property behavior). Do not change `ValidateContext`/`ValidateDeserialized` beyond leaving DgId unconstrained (a permissive optional field).

    Add facts to `ComputgraphContextSerializerTests` (keep all existing facts unchanged): (a) build a context, run `CgContextDgIdAssigner.AssignDgIds` (or set DgIds inline), serialize, assert the JSON contains `"dgId":"dg:` at least once; (b) round-trip preserves each entity's DgId; (c) idempotency `Serialize(Deserialize(s)) == s` holds with dgIds present; (d) deserialize a hand-built cg-context-1 JSON string with NO dgId keys succeeds and leaves DgId null (backward compat).

    <!-- planner-discipline-allow: "dgId":"dg: -->
  </action>
  <verify>
    <automated>dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~ComputgraphContextSerializerTests"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~ComputgraphContextSerializerTests"` exits 0 with the pre-existing Phase 32 facts still passing plus the new dgId facts
    - The serializer emits a camelCase `dgId` key for entities whose `DgId` is set
    - A fact asserts a cg-context-1 payload with no dgId keys deserializes successfully with DgId null on every entity (backward compatibility)
    - A fact asserts `Serialize(Deserialize(s)) == s` with dgIds present (idempotency preserved)
    - `ComputgraphContextSerializer.cs` still declares `SchemaVersion` / the version literal `cg-context-1` (no bump)
    - The entity models still carry no `[JsonPropertyName]` attributes (DTO-owns-serialization invariant intact)
  </acceptance_criteria>
  <done>cgContextJson round-trips a per-entity dgId as an additive camelCase key with the schemaVersion unchanged, idempotency preserved, and pre-32.1 payloads (no dgId) still deserialize — DGID-01's envelope-carriage requirement met without breaking the versioned contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cgContextJson string → Deserialize | The dgId field widens the deserialization surface Phase 32 already guards; a forged/absent dgId must not break the reader or corrupt identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-05a | Tampering | dgId in cgContextJson | low | mitigate | dgId is an additive nullable field with no validation coupling; Phase 32's version-check-first + ValidateDeserialized still gate the payload. A forged dgId is re-mintable deterministically by the assigner and is re-verified server-side at publish (Phase 36) against the same hash contract. |
| T-32.1-05b | Tampering | additive-field breakage of old readers | medium | mitigate | The field is strictly additive/nullable and schemaVersion is unchanged; a backward-compat fact asserts a pre-32.1 payload (no dgId) still deserializes, and all pre-existing Phase 32 serializer facts must stay green. |

No package-manager installs in this plan — no T-32.1-SC legitimacy gate required.
</threat_model>

<verification>
- `dotnet build ./DG/src/DG.Core/DG.Core.csproj -c Debug` exits 0
- `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~CgContextDgIdAssignerTests"` exits 0
- `dotnet test ./DG/tests/DG.Tests/DG.Tests.csproj --filter "FullyQualifiedName~ComputgraphContextSerializerTests"` exits 0 (pre-existing Phase 32 facts still green)
- Zero Grasshopper references under `DG/src/DG.Core/Models/Computgraph/` and `DG/src/DG.Core/Services/CgContextDgIdAssigner.cs`
- If net9.0 runtime is reported absent, prefix with `DOTNET_ROLL_FORWARD=LatestMajor`
</verification>

<success_criteria>
Every tagged Computgraph entity carries a deterministic, project-scoped, dg-prefixed identity after a GH-free assignment pass, and the `cgContextJson v1` envelope round-trips that dgId as an additive camelCase field without a version bump and without breaking pre-32.1 readers — satisfying DGID-01's per-entity carriage and the Computgraph half of DGID-03.
</success_criteria>

<output>
Create `.planning/phases/32.1-cross-platform-identity-and-mapping-dg-id/32.1-05-SUMMARY.md` when done. Record the entityKey derivation and definitionId=DocumentId decision as the cross-phase contract for Phase 36.
</output>
