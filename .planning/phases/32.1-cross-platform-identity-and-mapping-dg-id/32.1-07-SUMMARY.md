---
phase: 32.1-cross-platform-identity-and-mapping-dg-id
plan: 07
subsystem: documentation, schema-propagation
tags: [schema-change, identity-registry, dgid, shacl, documentation]
requires: 32.1-03, 32.1-04, 32.1-05, 32.1-06
provides: [DATABASE.md-identity-section, SHACL-representation-shape, SHACL-shared-property-shape, dg-context-allow-lists]
affects: [spec/DATABASE.md, cypher_template.txt, training/dataset_schema.json, CLAUDE.md, .github/copilot-instructions.md, README.md, ontology/dg-shapes.ttl, data-service/dg_context.py, spec/RULE-PARTITION-POLICY.md]
tech-stack:
  added: [dgc prefix in dg-shapes.ttl]
  patterns: [document-only-comment-block, additive-allow-list-append]
key-files:
  created: []
  modified:
    - spec/DATABASE.md
    - cypher_template.txt
    - training/dataset_schema.json
    - CLAUDE.md
    - .github/copilot-instructions.md
    - README.md
    - ontology/dg-shapes.ttl
    - data-service/dg_context.py
    - spec/RULE-PARTITION-POLICY.md
decisions:
  - "dgId gets no OWL annotation property in V7 (runtime/persistence identity, not ontological vocabulary) — recorded in spec/DATABASE.md §Identity Registry with ADR cross-reference"
  - "RULE-PARTITION-POLICY.md decision table gets a new row for Computgraph identity-registry data-integrity shapes (SHACL-side)"
metrics:
  duration: "~15 min"
  tasks: 3
  commits: 3
  files_modified: 9
status: complete
---

# Phase 32.1 Plan 07: Cross-Platform Identity & Mapping — Schema Propagation

One-liner: Executed the schema-change propagation checklist for the Phase 32.1 graph schema additions — `dgId` property, `Representation` and `SharedProperty` node labels, `HAS_REPRESENTATION` and `HAS_SHARED_PROPERTY` relationships — across all 8 propagation surfaces required by CLAUDE.md's standing rule, plus the partition-policy decision table.

## Objective

Make the identity-registry shape (DGID-02), the per-entity dgId (DGID-01), and the shared-property records (DGID-04) visible to every schema consumer: spec/DATABASE.md, cypher_template.txt, training/dataset_schema.json, CLAUDE.md, .github/copilot-instructions.md, README.md, ontology/dg-shapes.ttl, and data-service/dg_context.py.

## Task Results

### Task 1: spec/DATABASE.md + cypher_template.txt + training/dataset_schema.json

**Acceptance:** PASSED

- **spec/DATABASE.md:** Added Computgraph graph separation row, dgId property documentation, Representation and SharedProperty node tables with exact property names (nativeId, nativeIdKind, platform, connector, boundAt, dgId, propertyName, value, writtenAt), HAS_REPRESENTATION/HAS_SHARED_PROPERTY relationship rows, a formal Identity Registry section, the no-OWL-annotation recorded decision with ADR cross-reference, and cross-reference to spec/DG-ID.md.
- **cypher_template.txt:** Appended a document-only identity-registry comment block (following the DesignState precedent) stating these nodes are written ONLY by the data-service identity API, never by LLM ingest. No existing MERGE blocks altered.
- **training/dataset_schema.json:** Added Representation and SharedProperty entries to schema_reference following the Phase 14 document-only pattern, plus IdentityRelationships enum. JSON validated.

**Commits:** `135c1b0`

### Task 2: CLAUDE.md + .github/copilot-instructions.md + README.md

**Acceptance:** PASSED

- **CLAUDE.md:** Added Representation (Computgraph, key nativeId+platform+project) and SharedProperty (Computgraph, key dgId+propertyName+project) rows to Node Labels table; extended Relationships line with HAS_REPRESENTATION and HAS_SHARED_PROPERTY; added dgId property note with spec/DG-ID.md cross-reference.
- **.github/copilot-instructions.md:** Added Computgraph graph value, Representation/SharedProperty canonical node descriptions, HAS_REPRESENTATION/HAS_SHARED_PROPERTY relationship rows, dgId property note.
- **README.md:** Added Computgraph to graph values list, identity-registry note referencing spec/DG-ID.md and /identity/ API.

**Commits:** `1714768`

### Task 3: SHACL shapes + dg_context.py allow-lists

**Acceptance:** PASSED

- **ontology/dg-shapes.ttl:** Added `dgc:` prefix (http://example.org/design-grammar/comp#), RepresentationShape with platform sh:in (Grasshopper/Revit/IFC/Speckle) and nativeIdKind sh:in (InstanceGuid/UniqueId/GlobalId/ApplicationId) enum constraints, SharedPropertyShape with dgId sh:pattern (^dg:[0-9A-F]{16}$), required properties, and dgsh:howToFix remediation per existing style.
- **data-service/dg_context.py:** Additive appends to ALLOWED_LABELS (Representation, SharedProperty), ALLOWED_RELATIONSHIPS (HAS_REPRESENTATION, HAS_SHARED_PROPERTY), and new ALLOWED_PROPERTIES constant (dgId, nativeId, nativeIdKind, platform, connector, boundAt, propertyName, writtenAt). Strictly additive — no reordering or restructuring. All 32 core logic tests pass.
- **spec/RULE-PARTITION-POLICY.md:** Added identity-registry data-integrity row to the What Belongs Where decision table (SHACL-side, per the Consistency & Propagation trigger).

**Commits:** `93ff78a`

## Verification

| Check | Result |
|-------|--------|
| `python -c "import json; json.load(open('training/dataset_schema.json'))"` | JSON_OK |
| `python -m pytest data-service/tests/test_dg_context.py::TestValidator ... -q` | 32 passed |
| grep gates (PROP1_OK, PROP2_OK, PROP3_OK) | All passed |
| git diff on cypher_template.txt and dg_context.py | Strictly additive only |
| Property names consistent across all 8 surfaces + spec/DG-ID.md | All match |

## Self-Check

- All 9 files exist and were modified
- 3 commits recorded with correct format
- Property/label/relationship strings match the Implemented Cypher (nativeIdKind not native_id_kind — Neo4j camelCase convention)
- All verification passes

## Success Criteria

Every schema-propagation surface named by CLAUDE.md's standing rule documents the dgId property, Representation/SharedProperty labels, and HAS_REPRESENTATION/HAS_SHARED_PROPERTY relationships exactly as Plans 03/04 implemented them, with the no-OWL-annotation decision recorded. The partition-policy decision table gained a new Computgraph registry row.

## Deviations from Plan

None — plan executed exactly as written.

## RULE-PARTITION-POLICY.md Update

As noted by the consistency check triggered by adding two new node labels (Representation, SharedProperty) to the SHACL shapes, the decision table in spec/RULE-PARTITION-POLICY.md received a new row for "Identity-registry data integrity" — classified as SHACL-side since it constrains enum membership and structural format of registry node properties, not architect-authored business content.
