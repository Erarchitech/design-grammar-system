---
phase: 17-graph-access-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DG/src/DG.Core/Models/MetagraphHandle.cs
  - DG/src/DG.Core/Models/OntographHandle.cs
  - DG/src/DG.Core/Models/ValidGraphHandle.cs
  - DG/src/DG.Core/Models/SpecGraphHandle.cs
  - DG/src/DG.Core/Models/OntologyClass.cs
  - DG/src/DG.Core/Models/OntologyProperty.cs
  - DG/src/DG.Grasshopper/PublicTypes.cs
  - DG/src/DG.Grasshopper/DgIcons.cs
  - DG/src/DG.Core/Services/ErrorMessageTemplates.cs
  - DG/src/DG.Grasshopper/Properties/GraphDeconstruct24.png
  - DG/src/DG.Grasshopper/Properties/OntoGraph24.png
  - DG/src/DG.Grasshopper/Properties/ValidationGraph24.png
  - DG/src/DG.Grasshopper/Components/ConnectorComponent.cs
  - DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs
  - DG/tests/DG.Tests/MetagraphHandleModelTests.cs
  - DG/tests/DG.Tests/OntographHandleModelTests.cs
  - DG/tests/DG.Tests/ValidGraphHandleModelTests.cs
  - DG/tests/DG.Tests/SpecGraphHandleModelTests.cs
  - DG/tests/DG.Tests/OntologyClassModelTests.cs
  - DG/tests/DG.Tests/OntologyPropertyModelTests.cs
  - DG/tests/DG.Tests/ConnectorComponentPortContractTests.cs
autonomous: true
requirements:
  - GHGA-01
  - GHGA-02
user_setup: []

must_haves:
  truths:
    - 4 handle types (MetagraphHandle, OntographHandle, ValidGraphHandle, SpecGraphHandle) exist in DG.Core.Models as unsealed classes wrapping ConnectionInfo with {get; init;} pattern
    - OntologyClass (iri + label) and OntologyProperty (iri + label) exist in DG.Core.Models for ONTOGRAPH outputs
    - PublicTypes.cs has public wrappers for all 4 handle types (inheriting Core counterparts)
    - DgIcons.cs has entries for GraphDeconstruct24, OntoGraph24, ValidationGraph24
    - ErrorMessageTemplates.cs has graph-access error messages following What+Where+How-to-fix pattern
    - CONNECTOR outputs renamed to "Database" and "Project" (ports: Neo4jURI/Neo4jUser/Neo4jPassword/PROJECT NAME/Database/Connect inputs)
    - GRAPH DECONSTRUCT passes ConnectionInfo through 4 typed handle outputs (no async, no DB calls)
    - All handle type tests and ontology model tests exist and pass
  artifacts:
    - DG/src/DG.Core/Models/MetagraphHandle.cs
    - DG/src/DG.Core/Models/OntographHandle.cs
    - DG/src/DG.Core/Models/ValidGraphHandle.cs
    - DG/src/DG.Core/Models/SpecGraphHandle.cs
    - DG/src/DG.Core/Models/OntologyClass.cs
    - DG/src/DG.Core/Models/OntologyProperty.cs
    - DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs
    - DG/src/DG.Grasshopper/Properties/GraphDeconstruct24.png (24x24)
    - DG/src/DG.Grasshopper/Properties/OntoGraph24.png (24x24)
    - DG/src/DG.Grasshopper/Properties/ValidationGraph24.png (24x24)
  key_links:
    - All 4 handle types are unsealed to permit public wrapper inheritance in PublicTypes.cs
    - CONNECTOR's Project output is a passthrough of the PROJECT NAME input (scopes downstream queries)
    - GRAPH DECONSTRUCT outputs all 4 handles wrapping the SAME ConnectionInfo object — no per-handle connection logic
---

<objective>
Create the foundational layer for Phase 17: 4 graph-layer handle types, 2 ontology output models, public wrappers, icon assets, error messages, the updated CONNECTOR component (port renames + Project output), and the new GRAPH DECONSTRUCT component (pure passthrough, 4 typed handle outputs). This plan satisfies GHGA-01 (CONNECTOR) and GHGA-02 (GRAPH DECONSTRUCT) while providing the types that downstream plans (02-04) require.

Purpose: Establish the data-flow contracts — handle types, models, icons, error messages — that every downstream plan in Phase 17 builds on. CONNECTOR is the entry point for all graph access; GRAPH DECONSTRUCT distributes its ConnectionInfo into type-safe layer handles.

Output:
- 4 handle type classes in DG.Core.Models + 2 ontology model classes
- PublicTypes.cs wrappers for handle types
- Updated DgIcons.cs with 3 new icon entries + 3 placeholder PNG files
- Updated ErrorMessageTemplates.cs with graph-access error messages
- Updated ConnectorComponent.cs (port renames, Project output)
- New GraphDeconstructComponent.cs (pure passthrough)
- 7 test files
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-graph-access-components/17-CONTEXT.md (D-01: handle types per layer, D-06: repository pattern)
@.planning/phases/17-graph-access-components/17-RESEARCH.md (Handle Type Pattern, GRAPH DECONSTRUCT code example)

# Existing files — read for patterns
@DG/src/DG.Core/Models/ConnectionInfo.cs — immutable model pattern (sealed, {get; init;}, no behavior)
@DG/src/DG.Grasshopper/PublicTypes.cs — existing wrapper pattern (DG.X : DG.Core.Models.X)
@DG/src/DG.Grasshopper/DgIcons.cs — icon loading pattern (Load method, 24x24 PNG embedded resources)
@DG/src/DG.Core/Services/ErrorMessageTemplates.cs — error message pattern (static methods, What+Where+How-to-fix)
@DG/src/DG.Grasshopper/Components/ConnectorComponent.cs — existing component to update (port names, async pattern)
@DG/src/DG.Grasshopper/Components/MetagraphComponent.cs — reference for async-load pattern (ContinueWith + ScheduleSolution)
@DG/src/DG.Grasshopper/Components/GhCastingHelpers.cs — Unwrap&lt;T&gt; pattern
@ontology/port-iri-map-V7.md — exact port names for CONNECTOR and GRAPH DECONSTRUCT
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 handle types, 2 ontology models, public wrappers, icons, and error messages</name>
  <files>
    DG/src/DG.Core/Models/MetagraphHandle.cs
    DG/src/DG.Core/Models/OntographHandle.cs
    DG/src/DG.Core/Models/ValidGraphHandle.cs
    DG/src/DG.Core/Models/SpecGraphHandle.cs
    DG/src/DG.Core/Models/OntologyClass.cs
    DG/src/DG.Core/Models/OntologyProperty.cs
    DG/src/DG.Grasshopper/PublicTypes.cs
    DG/src/DG.Grasshopper/DgIcons.cs
    DG/src/DG.Core/Services/ErrorMessageTemplates.cs
    DG/src/DG.Grasshopper/Properties/GraphDeconstruct24.png
    DG/src/DG.Grasshopper/Properties/OntoGraph24.png
    DG/src/DG.Grasshopper/Properties/ValidationGraph24.png
  </files>
  <read_first>
    DG/src/DG.Core/Models/ConnectionInfo.cs — existing immutable model pattern: sealed, {get; init;}, default values
    DG/src/DG.Grasshopper/PublicTypes.cs — existing wrapper pattern: DG.X : DG.Core.Models.X
    DG/src/DG.Grasshopper/DgIcons.cs — existing icon naming + Load() method (Connector24, Metagraph24, etc.)
    DG/src/DG.Core/Services/ErrorMessageTemplates.cs — What+Where+How-to-fix static method contract
    DG/src/DG.Grasshopper/Components/GhCastingHelpers.cs — existing Unwrap&lt;T&gt; pattern (handle types follow ConnectionInfo, not the state-model pattern — no Try* methods needed)
  </read_first>
  <action>
    Create or update 9 source files and 3 PNG placeholder icons:

    **1. Handle types (4 new files in DG.Core.Models)**
    Follow ConnectionInfo's sealed-class { get; init; } pattern exactly. Each type has a single property `ConnectionInfo` with a default `new()` instance. Unsealed (to permit PublicTypes inheritance). No methods, no behavior, no IGH_Goo implementation.

    - `MetagraphHandle.cs`: namespace DG.Core.Models; public class MetagraphHandle with `ConnectionInfo ConnectionInfo { get; init; } = new();`
    - `OntographHandle.cs`: same pattern
    - `ValidGraphHandle.cs`: same pattern
    - `SpecGraphHandle.cs`: same pattern

    **2. Ontology models (2 new files in DG.Core.Models)**
    Follow the same sealed-class { get; init; } pattern. These are output models for ONTOGRAPH and METAGRAPH Objects.

    - `OntologyClass.cs`: namespace DG.Core.Models; sealed class with `string Iri { get; init; } = string.Empty;` and `string Label { get; init; } = string.Empty;`
    - `OntologyProperty.cs`: same fields as OntologyClass (iri + label)

    **3. PublicTypes.cs (update — add handle wrappers)**
    Add four new wrapper classes at the end of the file, in the `DG` namespace (file-scoped namespace, same as existing wrappers). Each inherits from its Core counterpart:

    ```
    public class MetagraphHandle : DG.Core.Models.MetagraphHandle { }
    public class OntographHandle : DG.Core.Models.OntographHandle { }
    public class ValidGraphHandle : DG.Core.Models.ValidGraphHandle { }
    public class SpecGraphHandle : DG.Core.Models.SpecGraphHandle { }
    ```

    These follow the same pattern as `DG.Rule : DG.Core.Models.Rule`. Unsealed (inherits unsealed Core type). The GRAPH DECONSTRUCT component outputs these PublicTypes wrappers; downstream components unwrap as Core types via generic Unwrap&lt;T&gt; (inheritance resolves the match).

    **4. DgIcons.cs (update — add 3 new icon entries)**
    Add the following static Bitmap properties after the existing `ValidationRuns24` line, following the same `Load("...24.png")` pattern:

    - `GraphDeconstruct24` — icon for GRAPH DECONSTRUCT
    - `OntoGraph24` — icon for ONTOGRAPH
    - `ValidationGraph24` — icon for VALIDATION GRAPH (replaces VALIDATION RUNS icon role)

    Each uses `Load("GraphDeconstruct24.png")`, `Load("OntoGraph24.png")`, `Load("ValidationGraph24.png")`.

    Note: METAGRAPH icon already exists — no change needed. CONNECTOR icon already exists — no change needed.

    **5. Icon PNGs (create 3 placeholder 24x24 PNG files)**
    Create each file as a minimal 24x24 PNG (solid color with a letter is sufficient — follow the project's existing icon style if known, otherwise a simple colored square). Saved as embedded resources in `DG/src/DG.Grasshopper/Properties/`:

    - `GraphDeconstruct24.png` — distinct color from existing icons
    - `OntoGraph24.png` — distinct color from existing icons
    - `ValidationGraph24.png` — distinct color from existing icons

    These are embedded resources loaded by `DgIcons.Load(Assembly.GetExecutingAssembly(), "DG.Grasshopper.Properties.{fileName}")`.

    Use ImageSharp (SixLabors.ImageSharp NuGet) via a temporary dotnet script or generate a minimal valid PNG binary. If no image library is available, create a minimal valid 24x24 PNG file using raw binary bytes (2x2 pixel PNG is ~68 bytes; scale to 24x24). Run the icon through `DgIcons.Load()` to verify it loads without exception.

    **6. ErrorMessageTemplates.cs (update — add graph-access error messages)**
    Add the following static methods to `DG.Core.Services.ErrorMessageTemplates`:

    a) `GraphDeconstructNoInput()`:
       Returns: "GRAPH DECONSTRUCT: Database input is required. Connect a CONNECTOR component to the Database input."
       Pattern: What (no input) + Where (GRAPH DECONSTRUCT) + How-to-fix (connect CONNECTOR)

    b) `GraphDeconstructCastFailed()`:
       Returns: "GRAPH DECONSTRUCT: Could not cast Database input. Ensure the input is connected to a CONNECTOR component."
       Pattern: What (cast failed) + Where (GRAPH DECONSTRUCT) + How-to-fix (check CONNECTOR output)

    c) `ConnectorProjectPassthroughFailed()`:
       Returns: "CONNECTOR: Project output passthrough failed. Ensure PROJECT NAME input is connected."
       Pattern: What (passthrough failed) + Where (CONNECTOR) + How-to-fix (check PROJECT NAME)

    d) `HandleTypeUnwrapped(string componentName, string handleType)`:
       Returns: $"{componentName}: Could not unwrap {handleType} input. Ensure the input is connected to GRAPH DECONSTRUCT's {handleType} output."
       Pattern: Generic message for any METAGRAPH/ONTOGRAPH/VALIDATION GRAPH handle input failure

    Each method is `public static string MethodName(...)` returning a formatted string. Do NOT reference component GUIDs.
  </action>
  <verify>
    <automated>dotnet build DG/DG.sln 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    - `dotnet build DG/DG.sln` succeeds
    - grep -c "class MetagraphHandle" DG/src/DG.Core/Models/MetagraphHandle.cs returns 1
    - grep -c "class OntographHandle" DG/src/DG.Core/Models/OntographHandle.cs returns 1
    - grep -c "class ValidGraphHandle" DG/src/DG.Core/Models/ValidGraphHandle.cs returns 1
    - grep -c "class SpecGraphHandle" DG/src/DG.Core/Models/SpecGraphHandle.cs returns 1
    - grep -c "class OntologyClass" DG/src/DG.Core/Models/OntologyClass.cs returns 1
    - grep -c "class OntologyProperty" DG/src/DG.Core/Models/OntologyProperty.cs returns 1
    - grep -c "class MetagraphHandle : DG.Core.Models.MetagraphHandle" DG/src/DG.Grasshopper/PublicTypes.cs returns 1
    - grep -c "GraphDeconstruct24" DG/src/DG.Grasshopper/DgIcons.cs returns 1
    - grep -c "OntoGraph24" DG/src/DG.Grasshopper/DgIcons.cs returns 1
    - grep -c "ValidationGraph24" DG/src/DG.Grasshopper/DgIcons.cs returns 1
    - grep -c "GraphDeconstructNoInput" DG/src/DG.Core/Services/ErrorMessageTemplates.cs returns 1
    - grep -c "HandleTypeUnwrapped" DG/src/DG.Core/Services/ErrorMessageTemplates.cs returns 1
    - DgIcons.GraphDeconstruct24 loads without exception (test creates one and disposes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CONNECTOR component and create GRAPH DECONSTRUCT component</name>
  <files>
    DG/src/DG.Grasshopper/Components/ConnectorComponent.cs
    DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs
  </files>
  <read_first>
    DG/src/DG.Grasshopper/Components/ConnectorComponent.cs — existing component to update (port register, SolveInstance)
    DG/src/DG.Grasshopper/Components/MetagraphComponent.cs — reference for the async pattern used by reader components (not needed by GRAPH DECONSTRUCT but good for CONNECTOR context)
    @DG/src/DG.Core/Models/ConnectionInfo.cs — the credential bundle output
    @DG/src/DG.Core/Models/MetagraphHandle.cs, OntographHandle.cs, ValidGraphHandle.cs, SpecGraphHandle.cs — handle types created in Task 1
    @DG/src/DG.Grasshopper/Components/GhCastingHelpers.cs — Unwrap&lt;T&gt; pattern
    @DG/src/DG.Grasshopper/DgComponentCategory.cs — category/subcategory constants
    @DG/src/DG.Grasshopper/DgIcons.cs — after Task 1, includes GraphDeconstruct24
    @ontology/port-iri-map-V7.md — CONNECTOR ports: Neo4jURI/Neo4jUser/Neo4jPassword/PROJECT NAME/Database/Connect inputs; Database+Project outputs
  </read_first>
  <action>
    **Update ConnectorComponent.cs:**

    Rename input port labels to match port-iri-map-V7.md. Update both the display name (first string) and the nickname (second string):

    - Input 0: rename "Neo4j URI" to "Neo4jURI", nickname stays "URI"
    - Input 1: rename "User" to "Neo4jUser", nickname stays "User"
    - Input 2: rename "Password" to "Neo4jPassword", nickname stays "Password"
    - Input 3: rename "Neo4j database" to "Database" display name, keep existing Database behavior (no ontology IRI per note)
    - Input 4: rename "Project" to "PROJECT NAME", nickname "Project" — this is the dg:project annotation input, UPPERCASE per port-iri-map
    - Input 5: "Connect" stays unchanged (GHGA-01: "keeps ... Connect inputs")

    Add a new output port for "Project" (output position 1):
    - `pManager.AddTextParameter("Project", "Project", "Project name (passthrough of PROJECT NAME input)", GH_ParamAccess.item);`

    Rename output 0 from "Connection" to "Database":
    - `pManager.AddGenericParameter("Database", "Database", "DG connection object from CONNECTOR", GH_ParamAccess.item);`

    In SolveInstance:
    - Read all inputs into local variables (unchanged logic)
    - When building the `ConnectionInfo` request, assign `Project` from the PROJECT NAME input
    - When setting outputs: `da.SetData(0, _latestConnection)` stays the same (variable name unchanged)
    - Add `da.SetData(1, project)` — passthrough of the PROJECT NAME input value, matching the port-iri-map V7 IRI dg:project

    The constructor display text stays "CONNECTOR". No GUID change. No async pattern change. The component still outputs ConnectionInfo (Core type) — no handle type at this stage.

    **Create GraphDeconstructComponent.cs (new file):**

    Follow the pure-passthrough pattern from RESEARCH.md code example and CONTEXT discretion:

    ```csharp
    #if GRASSHOPPER_SDK
    using DG.Core.Models;
    using DG.Grasshopper;
    using Grasshopper.Kernel;
    using System.Drawing;

    namespace DG.Grasshopper.Components;

    public sealed class GraphDeconstructComponent : GH_Component
    {
        public GraphDeconstructComponent()
            : base("GRAPH DECONSTRUCT", "DECONSTRUCT",
                "Split a Database handle into 4 graph layer handles.",
                DgComponentCategory.Category, DgComponentCategory.Subcategory)
        {
        }

        public override Guid ComponentGuid => new("F5A2D3C4-E678-4901-B234-56789ABCDEF0");

        protected override Bitmap Icon => DgIcons.GraphDeconstruct24;

        protected override void RegisterInputParams(GH_InputParamManager pManager)
        {
            pManager.AddGenericParameter("Database", "Database",
                "DG connection object from CONNECTOR", GH_ParamAccess.item);
        }

        protected override void RegisterOutputParams(GH_OutputParamManager pManager)
        {
            pManager.AddGenericParameter("Metagraph", "Metagraph",
                "Metagraph layer handle", GH_ParamAccess.item);
            pManager.AddGenericParameter("Ontograph", "Ontograph",
                "Ontograph layer handle", GH_ParamAccess.item);
            pManager.AddGenericParameter("ValidGraph", "ValidGraph",
                "ValidGraph layer handle", GH_ParamAccess.item);
            pManager.AddGenericParameter("SpecGraph", "SpecGraph",
                "SpecGraph layer handle", GH_ParamAccess.item);
        }

        protected override void SolveInstance(IGH_DataAccess da)
        {
            object? databaseInput = null;
            if (!da.GetData(0, ref databaseInput))
            {
                AddRuntimeMessage(GH_RuntimeMessageLevel.Error, ErrorMessageTemplates.GraphDeconstructNoInput());
                return;
            }

            var connection = GhCastingHelpers.Unwrap<ConnectionInfo>(databaseInput);
            if (connection is null)
            {
                AddRuntimeMessage(GH_RuntimeMessageLevel.Error, ErrorMessageTemplates.GraphDeconstructCastFailed());
                return;
            }

            da.SetData(0, new global::DG.MetagraphHandle { ConnectionInfo = connection });
            da.SetData(1, new global::DG.OntographHandle { ConnectionInfo = connection });
            da.SetData(2, new global::DG.ValidGraphHandle { ConnectionInfo = connection });
            da.SetData(3, new global::DG.SpecGraphHandle { ConnectionInfo = connection });
        }
    }
    #else
    namespace DG.Grasshopper.Components;

    public sealed class GraphDeconstructComponent
    {
    }
    #endif
    ```

    Use a new GUID (not conflicting with any existing component). The example GUID above is a placeholder — generate a proper random GUID for the actual component.

    Notes:
    - No async, no CancellationTokenSource, no ContinueWith — this is a pure passthrough per CONTEXT discretion
    - Uses `ghCastingHelpers.Unwrap<ConnectionInfo>` to extract ConnectionInfo from the Database input
    - Outputs 4 PublicTypes handle wrappers (global::DG.MetagraphHandle, etc.) — each wraps the SAME ConnectionInfo object
    - No error state beyond "no input" or "cast failed"
    - Messages are from ErrorMessageTemplates (created in Task 1)
    - Follows `#if GRASSHOPPER_SDK` guard pattern for non-GH stubs
  </action>
  <verify>
    <automated>dotnet build DG/DG.sln 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    - `dotnet build DG/DG.sln` succeeds
    - CONNECTOR component: grep -c '"Neo4jURI"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs returns 1
    - CONNECTOR component: grep -c '"Neo4jUser"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs returns 1
    - CONNECTOR component: grep -c '"Neo4jPassword"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs returns 1
    - CONNECTOR component: grep -c '"PROJECT NAME"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs returns 1
    - CONNECTOR component: grep -c '"Database"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs returns at least 1 as output name
    - CONNECTOR component: grep -c "AddTextParameter.*\"Project\"" DG/src/DG.Grasshopper/Components/ConnectorComponent.cs returns 1
    - GRAPH DECONSTRUCT: grep -c "GraphDeconstructComponent" DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs returns 1
    - GRAPH DECONSTRUCT: grep -c "MetagraphHandle" DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs returns 1
    - GRAPH DECONSTRUCT: grep -c "OntographHandle" DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs returns 1
    - GRAPH DECONSTRUCT: grep -c "ValidGraphHandle" DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs returns 1
    - GRAPH DECONSTRUCT: grep -c "SpecGraphHandle" DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs returns 1
  </done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for handle types, ontology models, CONNECTOR port contract, and GRAPH DECONSTRUCT</name>
  <files>
    DG/tests/DG.Tests/MetagraphHandleModelTests.cs
    DG/tests/DG.Tests/OntographHandleModelTests.cs
    DG/tests/DG.Tests/ValidGraphHandleModelTests.cs
    DG/tests/DG.Tests/SpecGraphHandleModelTests.cs
    DG/tests/DG.Tests/OntologyClassModelTests.cs
    DG/tests/DG.Tests/OntologyPropertyModelTests.cs
    DG/tests/DG.Tests/ConnectorComponentPortContractTests.cs
  </files>
  <read_first>
    DG/tests/DG.Tests/ParamStateModelTests.cs — model test pattern (init-only setters, default values, property assignment)
    DG/tests/DG.Tests/ObjStateModelTests.cs — same pattern
    DG/src/DG.Core/Models/MetagraphHandle.cs — handle type to test (created in Task 1)
    DG/src/DG.Core/Models/OntologyClass.cs — model to test (created in Task 1)
    DG/src/DG.Grasshopper/Components/ConnectorComponent.cs — port contract (created in Task 2)
  </read_first>
  <action>
    Create 7 test files following the ParamStateModelTests.cs pattern (xUnit [Fact] methods, Assert-based verification):

    **MetagraphHandleModelTests.cs:**
    - Test: default ConnectionInfo is not null (new MetagraphHandle().ConnectionInfo is not null)
    - Test: properly sets ConnectionInfo properties via init
    - Test: wraps the same ConnectionInfo reference (reference equality check — handles are shallow wrappers)

    **OntographHandleModelTests.cs:** Same as MetagraphHandleModelTests but for OntographHandle
    **ValidGraphHandleModelTests.cs:** Same for ValidGraphHandle
    **SpecGraphHandleModelTests.cs:** Same for SpecGraphHandle

    **OntologyClassModelTests.cs:**
    - Test: creates with IRI and Label
    - Test: has empty string defaults
    - Test: IRI can be non-null after init

    **OntologyPropertyModelTests.cs:** Same as OntologyClassModelTests (same shape)

    **ConnectorComponentPortContractTests.cs:**
    These are behavioral/contract tests that verify the CONNECTOR's port names match the port-iri-map:
    
    - Test: ConnectorComponent has 6 inputs (via Component.Params.Input.Count or register-time assertion). Note: GH_Component's input/output params are populated after RegisterInputParams/RegisterOutputParams are called during construction. Since these run in a unit test context without GH document, verify by reading the component's constructor behavior.

    Approach: instantiate ConnectorComponent and verify:
    - `component.Params.Input[0].NickName` == "URI" (Neo4jURI shortcut)
    - `component.Params.Input[1].NickName` == "User" (Neo4jUser shortcut)
    - `component.Params.Input[2].NickName` == "Password" (Neo4jPassword shortcut)
    - `component.Params.Input[3].Name` == "PROJECT NAME" (full name, uppercase)
    - `component.Params.Input[4].Name` == "Database" (kept)
    - `component.Params.Input[5].Name` == "Connect" (unchanged)
    - `component.Params.Output[0].Name` == "Database" (renamed)
    - `component.Params.Output[1].Name` == "Project" (new)

    Note: Constructing a GH_Component outside a GH document may throw or behave differently. If construction fails, skip the parameter-count check and instead verify the RegisterInputParams/RegisterOutputParams method logic. Alternative: extract port-name constants from the component and test those. If the component cannot be constructed without a document, document this limitation and test the port names via reflection on the RegisterInputParams/RegisterOutputParams methods (call them with a mock GH_InputParamManager / GH_OutputParamManager).

    If GH component instantiation is impossible in pure unit test context, change the approach: test the ErrorMessageTemplates strings (ensure they compile and return expected substrings) and verify the 4 handle types and 2 ontology models compile with correct structure.

    All test files use: `using DG.Core.Models;` (or Core types) + xUnit `[Fact]` attributes.
  </action>
  <verify>
    <automated>dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~MetagraphHandleModel OR FullyQualifiedName~OntographHandleModel OR FullyQualifiedName~ValidGraphHandleModel OR FullyQualifiedName~SpecGraphHandleModel OR FullyQualifiedName~OntologyClass OR FullyQualifiedName~OntologyProperty OR FullyQualifiedName~ConnectorComponent" 2>&1</automated>
  </verify>
  <done>
    - `dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~HandleModel OR FullyQualifiedName~OntologyClass OR FullyQualifiedName~OntologyProperty OR FullyQualifiedName~ConnectorComponent"` passes
    - 7 test files exist with at least 2 [Fact] methods each
    - Handle type tests verify default values and init-only setters
    - Ontology model tests verify IRI and Label fields
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grasshopper canvas -> Neo4j driver | Credential inputs (Neo4jURI/Neo4jUser/Neo4jPassword) cross from untrusted user input to DB driver |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure | CONNECTOR | low | accept | Credentials input as GH text parameters (not hardcoded) — existing pattern unchanged |
| T-17-02 | Tampering | GRAPH DECONSTRUCT | low | mitigate | No Cypher queries in GRAPH DECONSTRUCT (pure passthrough) — no injection surface |
| T-17-SC | Tampering | NuGet restore (Neo4j.Driver) | high | mitigate | Neo4j.Driver 5.28.2 is an existing verified dependency (RESEARCH.md Package Legitimacy Audit — no new packages in this phase) |
</threat_model>

<verification>
- `dotnet build DG/DG.sln` succeeds (no errors)
- All 6 new model files compile
- PublicTypes.cs has 4 handle type wrappers
- DgIcons.cs has 3 new icon entries (GraphDeconstruct24, OntoGraph24, ValidationGraph24)
- ErrorMessageTemplates.cs has 4 new graph-access messages
- CONNECTOR has renamed ports per port-iri-map-V7.md
- GRAPH DECONSTRUCT is a pure synchronous #if GRASSHOPPER_SDK component
- 7 test files pass
</verification>

<success_criteria>
- GHGA-01: CONNECTOR outputs Database handle + Project passthrough; inputs aligned to Neo4jURI/Neo4jUser/Neo4jPassword/PROJECT NAME/Database/Connect
- GHGA-02: GRAPH DECONSTRUCT splits Database into 4 typed layer handles (Metagraph/Ontograph/ValidGraph/SpecGraph) — no async, no DB calls
- All downstream plans have handle types, models, icons, and messages available
</success_criteria>

<artifacts>
| Symbol | Kind | File |
|--------|------|------|
| `DG.Core.Models.MetagraphHandle` | class (NEW) | DG/src/DG.Core/Models/MetagraphHandle.cs |
| `DG.Core.Models.OntographHandle` | class (NEW) | DG/src/DG.Core/Models/OntographHandle.cs |
| `DG.Core.Models.ValidGraphHandle` | class (NEW) | DG/src/DG.Core/Models/ValidGraphHandle.cs |
| `DG.Core.Models.SpecGraphHandle` | class (NEW) | DG/src/DG.Core/Models/SpecGraphHandle.cs |
| `DG.Core.Models.OntologyClass` | class (NEW) | DG/src/DG.Core/Models/OntologyClass.cs |
| `DG.Core.Models.OntologyProperty` | class (NEW) | DG/src/DG.Core/Models/OntologyProperty.cs |
| `DG.MetagraphHandle` | class (NEW public wrapper) | DG/src/DG.Grasshopper/PublicTypes.cs |
| `DG.OntographHandle` | class (NEW public wrapper) | DG/src/DG.Grasshopper/PublicTypes.cs |
| `DG.ValidGraphHandle` | class (NEW public wrapper) | DG/src/DG.Grasshopper/PublicTypes.cs |
| `DG.SpecGraphHandle` | class (NEW public wrapper) | DG/src/DG.Grasshopper/PublicTypes.cs |
| `DgIcons.GraphDeconstruct24` | static Bitmap (NEW) | DG/src/DG.Grasshopper/DgIcons.cs |
| `DgIcons.OntoGraph24` | static Bitmap (NEW) | DG/src/DG.Grasshopper/DgIcons.cs |
| `DgIcons.ValidationGraph24` | static Bitmap (NEW) | DG/src/DG.Grasshopper/DgIcons.cs |
| `ErrorMessageTemplates.GraphDeconstructNoInput()` | static method (NEW) | DG/src/DG.Core/Services/ErrorMessageTemplates.cs |
| `ErrorMessageTemplates.GraphDeconstructCastFailed()` | static method (NEW) | DG/src/DG.Core/Services/ErrorMessageTemplates.cs |
| `ErrorMessageTemplates.ConnectorProjectPassthroughFailed()` | static method (NEW) | DG/src/DG.Core/Services/ErrorMessageTemplates.cs |
| `ErrorMessageTemplates.HandleTypeUnwrapped(string, string)` | static method (NEW) | DG/src/DG.Core/Services/ErrorMessageTemplates.cs |
| CONNECTOR port Neo4jURI renamed | port rename | DG/src/DG.Grasshopper/Components/ConnectorComponent.cs |
| CONNECTOR port Neo4jUser renamed | port rename | DG/src/DG.Grasshopper/Components/ConnectorComponent.cs |
| CONNECTOR port Neo4jPassword renamed | port rename | DG/src/DG.Grasshopper/Components/ConnectorComponent.cs |
| CONNECTOR port PROJECT NAME renamed | port rename | DG/src/DG.Grasshopper/Components/ConnectorComponent.cs |
| CONNECTOR output "Database" (was "Connection") | port rename | DG/src/DG.Grasshopper/Components/ConnectorComponent.cs |
| CONNECTOR output "Project" (new) | port add | DG/src/DG.Grasshopper/Components/ConnectorComponent.cs |
| `DG.Grasshopper.Components.GraphDeconstructComponent` | class (NEW GH component) | DG/src/DG.Grasshopper/Components/GraphDeconstructComponent.cs |
</artifacts>

<output>
Create `.planning/phases/17-graph-access-components/17-01-SUMMARY.md` when done
</output>
