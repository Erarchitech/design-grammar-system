---
phase: 822-owl-2-dl-reasoning-integration-reasoner-screen-wiring
plan: 03
type: execute
wave: 2
depends_on: ["822-01", "822-02"]
files_modified:
  - ui-v2/src/lib/reasonerApi.js
  - ui-v2/src/screens/ReasonerScreen.jsx
autonomous: true
requirements: [REAS-06]
must_haves:
  truths:
    - "Clicking Run check on the HermiT card issues POST /reasoner/consistency {project, engine:'hermit'} and renders one of four verdict states inside the HermiT card (D-01, D-05, D-08)"
    - "Inconsistent verdict shows the unsatisfiable-class count and a bulleted list of human labels (entry.label), framed as 'schema consistency', never 'valid/passed/compliant' (D-02, D-03)"
    - "A sidecar semantic-timeout body renders Unknown/Inconclusive (achromatic), never Consistent or Inconsistent (D-08, criterion 3)"
    - "A transport/hard error renders a distinct error line with inline Retry, not a verdict (D-09)"
    - "Cancel aborts the client fetch via AbortController → Cancelled state, kept distinct from Unknown (D-07)"
    - "Last checked <relative time> shows with each completed result and the last result persists across screen revisits within the session (D-11)"
    - "Only the integrated engine (HermiT) shows a Run button; Pellet keeps its Integration pending badge and exposes no run path (D-04, D-06)"
  artifacts:
    - "ui-v2/src/lib/reasonerApi.js — runConsistencyCheck(project,{signal})"
    - "ui-v2/src/screens/ReasonerScreen.jsx — run-state machine, Run/Cancel controls, spinner+elapsed, four verdict regions, D-09 error line, Last-checked, phantom-token fix"
  key_links:
    - "handleRunCheck branch (RESEARCH Pitfall 1, safety-critical): body.error==='timeout' && 'consistent' in body → Unknown (D-08); else !ok → error line (D-09); else consistent/inconsistent from body.consistent"
  prohibitions:
    - "The UI NEVER renders entry.iri or any raw full IRI to the architect — only entry.label (D-02)"
    - "runConsistencyCheck does NOT reuse getJson and does NOT throw on non-2xx — it returns {ok,status,body} so the caller branches on body shape (D-08/D-09)"
    - "No active staleness detection / 'ontology changed' banner is implemented — timestamp only (D-12)"
    - "No client-side response store or result reuse across runs — every Run is a fresh POST (D-10, criterion 4)"
    - "Phantom tokens --color-accent / --surface-focus are not left in any line this plan touches"
---

<objective>
Wire the HermiT card's live schema-consistency check into `ui-v2` — the user-facing half of REAS-06.
Add `runConsistencyCheck` to `reasonerApi.js`, then build the run-state machine and result rendering
in `ReasonerScreen.jsx`: Run/Cancel controls, spinner + live elapsed counter, four distinct verdict
states inside the HermiT card (Consistent / Inconsistent+labeled list / Unknown / Cancelled), a
distinct transport-error line, a "Last checked" timestamp, and the in-scope phantom-token cleanup.

Depends on Plan 01 (settled `{iri,label}` shape — the UI renders `entry.label`) and Plan 02 (HermiT
`integrated` status + reconciled proxy timeout so the D-08 semantic timeout can actually reach the UI
distinct from a transport error). Both contracts are fully known.

Purpose: Turn the persisted placeholder into a trustworthy consistency check the architect can never
mistake for a design-compliance verdict (criteria 1-4).
Output: A live, four-state HermiT card.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/822-owl-2-dl-reasoning-integration-reasoner-screen-wiring/822-CONTEXT.md
@.planning/phases/822-owl-2-dl-reasoning-integration-reasoner-screen-wiring/822-RESEARCH.md
@.planning/phases/822-owl-2-dl-reasoning-integration-reasoner-screen-wiring/822-PATTERNS.md
@.planning/phases/822-owl-2-dl-reasoning-integration-reasoner-screen-wiring/822-UI-SPEC.md
@ui-v2/src/lib/reasonerApi.js
@ui-v2/src/screens/ReasonerScreen.jsx
</context>

## Artifacts this phase produces (Plan 03)

Newly-created symbols — exclude these from source-drift/convergence checks:

- `ui-v2/src/lib/reasonerApi.js`: `runConsistencyCheck(project, { signal })` → `{ ok, status, body }` (does not throw)
- `ui-v2/src/screens/ReasonerScreen.jsx`: React state `runState`, `runError`, `runResult`, `lastCheckedAt`, `elapsedSec`; ref `abortRef`; handlers `handleRunCheck`, `handleCancel`; lifted `formatRelativeDate` helper
- Consumes (from Plan 01/02, not created here): the `unsatisfiable_classes: [{iri,label}]` shape, the sidecar semantic-timeout body `{consistent:null, error:"timeout", ...}`, the transport-error body `{detail:{error,hint,code}}`, and the HermiT registry `status:"integrated"`

<tasks>

<task type="auto">
  <name>Task 1: Add runConsistencyCheck to reasonerApi.js (no-throw, body-shape-branchable)</name>
  <files>ui-v2/src/lib/reasonerApi.js</files>
  <read_first>
    - ui-v2/src/lib/reasonerApi.js (the file being modified; existing `getJson` L5-18 and `selectReasoner` L27-44 PUT-shape analog; module header L1-4)
    - 822-PATTERNS.md § "ui-v2/src/lib/reasonerApi.js" (new-pattern skeleton; DO-NOT-reuse-getJson rationale)
    - 822-RESEARCH.md § "Pitfall 1" and § "Anti-Patterns to Avoid" (why getJson loses the D-08/D-09 distinction)
  </read_first>
  <action>
    Add an exported async function `runConsistencyCheck(project, { signal } = {})` that issues a
    single POST to `/reasoner/consistency` with a JSON content-type header and body carrying `project`
    and a hard-coded `engine` of `hermit` (D-05 — the run always forces HermiT), passing the provided
    AbortController `signal` through to fetch (D-07). It must parse the response body defensively
    (fall back to an empty object if the body is not JSON) and return an object with `ok`, `status`,
    and `body` — it must NOT throw on a non-2xx response and must NOT call or reuse `getJson`, because
    the 504 status is ambiguous between a genuine sidecar semantic timeout (D-08) and a data-service
    transport timeout (D-09); the caller branches on body shape, not status (RESEARCH Pitfall 1).
    Issue a fresh network request on every call and hold no client-side response store and reuse no
    prior result across calls (D-10 — freshness by construction). Add no caching directive and no
    result-reuse comment to this file. Follow the existing one-line-per-concern comment style above
    the export; leave `getJson`, `getReasonerSettings`, and `selectReasoner` unchanged.
  </action>
  <!-- planner-discipline-allow: Cache-Control -->
  <verify>
    <automated>cd ui-v2 && npm run build 2>&1 | tail -5 && grep -q "export async function runConsistencyCheck" src/lib/reasonerApi.js && grep -q 'engine' src/lib/reasonerApi.js && grep -q 'signal' src/lib/reasonerApi.js && ! grep -nE "Cache-Control|memoize" src/lib/reasonerApi.js && echo "PASS reasonerApi assertions"</automated>
  </verify>
  <acceptance_criteria>
    - `reasonerApi.js` contains `export async function runConsistencyCheck`
    - The function posts to `/reasoner/consistency`, sends `engine` as `hermit`, and forwards `signal`
    - The function returns `{ ok, status, body }` and does not `throw` on non-2xx (no reuse of `getJson`)
    - `grep -nE "Cache-Control|memoize" ui-v2/src/lib/reasonerApi.js` returns no matches (D-10 freshness)
    - `npm --prefix ui-v2 run build` exits 0 (JSX/JS compiles)
  </acceptance_criteria>
  <done>A dedicated, no-throw consistency-check client exists that lets the screen branch on body shape.</done>
</task>

<task type="auto">
  <name>Task 2: ReasonerScreen run-state machine, Run/Cancel, elapsed timer, Last-checked, phantom-token fix</name>
  <files>ui-v2/src/screens/ReasonerScreen.jsx</files>
  <read_first>
    - ui-v2/src/screens/ReasonerScreen.jsx (the file being modified; existing load/select state L9-60, active-effect L20-32, reasoner card block L164-235, phantom tokens L176/L179/L227, "Selected —…" line L222-232)
    - 822-PATTERNS.md § "ui-v2/src/screens/ReasonerScreen.jsx" (Analog 1 state pairing; Analog 2 elapsed-timer L527-534; Analog 3 formatRelativeDate L27-40) and § "No Analog Found" (AbortController skeleton)
    - 822-UI-SPEC.md § "Interaction States", § "Copywriting Contract", § "Color" (phantom-token correction: use --accent-selection / --accent-selection-bg)
    - ui-v2/src/components/display/SessionHistory.jsx L27-40 (formatRelativeDate — module-private, lift verbatim; do not write a second formatter)
  </read_first>
  <action>
    Add the run-state machine to `ReasonerScreen` alongside the existing load/select state (mirror the
    loading/loadError pairing). New state: `runState` (one of idle | running | consistent |
    inconsistent | unknown | cancelled | error), `runError` (string, D-09 transport/hard error, kept
    separate from verdicts), `runResult` (last full response body), `lastCheckedAt` (ISO string,
    D-11), `elapsedSec` (number); plus a `React.useRef(null)` `abortRef`.
    Lift the `formatRelativeDate` logic verbatim from SessionHistory.jsx as a module-level helper in
    this file (it is not exported there); do not author a divergent formatter.
    Implement `handleRunCheck`: create a new `AbortController`, store it in `abortRef`, set `runState`
    to running, clear `runError`, capture `t0 = Date.now()`, start a `setInterval` (~1s) that ticks
    `elapsedSec` to `Math.floor((Date.now()-t0)/1000)`, then `await runConsistencyCheck(project, {
    signal })`. Branch exactly per the key_link (RESEARCH Pitfall 1): if `body.error === "timeout" &&
    "consistent" in body` → set `runState` unknown (D-08); else if `!ok` → set `runError` from
    `body?.detail?.error` (fallback to a generic "Reasoner unavailable") and set `runState` error
    (D-09); else set `runResult` to body and `runState` to consistent when `body.consistent` is
    truthy else inconsistent. On consistent/inconsistent/unknown, set `lastCheckedAt` to the current
    ISO time (do NOT set it for cancelled/error). Wrap in try/catch: on `err.name === "AbortError"`
    set `runState` cancelled (D-07); on any other throw set `runError` and `runState` error. In a
    `finally`, always `clearInterval` and reset `abortRef.current` to null (mirror GraphScreen's
    clear-on-every-exit-path).
    Implement `handleCancel` to call `abortRef.current?.abort()` (client-side abort only; it does not
    stop the server run — acceptable per D-07).
    Do NOT reset `runState`/`runResult`/`lastCheckedAt` inside the `active`-keyed load effect
    (L20-32); the screen stays mounted across region switches (App wraps it in `<Layer active>`), so
    leaving that state intact makes the last verdict persist across revisits within the session (D-11).
    Do NOT add any ontology-change / staleness detection — the "Last checked" timestamp is the only
    freshness cue (D-12).
    Gate the run affordance on the engine being integrated: render the controls only when
    `r.status === "integrated"` (HermiT, per Plan 02), and keep the "Integration pending" badge branch
    (`r.status === "placeholder"`) untouched so Pellet keeps its badge and shows no Run button (D-04,
    D-06). Leave the existing `handleSelect`/PUT-settings selection behavior unchanged (D-06).
    In the integrated card's result region (replacing the "Selected — will be used when integration is
    complete" block at L222-232 for that card): when `runState` is idle show the framing disclaimer
    plus an enabled Run check button; when running show a spinner + "Running… {n}s" caption
    (`--text-muted`) with `elapsedSec`, a Cancel button, and a disabled Run button. Use
    `Button variant="outline" size="sm"` for both Run and Cancel (achromatic — Cancel is not
    destructive). Copy exactly per UI-SPEC Copywriting Contract; the framing disclaimer text is
    "Schema-consistency check — verifies the ontology's logical structure, not design compliance."
    Fix the phantom tokens on the lines this task edits: replace `--color-accent` (L176 selection
    ring, L227 selected-line text) with `--accent-selection`, and `--surface-focus` (L179) with
    `--accent-selection-bg`. Terminal verdict rendering (Consistent/Inconsistent/Unknown/Cancelled)
    and the D-09 error line are built in Task 3.
  </action>
  <verify>
    <automated>cd ui-v2 && npm run build 2>&1 | tail -6 && grep -q "AbortController" src/screens/ReasonerScreen.jsx && grep -q "runConsistencyCheck" src/screens/ReasonerScreen.jsx && grep -q 'status === "integrated"' src/screens/ReasonerScreen.jsx && ! grep -nE "\-\-color-accent|\-\-surface-focus" src/screens/ReasonerScreen.jsx && echo "PASS state-machine assertions"</automated>
  </verify>
  <acceptance_criteria>
    - `ReasonerScreen.jsx` imports/uses `runConsistencyCheck` and defines `handleRunCheck` + `handleCancel` using `AbortController` (via `abortRef`)
    - handleRunCheck branches: `body.error === "timeout" && "consistent" in body` → unknown; `!ok` → runError+error; else consistent/inconsistent (source assertion)
    - Run/Cancel controls render only when `r.status === "integrated"`; the `r.status === "placeholder"` "Integration pending" badge branch is intact (Pellet unaffected)
    - The `active`-keyed effect does not clear `runState`/`runResult`/`lastCheckedAt` (D-11 persistence)
    - `grep -nE "\-\-color-accent|\-\-surface-focus" ui-v2/src/screens/ReasonerScreen.jsx` returns no matches (phantom tokens removed)
    - No staleness/ontology-change detection code is added (D-12)
    - `npm --prefix ui-v2 run build` exits 0
  </acceptance_criteria>
  <done>The card runs a check with a live elapsed counter, can be cancelled, correctly classifies the response into a run-state, persists the last result across revisits, and uses only real tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Render the four verdict states + D-09 error line + Last-checked (D-02/D-03/D-08/D-09)</name>
  <files>ui-v2/src/screens/ReasonerScreen.jsx</files>
  <read_first>
    - ui-v2/src/screens/ReasonerScreen.jsx (the file being modified; existing error-line pattern L124-132 to mirror for D-09; the result region wired in Task 2)
    - 822-UI-SPEC.md § "Verdict State Matrix", § "Copywriting Contract", § "Color" (token per state; Signal Red reserved for Inconsistent + error only; Consistent is achromatic ink, Unknown mid-gray, Cancelled faint --ink-a56)
    - 822-PATTERNS.md § "Error-line pattern to reuse verbatim for D-09" and § "Button/Badge primitives" (Badge variants: solid=Consistent, signal=Inconsistent, outline=Unknown, none=Cancelled)
  </read_first>
  <action>
    In the integrated HermiT card's result region, render each terminal `runState` per the UI-SPEC
    Verdict State Matrix and Copywriting Contract. The framing disclaimer stays visible above every
    verdict (D-03).
    Consistent: ink text (`--color-ink`) + `Badge variant="solid"` "Consistent" + copy "✓ Schema
    consistent — no unsatisfiable classes found."
    Inconsistent: signal treatment (`--color-signal` text, `Badge variant="signal"` "Inconsistent",
    `--color-signal-soft` wash) + headline "Schema inconsistent — {N} unsatisfiable class{es}." where
    N = `runResult.unsatisfiable_classes.length` (singular "class" when N===1) + a bulleted list
    rendering each entry's `label` only. NEVER render `entry.iri` or any raw full IRI (D-02). Empty-list
    edge case: if `consistent` is false but the list is empty, show the count line only, no list.
    Unknown: mid-gray (`--text-muted`) + `Badge variant="outline"` "Inconclusive" + copy "– Inconclusive
    — the reasoner timed out before finishing. Try again." Never green/red (D-08, criterion 3).
    Cancelled: faint (`--ink-a56`) + no badge + copy "× Run cancelled — no result." Quieter than
    Unknown, and never read as a verdict (D-07).
    Error (D-09): a distinct error line mirroring the existing load-error pattern (L124-132) — copy
    "Reasoner unavailable · {runError}" with an inline Retry span that re-invokes `handleRunCheck`;
    use `--color-signal-ink` (UI-SPEC destructive role). This line is never styled as a verdict and is
    never folded into Unknown.
    Last checked: for consistent/inconsistent/unknown, render a 12px `--text-muted` caption "Last
    checked {formatRelativeDate(lastCheckedAt)}" (D-11). New elements sit on the 4px grid per UI-SPEC
    Spacing (16px above the control row, 12px result-region stack, 8px list-row gap). Verdict copy must
    say "schema consistent/inconsistent", never "valid/passed/compliant" (D-03).
  </action>
  <verify>
    <automated>cd ui-v2 && npm run build 2>&1 | tail -6 && grep -q "Schema inconsistent" src/screens/ReasonerScreen.jsx && grep -q "Inconclusive" src/screens/ReasonerScreen.jsx && grep -q "Run cancelled" src/screens/ReasonerScreen.jsx && grep -q "Reasoner unavailable" src/screens/ReasonerScreen.jsx && grep -q "Last checked" src/screens/ReasonerScreen.jsx && ! grep -q "\.iri" src/screens/ReasonerScreen.jsx && echo "PASS verdict-render assertions"</automated>
  </verify>
  <acceptance_criteria>
    - Four distinct verdict regions render with the UI-SPEC tokens/badges/copy: Consistent (ink, solid badge, ✓), Inconsistent (signal, signal badge, count + label list), Unknown (mid-gray, outline badge "Inconclusive"), Cancelled (faint --ink-a56, no badge)
    - Inconsistent list renders only `entry.label`; `grep -q "\.iri" ui-v2/src/screens/ReasonerScreen.jsx` returns nothing (no raw IRI rendered, D-02)
    - Inconsistent headline shows the unsatisfiable-class count with correct singular/plural
    - D-09 error line ("Reasoner unavailable · …" + Retry, `--color-signal-ink`) is distinct from the Unknown verdict
    - "Last checked" relative timestamp renders for completed verdicts (D-11); copy uses "schema consistent/inconsistent" never "valid/passed/compliant" (D-03)
    - `npm --prefix ui-v2 run build` exits 0
  </acceptance_criteria>
  <done>All four verdicts + a distinct transport-error line render inside the HermiT card with correct tokens, copy, and label-only class lists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → data-service `/reasoner/consistency` (via nginx `/reasoner/` proxy) | Client-supplied `project`; same trust level as every existing ui-v2→data-service call — no new authz boundary (ASVS V4 n/a) |
| response body → rendered DOM | Verdict labels + error detail text are rendered to the architect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-822-05 | Information Disclosure | verdict + error rendering | low | mitigate | The UI renders only `entry.label` (never the raw IRI, D-02) and only the pre-written `detail.error` message (never a stack trace); Unknown/Cancelled/Error are copy-only |
| T-822-06 | Spoofing/Tampering | client-supplied `project` in the POST | low | accept | Matches every existing ui-v2→data-service call; `project` is parameterized (`$project`) server-side; no new authorization boundary introduced (RESEARCH Security Domain) |
| T-822-SC | Tampering | npm installs | low | accept | No package added — `AbortController`/`fetch` are browser-native (RESEARCH Standard Stack: "no npm install needed"); no `[ASSUMED]`/`[SUS]` package to gate |
</threat_model>

<verification>
- `npm --prefix ui-v2 run build` exits 0 (JSX compiles)
- Source assertions in each task's `<automated>` pass (identifiers, copy strings, absence of phantom tokens, absence of raw `.iri` render, D-10 freshness grep)
- Behavioral proof of the four verdict states + Cancel + error line is the manual UAT in Plan 04 (no frontend test infra in ui-v2 — RESEARCH Validation Architecture)
</verification>

<success_criteria>
- REAS-06 criteria 1-4 frontend half: a real run executes (crit 1), a clear "schema consistency" pass/fail + unsat count/labels renders (crit 2), a timeout is a distinct Unknown that never blocks or silently passes/fails (crit 3), and every run is a fresh POST (crit 4)
- D-01/D-04/D-05/D-06/D-07/D-08/D-09/D-11/D-12/D-13 realized in the UI
- Only real design tokens used; no raw IRI ever shown
</success_criteria>

<output>
Create `.planning/phases/822-owl-2-dl-reasoning-integration-reasoner-screen-wiring/822-03-SUMMARY.md` when done
</output>
