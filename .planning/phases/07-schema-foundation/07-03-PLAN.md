---
phase: 07-schema-foundation
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - cypher_template.txt
  - DG/src/DG.Core/Data/Neo4jRuleRepository.cs
  - migrations/2026-06-23_var_project_merge_key.cypher
  - DG/tests/DG.Tests/Neo4jRuleRepositoryVariableKindTests.cs
autonomous: true
requirements: [SCHM-01, SCHM-02]

must_haves:
  truths:
    - "MATCH (v:Var) WHERE NOT EXISTS(v.project) RETURN count(v) returns 0 after the migration script runs against the live Neo4j database (Phase 7 ROADMAP success criterion 2)"
    - "cypher_template.txt's Var MERGE pattern includes project in the MERGE key itself, not only in a subsequent SET"
    - "Neo4jRuleRepository.PopulateVariables populates Variable.Kind via VariableTypeInferrer.Infer for every variable on every loaded rule"
    - "The new :DesignState node MERGE pattern in cypher_template.txt includes project in the MERGE key from the start (does not repeat the Var bug on the new node type)"
  artifacts:
    - path: "cypher_template.txt"
      provides: "Fixed Var MERGE key (name+project) and new :DesignState{kind} MERGE block with project in the MERGE key"
      contains: "MERGE (vEntity:Var {name:.*project:"
    - path: "migrations/2026-06-23_var_project_merge_key.cypher"
      provides: "One-time backfill sweeping orphaned Var nodes missing project, using coalesce(v.project, 'default-project')"
      contains: "coalesce(v.project, 'default-project')"
    - path: "DG/src/DG.Core/Data/Neo4jRuleRepository.cs"
      provides: "PopulateVariables wired to VariableTypeInferrer.Infer, setting Variable.Kind on every loaded Variable"
      exports: ["Neo4jRuleRepository"]
  key_links:
    - from: "DG/src/DG.Core/Data/Neo4jRuleRepository.cs"
      to: "DG/src/DG.Core/Parsing/VariableTypeInferrer.cs"
      via: "PopulateVariables calls VariableTypeInferrer.Infer(rule, variableName) and assigns the result to Variable.Kind"
      pattern: "VariableTypeInferrer\\.Infer"
    - from: "cypher_template.txt"
      to: "migrations/2026-06-23_var_project_merge_key.cypher"
      via: "migration script back-fills existing Var nodes to match the new fixed MERGE key shape before the template change takes effect on next ingest"
      pattern: "coalesce\\(v\\.project"
---

<objective>
Fix the latent v2.0 cross-project Var merge-key bug (SCHM-02, PITFALLS.md P4) in `cypher_template.txt`, ship the one-time migration script that backfills `project` onto existing orphaned `Var` nodes, add the new `:DesignState{kind}` single-label MERGE pattern (SCHM-01) to the same template, and wire `Neo4jRuleRepository.PopulateVariables` to call `VariableTypeInferrer.Infer` (from 07-01-PLAN.md) so `Variable.Kind` is populated on every rule read from Neo4j.

Purpose: This is the Phase 7 prerequisite fix explicitly called out in CONTEXT.md and research SUMMARY.md Q5 — cross-rule Object variable identity in Phases 8+ is built on top of `Var` node identity, so the merge-key isolation bug must be eliminated here, before any cross-rule feature exists. The `:DesignState{kind}` Cypher block establishes the exact node shape that Phase 9-11 components will MERGE into Neo4j.

Output: Fixed `cypher_template.txt` (Var merge key + DesignState block + schema version stamp), `migrations/2026-06-23_var_project_merge_key.cypher` migration script, `Neo4jRuleRepository.cs` wired to `VariableTypeInferrer`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-schema-foundation/07-CONTEXT.md
@.planning/phases/07-schema-foundation/07-PATTERNS.md
@.planning/research/PITFALLS.md

@cypher_template.txt
@DG/src/DG.Core/Data/Neo4jRuleRepository.cs
@DG/src/DG.Core/Parsing/VariableTypeInferrer.cs
@DG/src/DG.Core/Models/Variable.cs
@DG/src/DG.Core/Models/VariableKind.cs

.planning/phases/07-schema-foundation/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Var merge key and add DesignState block in cypher_template.txt, write migration script</name>
  <files>cypher_template.txt, migrations/2026-06-23_var_project_merge_key.cypher</files>
  <read_first>
    cypher_template.txt (full file — the buggy Var MERGE block at lines 108-115, pattern is MERGE-open-paren-vEntity-colon-Var-brace-name-colon-quote-question-ENTITY_VAR-placeholder followed by a separate SET line setting project and graph; the `Rule.kind` single-label-plus-property pattern at lines 101-106 to mirror for the new `:DesignState{kind}` block; the `GRAPH SCHEMA` documentation section at lines 18-46 that must also be updated to list the new `DesignState` node and corrected `Var` key)
    .planning/phases/07-schema-foundation/07-CONTEXT.md (Var Merge Key Fix section — exact fix: `MERGE (v:Var {name: ..., project: ...})`; migration: re-ingest re-creates Var nodes correctly, remaining untagged nodes swept via `coalesce(v.project, 'default-project')`, verified via `MATCH (v:Var) WHERE NOT EXISTS(v.project) RETURN count(v)` = 0)
    .planning/research/PITFALLS.md (Pitfall 4 — full bug mechanism and exact recovery Cypher: `MATCH (v:Var) SET v.project = coalesce(v.project, 'default-project')`; Pitfall 9 — schema version stamp requirement: add `# SCHEMA VERSION: v3.1` comment to cypher_template.txt and mirror it in the n8n workflow JSON, verified by the propagation plan 07-04)
    .planning/phases/07-schema-foundation/07-PATTERNS.md (cypher_template.txt section — exact before/after MERGE block diff already worked out; "Reference for kind-discriminated single-label pattern" — Rule node block to mirror for DesignState)
  </read_first>
  <action>
    In `cypher_template.txt`, change the `// 3. Metagraph — variables, literals` block: the `vEntity` MERGE currently opens `MERGE (vEntity:Var {name: '?` followed by an ENTITY_VAR placeholder token and closing `'})`, then a separate `SET vEntity.project = ...` line. Move the project placeholder assignment into the MERGE braces alongside `name` (so the MERGE pattern becomes `MERGE (vEntity:Var {name: '?` + ENTITY_VAR-placeholder + `', project: '` + PROJECT-placeholder + `'})`), leaving only `vEntity.graph = 'Metagraph'` in the subsequent SET clause. Apply the identical transformation to the `vValue` Var MERGE immediately below it (same restructuring: project placeholder moves from SET into the MERGE braces, graph stays in SET). Update the `GRAPH SCHEMA` documentation section's `Var` entry (around line 41-42) to document the key as name plus project instead of name alone, making the documented key match the fixed MERGE pattern. Add a new `DesignState` entry to the `GRAPH SCHEMA` section, structurally parallel to the existing `Rule` entry: key is StateId plus project; props are kind (DefState or ObjectState), project, graph equals Metagraph. Add a new `CYPHER TEMPLATE BLOCK` section (after the existing `// 2. Metagraph — rule node` block, before `// 3. Metagraph — variables, literals`) showing the DesignState MERGE pattern with both StateId and project inside the MERGE braces, and kind plus graph set afterward in a SET clause — project is in the MERGE key from the start (do not repeat the Var bug on this new node type, per CONTEXT.md and 07-PATTERNS.md). Add a schema version stamp comment near the top of the file (after the `TASK` header, before `OUTPUT RULES`) reading exactly: SCHEMA VERSION: v3.1 — this is the anchor the schema-propagation plan (07-04) will mirror into the n8n workflow JSON per Pitfall 9. Do NOT modify the `Class`/`Atom`/`DatatypeProperty`/`ObjectProperty`/`Builtin`/`Literal` MERGE blocks — the merge-key fix is explicitly scoped to `Var` only and the new `DesignState` block, per CONTEXT.md's "Phase 7 stays scoped strictly to REQUIREMENTS.md VTYP/SCHM items."

    Create `migrations/2026-06-23_var_project_merge_key.cypher` as a standalone Cypher script (new `migrations/` directory — first migration script precedent in the repo, per CONTEXT.md's discretion note). The script must: (1) backfill missing `project` on existing `Var` nodes via `MATCH (v:Var) WHERE NOT EXISTS(v.project) SET v.project = coalesce(v.project, 'default-project')`; (2) include a verification query as a trailing comment block showing the exact check to run after applying: `// Verify: MATCH (v:Var) WHERE NOT EXISTS(v.project) RETURN count(v) — expect 0`; (3) include a header comment documenting the migration's purpose (fixes PITFALLS.md P4 cross-project Var collision, SCHM-02), the expected execution method (`cypher-shell` or Neo4j Browser — this is additive documentation, no app code invokes this script automatically), and a one-line warning that running this script does NOT retroactively split already-merged cross-project Var nodes apart — it only tags currently-untagged orphans; nodes already incorrectly shared between two projects (the actual collision case) require manual identification via `MATCH (v:Var) WITH v.name AS name, collect(DISTINCT v.project) AS projects WHERE size(projects) > 1 RETURN name, projects` and are NOT auto-resolved by this script (the underlying bug had no audit trail to reconstruct which project a contaminated node "should" belong to — document this as a known migration-window limitation in the script's header comment, consistent with the threat_model's data-loss assessment below).
  </action>
  <verify>
    <automated>grep -c "MERGE (vEntity:Var {name:.*project:" cypher_template.txt</automated>
  </verify>
  <done>
    `cypher_template.txt`'s vEntity and vValue Var MERGE lines each have `project:` inside the same `{...}` brace group as `name:` — verify via `grep -c "MERGE (vEntity:Var {name:.*project:" cypher_template.txt` and `grep -c "MERGE (vValue:Var {name:.*project:" cypher_template.txt` each return 1. `cypher_template.txt` contains a `DesignState` MERGE block with `project` inside the `{...}` MERGE braces — verify via `grep -c "MERGE (ds:DesignState {StateId:.*project:" cypher_template.txt` returns 1. `cypher_template.txt` contains the literal string `SCHEMA VERSION: v3.1`. `migrations/2026-06-23_var_project_merge_key.cypher` exists and contains `coalesce(v.project, 'default-project')` and the verification query comment `MATCH (v:Var) WHERE NOT EXISTS(v.project) RETURN count(v)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire PopulateVariables to VariableTypeInferrer</name>
  <files>DG/src/DG.Core/Data/Neo4jRuleRepository.cs, DG/tests/DG.Tests/Neo4jRuleRepositoryVariableKindTests.cs</files>
  <read_first>
    DG/src/DG.Core/Data/Neo4jRuleRepository.cs (lines 212-256, `PopulateVariables` — the existing final loop `foreach (var variableName in names.OrderBy(...)) { rule.Variables.Add(new Variable { Name = variableName }); }` is the exact line to modify; also the `AtomsQuery` constant at lines 37-55 — confirm this query already filters by `project:$project` on both `Rule` and `Atom` matches, meaning no query-string change is needed here for SCHM-02 since `Neo4jRuleRepository` already scopes by project at the application layer even though the underlying `Var` MERGE key was unscoped — read both to confirm this distinction before editing)
    DG/src/DG.Core/Parsing/VariableTypeInferrer.cs (from 07-01-PLAN.md — `public static VariableKind? Infer(Rule rule, string variableName)` signature)
    DG/src/DG.Core/Models/Variable.cs (from 07-01-PLAN.md — `Kind` is `VariableKind?`, `init`-only)
    DG/tests/DG.Tests/SwrlRuleParserTests.cs (existing xUnit test conventions to mirror for the new test file)
  </read_first>
  <behavior>
    - Test 1: A `Rule` with a body `ClassAtom(?b)` and `DataPropertyAtom(?b,?h)` plus a head atom — after `PopulateVariables` runs (via `GetRulesAsync` or a directly-testable extraction of the variable-population logic), `rule.Variables.Single(v => v.Name == "?b").Kind` equals `VariableKind.Object`.
    - Test 2: The same rule's `?h` variable (appearing only at pos-2 of the DataPropertyAtom) has `Kind` equal to `VariableKind.Property`.
    - Test 3: A variable appearing only inside a `BuiltinAtom`'s args has `Kind` equal to `null` (not silently defaulted to Property) — confirms `PopulateVariables` propagates `VariableTypeInferrer.Infer`'s `null` return rather than coalescing it.
  </behavior>
  <action>
    In `Neo4jRuleRepository.cs`'s `PopulateVariables` method, change the final loop from `rule.Variables.Add(new Variable { Name = variableName })` to call `VariableTypeInferrer.Infer(rule, variableName)` and assign the result to a new `Variable { Name = variableName, Kind = VariableTypeInferrer.Infer(rule, variableName) }` — the `Infer` call must happen after `rule.BodyAtoms`/`rule.HeadAtoms` are fully populated (they already are, by this point in the method, since the atom-population branch runs earlier in the same loop iteration). Add `using DG.Core.Parsing;` if not already present (the file already has `using DG.Core.Parsing;` from the existing `SwrlRuleParser.Parse` call — confirm and reuse, do not duplicate the using directive). Do not change `AtomsQuery` or `RulesQuery` query strings in this task — both already filter by `project:$project` at the `Rule`/`Atom` match level (this is the application-layer scoping that exists independently of the `Var` node's own merge-key bug; the bug is specifically that the underlying `Var` node can be shared across projects in storage, not that this repository's read queries fail to filter — confirm this distinction in the read_first step before making any query-string edits here).
  </action>
  <verify>
    <automated>dotnet test DG/tests/DG.Tests/DG.Tests.csproj --filter Neo4jRuleRepositoryVariableKindTests</automated>
  </verify>
  <done>
    All 3 behavior cases pass. `grep -c "VariableTypeInferrer.Infer" DG/src/DG.Core/Data/Neo4jRuleRepository.cs` returns at least 1. `dotnet build DG/src/DG.Core/DG.Core.csproj` succeeds with zero errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live Neo4j database ↔ migration script | The migration script (`migrations/2026-06-23_var_project_merge_key.cypher`) operates directly on production/live graph data via `cypher-shell` or Neo4j Browser — this is the highest-risk artifact in this plan |
| cypher_template.txt change ↔ in-flight n8n ingest during migration window | Between the template fix landing and the migration script running, newly-ingested rules use the fixed MERGE key while pre-existing Var nodes may still be untagged — a transient inconsistency window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-07-07 | Tampering / Data Loss | migrations/2026-06-23_var_project_merge_key.cypher | mitigate | Script only SETs a missing property via `coalesce()` — it never DELETEs or merges/dedupes nodes, so it cannot destroy existing Var nodes or their ARG relationships; the script's header comment explicitly documents that it does NOT attempt to split already-cross-contaminated nodes (no automatic remediation of the actual collision case, only of untagged orphans) — this is a conscious scope limit, not an oversight |
| T-07-08 | Elevation of Privilege / Isolation Violation | Var merge-key fix during migration window | mitigate | The fix is applied to `cypher_template.txt` (affects only NEW ingests going forward) independently of the migration script (affects EXISTING nodes) — until the migration script runs, old untagged Var nodes remain queryable across projects exactly as before (no regression), and new ingests immediately get correctly-scoped Var nodes under the fixed MERGE key; the isolation gap is not widened by this change, only progressively closed |
| T-07-09 | Information Disclosure | `MATCH (v:Var) WITH v.name AS name, collect(DISTINCT v.project) AS projects WHERE size(projects) > 1 RETURN name, projects` (audit query in migration script comments) | accept | This read-only audit query is documentation only (a comment in the migration script, not auto-executed) — surfaces which Var names are currently shared cross-project so an operator can manually investigate; no automated cross-project data is returned to any UI or API consumer by this plan's code |
| T-07-10 | Tampering | npm/pip/cargo installs | accept | This plan introduces no new package-manager installs — all work is in-repo C#/Cypher; no `T-07-SC` package legitimacy checkpoint is required |
</threat_model>

<verification>
Run `dotnet build DG/src/DG.Core/DG.Core.csproj` — zero errors.
Run `dotnet test DG/tests/DG.Tests/DG.Tests.csproj --filter Neo4jRuleRepositoryVariableKindTests` — all tests pass.
Inspect `cypher_template.txt` for the corrected Var MERGE key and new DesignState block via the grep checks in Task 1's acceptance criteria.
Manual (deferred to live-environment verification, not this plan's automated gate): run `migrations/2026-06-23_var_project_merge_key.cypher` against the live Neo4j instance, then `MATCH (v:Var) WHERE NOT EXISTS(v.project) RETURN count(v)` and confirm it returns 0 — this is Phase 7 ROADMAP success criterion 2, executed during phase verification (not buildable as an automated CI check since it requires a running Docker Neo4j instance).
</verification>

<success_criteria>
`cypher_template.txt`'s Var MERGE key includes `project`; the new `:DesignState{kind}` MERGE block also includes `project` in its MERGE key from the start; a migration script exists to backfill orphaned Var nodes; `Neo4jRuleRepository.PopulateVariables` populates `Variable.Kind` via `VariableTypeInferrer.Infer` for every rule read from Neo4j, with `null` propagated correctly for Builtin-only variables (not silently coerced to Property).
</success_criteria>

<artifacts_produced>
- `cypher_template.txt` — fixed `Var` MERGE key (`name` + `project`), new `:DesignState{kind}` MERGE block, `SCHEMA VERSION: v3.1` stamp
- `migrations/2026-06-23_var_project_merge_key.cypher` — new file, one-time Var-project backfill script with embedded verification query
- `DG.Core.Data.Neo4jRuleRepository.PopulateVariables` — modified: now calls `VariableTypeInferrer.Infer` and sets `Variable.Kind` on every populated `Variable`
</artifacts_produced>

<output>
Create `.planning/phases/07-schema-foundation/07-03-SUMMARY.md` when done
</output>
