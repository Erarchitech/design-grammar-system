---
phase: 07-schema-foundation
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - training/dataset_schema.json
  - training/updated_cypher_reference_examples_v3.cypher
  - n8n/workflows/rules-to-metagraph.json
  - graph-viewer/config.template.js
  - data-service/app.py
autonomous: true
requirements: [SCHM-04, SCHM-06]

must_haves:
  truths:
    - "dataset_schema.json's Var node key block documents project alongside name, matching the cypher_template.txt fix from 07-03"
    - "dataset_schema.json has a new DesignState node entry with kind enum values DefState and ObjectState"
    - "n8n/workflows/rules-to-metagraph.json's embedded LLM prompt (Build LLM Prompt function node) Var MERGE example and SCHEMA CONSTRAINTS text both reflect the project-scoped Var merge key, not the old name-only pattern"
    - "graph-viewer/config.template.js's DesignState labels entry sets group: \"kind\" and visGroups defines DefState and ObjectState as separate keys with different color.background values, so NeoVis renders DefState and ObjectState nodes in visibly distinct colors from each other, not as an unlabelled grey blob and not as one shared color (Phase 7 ROADMAP success criterion 4)"
    - "data-service/app.py's statePayloadJson-adjacent comments/docstrings reference the DS_/OS_ prefix vocabulary consistently, with no behavior change to _project_state_summary in this phase"
  artifacts:
    - path: "training/dataset_schema.json"
      provides: "DesignState node schema entry + corrected Var key block, single source of truth for n8n prompt and validation"
      contains: "\"label\": \"DesignState\""
    - path: "n8n/workflows/rules-to-metagraph.json"
      provides: "LLM system prompt with project-scoped Var MERGE few-shot example and schema version stamp matching cypher_template.txt"
      contains: "project: '"
    - path: "graph-viewer/config.template.js"
      provides: "NeoVis labels entry with group: \"kind\" for DesignState, plus visGroups entries for DefState and ObjectState (kind property values) with distinct colors"
      contains: "group: \"kind\""
  key_links:
    - from: "n8n/workflows/rules-to-metagraph.json"
      to: "cypher_template.txt"
      via: "Build LLM Prompt function node's embedded few-shot Cypher and SCHEMA CONSTRAINTS text must textually match cypher_template.txt's fixed Var MERGE pattern and SCHEMA VERSION stamp (Pitfall 9 — n8n prompt desync)"
      pattern: "SCHEMA VERSION"
    - from: "graph-viewer/config.template.js"
      to: "cypher_template.txt"
      via: "DesignState label (with group: \"kind\") and DefState/ObjectState visGroups entries must exist before any :DesignState node is ever written to Neo4j, or NeoVis renders it as an unlabelled grey blob (Pitfall 3)"
      pattern: "DefState:|ObjectState:"
---

<objective>
Propagate the schema decisions locked in 07-01/07-02/07-03 (VariableKind, DefState/ObjectState/ObjectInstance, the Var merge-key fix, the `:DesignState{kind}` Cypher pattern) across the remaining schema-propagation surfaces named in CLAUDE.md's "Schema Change Propagation" section: `training/dataset_schema.json`, `training/updated_cypher_reference_examples_v3.cypher`, `n8n/workflows/rules-to-metagraph.json`, `graph-viewer/config.template.js`, and `data-service/app.py`. Per SCHM-04 and SCHM-06.

SCHM-06 scope note: the OWL-level enum-discriminator pattern (ObjectProperty + NamedIndividuals in dedicated enum classes) already exists in `ontology/DesignGrammar-V6.owl` (confirmed at v6.1, ahead of this v3.0 milestone — it already has `VariableScopeValue`, `ValidationStatusValue`, `DesignStateParameterTypeValue` enum classes and `ObjectInstance`/`GeoRef`/`IdRef` classes). That `.owl` file is a standalone documentation/ontology-modeling artifact with no runtime consumer (no Python/C#/JS/n8n code imports it) and is explicitly NOT one of the six schema-propagation surfaces in CLAUDE.md. This plan's SCHM-06 obligation is satisfied by making `dataset_schema.json`'s new `DesignState.kind` field documentation consistent with that already-established enum-discriminator pattern (Task 1) — do NOT edit `ontology/DesignGrammar-V6.owl` in this plan; it is out of scope.

Purpose: PITFALLS.md documents two failure modes this plan exists to prevent: P3 (NeoVis renders new node labels as unlabelled grey blobs if `config.template.js` is not updated in the same change) and P9 (the n8n-embedded LLM prompt silently generates Cypher against the OLD schema if `cypher_template.txt` changes are not mirrored into the workflow JSON). Both are explicit Phase 7 ROADMAP success criteria (#3 — six surfaces consistent on DS_/OS_ prefixes and the single-label pattern; #4 — NeoVis renders DefState/ObjectState with distinct colors via one `:DesignState` label config entry).

Output: `dataset_schema.json` with the new `DesignState` node schema and corrected `Var` key; `updated_cypher_reference_examples_v3.cypher` with the corrected Var MERGE pattern; `rules-to-metagraph.json`'s embedded LLM prompt updated to match `cypher_template.txt`; `config.template.js` with `DesignState` NeoVis labels/visGroups; `data-service/app.py` vocabulary alignment (no behavior change).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-schema-foundation/07-CONTEXT.md
@.planning/phases/07-schema-foundation/07-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md

@cypher_template.txt
@training/dataset_schema.json
@graph-viewer/config.template.js

.planning/phases/07-schema-foundation/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dataset_schema.json and training cypher reference examples</name>
  <files>training/dataset_schema.json, training/updated_cypher_reference_examples_v3.cypher</files>
  <read_first>
    training/dataset_schema.json (full file — the existing `Var` node block at lines 86-95 documents key as `{ "name": "?<varName>" }` only; the `rule_node` block at lines 24-35 is the structural template to mirror for the new `DesignState` entry: a top-level `"key"` + `"props"` shape parallel to `rule_node`, not nested inside the `nodes` array's generic shape unless that is a closer structural fit — inspect both `rule_node` (top-level, singular) and `nodes[]` array entries (list of node-type templates) before deciding where `DesignState` belongs; `DesignState` is a generic reusable node-type template like `Var`/`Literal`/`Atom`, so it belongs in the `nodes[]` array, not as a `rule_node`-style singular top-level key)
    cypher_template.txt (after 07-03's edits — confirm the exact final Var MERGE shape and the new DesignState MERGE block before mirroring it into JSON form)
    training/updated_cypher_reference_examples_v3.cypher (full file — contains the same buggy `MERGE (vb:Var {name: '?b'}) SET vb.project = 'default-project', ...` pattern duplicated from the original Example 1 walkthrough; this file is a flat list of example Cypher statements, not JSON, so the fix is a direct text edit of every `MERGE (...:Var {name: ...})` occurrence in the file)
  </read_first>
  <action>
    In `training/dataset_schema.json`, update the existing `Var` node entry's `"key"` block (currently `{ "name": "?<varName>" }`) to `{ "name": "?<varName>", "project": "default-project" }`, matching the corrected `cypher_template.txt` MERGE key from 07-03. Add a new entry to the `metagraph.nodes[]` array, structurally parallel to the existing `Var`/`Literal`/`Atom` entries: `"label": "DesignState"`, `"key": { "StateId": "DS_<hash> | OS_<hash>", "project": "default-project" }`, `"props": { "kind": "DefState | ObjectState", "project": "default-project", "graph": "Metagraph" }`. Bump the top-level `"version"` field inside `metadata` (currently `"v3"`) to `"v3.1"` to match the `SCHEMA VERSION: v3.1` stamp added to `cypher_template.txt` in 07-03 — this is the schema version alignment Pitfall 9 requires.

    In `training/updated_cypher_reference_examples_v3.cypher`, find every `MERGE (<var>:Var {name: '?<name>'})` occurrence (there are at least 2 in the file's Example 1 walkthrough, lines 30-36 as read) and apply the identical restructuring from 07-03: move the `project: 'default-project'` assignment from the subsequent `SET` line into the `MERGE` braces alongside `name`, leaving only `graph = 'Metagraph'` in the SET clause. Do not alter any other node type's MERGE pattern in this file (Class, DatatypeProperty, Builtin, Atom, Literal all keep their existing MERGE-then-SET shape, matching the explicit Var-only scope from 07-03).
  </action>
  <verify>
    <automated>grep -c "DesignState" training/dataset_schema.json</automated>
  </verify>
  <done>
    `training/dataset_schema.json` contains a `"label": "DesignState"` node entry with `kind` in its `props` documented as `DefState | ObjectState`, and the `Var` entry's `"key"` block now includes `"project"`. The `metadata.version` field is `"v3.1"`. `training/updated_cypher_reference_examples_v3.cypher` has zero remaining occurrences of a Var MERGE with `name` alone in the braces — verify via `grep -c "MERGE (v.:Var {name: '?[a-zA-Z]*'})" training/updated_cypher_reference_examples_v3.cypher` returns 0 (the old bare-name-only MERGE pattern is fully replaced).
  </done>
</task>

<task type="auto">
  <name>Task 2: n8n LLM prompt sync, NeoVis config, data-service vocabulary alignment</name>
  <files>n8n/workflows/rules-to-metagraph.json, graph-viewer/config.template.js, data-service/app.py</files>
  <read_first>
    n8n/workflows/rules-to-metagraph.json (the "Build LLM Prompt" function node, `parameters.functionCode` — a ~12KB embedded JavaScript string containing both a `fewShot` array with a hardcoded Cypher walkthrough using `MERGE (vb:Var {name: '?b'}) SET vb.project = '" + project + "', ...` and a `SCHEMA CONSTRAINTS (v3)` text block with `- Var key: name (?<varName>).` — both locations embed the same buggy Var pattern that cypher_template.txt had before 07-03's fix and must be edited in lockstep; this is a JSON file with the prompt text living inside a JS string literal, so edits must preserve valid JSON/JS string escaping)
    graph-viewer/config.template.js (full file — `labels` map at lines 17-31 and `visGroups` map at lines 41-55; every existing entry keys `labels.<Label>` by Neo4j label name with no `group:` directive, and keys `visGroups` directly by that same label name, e.g. `Var: { label: "name" }` / `Var: { color: {...} }` — this is why all `Rule` nodes currently render one color regardless of `Rule.kind`, an existing out-of-scope gap; do NOT copy that no-`group:` pattern for `DesignState`)
    .planning/research/PITFALLS.md (Pitfall 3 — NeoVis.js's `labels.<Label>.group` config key accepts any node property name, not just `"community"` — confirmed via neo4j-contrib/neovis.js README example `labels: { Character: { label: "name", group: "community" } }`, where `group` may be set to any property. Setting `group: "kind"` on the `DesignState` label entry makes NeoVis key `visGroups` by the VALUES of the `kind` property — `DefState` and `ObjectState` — instead of by the label name. This is the mechanism to use; it is not a fallback, it exists and is required.)
    data-service/app.py (lines 161, 415, 432, 517-578, 864 — `statePayloadJson` field, `_project_state_summary()` function, and its call sites; CONTEXT.md confirms "full extension lands in Phase 11, but the node/prefix vocabulary must agree starting here" — this task only touches comments/docstrings referencing node vocabulary, never `_project_state_summary`'s actual logic or the `statePayloadJson` field's type)
  </read_first>
  <action>
    In `n8n/workflows/rules-to-metagraph.json`'s "Build LLM Prompt" node `functionCode` string: locate the `fewShot` array's two Var MERGE lines (`"MERGE (vb:Var {name: '?b'}) SET vb.project = '" + project + "', vb.graph = 'Metagraph'"` and the equivalent `vh` line) and restructure each to move the project placeholder into the MERGE braces, matching 07-03's `cypher_template.txt` fix exactly: `"MERGE (vb:Var {name: '?b', project: '" + project + "'}) SET vb.graph = 'Metagraph'"`. Locate the `SCHEMA CONSTRAINTS (v3)` text block's `'- Var key: name (?<varName>).'` line and change it to `'- Var key: name + project (?<varName>, <PROJECT>).'`. Add one new line to the same `NODE KEY PROPERTIES` text block: `'- DesignState key: StateId + project. Props: kind (DefState|ObjectState).'`. Add `DesignState` to the `'Allowed node labels:'` line's enumerated list (currently `Class, DatatypeProperty, ObjectProperty, Builtin, Rule, Atom, Var, Literal` — append `, DesignState`). This is a direct string edit inside the JSON file's `functionCode` value — preserve the existing JS string-concatenation/array-join structure exactly, only mutate the literal text content of the array elements being changed; do not reformat or restructure the surrounding JavaScript.

    In `graph-viewer/config.template.js`, add a `DesignState: { label: "StateId", group: "kind" }` entry to the `labels` map (alongside the existing `Var: { label: "name" }` entry) — the `group: "kind"` directive tells NeoVis to key `visGroups` off the `DesignState.kind` property's values (`DefState` / `ObjectState`) rather than off the `DesignState` label itself. Add TWO entries to the `visGroups` map, keyed by the `kind` property's VALUES, not by the label: `DefState: { color: { background: "#a8d8ea", border: "#6fb3cf" } }` and `ObjectState: { color: { background: "#f4a261", border: "#d4823a" } }` — these must use visibly different `color.background` hex values. Add a one-line comment directly above these two `visGroups` entries noting the different keying convention: `// DesignState uses group: "kind" (see labels above) so visGroups is keyed by kind VALUES (DefState/ObjectState), not by the DesignState label — unlike every other entry in this file, which keys visGroups by label name directly.`

    In `data-service/app.py`, locate the `statePayloadJson` field declaration (line 161) and the `_project_state_summary()` function's docstring (line ~518). Add or update docstring/comment text near these locations to reference the `DS_`/`OS_` ID-prefix vocabulary established in 07-02 (e.g. a one-line docstring addition noting that `statePayloadJson` will carry `DS_`-prefixed DefState IDs and, from Phase 11 onward, `OS_`-prefixed ObjectState references) — this is a documentation-only change. Do NOT modify `_project_state_summary()`'s logic, the `statePayloadJson` field's Pydantic type, or any of the four call sites' behavior in this task.
  </action>
  <verify>
    <automated>grep -c "DesignState" graph-viewer/config.template.js && grep -c "DefState:" graph-viewer/config.template.js && grep -c "ObjectState:" graph-viewer/config.template.js</automated>
  </verify>
  <done>
    `n8n/workflows/rules-to-metagraph.json`'s embedded `functionCode` string contains zero occurrences of the old bare `MERGE (v.:Var {name: '?[a-z]'})` SET-only pattern — verify via extracting and grepping the `functionCode` string for `"MERGE (vb:Var {name: '?b'}) SET vb.project"` returns 0 matches, and the corrected `"MERGE (vb:Var {name: '?b', project:"` pattern returns 1 match. `graph-viewer/config.template.js` contains a `group: "kind"` directive under the `DesignState` entry in the `labels` map — verify via `grep -c 'group: "kind"' graph-viewer/config.template.js` returns at least 1. `visGroups` contains both a `DefState` key and an `ObjectState` key with DIFFERENT `color.background` hex values — verify via `grep -A1 "DefState:" graph-viewer/config.template.js` and `grep -A1 "ObjectState:" graph-viewer/config.template.js` showing two distinct `background:` hex codes (not the same string). `data-service/app.py` builds/imports without syntax errors after the docstring edit (verify via `python -c "import ast; ast.parse(open('data-service/app.py').read())"` exits 0) and `_project_state_summary`'s function body (excluding docstring) is byte-identical to its pre-edit state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| n8n "Build LLM Prompt" function node ↔ Ollama LLM | The embedded prompt text is sent verbatim to the local Ollama LLM; this plan only edits static prompt text, introducing no new user-input interpolation point beyond what already exists |
| graph-viewer/config.template.js ↔ browser (NeoVis runtime) | config.template.js is injected at container startup via `entrypoint.sh` sed substitution and served to the browser; this plan adds static label/color config, no new dynamic injection point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-07-11 | Tampering | n8n rules-to-metagraph.json embedded prompt edit | mitigate | Edit is a direct text-literal change inside an already-version-controlled JSON file reviewed via this plan's `<done>` grep checks; no new code path or external input source is introduced — the LLM's Cypher OUTPUT still passes through the existing "Parse LLM Output" node's structural validation (MERGE-pattern regex, bracket-nesting check) before being executed against Neo4j, unchanged by this plan |
| T-07-12 | Information Disclosure | data-service/app.py docstring additions referencing DS_/OS_ prefixes | accept | Docstrings are server-side source code comments, never returned in any API response body — no new client-facing disclosure surface |
| T-07-13 | Tampering | npm/pip/cargo installs | accept | This plan introduces no new package-manager installs — all work is static JSON/JS/Python text edits; no `T-07-SC` package legitimacy checkpoint is required |
</threat_model>

<verification>
Run `grep -c "DesignState" training/dataset_schema.json` — at least 1.
Run `grep -c 'group: "kind"' graph-viewer/config.template.js` — at least 1.
Run `grep -c "DefState:" graph-viewer/config.template.js` and `grep -c "ObjectState:" graph-viewer/config.template.js` — both at least 1, with different `color.background` hex values (manual diff of the two `visGroups` entries).
Confirm `n8n/workflows/rules-to-metagraph.json` remains valid JSON after edits: `node -e "JSON.parse(require('fs').readFileSync('n8n/workflows/rules-to-metagraph.json','utf8'))"` exits 0.
Confirm `data-service/app.py` remains valid Python after edits: `python -c "import ast; ast.parse(open('data-service/app.py').read())"` exits 0.
Cross-check `cypher_template.txt`'s `SCHEMA VERSION: v3.1` stamp (from 07-03) matches `training/dataset_schema.json`'s `metadata.version: "v3.1"` — manual diff, both must read v3.1.
</verification>

<success_criteria>
All five remaining schema-propagation surfaces (`dataset_schema.json`, `updated_cypher_reference_examples_v3.cypher`, `n8n/workflows/rules-to-metagraph.json`, `config.template.js`, `data-service/app.py`) consistently reference the `DS_`/`OS_` ID prefixes and the single-label `:DesignState{kind}` pattern established in 07-01/07-02/07-03 — satisfying Phase 7 ROADMAP success criterion 3 in full (combined with 07-01/07-02/07-03's C# and Cypher surfaces). NeoVis renders `DefState` and `ObjectState` nodes in visibly distinct colors from each other via the `DesignState` label's `group: "kind"` directive plus separate `DefState`/`ObjectState` `visGroups` entries, satisfying success criterion 4 in full — no fallback or single-shared-color path is used.
</success_criteria>

<artifacts_produced>
- `training/dataset_schema.json` — modified: `Var` node `"key"` block now includes `"project"`; new `"label": "DesignState"` entry added to `metagraph.nodes[]`; `metadata.version` bumped to `"v3.1"`
- `training/updated_cypher_reference_examples_v3.cypher` — modified: all `Var` MERGE statements now include `project` in the MERGE key
- `n8n/workflows/rules-to-metagraph.json` — modified: "Build LLM Prompt" function node's embedded prompt text (`fewShot` array, `SCHEMA CONSTRAINTS`, `NODE KEY PROPERTIES`, allowed-labels list) updated to the project-scoped Var pattern and `DesignState` vocabulary
- `graph-viewer/config.template.js` — modified: `labels.DesignState` entry added with `group: "kind"`; `visGroups.DefState` and `visGroups.ObjectState` entries added with distinct `color.background` values (keyed by `kind` property value, not by label — documented inline)
- `data-service/app.py` — modified: docstring/comment additions near `statePayloadJson` and `_project_state_summary()` referencing `DS_`/`OS_` vocabulary (no behavior change)
</artifacts_produced>

<output>
Create `.planning/phases/07-schema-foundation/07-04-SUMMARY.md` when done
</output>
