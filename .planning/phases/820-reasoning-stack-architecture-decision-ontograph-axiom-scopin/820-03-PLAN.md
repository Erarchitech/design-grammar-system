---
phase: 820-reasoning-stack-architecture-decision-ontograph-axiom-scopin
plan: 03
type: execute
wave: 2
depends_on: [820-01, 820-02]
files_modified:
  - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md
  - .planning/PROJECT.md
autonomous: true
requirements: [REAS-04]

must_haves:
  truths:
    - "820-DECISION.md records two decisions in ADR shape — (1) hybrid axiom-scoping, (2) dg-reasoner sidecar confirmation — each with a Context/Decision/Consequences body plus an Evidence subsection citing spike output (success criteria 1 and 4)"
    - "820-DECISION.md's Evidence cites the rdflib-parsed axiom counts (disjointWith == 0) and the HermiT before/after contrast (naive consistent vs hybrid SlidingDoor unsatisfiable) from Plan 01's spike output"
    - "PROJECT.md Key Decisions row 134 (dg-reasoner sidecar) flips from '— Pending — v8.2' to a shipped/decided outcome citing 820-DECISION.md, unblocking Phase 821 (success criterion 4)"
    - "PROJECT.md Key Decisions gains a new row recording the hybrid axiom-scoping decision with rationale, citing 820-DECISION.md and spec/LPG-OWL-MAPPING.md (success criterion 1)"
  artifacts:
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md"
    - ".planning/PROJECT.md (edited Key Decisions table)"
  key_links:
    - "820-DECISION.md Evidence sourced from spike/output/ logs (Plan 01) and references spec/LPG-OWL-MAPPING.md (Plan 02)"
    - "PROJECT.md Key Decisions rows are the durable, project-level record downstream phases read"
---

<objective>
Record the phase's decisions durably: write `820-DECISION.md` (two ADR-style entries — hybrid axiom-scoping and dg-reasoner sidecar confirmation — each with a spike-evidence subsection), then edit `.planning/PROJECT.md`'s Key Decisions table to flip the Pending sidecar row to decided and add a new row for the hybrid axiom-scoping decision.

Purpose: These are the artifacts that unblock Phase 821 — the sidecar Key Decision goes from Pending to decided (with evidence), and the axiom-scoping approach becomes a citable project decision. Without them the downstream phases have no authoritative record to build against.

Output: `820-DECISION.md` in the phase dir; two edits to `.planning/PROJECT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-CONTEXT.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New file:
- `.planning/phases/820-.../820-DECISION.md` — ADR-style decision record with spike evidence

New PROJECT.md Key Decisions content (edits to an existing file, not new code symbols):
- Edited row: `dg-reasoner` sidecar row (currently line ~134) — Outcome flipped from `— Pending — v8.2` to decided/shipped, citing `820-DECISION.md`
- New row: hybrid axiom-scoping decision (static V7 TBox ∪ live project-scoped export + curated disjointWith), Outcome citing `820-DECISION.md` and `spec/LPG-OWL-MAPPING.md`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write 820-DECISION.md with spike evidence</name>
  <files>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md
  </files>
  <read_first>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/naive_result.txt (Plan 01 evidence — the trivial-consistency result)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/hybrid_result.txt (Plan 01 evidence — SlidingDoor unsatisfiable)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/axiom_counts.txt (Plan 01 evidence — rdflib-parsed subClassOf/domain/range/disjointWith counts)
    - spec/LPG-OWL-MAPPING.md (Plan 02 output — reference it, do not restate it)
    - spec/DECISIONS.md (ADR three-part Context/Decision/Consequences shape to follow — read ADR-1 and ADR-2 as the template)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md (§ 820-DECISION.md — append an Evidence subsection to the ADR skeleton; § shared ADR pattern)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md (§ Live OntoGraph Characterization for the systemic mistagging callout; § Assumptions Log A3 — the v8-ui-smoke-is-QA-data note)
  </read_first>
  <action>
    Create 820-DECISION.md with two ADR-style entries, each using the Context/Decision/Consequences three-part shape from spec/DECISIONS.md, extended with a bolded Evidence subsection at the end (this phase's addition, since the spike produces evidence the existing terse ADRs never had).

    Entry 1 — Hybrid axiom-scoping: Context = OWL reasoning needs real axioms but the live OntoGraph is flat (zero SUBCLASS_OF/DOMAIN/RANGE/DISJOINT_WITH relationship types confirmed across the whole DB). Decision = union the static DesignGrammar-V7.owl TBox with the live project-scoped export plus structural/referential checks, curate disjointWith into the static TBox once, and explicitly defer extending LLM ingestion to emit axioms (avoids mid-milestone n8n prompt churn + the 9-file schema-propagation cost). Consequences = reasoning becomes non-trivial without touching the ingestion pipeline; disjointness is a design-time artifact. Evidence = the axiom counts from axiom_counts.txt (state the rdflib-parsed subClassOf/domain/range numbers and disjointWith == 0), and the two-part HermiT contrast: naive_result.txt shows no inconsistent classes (trivial consistency), hybrid_result.txt shows ex:SlidingDoor unsatisfiable after curating ex:Door owl:disjointWith ex:Window and seeding ex:SlidingDoor under both.

    Entry 2 — dg-reasoner sidecar confirmed: Context = STACK.md vs ARCHITECTURE.md conflict over embedded-vs-sidecar reasoning; the row was Pending in PROJECT.md. Decision = confirm the isolated dg-reasoner sidecar (Owlready2/HermiT/Openllet + pySHACL), not embedded in data-service. Consequences = isolates the JVM subprocess failure modes (hang/OOM/crash) from data-service's Speckle-publish/validation-run hot path; DL reasoning is worst-case exponential and the corpus grows unboundedly. Evidence = the spike ran HermiT as a local subprocess successfully (reference the spike README's reproduction steps and that HermiT shells out via a bundled JAR needing a JRE — the exact isolation concern the sidecar addresses); note no blocking issue surfaced, so the decision is not re-litigated.

    Add a short "Data-quality note" recording the systemic graph-property tagging drift found live (v8-ui-smoke's two R_DOOR_ORIENTATION_V atoms tagged graph:'OntoGraph'; phase14-smoke's DesignState/Run mistagged) as a documented gap left for Phase 821's exporter to handle defensively — do NOT scope a data-repair task here (Open Question 2 recommendation). Add the Assumption A3 note that v8-ui-smoke is QA/smoke data and confirm this satisfies the "real project's live OntoGraph data" success-criterion wording. Reference spec/LPG-OWL-MAPPING.md for the mapping contract rather than restating it.
  </action>
  <verify>
    <automated>test -s ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md" && grep -qi "Evidence" ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md" && grep -qi "SlidingDoor" ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md" && grep -qi "sidecar" ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md" && grep -q "LPG-OWL-MAPPING.md" ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md"</automated>
  </verify>
  <acceptance_criteria>
    - `820-DECISION.md` has two ADR-style entries, one for hybrid axiom-scoping and one for the dg-reasoner sidecar, each with Context / Decision / Consequences plus an Evidence subsection (grep `Context`, `Decision`, `Consequences`, `Evidence`).
    - The hybrid entry's Evidence cites the rdflib-parsed axiom counts including `disjointWith` == 0 and the naive-vs-hybrid HermiT contrast naming `SlidingDoor` (behavior/source assertion, drawn from the Plan 01 output files).
    - The sidecar entry confirms the isolated dg-reasoner service and notes no blocking issue surfaced in the spike.
    - A data-quality note records the graph-property mistagging as a documented gap (not a repair task) and the v8-ui-smoke QA-data assumption (A3) is addressed.
    - References `spec/LPG-OWL-MAPPING.md` instead of restating the mapping.
  </acceptance_criteria>
  <done>820-DECISION.md captures both decisions with defensible spike evidence and cross-references the mapping spec.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md Key Decisions — flip sidecar row, add hybrid axiom-scoping row</name>
  <files>
    - .planning/PROJECT.md
  </files>
  <read_first>
    - .planning/PROJECT.md (§ Key Decisions table — read the full table; the dg-reasoner sidecar row is currently line ~134 ending `— Pending — v8.2 (research-recommended, resolves STACK.md vs ARCHITECTURE.md conflict)`; note the shipped-row verb convention `✓ Shipped — vX.Y Phase N` vs `✓ Good` used elsewhere)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-DECISION.md (the file written in Task 1 — the Outcome column must cite it)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md (§ .planning/PROJECT.md Key Decisions table — edit the existing row in place, add one new row in the same 3-column | Decision | Rationale | Outcome | shape)
  </read_first>
  <action>
    Edit the existing dg-reasoner sidecar row in PROJECT.md's Key Decisions table (currently ending `— Pending — v8.2 (research-recommended, resolves STACK.md vs ARCHITECTURE.md conflict)`): change ONLY the Outcome column to `✓ Shipped — v8.2 Phase 820 (spike evidence: see 820-DECISION.md)`, matching the `✓ Shipped — vX.Y Phase N` verb convention used by other phase-tied shipped rows. Leave the Decision and Rationale columns unchanged. Use a scoped Edit that matches the existing row text exactly — do NOT rewrite the whole table.

    Add ONE new row to the Key Decisions table in the same `| Decision | Rationale | Outcome |` shape. Decision column = the hybrid axiom-scoping approach: union the static DesignGrammar-V7.owl TBox (real subClassOf/domain/range axioms) with the live project-scoped OntoGraph/Metagraph export plus structural checks, curate disjointWith into the static TBox once, LLM-ingestion axiom emission deferred. Rationale column = the live OntoGraph is flat (zero axiom-bearing relationship types confirmed DB-wide) so a naive export reasons as trivially consistent; the hybrid makes HermiT's answer non-trivial without mid-milestone n8n prompt churn or 9-file schema propagation. Outcome column = `✓ Shipped — v8.2 Phase 820 (see 820-DECISION.md; mapping spec spec/LPG-OWL-MAPPING.md)`.

    Leave the CONNECTOR additive-credential row (line ~135) untouched — it stays `— Pending — v8.2` because it is Phase 824's decision, out of this phase's scope.
  </action>
  <verify>
    <automated>grep -q "820-DECISION.md" .planning/PROJECT.md && grep -q "LPG-OWL-MAPPING.md" .planning/PROJECT.md && grep "dg-reasoner" .planning/PROJECT.md | grep -q "Shipped" && grep -qi "hybrid" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - The dg-reasoner sidecar row's Outcome column no longer reads `— Pending — v8.2`; it reads a `✓ Shipped — v8.2 Phase 820` outcome citing `820-DECISION.md` (source assertion: `grep "dg-reasoner" .planning/PROJECT.md | grep -q "Shipped"`).
    - The Decision and Rationale columns of that row are unchanged (only the Outcome column edited).
    - A new Key Decisions row exists recording the hybrid axiom-scoping decision, Outcome citing both `820-DECISION.md` and `spec/LPG-OWL-MAPPING.md` (grep `hybrid` and `LPG-OWL-MAPPING.md`).
    - The CONNECTOR additive-credential row still reads `— Pending — v8.2` (untouched).
    - The table remains well-formed (every row has three `|`-delimited columns).
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions durably records both the hybrid axiom-scoping decision and the confirmed sidecar, unblocking Phase 821.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 820-DECISION.md + PROJECT.md → git repo | Committed decision artifacts could leak credentials or raw non-test-project rule bodies via pasted spike output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-820-05 | Information Disclosure | 820-DECISION.md pasted spike-output excerpts | low | mitigate | Evidence cites axiom counts and HermiT class names (e.g. SlidingDoor) only — never connection strings, credentials, or full raw rule bodies; the spike targets v8-ui-smoke QA data |
| T-820-06 | Tampering | PROJECT.md whole-table overwrite | low | mitigate | Task 2 uses a scoped Edit matching the exact existing row text, never a whole-file Write, preserving all other Key Decisions rows |
</threat_model>

<verification>
- `820-DECISION.md` exists with two ADR entries + Evidence subsections citing the Plan 01 output files.
- `.planning/PROJECT.md` dg-reasoner row shows a Shipped v8.2 Phase 820 outcome; a new hybrid axiom-scoping row exists; the CONNECTOR row is unchanged.
- No credential literal appears in either committed file.
</verification>

<success_criteria>
Delivers REAS-04 success criteria 1 and 4: PROJECT.md Key Decisions records the chosen (hybrid) axiom-scoping approach with rationale, and the sidecar-vs-embedded decision is confirmed and recorded as a Key Decision, unblocking Phase 821.
</success_criteria>

<output>
Create `.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-03-SUMMARY.md` when done.
</output>
