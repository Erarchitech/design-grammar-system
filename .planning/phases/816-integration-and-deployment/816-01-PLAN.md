---
phase: 816-integration-and-deployment
plan: "816-01"
wave: 3
autonomous: true
requirements: [INTG-01, INTG-02]
files_modified:
  - ui-v2/nginx.conf (only if a route is missing)
  - docker-compose.yml (only if data-service volume/env needs the new stores)
---

# Plan 816-01: Integration & Deployment

## Objective

The full connector lifecycle works end-to-end through the deployed stack, and the rebuilt `design-grammars` container ships all seven regions with no v8.0 regressions.

## Tasks

### Task 1: proxy & compose audit

Verify nginx routes cover everything the four new screens call (`/llm/`, `/data-service/` → connectors/reasoner endpoints — check trailing-slash/prefix rewrite against how `modelApi.js` builds URLs). Verify docker-compose mounts/persists `data-service/data/` so connector credentials and reasoner selection survive restarts. Fix only what's missing.

### Task 2: E2E connector lifecycle (INTG-01)

Against the running stack (`docker compose up -d`, data-service + design-grammars):
1. Create a credential for `grasshopper` via `POST` through the nginx proxy (as the UI would)
2. Simulate a connector: `curl -X POST .../connectors/heartbeat -H "Authorization: Bearer dgc_..."`
3. Verify `GET /connectors` reports `active` + fresh `last_connection` for grasshopper
4. Revoke the credential; verify heartbeat now returns 401
Record the transcript in the SUMMARY. Clean up the test credential afterwards.

### Task 3: deployment cutover (INTG-02)

`docker compose build --no-cache design-grammars && docker compose up -d design-grammars` (the `--no-cache` is mandatory — known stale-layer gotcha). Then smoke-check on :8080: landing renders 7 callouts; each new region loads real data (AI Engine settings, connectors registry, reasoner registry, docs tree); Graph Viewer / Model Viewer / Projects / auth still work (v8.0 smoke: graph datascape renders for the active project, model viewer lists runs, projects grid loads). Run `python -m pytest data-service/tests/ -q` and `npm --prefix ui-v2 run build` as final gates.

### Task 4: docs touch-up

Update `CLAUDE.md` service-map/architecture lines ONLY if the new endpoints change routing facts stated there (connector/reasoner endpoints live inside data-service, so likely no change). Do not rewrite planning docs.

## Verification criteria

Success criteria 1–2 of Phase 816 in `.planning/ROADMAP.md`.
