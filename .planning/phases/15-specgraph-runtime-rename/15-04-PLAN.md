---
phase: 15-specgraph-runtime-rename
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - graph-viewer/config.template.js
  - graph-viewer/index.html
autonomous: true
requirements: [SPEC-04]
nyquist_compliant: true

must_haves:
  truths:
    - "config.template.js NeoVis label keys (L27-30) rename KnowledgeNote/KnowledgeTag/KnowledgeSession/KnowledgeClass -> SpecNote/SpecTag/SpecSession/SpecClass, keeping each display-property value ({label:'title'|'name'|'mode'|'label'}) unchanged (D-11)"
    - "config.template.js visGroups keys (L54-57) rename the same four Knowledge*->Spec*, keeping the color hex values (teal #4ecdc4, yellow #ffe66d, purple #a78bfa, pink #f472b6) unchanged (D-11)"
    - "index.html view key + Cypher: the FOUR in-scope sites (L1439 `view === \"KnowledgeGraph\"`, L1441 + L1442 `graph:'KnowledgeGraph'` Cypher literals, L2916 `buildCypher(\"KnowledgeGraph\")`) become SpecGraph (D-11)"
    - "The out-of-scope UI notes-panel identifiers are PRESERVED unchanged: knowledgeMode/knowledgePromptText/knowledge* React state, the 'Insert Knowledge'/'Query Knowledge'/'Update Knowledge' labels, the 'Specs&Notes' tab (D-10), and the lowercase workflow polling keys 'knowledge-ingest'/'knowledge-query' (RESEARCH § Scope Boundaries)"
  artifacts:
    - "graph-viewer/config.template.js (edited in place)"
    - "graph-viewer/index.html (4 sites edited in place)"
  key_links:
    - "SpecGraph view-key literal + Spec* NeoVis label keys MUST match the DB labels the 15-01 migration produces and 15-02 data-service writes (NeoVis label keys map 1:1 to Neo4j node labels)"
  prohibitions:
    - "Do NOT blind find/replace 'knowledge'->'spec' in index.html — only the 4 KnowledgeGraph graph-view sites change. The ~80 lowercase knowledge* notes-panel identifiers and user-facing 'Insert/Query/Update Knowledge' labels stay (they are not the graph layer and are not caught by SC#4)"
    - "Do NOT rename the 'Specs&Notes' sidebar tab (D-10 — already Spec*-aligned)"
    - "Do NOT change NeoVis colors — only the label/visGroups keys rename"

security: "No new threat surface. Client-side config + static SPA edits: renaming NeoVis label keys and a Cypher view-key literal. No new network calls, no auth change. Endpoint/webhook URLs the UI calls are untouched by this plan."
---

<objective>
Make the UI's graph view speak Spec*: rename the four NeoVis label/visGroups keys in `config.template.js` and the four `KnowledgeGraph` view-key/Cypher sites in `index.html`, so the "Specs&Notes" NeoVis view queries `graph:'SpecGraph'` and styles `Spec*` labels. This is a precision edit — the surrounding notes-panel UI (which uses lowercase `knowledge*` naming and user-facing "Knowledge" labels) is deliberately left alone.

Purpose: NeoVis label keys map 1:1 to Neo4j node labels; after the migration renames labels to Spec*, the config must match or nodes render unstyled/ungrouped.
Output: edited `config.template.js` (8 keys) + `index.html` (4 sites).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-specgraph-runtime-rename/15-CONTEXT.md
@.planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md
@.planning/phases/15-specgraph-runtime-rename/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename NeoVis label + visGroups keys in config.template.js</name>
  <files>graph-viewer/config.template.js</files>
  <read_first>
    - graph-viewer/config.template.js L27-30 (labels block) and L54-57 (visGroups block) — confirm the exact key names and that colors are on the visGroups side
    - .planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md § "Current-State Map"
  </read_first>
  <action>
    In the `labels` object (L27-30): rename keys `KnowledgeNote`->`SpecNote`, `KnowledgeTag`->`SpecTag`, `KnowledgeSession`->`SpecSession`, `KnowledgeClass`->`SpecClass`. Keep each value verbatim (`{ label: "title" }`, `{ label: "name" }`, `{ label: "mode" }`, `{ label: "label" }`). In the `visGroups`/groups object (L54-57): rename the same four keys, keeping the `color.background`/`color.border` hex values byte-for-byte (teal/yellow/purple/pink). No other edits.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && f=graph-viewer/config.template.js && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' "$f")" && grep -q "SpecNote" "$f" && grep -q "SpecTag" "$f" && grep -q "SpecSession" "$f" && grep -q "SpecClass" "$f" && grep -q "#4ecdc4" "$f" && grep -q "#ffe66d" "$f" && grep -q "#a78bfa" "$f" && grep -q "#f472b6" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero Knowledge* tokens in config.template.js; SpecNote/SpecTag/SpecSession/SpecClass present in both labels and visGroups.
    - All four color hex values preserved.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename the 4 KnowledgeGraph view-key/Cypher sites in index.html</name>
  <files>graph-viewer/index.html</files>
  <read_first>
    - graph-viewer/index.html L1439-1442 (buildCypher view branch + Cypher literals) and L2916 (buildCypher call site)
    - .planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md § "Scope Boundaries" (the out-of-scope list this task must NOT touch)
  </read_first>
  <action>
    Change exactly these four sites, all `KnowledgeGraph` -> `SpecGraph`:
    1. L1439: `if (view === "KnowledgeGraph")` -> `"SpecGraph"`.
    2. L1441: the Cypher `"MATCH (n {graph:'KnowledgeGraph'" + pf + "}) "` -> `'SpecGraph'`.
    3. L1442: the Cypher `"OPTIONAL MATCH (n)-[r]-(m {graph:'KnowledgeGraph'" + pf + "}) "` -> `'SpecGraph'`.
    4. L2916: `const q = buildCypher("KnowledgeGraph");` -> `buildCypher("SpecGraph")`.
    Touch nothing else. Explicitly leave unchanged: all `knowledgeMode`/`setKnowledgeMode`/`knowledgePromptText`/`knowledge*` React state (~L1490-3828), the KNOWLEDGE_OPTIONS labels "Insert Knowledge"/"Query Knowledge"/"Update Knowledge", the polling keys "knowledge-ingest"/"knowledge-query", and the "Specs&Notes" tab label.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && f=graph-viewer/index.html && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' "$f")" && grep -q 'view === "SpecGraph"' "$f" && grep -q "buildCypher(\"SpecGraph\")" "$f" && grep -q "graph:'SpecGraph'" "$f" && grep -q "knowledgeMode" "$f" && grep -q "Insert Knowledge" "$f" && grep -q "knowledge-ingest" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero Knowledge* graph-layer tokens (KnowledgeGraph/Note/Tag/Session/Class) remain in index.html.
    - `view === "SpecGraph"`, `buildCypher("SpecGraph")`, and `graph:'SpecGraph'` present.
    - Out-of-scope PRESERVED: `knowledgeMode`, `Insert Knowledge`, and `knowledge-ingest` still present (proves no blind find/replace).
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
No new symbols. In-place key renames in config.template.js (4 labels + 4 visGroups) and 4 literal edits in index.html. The view-key string `SpecGraph` replaces `KnowledgeGraph`; NeoVis label keys SpecNote/SpecTag/SpecSession/SpecClass replace Knowledge*.
</artifacts_produced>

<verification>
- config.template.js: zero Knowledge* tokens, four Spec* keys in labels + visGroups, colors preserved.
- index.html: the 4 graph-view sites are SpecGraph; the notes-panel knowledge* identifiers and user-facing labels are untouched.
</verification>

<success_criteria>
SPEC-04 satisfied: the NeoVis "Specs&Notes" view queries `graph:'SpecGraph'` and styles the four `Spec*` labels with unchanged colors, while the notes-panel UI naming and user-facing labels are intentionally preserved (D-10/D-11).
</success_criteria>

<output>
Create `.planning/phases/15-specgraph-runtime-rename/15-04-SUMMARY.md` when done.
</output>
