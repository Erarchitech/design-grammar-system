---
phase: 15-specgraph-runtime-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data-service/app.py
  - knowledge_schema.cypher
  - spec_schema.cypher
  - data-service/tests/test_update_flow.py
autonomous: true
requirements: [SPEC-02]
nyquist_compliant: true

must_haves:
  truths:
    - "data-service/app.py declares SPEC_GRAPH = 'SpecGraph' (renamed from KNOWLEDGE_GRAPH = 'KnowledgeGraph', L42) and every reference to the constant is updated"
    - "app.py function ensure_knowledge_indexes() is renamed ensure_spec_indexes() AND its startup call site is updated (D-09)"
    - "app.py fulltext index creation targets spec_note_search FOR (n:SpecNote), and db.index.fulltext.queryNodes uses 'spec_note_search' (L736-737, L1344)"
    - "app.py KnowledgeClass hub MERGEs use SpecClass with name/label 'SpecNote'/'SpecSession' (L741-758) — matching the 15-01 migration hub normalization exactly (no duplicate hubs)"
    - "Every Knowledge* node label in app.py CRUD Cypher (L1155-1438) is Spec* (SpecNote/SpecTag/SpecSession/SpecClass)"
    - "knowledge_schema.cypher is renamed to spec_schema.cypher and its content (index name, 3 node blocks, graph-isolation notes) is fully Spec* / graph:'SpecGraph'"
    - "data-service/tests/test_update_flow.py asserts 'SpecSession' in written_queries (was 'KnowledgeSession', L108)"
  artifacts:
    - "data-service/app.py (edited in place)"
    - "spec_schema.cypher (renamed from knowledge_schema.cypher, content Spec*)"
    - "data-service/tests/test_update_flow.py (assertion updated)"
  key_links:
    - "SPEC_GRAPH value 'SpecGraph', the four Spec* labels, spec_note_search, and hub names SpecNote/SpecSession MUST match the 15-01 migration end-state 1:1"
  prohibitions:
    - "Do NOT change any HTTP route path — /knowledge/* endpoint URLs are preserved for UI compatibility (SPEC-02). Only Cypher labels, the graph literal, index name, function name, and constant change."
    - "Do NOT rename lowercase URL-facing tokens or the /knowledge/ route decorators"

security: "No new threat surface. This plan edits Cypher label strings, one constant, one function name, and a test assertion. No auth, network, or input-handling change. Endpoint URLs (the only externally reachable surface) are deliberately unchanged."
---

<objective>
Rename every graph-layer identifier the data-service *writes and reads* from Knowledge* to Spec*, while leaving the externally reachable `/knowledge/*` endpoint URLs untouched (SPEC-02). After this plan, `data-service/app.py` and the canonical `spec_schema.cypher` speak Spec* end-to-end and agree with the 15-01 migration's DB end-state.

Purpose: the migration (15-01) renames existing data; this plan renames the code that maintains it going forward, including the fresh-DB index/hub bootstrap in `ensure_spec_indexes()`.
Output: edited `app.py`, `knowledge_schema.cypher`→`spec_schema.cypher`, updated update-flow test assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-specgraph-runtime-rename/15-CONTEXT.md
@.planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md
@.planning/phases/15-specgraph-runtime-rename/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Knowledge*->Spec* across data-service/app.py</name>
  <files>data-service/app.py</files>
  <read_first>
    - data-service/app.py L42 (KNOWLEDGE_GRAPH constant), L732-758 (ensure_knowledge_indexes: index + hub MERGEs), L1155-1438 (CRUD + update-flow Cypher), L1344 (queryNodes fulltext call)
    - .planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md § "app.py precise targets" and § "Scope Boundaries"
    - .planning/phases/15-specgraph-runtime-rename/15-01-PLAN.md must_haves (the DB end-state this code must agree with)
  </read_first>
  <action>
    Apply the full Knowledge*->Spec* rename to app.py with these concrete targets:
    1. L42: `KNOWLEDGE_GRAPH = "KnowledgeGraph"` -> `SPEC_GRAPH = "SpecGraph"`. Update EVERY use of the identifier `KNOWLEDGE_GRAPH` to `SPEC_GRAPH` (~20 references across CRUD params).
    2. Function `def ensure_knowledge_indexes()` (L732) -> `def ensure_spec_indexes()`. Find and update its call site (grep `ensure_knowledge_indexes` — likely a startup/lifespan hook).
    3. Index bootstrap (L736-737): `CREATE FULLTEXT INDEX knowledge_note_search ... FOR (n:KnowledgeNote)` -> `spec_note_search ... FOR (n:SpecNote)`.
    4. Hub MERGEs (L741-758): `KnowledgeClass {name:'KnowledgeNote'...}` -> `SpecClass {name:'SpecNote'...}` with `SET c.label='SpecNote'`; same for 'KnowledgeSession'->'SpecSession'; all `graph:'KnowledgeGraph'` -> the SPEC_GRAPH literal/value 'SpecGraph'. These MUST match the 15-01 migration hub normalization.
    5. All CRUD/update Cypher (L1155-1438): labels KnowledgeNote/KnowledgeTag/KnowledgeSession/KnowledgeClass -> SpecNote/SpecTag/SpecSession/SpecClass.
    6. L1344: `queryNodes('knowledge_note_search', ...)` -> `'spec_note_search'`.
    PRESERVE: every `/knowledge/...` route path/decorator, and any lowercase URL string — SPEC-02. Do not touch them.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && grep -rin "KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass" data-service/app.py; test -z "$(grep -rin 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' data-service/app.py)" && ! grep -q "KNOWLEDGE_GRAPH\|ensure_knowledge_indexes\|knowledge_note_search" data-service/app.py && grep -q "SPEC_GRAPH = \"SpecGraph\"" data-service/app.py && grep -q "def ensure_spec_indexes" data-service/app.py && grep -q "/knowledge/" data-service/app.py && python -c "import ast; ast.parse(open('data-service/app.py').read())" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero graph-layer Knowledge* tokens (KnowledgeGraph/Note/Tag/Session/Class) remain in app.py.
    - No `KNOWLEDGE_GRAPH`, `ensure_knowledge_indexes`, or `knowledge_note_search` identifiers remain.
    - `SPEC_GRAPH = "SpecGraph"`, `def ensure_spec_indexes`, `spec_note_search`, and `SpecClass{name:'SpecNote'/'SpecSession'}` present.
    - `/knowledge/` route paths still present (SPEC-02 preserved).
    - `ast.parse` succeeds (valid Python).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename knowledge_schema.cypher -> spec_schema.cypher and rewrite content</name>
  <files>knowledge_schema.cypher, spec_schema.cypher</files>
  <read_first>
    - knowledge_schema.cypher (full — 46 lines: header, index, 3 node blocks, graph-isolation notes)
    - data-service/app.py (the just-edited Spec* index name + node shapes this doc must mirror)
  </read_first>
  <action>
    Rename the file with `git mv knowledge_schema.cypher spec_schema.cypher` (preserve history). Then rewrite its content Knowledge*->Spec*: title "KnowledgeGraph Schema Reference" -> "SpecGraph Schema Reference"; `knowledge_note_search`->`spec_note_search`; `KnowledgeNote`/`KnowledgeTag`/`KnowledgeSession`/`KnowledgeClass`->`Spec*`; every `graph: 'KnowledgeGraph'` -> `'SpecGraph'`; the graph-isolation prose (`graph: 'KnowledgeGraph'`, the OntoGraph/Metagraph/ValidationGraph comparison line -> reference ValidGraph/SpecGraph). Keep it a comment-only reference doc (all statements stay commented/idempotent as before).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test -f spec_schema.cypher && test ! -f knowledge_schema.cypher && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' spec_schema.cypher)" && grep -q "spec_note_search" spec_schema.cypher && grep -q "SpecNote" spec_schema.cypher && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - knowledge_schema.cypher no longer exists; spec_schema.cypher exists (renamed via git mv, history preserved).
    - Zero Knowledge* graph-layer tokens in spec_schema.cypher; index name is spec_note_search; node blocks are Spec*.
    - File stays a comment-only idempotent reference doc.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update the update-flow test assertion</name>
  <files>data-service/tests/test_update_flow.py</files>
  <read_first>
    - data-service/tests/test_update_flow.py L108 (the `"KnowledgeSession" in written_queries` assertion) and surrounding context to confirm the exact assertion string
  </read_first>
  <action>
    Change the assertion `"KnowledgeSession" in written_queries` to `"SpecSession" in written_queries` (and any sibling KnowledgeSession/KnowledgeGraph literal in the same test). This test verifies app.py's update flow writes the session node; after Task 1 that write targets SpecSession, so the assertion must follow.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' data-service/tests/test_update_flow.py)" && grep -q "SpecSession" data-service/tests/test_update_flow.py && python -c "import ast; ast.parse(open('data-service/tests/test_update_flow.py').read())" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - No Knowledge* graph-layer tokens remain in test_update_flow.py.
    - The session-write assertion checks for "SpecSession".
    - File parses as valid Python.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
No new symbols beyond the rename: constant `SPEC_GRAPH` (was KNOWLEDGE_GRAPH), function `ensure_spec_indexes` (was ensure_knowledge_indexes), file `spec_schema.cypher` (was knowledge_schema.cypher). No new routes, config keys, or classes. Endpoint URLs unchanged.
</artifacts_produced>

<verification>
- app.py: zero graph-layer Knowledge* tokens, SPEC_GRAPH/ensure_spec_indexes/spec_note_search present, /knowledge/ routes preserved, valid Python.
- spec_schema.cypher exists (knowledge_schema.cypher gone), fully Spec*.
- test_update_flow.py asserts SpecSession, valid Python.
</verification>

<success_criteria>
SPEC-02 satisfied: the data-service operates on Spec* labels + graph:'SpecGraph' + spec_note_search everywhere it reads/writes the knowledge layer, while the `/knowledge/*` endpoint URLs stay unchanged for UI compatibility. The canonical schema-reference file is renamed and Spec*-clean, and the update-flow test follows the SpecSession write.
</success_criteria>

<output>
Create `.planning/phases/15-specgraph-runtime-rename/15-02-SUMMARY.md` when done.
</output>