---
phase: 15-specgraph-runtime-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/2026-07-03_knowledge_to_spec_rename.cypher
  - test/seed_knowledge_nodes.cypher
autonomous: true
requirements: [SPEC-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A single dated migration file migrations/2026-07-03_knowledge_to_spec_rename.cypher performs all four operations (D-06): (1) graph property value KnowledgeGraph->SpecGraph, (2) node labels KnowledgeNote/Tag/Session/Class->Spec* via SET/REMOVE, (3) DROP INDEX knowledge_note_search, (4) CREATE FULLTEXT INDEX spec_note_search FOR (n:SpecNote) ON EACH [n.title, n.content]"
    - "The migration also normalizes KnowledgeClass hub-node name/label property VALUES ('KnowledgeNote'->'SpecNote', 'KnowledgeSession'->'SpecSession') so migrated hub nodes MERGE-match what data-service ensure_spec_indexes() writes (no duplicate hub nodes) (SPEC-01, ties to 15-02)"
    - "Migration carries a DEV-DATABASES-ONLY guard block and Step 0 dry-run counts, matching the Phase 14 migration header pattern"
    - "Every write step is idempotent — re-running the migration is a no-op (WHERE guards / IF EXISTS / IF NOT EXISTS)"
    - "A seed script test/seed_knowledge_nodes.cypher creates test Knowledge* nodes (KnowledgeNote+KnowledgeTag+KnowledgeSession+KnowledgeClass at graph:'KnowledgeGraph') so seed->migrate->verify yields live before/after counts (D-07, SC#1 proof)"
    - "Read-only VERIFICATION queries in the migration assert MATCH (n {graph:'KnowledgeGraph'}) RETURN count(n) EXPECTs 0 and Spec* labels exist after migration (SC#1)"
  artifacts:
    - "migrations/2026-07-03_knowledge_to_spec_rename.cypher (new)"
    - "test/seed_knowledge_nodes.cypher (new — dev-only seed for migration proof)"
  key_links:
    - "SpecGraph literal + Spec* labels + spec_note_search index name MUST match data-service (15-02), n8n (15-03), and NeoVis (15-04) 1:1 — this migration is the DB-side end-state those code changes assume"
    - "Hub-node name values SpecNote/SpecSession MUST match ensure_spec_indexes() MERGE keys in 15-02 or duplicate SpecClass hubs appear"
  prohibitions:
    - "No APOC procedures — label rename uses manual SET n:SpecX REMOVE n:KnowledgeX only (D-08)"
    - "Migration is NOT invoked by any application code — it is run manually against dev Neo4j (matches Phase 14 execution method)"
    - "No production-database execution — the dev-only guard and identity-check query must be present and unremoved"

security: "Threat surface T-15-MIG: a destructive bulk label/property migration. Mitigated exactly as Phase 14 (T-14-MIG): DEV-DATABASES-ONLY warning block + a target-identity check query the operator runs before any write step. No secrets, no network surface; the file is manual-run only."
---

<objective>
Author the SPEC-01 database migration that brings a live Neo4j instance from the Knowledge* end-state to the Spec* end-state in one dated, idempotent, dev-guarded `.cypher` file — plus the seed script that produces live before/after proof for SC#1. This is the DB-side contract that 15-02 (data-service), 15-03 (n8n), and 15-04 (UI) all assume; it lands first so the runtime literals in those plans have a real target.

Purpose: the runtime code changes rename what the app *writes*; this migration renames what already *exists* in the DB. Both sides must agree on `SpecGraph`, the four `Spec*` labels, the hub-node name values, and the `spec_note_search` index.
Output: `migrations/2026-07-03_knowledge_to_spec_rename.cypher` + `test/seed_knowledge_nodes.cypher`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-specgraph-runtime-rename/15-CONTEXT.md
@.planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md
@migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the seed script for migration proof</name>
  <files>test/seed_knowledge_nodes.cypher</files>
  <read_first>
    - migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher (SECTION B dry-run/verify style + dev-only header to mirror)
    - knowledge_schema.cypher (the canonical Knowledge* node shapes: KnowledgeNote{noteId,title,content,source,createdAt,updatedAt,graph,project}, KnowledgeTag{name,graph,project}, KnowledgeSession{sessionId,mode,prompt,result,createdAt,graph,project}, KnowledgeClass{name,label,graph,project})
    - .planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md § "Reusable Pattern"
  </read_first>
  <action>
    Create a dev-only seed script that MERGEs a small, self-labeled test set at `graph:'KnowledgeGraph'`, `project:'phase15-smoke'`: at least one KnowledgeNote (with title+content so the fulltext index has something to match), one KnowledgeTag with a TAGGED_WITH edge from the note, one KnowledgeSession, and the two KnowledgeClass hub nodes (name 'KnowledgeNote' and 'KnowledgeSession', each with a matching `label` property) plus INSTANCE_OF edges from the note/session to their hub. Use MERGE (idempotent). Put a short header comment: this seeds the PRE-migration state on a DEV database only, used to prove SC#1. Include a trailing commented count query (`MATCH (n {graph:'KnowledgeGraph', project:'phase15-smoke'}) RETURN labels(n), count(*)`).
    Keep it minimal — this is proof scaffolding, not a fixture library.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test -f test/seed_knowledge_nodes.cypher && grep -q "KnowledgeNote" test/seed_knowledge_nodes.cypher && grep -q "phase15-smoke" test/seed_knowledge_nodes.cypher && grep -qi "dev" test/seed_knowledge_nodes.cypher && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - test/seed_knowledge_nodes.cypher exists and MERGEs KnowledgeNote, KnowledgeTag, KnowledgeSession, and two KnowledgeClass hub nodes at graph:'KnowledgeGraph', project:'phase15-smoke'.
    - Note carries title+content; a TAGGED_WITH edge and INSTANCE_OF edges exist.
    - Header marks it dev-only; a commented count query is present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the KnowledgeGraph->SpecGraph migration</name>
  <files>migrations/2026-07-03_knowledge_to_spec_rename.cypher</files>
  <read_first>
    - migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher (FULL — copy its header structure: PURPOSE, EXECUTION METHOD, WARNING/DEV-ONLY block with the labels-identity check query, Step 0 dry-run, idempotent SET, read-only VERIFICATION block with `-- EXPECT:` annotations)
    - test/seed_knowledge_nodes.cypher (the state this migration must transform — from Task 1)
    - data-service/app.py lines 732-758 (ensure_knowledge_indexes: the index name knowledge_note_search, the KnowledgeClass hub MERGE keys name='KnowledgeNote'/'KnowledgeSession' — the migration's hub-name normalization must match what 15-02 will rewrite these to: 'SpecNote'/'SpecSession')
  </read_first>
  <action>
    Author the dated migration mirroring the Phase 14 file's structure. Header: PURPOSE (Knowledge*->Spec* runtime rename, SPEC-01), EXECUTION METHOD (manual via cypher-shell -f or Neo4j Browser; NOT auto-invoked), and a DEV-DATABASES-ONLY WARNING block carrying the same identity-check query Phase 14 uses (`MATCH (n) RETURN DISTINCT labels(n), count(*) ORDER BY count DESC LIMIT 10`).
    Then, in order, with concrete operations:
    1. Step 0 DRY-RUN (read-only): count nodes at `graph:'KnowledgeGraph'` grouped by label; count each of KnowledgeNote/KnowledgeTag/KnowledgeSession/KnowledgeClass.
    2. Rename the layer property VALUE: match nodes at `graph:'KnowledgeGraph'`, SET `graph='SpecGraph'` (idempotent — WHERE-guarded by the property value).
    3. Normalize KnowledgeClass hub-node property VALUES BEFORE the label rename: set `name`/`label` 'KnowledgeNote'->'SpecNote' and 'KnowledgeSession'->'SpecSession' (so migrated hubs match ensure_spec_indexes() MERGE keys in 15-02).
    4. Rename the four node LABELS with manual SET/REMOVE (D-08, no APOC): KnowledgeNote->SpecNote, KnowledgeTag->SpecTag, KnowledgeSession->SpecSession, KnowledgeClass->SpecClass. Each as `MATCH (n:KnowledgeX) SET n:SpecX REMOVE n:KnowledgeX`.
    5. `DROP INDEX knowledge_note_search IF EXISTS`.
    6. `CREATE FULLTEXT INDEX spec_note_search IF NOT EXISTS FOR (n:SpecNote) ON EACH [n.title, n.content]`.
    7. Read-only VERIFICATION block (commented, with `-- EXPECT:` like Phase 14): `MATCH (n {graph:'KnowledgeGraph'}) RETURN count(n)` EXPECT 0; each of `count{(n:KnowledgeNote)}`...`KnowledgeClass` EXPECT 0; `MATCH (n {graph:'SpecGraph'}) RETURN labels(n), count(*)` shows Spec* labels; `SHOW FULLTEXT INDEXES` includes spec_note_search and not knowledge_note_search.
    Relationships (TAGGED_WITH, INSTANCE_OF) are untouched — they survive the label rename automatically (Neo4j relationships bind to node identity, not label).
    Do NOT include any hedging/"v1/for-now" language — this is the full SPEC-01 migration.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && f=migrations/2026-07-03_knowledge_to_spec_rename.cypher && test -f "$f" && grep -qi "DEV DATABASES ONLY" "$f" && grep -q "SET n.graph = 'SpecGraph'" "$f" && grep -q "SET n:SpecNote" "$f" && grep -q "REMOVE n:KnowledgeNote" "$f" && grep -q "DROP INDEX knowledge_note_search IF EXISTS" "$f" && grep -q "CREATE FULLTEXT INDEX spec_note_search IF NOT EXISTS" "$f" && grep -q "SpecClass" "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists with a DEV-DATABASES-ONLY guard + identity-check query + Step 0 dry-run.
    - All four operations present: graph value rename, four label SET/REMOVE renames, index DROP, index CREATE for (n:SpecNote).
    - KnowledgeClass hub `name`/`label` values normalized to SpecNote/SpecSession before the label rename.
    - VERIFICATION block asserts `graph:'KnowledgeGraph'` count EXPECT 0 and Spec* presence.
    - Re-running the whole file is a no-op (idempotent) — no statement errors on second run.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Live seed -> migrate -> verify on dev Neo4j (SC#1 proof)</name>
  <files>migrations/2026-07-03_knowledge_to_spec_rename.cypher</files>
  <read_first>
    - migrations/2026-07-03_knowledge_to_spec_rename.cypher (the file under test)
    - test/seed_knowledge_nodes.cypher
  </read_first>
  <action>
    On the DEV Neo4j only: run the seed script; capture Step 0 dry-run counts; run the migration write steps; run the VERIFICATION block. Record before/after counts (KnowledgeGraph N -> 0; SpecGraph 0 -> N; four Knowledge* labels -> 0; four Spec* labels present; spec_note_search index present). Paste the captured counts into 15-01-SUMMARY.md as SC#1 evidence. This is a manual step because it needs a running Neo4j instance.
  </action>
  <acceptance_criteria>
    - 15-01-SUMMARY.md contains before/after counts showing `graph:'KnowledgeGraph'` went to 0 and Spec* labels/`graph:'SpecGraph'` are populated.
    - `SHOW FULLTEXT INDEXES` output confirms spec_note_search exists, knowledge_note_search gone.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
New files: `migrations/2026-07-03_knowledge_to_spec_rename.cypher`, `test/seed_knowledge_nodes.cypher`. No new application symbols or config keys. New DB identifiers introduced (asserted by later plans): graph value `SpecGraph`; labels `SpecNote`/`SpecTag`/`SpecSession`/`SpecClass`; fulltext index `spec_note_search`.
</artifacts_produced>

<verification>
- Migration file has the four operations + dev-only guard + idempotent guards + verification block.
- Seed file creates the pre-migration Knowledge* state.
- Manual dev-DB run captures SC#1 before/after counts into the summary.
</verification>

<success_criteria>
SPEC-01 satisfied: a single dated, dev-guarded, idempotent migration renames the graph value, four labels, and the fulltext index (with hub-node name normalization), proven live by seed->migrate->verify counts showing zero remaining `graph:'KnowledgeGraph'` nodes.
</success_criteria>

<output>
Create `.planning/phases/15-specgraph-runtime-rename/15-01-SUMMARY.md` when done (include the captured before/after counts).
</output>
