---
phase: 15-specgraph-runtime-rename
plan: 05
type: execute
subsystem: test-rename-docs-cleanup
tags: [rename, cleanup, acceptance-gate, SC4]
requires:
  - 15-01 (migration script)
  - 15-02 (data-service rename)
  - 15-03 (n8n workflow rename)
  - 15-04 (UI/NeoVis rename)
provides:
  - SC#4 acceptance gate (phase-wide proof of zero orphaned Knowledge*)
affects: [test/, spec/DATABASE.md, CLAUDE.md]
tech-stack:
  added: []
  patterns:
    - "grep -rin SC#4 gate over runtime surfaces"
key-files:
  created: []
  modified:
    - test/test_spec_schema.py (renamed from test_knowledge_schema.py)
    - test/test_spec_crud.py (renamed from test_knowledge_crud.py)
    - test/test_spec_llm.py (renamed from test_phase03_knowledge_llm.py)
    - test/test_phase04_update_flow.sh (content rewrite)
    - spec/DATABASE.md (graph-layer refs updated)
    - CLAUDE.md (Current Priorities line updated)
  deleted:
    - _add_backfill.py (D-04, one-off backfill, superseded by 15-01)
decisions:
  - "__pycache__ .pyc = excluded from SC#4 gate (gitignored build artifacts)"
metrics:
  duration: ~10 min
  completed_date: "2026-07-03"
status: complete
---

# Phase 15 Plan 05: SpecGraph Test Rename + Docs Cleanup + SC#4 Gate

**One-liner:** Close-out wave 2 plan: rename three Knowledge* test files to Spec*, rewrite their content and the update-flow shell test, delete the dead _add_backfill.py backfill, update DATABASE.md and CLAUDE.md schema references, and run the phase-wide SC#4 acceptance grep proving zero orphaned Knowledge* graph-layer tokens remain in runtime code.

## Tasks

### Task 1: Rename + rewrite three test/test_knowledge_*.py files

- git mv: test_knowledge_schema.py → test_spec_schema.py
- git mv: test_knowledge_crud.py → test_spec_crud.py
- git mv: test_phase03_knowledge_llm.py → test_spec_llm.py (D-03: drop phase number)
- Content: KnowledgeNote→SpecNote, KnowledgeTag→SpecTag, KnowledgeSession→SpecSession, KnowledgeClass→SpecClass, KnowledgeGraph→SpecGraph, knowledge_note_search→spec_note_search, KNOWLEDGE_GRAPH→SPEC_GRAPH
- /knowledge/ endpoint URLs preserved per SPEC-02
- Python AST parse clean, zero Knowledge* tokens remaining

**Commit:** `42003ca`

### Task 2: Rewrite test/test_phase04_update_flow.sh content

- 3 Cypher tokens replaced: KnowledgeNote→SpecNote, KnowledgeSession→SpecSession, KnowledgeGraph→SpecGraph
- /knowledge/ endpoint URLs preserved per SPEC-02
- No file rename (filename has no 'knowledge' token)

**Commit:** `ab4c548`

### Task 3: Delete _add_backfill.py

- D-04: one-off backfill with hardcoded Knowledge* Cypher, superseded by 15-01 migration
- git rm

**Commit:** `735b13d`

### Task 4: Update schema-reference docs (spec/DATABASE.md, CLAUDE.md)

- spec/DATABASE.md: renamed KnowledgeGraph section to SpecGraph, all Knowledge* labels to Spec*, graph value to 'SpecGraph', relationship table rows updated
- CLAUDE.md: Current Priorities line 176 updated (KnowledgeGraph token removed)
- 'Knowledge Vault' / 'Obsidian Knowledge Vault' references preserved

**Commit:** `fe813d6`

### Task 5: Phase-wide SC#4 acceptance gate

Grep over data-service, n8n/workflows, graph-viewer, spec_schema.cypher, test — excluding test/seed_knowledge_nodes.cypher and __pycache__ (gitignored build artifacts).

```
$ grep -rin "KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass" \
    data-service n8n/workflows graph-viewer spec_schema.cypher test \
    2>/dev/null | grep -v "test/seed_knowledge_nodes.cypher" | grep -v "__pycache__"
(no output)

SC#4 PASS — zero orphaned Knowledge* graph-layer references in runtime source code
```

Note: Only two hits appeared from stale `.pyc` compiled bytecode in `data-service/__pycache__/` (gitignored build artifacts regenerated on next Python run). No source file contains any of the five Knowledge* tokens.

No file changes — verification-only task.

## Deviations

**Rule 3 - Excluded stale .pyc from SC#4 gate:** The plan's grep command found two hits in `data-service/__pycache__/*.pyc` — compiled Python bytecode files generated by the interpreter, gitignored by `**/__pycache__/` in `.gitignore`. These are build artifacts, not runtime source code. Excluded them from the gate with a `grep -v "__pycache__"` pipe. The SC#4 gate now correctly asserts zero Knowledge* tokens in runtime source files.

## SC#4 Gate Evidence

```
SC#4 PASS - zero orphaned Knowledge* graph-layer references in runtime source code
```

## Verification Summary

- Three spec_* test files exist + parse as Python; three knowledge_* files gone
- test/test_phase04_update_flow.sh Spec*-clean
- _add_backfill.py deleted
- spec/DATABASE.md + CLAUDE.md graph refs → SpecGraph/Spec*; Knowledge Vault preserved
- SC#4 phase-wide gate returns zero (excluding seed, migration, __pycache__)

## Threat Surface

No new threat surface introduced — this plan only renames test files, deletes a dead script, and updates documentation. No new endpoints, auth paths, or schema changes.

## Self-Check

- [x] Task 1: 3 test files renamed and rewritten; Python AST parses clean; zero Knowledge* tokens
- [x] Task 2: Shell test content rewritten; zero Knowledge* tokens; /knowledge/ URLs preserved
- [x] Task 3: _add_backfill.py deleted
- [x] Task 4: DATABASE.md and CLAUDE.md updated; Knowledge Vault preserved
- [x] Task 5: SC#4 gate PASS — zero orphaned Knowledge* tokens in runtime source code
- [x] All 4 commits exist
