---
phase: 15-specgraph-runtime-rename
plan: 05
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified:
  - test/test_knowledge_schema.py
  - test/test_spec_schema.py
  - test/test_knowledge_crud.py
  - test/test_spec_crud.py
  - test/test_phase03_knowledge_llm.py
  - test/test_spec_llm.py
  - test/test_phase04_update_flow.sh
  - _add_backfill.py
  - spec/DATABASE.md
  - CLAUDE.md
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-04]
nyquist_compliant: true

must_haves:
  truths:
    - "test/test_knowledge_schema.py -> test/test_spec_schema.py, test/test_knowledge_crud.py -> test/test_spec_crud.py, test/test_phase03_knowledge_llm.py -> test/test_spec_llm.py (drop phase# per D-03), all via git mv, with content Knowledge*->Spec* (D-01)"
    - "test/test_phase04_update_flow.sh content is Knowledge*->Spec* (no rename — filename has no 'knowledge' token)"
    - "_add_backfill.py is DELETED (D-04 — one-off backfill superseded by the 15-01 migration)"
    - "spec/DATABASE.md and CLAUDE.md graph-schema references rename KnowledgeGraph->SpecGraph and Knowledge* labels->Spec* (Claude's-discretion: done now to prevent drift for Phases 16-19); the Obsidian 'Knowledge Vault' wording is NOT a graph-layer token and stays"
    - "PHASE-WIDE ACCEPTANCE GATE (SC#4): grep -ri for the five Knowledge* graph-layer tokens over data-service/, n8n/workflows/, graph-viewer/, spec_schema.cypher, and test/ returns ZERO, excluding the intentional holders (test/seed_knowledge_nodes.cypher and migrations/)"
  artifacts:
    - "test/test_spec_schema.py, test/test_spec_crud.py, test/test_spec_llm.py (renamed)"
    - "spec/DATABASE.md, CLAUDE.md (schema refs updated)"
  key_links:
    - "The SC#4 gate is the phase-level proof that 15-01..15-04 left no orphaned Knowledge* graph-layer reference in runtime code"
  prohibitions:
    - "Do NOT flag or edit test/seed_knowledge_nodes.cypher (15-01) or migrations/2026-07-03_knowledge_to_spec_rename.cypher — they legitimately hold Knowledge* tokens (pre-migration seed + migration MATCH/REMOVE clauses); the gate excludes them"
    - "Do NOT rename 'Knowledge Vault' / Obsidian references in CLAUDE.md — not a graph-layer token, out of scope"
    - "Do NOT panic at .claude/worktrees/ copies — the gate does not scan them (RESEARCH L-2)"
---

<objective>
Close out the phase: rename the three `test/test_knowledge_*.py` files and edit their content + the update-flow shell test, delete the dead `_add_backfill.py`, update the schema-reference docs, and run the phase-wide SC#4 acceptance grep that proves zero orphaned Knowledge* graph-layer references remain across the runtime. This plan is wave 2 — it depends on 15-01..15-04 so the final gate is meaningful.

Purpose: SC#4 is the acceptance gate for the whole phase; this plan owns it. The test/doc edits are the last Knowledge* holders outside the migration + seed.
Output: 3 renamed test files, 1 edited shell test, `_add_backfill.py` deleted, 2 docs updated, SC#4 = 0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-specgraph-runtime-rename/15-CONTEXT.md
@.planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md
@.planning/phases/15-specgraph-runtime-rename/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename + rewrite the three test/test_knowledge_*.py files</name>
  <files>test/test_knowledge_schema.py, test/test_spec_schema.py, test/test_knowledge_crud.py, test/test_spec_crud.py, test/test_phase03_knowledge_llm.py, test/test_spec_llm.py</files>
  <read_first>
    - test/test_knowledge_schema.py (31 Knowledge* refs — the heaviest), test/test_knowledge_crud.py (1), test/test_phase03_knowledge_llm.py (7)
    - spec_schema.cypher (the renamed Spec* schema these tests assert against, from 15-02)
  </read_first>
  <action>
    `git mv` each: test_knowledge_schema.py -> test_spec_schema.py; test_knowledge_crud.py -> test_spec_crud.py; test_phase03_knowledge_llm.py -> test_spec_llm.py (drop the phase number, D-03). In each file, replace graph-layer tokens: KnowledgeNote/KnowledgeTag/KnowledgeSession/KnowledgeClass -> Spec*, graph:'KnowledgeGraph'/'KnowledgeGraph' -> 'SpecGraph', index name knowledge_note_search -> spec_note_search, and any reference to the schema file knowledge_schema.cypher -> spec_schema.cypher. If a test imports/reads a renamed artifact by path, update the path. Do not change lowercase URL tokens (/knowledge/ endpoints) the tests may hit — SPEC-02.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test -f test/test_spec_schema.py && test -f test/test_spec_crud.py && test -f test/test_spec_llm.py && test ! -f test/test_knowledge_schema.py && test ! -f test/test_knowledge_crud.py && test ! -f test/test_phase03_knowledge_llm.py && test -z "$(grep -rin 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' test/test_spec_schema.py test/test_spec_crud.py test/test_spec_llm.py)" && for x in test/test_spec_schema.py test/test_spec_crud.py test/test_spec_llm.py; do python -c "import ast; ast.parse(open('$x').read())" || exit 1; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The three knowledge_* test files are gone; the three spec_* files exist and parse as Python.
    - Zero Knowledge* graph-layer tokens in the renamed test files; spec_note_search / spec_schema.cypher references updated.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite test/test_phase04_update_flow.sh content</name>
  <files>test/test_phase04_update_flow.sh</files>
  <read_first>
    - test/test_phase04_update_flow.sh (3 Knowledge* refs — confirm they are graph-layer labels vs. URL tokens)
  </read_first>
  <action>
    Replace graph-layer Knowledge* tokens (KnowledgeSession/KnowledgeNote/etc., graph:'KnowledgeGraph') with Spec* / 'SpecGraph'. No file rename — the filename carries no 'knowledge' token. Preserve any /knowledge/ endpoint URL the script curls (SPEC-02).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' test/test_phase04_update_flow.sh)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero Knowledge* graph-layer tokens in the shell test; any /knowledge/ endpoint URLs preserved.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete _add_backfill.py</name>
  <files>_add_backfill.py</files>
  <read_first>
    - _add_backfill.py (confirm it is the one-off backfill with hardcoded Knowledge* Cypher, per D-04)
    - .planning/phases/15-specgraph-runtime-rename/15-CONTEXT.md D-04
  </read_first>
  <action>
    `git rm _add_backfill.py`. It is a historical one-off; the 15-01 migration supersedes it. If already run it is dead code; if not run it would now carry wrong labels (D-04).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test ! -f _add_backfill.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - _add_backfill.py is removed from the working tree and staged for deletion.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update schema-reference docs (spec/DATABASE.md, CLAUDE.md)</name>
  <files>spec/DATABASE.md, CLAUDE.md</files>
  <read_first>
    - spec/DATABASE.md (13 Knowledge* refs — the KnowledgeGraph section)
    - CLAUDE.md (1 Knowledge* graph-layer ref — locate it; the '## Knowledge Vault' / Obsidian wording is NOT it and stays)
  </read_first>
  <action>
    In spec/DATABASE.md: rename the KnowledgeGraph layer section and its Knowledge* labels to SpecGraph / Spec* + graph:'SpecGraph'; update the index name to spec_note_search. In CLAUDE.md: update the single graph-layer Knowledge* reference to SpecGraph/Spec*. Do NOT touch the 'Knowledge Vault' / 'Obsidian Knowledge Vault' phrases (documentation of the DG_OBSIDIAN vault, not the graph layer — and not caught by the token grep). Keep edits minimal and factual.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' spec/DATABASE.md CLAUDE.md)" && grep -q "SpecGraph" spec/DATABASE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero Knowledge* graph-layer tokens in spec/DATABASE.md and CLAUDE.md; SpecGraph present in DATABASE.md.
    - 'Knowledge Vault' Obsidian references preserved in CLAUDE.md.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Phase-wide SC#4 acceptance gate</name>
  <files>data-service, n8n/workflows, graph-viewer, spec_schema.cypher, test</files>
  <read_first>
    - .planning/phases/15-specgraph-runtime-rename/15-RESEARCH.md § "Execution Landmines" L-2 (why the gate is scoped, what to exclude)
    - .planning/ROADMAP.md Phase 15 SC#4 wording
  </read_first>
  <action>
    Run the phase-wide acceptance grep for the five Knowledge* graph-layer tokens over the runtime surfaces (data-service, n8n/workflows, graph-viewer, spec_schema.cypher, test), excluding the two intentional Knowledge* holders (test/seed_knowledge_nodes.cypher and anything under migrations/, which is not scanned). It MUST return nothing. If it returns any hit, that file was missed by 15-01..15-04 or this plan — fix it before completing. This is the SC#4 gate for the whole phase.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && MATCHES=$(grep -rin "KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass" data-service n8n/workflows graph-viewer spec_schema.cypher test 2>/dev/null | grep -v "test/seed_knowledge_nodes.cypher"); if [ -n "$MATCHES" ]; then echo "SC#4 FAIL:"; echo "$MATCHES"; exit 1; fi; echo "SC#4 PASS — zero orphaned Knowledge* graph-layer references"</automated>
  </verify>
  <acceptance_criteria>
    - The scoped grep over runtime surfaces returns zero Knowledge* graph-layer tokens (SC#4), excluding the seed script and migration.
    - Any hit is treated as a defect and fixed before the plan completes.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
Renamed: test/test_spec_schema.py, test/test_spec_crud.py, test/test_spec_llm.py (from test_knowledge_*). Deleted: _add_backfill.py. Edited in place: test/test_phase04_update_flow.sh, spec/DATABASE.md, CLAUDE.md. No new symbols. Task 5 introduces no artifact — it is the phase acceptance gate.
</artifacts_produced>

<verification>
- Three spec_* test files exist + parse; three knowledge_* gone; shell test Spec*-clean.
- _add_backfill.py deleted.
- spec/DATABASE.md + CLAUDE.md graph refs are SpecGraph/Spec*; Knowledge Vault wording preserved.
- SC#4 phase-wide gate returns zero (excluding seed + migration).
</verification>

<success_criteria>
The phase is Knowledge*-clean: all test files renamed/edited to Spec*, the dead backfill removed, docs aligned, and the SC#4 acceptance grep over data-service/ + n8n/workflows/ + graph-viewer/ + spec_schema.cypher + test/ returns only the intentional migration + seed references (i.e., zero in the scoped gate). Satisfies SC#4 and completes SPEC-01..04 coverage at the phase level.
</success_criteria>

<output>
Create `.planning/phases/15-specgraph-runtime-rename/15-05-SUMMARY.md` when done (paste the SC#4 gate output as evidence).
</output>
