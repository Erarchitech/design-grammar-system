---
phase: 16-dg-core-state-models-and-state-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DG/src/DG.Core/Services/DesignStateIdGenerator.cs
  - DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
user_setup: []

must_haves:
  truths:
    - DesignStateIdGenerator.ComputeDefStateId is renamed to ComputeParamStateId (same logic, same DS_ prefix)
    - DesignStateIdGenerator.ComputePropStateId exists (PS_ prefix, hash input = RuleIri|DataPropertyIri|propValue lex)
    - DesignStateIdGenerator.ComputeDesignStateId exists (DS_ prefix, hash input = sorted member StateIds)
    - DesignStateIdGenerator.ComputeObjectInstanceId is removed (no call sites per D-15, OI_ prefix constant removed)
    - All four ID methods produce deterministic 16-char hex prefixed hashes
    - Existing DesignStateIdGeneratorTests updated: ComputeDefStateId → ComputeParamStateId; ComputeObjectInstanceId test removed; new tests for ComputePropStateId and ComputeDesignStateId
  artifacts:
    - DG/src/DG.Core/Services/DesignStateIdGenerator.cs (updated — method rename, 2 new methods, 1 removed, OI_ constant removed)
    - DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs (updated)
  key_links:
    - ComputeParamStateId is sequential: calling code in ParameterStateComponent and downstream phases must use the new name
    - PropState's StateId is computed by ComputePropStateId — ID contract must match PropState model's StateId expectations
    - DesignState's aggregate StateId is computed by ComputeDesignStateId — input is sorted member StateIds, NOT the constituent models directly
---

<objective>
Update DesignStateIdGenerator to support Phase 16 models: rename ComputeDefStateId to ComputeParamStateId, add ComputePropStateId and ComputeDesignStateId, remove ComputeObjectInstanceId and the OI_ prefix constant. Update all existing tests and add new tests.

Purpose: The ID generator is the single source of truth for deterministic content-addressed StateId generation across all state models. Every downstream serialization, storage, and retrieval path relies on these IDs for dedup (MERGE by StateId+project).

Output: Updated DesignStateIdGenerator.cs, updated DesignStateIdGeneratorTests.cs
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dg-core-state-models-and-state-components/16-CONTEXT.md (D-11, D-15)
@.planning/phases/16-dg-core-state-models-and-state-components/16-RESEARCH.md

@DG/src/DG.Core/Services/DesignStateIdGenerator.cs
@DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs
@DG/src/DG.Core/Models/DesignStateParameter.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update DesignStateIdGenerator with Phase 16 methods</name>
  <files>
    DG/src/DG.Core/Services/DesignStateIdGenerator.cs
  </files>
  <read_first>
    DG/src/DG.Core/Services/DesignStateIdGenerator.cs — current state: 3 methods + HashToHex16 helper, 3 prefix constants
    16-RESEARCH.md — Code Examples section: DesignStateIdGenerator Additions (exact method signatures, hash input format)
    16-CONTEXT.md — D-11 (ComputePropStateId: PS_ + SHA-256(ruleIri|dataPropertyIri|propValue)), D-15 (rename + add + remove)
  </read_first>
  <behavior>
    - ComputeDefStateId method is renamed to ComputeParamStateId — same body, same return prefix (DS_)
    - ComputePropStateId uses PS_ prefix, hash input = $"{ruleIri}|{dataPropertyIri}|{lex}" where lex is the DesignStateParameter's type-specific value string
    - ComputeDesignStateId uses DS_ prefix, hash input = concatenated sorted member StateIds
    - ComputeObjectInstanceId method removed
    - OI_ prefix constant (ObjectInstancePrefix) removed
    - IdRefPrefix constant (IDR_) preserved — not related to Phase 16 scope
    - DefStatePrefix constant renamed to ParamStatePrefix (value "DS_" unchanged)
    - ObjectStatePrefix constant ("OS_") preserved unchanged
  </behavior>
  <action>
    Modify DG/src/DG.Core/Services/DesignStateIdGenerator.cs:

    Constant renames:
    - Rename `DefStatePrefix` to `ParamStatePrefix` (value still "DS_")
    - Remove `ObjectInstancePrefix` constant (value "OI_") entirely per D-15 Claude's discretion — keeping dead constants creates confusion
    - Keep `ObjectStatePrefix` ("OS_") and `IdRefPrefix` ("IDR_") unchanged

    Method rename:
    - Rename `ComputeDefStateId(IEnumerable&lt;DesignStateParameter&gt; parameters)` to `ComputeParamStateId(...)` — same body, same DS_ prefix via ParamStatePrefix. Update XML doc comment to reference ParamState.

    New method — ComputePropStateId:
    ```csharp
    public static string ComputePropStateId(string ruleIri, string dataPropertyIri, DesignStateParameter propValue)
    ```
    - Hash input: concatenate ruleIri + "|" + dataPropertyIri + "|" + propValue's lex with `CultureInfo.InvariantCulture`
    - Lex value follows the same type-switch pattern as ComputeParamStateId (Number → "R" format, Integer → invariant, Boolean → invariant, null → "null")
    - Return: `PropStatePrefix + HashToHex16(input)` — but no PropStatePrefix constant exists yet. Add a private const `PropStatePrefix = "PS_";` per D-11
    - Follow the same XML doc comment pattern as existing methods

    New method — ComputeDesignStateId:
    ```csharp
    public static string ComputeDesignStateId(IEnumerable<string> memberStateIds)
    ```
    - Sort the memberStateIds with `StringComparer.Ordinal`, concatenate into StringBuilder (no separator between IDs — the IDs have fixed-length prefixes so adjacent IDs are self-delimiting)
    - Return: DS_ prefix + HashToHex16 of the concatenated sorted string
    - No new prefix constant needed — reuse DesignStatePrefix (which doesn't exist yet either). Actually, use a private const `DesignStatePrefix = "DS_"` — note this shares the same literal "DS_" as ParamStatePrefix but the hash input domains are different (parameters vs. member StateId concatenation), so IDs from the two methods are distinct
    - Add XML doc explaining the aggregate hash: "Deterministic over sorted member StateIds. Same member set → same StateId → dedup across validation runs via MERGE by StateId+project."

    Removal:
    - Remove `ComputeObjectInstanceId(string projectId, string variableName, string seed)` entirely — method body and XML doc per D-15
    - Remove `ObjectInstancePrefix` constant

    Keep unchanged:
    - `ComputeObjectStateId(string projectId, string objectInstanceId, string variableName)` — existing method stays, OS_ prefix unchanged
    - `HashToHex16(string input)` — private helper unchanged
    - `IdRefPrefix` — preserved

    Add new prefix constants at the top of the class with the existing ones:
    - `private const string PropStatePrefix = "PS_";`
    - `private const string DesignStatePrefix = "DS_";` (note: same literal as ParamStatePrefix but different semantic domain per Research Finding 6)
  </action>
  <verify>
    <automated>dotnet build DG/DG.sln 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    - `dotnet build DG/DG.sln` succeeds
    - grep -c "ComputeDefStateId" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 0
    - grep -c "ComputeParamStateId" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 1
    - grep -c "ComputePropStateId" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 1
    - grep -c "ComputeDesignStateId" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 1
    - grep -c "ComputeObjectInstanceId" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 0
    - grep -c "ObjectInstancePrefix\|OI_" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 0
    - grep -c "PropStatePrefix.*PS_" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 1
    - grep -c "DesignStatePrefix.*DS_" DG/src/DG.Core/Services/DesignStateIdGenerator.cs returns 1
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update DesignStateIdGeneratorTests for Phase 16 methods</name>
  <files>
    DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs
  </files>
  <read_first>
    DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs — current tests calling ComputeDefStateId, ComputeObjectStateId, ComputeObjectInstanceId
    DG/src/DG.Core/Services/DesignStateIdGenerator.cs — updated file from Task 1
  </read_first>
  <behavior>
    - All existing ComputeDefStateId calls become ComputeParamStateId
    - ComputeObjectInstanceId test is removed
    - New ComputePropStateId test for determinism and PS_ prefix
    - New ComputeDesignStateId test for sorted-member hash and DS_ prefix
    - ComputeObjectStateId tests preserved unchanged (OS_ prefix, 3 string params)
  </behavior>
  <action>
    Modify DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs:

    Update existing tests:
    - Rename `ComputeDefStateId_ShouldBeDeterministic_RegardlessOfInputOrder` → `ComputeParamStateId_ShouldBeDeterministic_RegardlessOfInputOrder` — change the method call from `ComputeDefStateId` to `ComputeParamStateId`. Assert.StartsWith "DS_" still correct (prefix value unchanged).
    - Rename `ComputeDefStateId_ShouldChange_WhenParameterIsAdded` → `ComputeParamStateId_ShouldChange_WhenParameterIsAdded` — same body with ComputeParamStateId call. Assert.NotEqual behavior unchanged.
    - Keep `ComputeObjectStateId_ShouldBeDeterministic_ForSameInputs` unchanged (OS_ prefix, 3 string params)
    - Keep `ComputeObjectStateId_ShouldHaveExactlyThreeStringParameters_ProvingCrossRuleIdentity` unchanged
    - Keep `ComputeObjectStateId_ShouldChange_WhenObjectInstanceIdChanges` unchanged
    - Remove `ComputeObjectInstanceId_ShouldProduceOiPrefixedString` entirely — both the [Fact] method and its body

    Add new tests for ComputePropStateId:
    - `ComputePropStateId_ShouldBeDeterministic_ForSameInputs` — call with same ruleIri+dataPropertyIri+propValue twice, Assert.Equal the IDs, Assert.StartsWith("PS_", id)
    - `ComputePropStateId_ShouldChange_WhenRuleIriChanges` — same dataPropertyIri and propValue, different ruleIri → Assert.NotEqual
    - `ComputePropStateId_ShouldChange_WhenValueChanges` — same IRIs, different propValue → Assert.NotEqual
    - Follow existing BuildParameters() pattern — create DesignStateParameter instances inline

    Add new tests for ComputeDesignStateId:
    - `ComputeDesignStateId_ShouldBeDeterministic_ForSameMemberIds` — same sorted list of StateId strings twice → Assert.Equal, Assert.StartsWith("DS_", id)
    - `ComputeDesignStateId_ShouldBeDeterministic_RegardlessOfOrder` — ["OS_abc", "PS_def", "DS_ghi"] vs reversed → Assert.Equal (per D-04: sorted member StateIds)
    - `ComputeDesignStateId_ShouldChange_WhenMembersChange` — different member sets → Assert.NotEqual
  </action>
  <verify>
    <automated>dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~DesignStateIdGenerator" 2>&1</automated>
  </verify>
  <done>
    - `dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~DesignStateIdGenerator"` passes (all tests green)
    - grep -c "ComputeDefStateId" DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs returns 0
    - grep -c "ComputeParamStateId" DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs returns 2+ (method calls in tests)
    - grep -c "ComputeObjectInstanceId" DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs returns 0
    - grep -c "ComputePropStateId" DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs returns 3+ (method calls)
    - grep -c "ComputeDesignStateId" DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs returns 3+ (method calls)
  </done>
</task>

</tasks>

<artifacts>
| Symbol | Kind | File |
|--------|------|------|
| `DesignStateIdGenerator.ComputeParamStateId` | static method (RENAMED) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.ComputePropStateId(string, string, DesignStateParameter)` | static method (NEW) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.ComputeDesignStateId(IEnumerable&lt;string&gt;)` | static method (NEW) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.ComputeDefStateId` | static method (REMOVED) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.ComputeObjectInstanceId` | static method (REMOVED) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.ParamStatePrefix` | private const (RENAMED from DefStatePrefix) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.PropStatePrefix` | private const (NEW) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.DesignStatePrefix` | private const (NEW) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
| `DesignStateIdGenerator.ObjectInstancePrefix` | private const (REMOVED) | DG/src/DG.Core/Services/DesignStateIdGenerator.cs |
</artifacts>

<verification>
- Full ID generator test suite passes: `dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~DesignStateIdGenerator"`
- `dotnet build DG/DG.sln` succeeds
- grep confirms all rename/remove/add changes
</verification>

<success_criteria>
- DesignStateIdGenerator has 4 public methods (ComputeParamStateId, ComputeObjectStateId, ComputePropStateId, ComputeDesignStateId)
- All 8+ tests pass (3 ComputeParamStateId, 3 ComputeObjectStateId, 3 ComputePropStateId, 3 ComputeDesignStateId)
- No ComputeDefStateId, ComputeObjectInstanceId, or OI_ references remain
</success_criteria>
