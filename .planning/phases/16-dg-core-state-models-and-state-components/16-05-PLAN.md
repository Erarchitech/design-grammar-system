---
phase: 16-dg-core-state-models-and-state-components
plan: 05
type: execute
wave: 3
depends_on:
  - 16-03
files_modified:
  - DG/src/DG.Grasshopper/Components/DesignStateComponent.cs
  - DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs
  - DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs
  - DG/src/DG.Grasshopper/DgIcons.cs
  - DG/src/DG.Grasshopper/Properties/DesignState24.png
  - DG/src/DG.Grasshopper/Properties/ParameterState24.png
  - DG/src/DG.Grasshopper/Properties/ObjectState24.png
autonomous: true
requirements:
  - GHST-01
  - GHST-02
user_setup: []

must_haves:
  truths:
    - PARAMETER STATE component exists with a new GUID (replaces B3D1E7F2-A945-4C8B-B6F0-1A0D3C4E9B72), same IGH_VariableParameterComponent pattern, same capture logic per D-13
    - PARAMETER STATE outputs DG.ParamState (not DesignStateSnapshot) per D-14
    - PARAMETER STATE component name is "PARAMETER STATE" (not "DESIGN STATE") with abbreviation "PARAMSTATE"
    - OBJECT STATE component exists with fixed inputs Object/Geometry/Label, outputs DG.ObjState per GHST-01
    - OBJECT STATE enforces equal-length enforcement for all three parallel inputs per D-03, using ErrorMessageTemplates.ObjStateMismatchedListLengths
    - Both components compile under #if GRASSHOPPER_SDK guard, following existing component pattern
    - DesignState24.png icon is renamed to ParameterState24.png; ObjectState24.png icon created as placeholder
  artifacts:
    - DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs (renamed from DesignStateComponent.cs)
    - DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs (NEW)
    - DG/src/DG.Grasshopper/Properties/ParameterState24.png (renamed from DesignState24.png)
    - DG/src/DG.Grasshopper/Properties/ObjectState24.png (NEW)
    - DG/src/DG.Grasshopper/DgIcons.cs (updated — DesignState24 renamed to ParameterState24, ObjectState24 added)
  key_links:
    - ParameterStateComponent reuses the exact UnwrapScalar+BuildParameter+BuildSnapshot pattern from DesignStateComponent — capture logic is unchanged
    - ObjectStateComponent's index-mismatch guard fires BEFORE iteration, preventing partial iteration — da.SetData only called if guard passes
    - DgIcons.cs must have ParameterState24 entry (pointing to ParameterState24.png) and ObjectState24 entry
---

<objective>
Rename the v2.0 DESIGN STATE component to PARAMETER STATE (new GUID, updated output type, DgIcons entry). Create the new OBJECT STATE component with fixed Object/Geometry/Label inputs, index-mismatch guard, and ObjState output.

Purpose: PARAMETER STATE preserves the dynamic-input capture workflow from v2.0 while aligning the component name with the ontology (dg:ParamState). OBJECT STATE enables architects to capture object identity (Object reference + geometry handle + label) as a composable ObjState for downstream DESIGN STATE aggregation.

Output: ParameterStateComponent.cs, ObjectStateComponent.cs, updated DgIcons.cs, icon assets
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-dg-core-state-models-and-state-components/16-CONTEXT.md (D-03, D-13, D-14)
@.planning/phases/16-dg-core-state-models-and-state-components/16-RESEARCH.md (Pattern 3: IGH_VariableParameterComponent, Pattern 4: Fixed-Input PublicType, DesignStateComponent.cs filename conflict, Key Finding 7)

@DG/src/DG.Grasshopper/Components/DesignStateComponent.cs — rename target, contains IGH_VariableParameterComponent pattern
@DG/src/DG.Grasshopper/Components/GhCastingHelpers.cs — Unwrap<T> and TryObjState helpers
@DG/src/DG.Grasshopper/PublicTypes.cs — DG.ParamState, DG.ObjState wrappers
@DG/src/DG.Grasshopper/DgIcons.cs — icon loading pattern
@DG/src/DG.Grasshopper/DgComponentCategory.cs — category constant
@DG/src/DG.Core/Services/ErrorMessageTemplates.cs — ObjStateMismatchedListLengths message
@DG/src/DG.Core/Models/ObjState.cs — model class
@DG/src/DG.Core/Models/ParamState.cs — model class
@DG/src/DG.Core/Services/DesignStateIdGenerator.cs — ComputeParamStateId
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename DesignStateComponent to ParameterStateComponent with new GUID and ParamState output</name>
  <files>
    DG/src/DG.Grasshopper/Components/DesignStateComponent.cs
    DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs
    DG/src/DG.Grasshopper/DgIcons.cs
    DG/src/DG.Grasshopper/Properties/DesignState24.png
    DG/src/DG.Grasshopper/Properties/ParameterState24.png
  </files>
  <read_first>
    DG/src/DG.Grasshopper/Components/DesignStateComponent.cs — full current implementation (276 lines, #if GRASSHOPPER_SDK guarded)
    DG/src/DG.Grasshopper/DgIcons.cs — icon property naming pattern
    DG/src/DG.Core/Services/DesignStateIdGenerator.cs — ComputeParamStateId (renamed from ComputeDefStateId) available for BuildSnapshot
    DG/src/DG.Core/Models/ParamState.cs — the model to output
    16-CONTEXT.md — D-13 (rename to PARAMETER STATE, new GUID), D-14 (output ParamState instead of DesignStateSnapshot)
  </read_first>
  <action>
    Rename DesignStateComponent.cs to ParameterStateComponent.cs. Four changes in the renamed file:

    1. Class rename: `DesignStateComponent` → `ParameterStateComponent`
    2. Component metadata:
       - name: "PARAMETER STATE" (was "DESIGN STATE")
       - abbreviation: "PARAMSTATE" (was "DSGSTATE")
       - description: "Capture named Number, Integer, and Boolean parameters into a reusable ParamState." (remove reference to "design state snapshot" — use "ParamState")
       - Output parameter description: "DG.ParamState — wire to DESIGN STATE ObjState input to compose into a DesignState, or to downstream state consumers." (update to reference ParamState)
       - Input parameter description: same as before — "Number, Integer, or Boolean parameter. Rename this NickName to set the ParameterId (used for reinstatement matching)."
    3. GUID: `new("B3D1E7F2-A945-4C8B-B6F0-1A0D3C4E9B72")` → generate a NEW GUID. Use any valid v4 UUID not present in the codebase. Pick: `new("A2E8C4F1-6B3D-4A9C-8E5F-2D7B0C1A3F6E")`
    4. Output type: Change from `DesignStateSnapshot` to `ParamState`:
       - In BuildSnapshot: return type changes from `DesignStateSnapshot` to `ParamState`
       - BuildSnapshot body: create `new ParamState { StateId = ..., CapturedAtUtc = ... }` instead of `new DesignStateSnapshot { ... }`
       - Keep ComputeStateId method — rename to `ComputeParamStateId`, delegate to `DesignStateIdGenerator.ComputeParamStateId(parameters)` instead of inline SHA-256 hash. Remove the local SHA256/Encoding imports and inline hash logic — the ID generator is the canonical home.
       - Actually wait — the current DesignStateComponent has its OWN ComputeStateId method doing the hash. Since we now have DesignStateIdGenerator.ComputeParamStateId, replace the local hash with a call to the ID generator. Remove the `using System.Security.Cryptography;` and `using System.Text;` imports, and replace the local `ComputeStateId` call with `DesignStateIdGenerator.ComputeParamStateId(parameters)`. Remove the local ComputeStateId method entirely.

    Icon updates in DgIcons.cs:
    - Rename property: `DesignState24` → `ParameterState24` (loads ParameterState24.png)
    - Keep the existing DesignState24.png file, copy/rename to ParameterState24.png in the Properties folder (same bitmap content — the capture component is the same, just renamed)

    #else branch update:
    - Change `#else` class name from `DesignStateComponent` to `ParameterStateComponent`

    Delete: Remove the original DesignStateComponent.cs file (after creating ParameterStateComponent.cs with all changes).

    Update all references within DG.Grasshopper assembly:
    - Check DG.Grasshopper for any `DesignStateComponent` type reference — update to `ParameterStateComponent`
  </action>
  <verify>
    <automated>dotnet build DG/DG.sln 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    - `dotnet build DG/DG.sln` succeeds
    - DG/src/DG.Grasshopper/Components/DesignStateComponent.cs no longer exists
    - DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs exists
    - grep -c "class ParameterStateComponent" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 1 (GH_SDK) + 1 (else) = 2
    - grep -c "PARAM STATE\|PARAMSTATE\|ParameterStateComponent\|PARAMETER STATE" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 4+ (class name, display name, abbreviation, description)
    - grep -c "B3D1E7F2" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 0 (old GUID removed)
    - grep -c "A2E8C4F1-6B3D-4A9C-8E5F-2D7B0C1A3F6E" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 1 (new GUID)
    - grep -c "DesignStateIdGenerator.ComputeParamStateId" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 1
    - grep -c "ComputeStateId\|SHA256\|SHA256.HashData" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 0 (internal hash removed, delegated to ID generator)
    - grep -c "DesignStateSnapshot" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 0
    - grep -c "ParamState" DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs returns 4+ (output type, helpers)
    - DG/src/DG.Grasshopper/Properties/ParameterState24.png exists
    - DG/src/DG.Grasshopper/Properties/DesignState24.png exists (or renamed — keep original if renaming, or copy)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ObjectStateComponent with index-mismatch guard</name>
  <files>
    DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs
    DG/src/DG.Grasshopper/DgIcons.cs
    DG/src/DG.Grasshopper/Properties/ObjectState24.png
  </files>
  <read_first>
    DG/src/DG.Grasshopper/Components/DesignStateComponent.cs — existing component pattern (GH_Component, RegisterInputParams, RegisterOutputParams, SolveInstance, Icon, ComponentGuid)
    DG/src/DG.Grasshopper/Components/GhCastingHelpers.cs — TryObjState, TryElementRef, Unwrap&lt;T&gt; patterns
    DG/src/DG.Grasshopper/DgIcons.cs — icon property addition pattern
    DG/src/DG.Core/Services/ErrorMessageTemplates.cs — ObjStateMismatchedListLengths method
    DG/src/DG.Core/Models/ObjState.cs — model to construct
    DG/src/DG.Grasshopper/PublicTypes.cs — DG.ObjState wrapper
    DG/src/DG.Grasshopper/DgComponentCategory.cs — Category, Subcategory constants
    16-CONTEXT.md — D-03 (index-mismatch guard in leaf components), GHST-01 requirement
  </read_first>
  <action>
    Create ObjectStateComponent.cs in DG.Grasshopper.Components namespace, following the #if GRASSHOPPER_SDK pattern.

    Class: `ObjectStateComponent : GH_Component` (no IGH_VariableParameterComponent — fixed-input per research)

    Constructor:
    - base("OBJECT STATE", "OBJSTATE", "Compose Object, Geometry, and Label into an ObjState for downstream DESIGN STATE composition.", DgComponentCategory.Category, DgComponentCategory.Subcategory)

    ComponentGuid: `new("F9A1D8B2-C4E6-4F7A-9B5D-3E8C2A1F6D0B")` (new GUID, not used elsewhere)

    Icon: DgIcons.ObjectState24 (add to DgIcons.cs: `public static Bitmap ObjectState24 { get; } = Load("ObjectState24.png");`)
    - Create a placeholder 24x24 PNG as ObjectState24.png in Properties folder (copy any existing icon as template)

    RegisterInputParams:
    - Input 0: "Object", "Object", "Object reference (ElementRef, GUID, or string identifier).", GH_ParamAccess.list
    - Input 1: "Geometry", "Geometry", "Rhino geometry reference (optional, in-process handle, not serialized).", GH_ParamAccess.list
    - Input 2: "Label", "Label", "User-supplied display label (optional).", GH_ParamAccess.list
    - All three Optional = true

    RegisterOutputParams:
    - Output 0: "ObjState", "ObjState", "DG.ObjState — wire to DESIGN STATE ObjState input to compose into a DesignState.", GH_ParamAccess.list

    SolveInstance logic:
    1. Retrieve all three input lists via da.GetDataList
    2. Index-mismatch guard (per D-03): Check if Object.Count != Geometry.Count or Object.Count != Label.Count. If mismatched, call AddRuntimeMessage(GH_RuntimeMessageLevel.Error, ErrorMessageTemplates.ObjStateMismatchedListLengths(...)) and da.SetData(0, null); return;
    3. Iterate with for loop over the common length i:
       a. Get input[i] for Object, Geometry, Label
       b. TryElementRef for Object input to extract DgEntityId/DisplayName as ObjectRef
       c. Use raw value for Geometry (just pass through as object? — the existing pattern from GhCastingHelpers.ToElementRef)
       d. For Label: convert to string if possible, otherwise null
       e. Build StateId via DesignStateIdGenerator.ComputeObjectStateId — but wait, ComputeObjectStateId takes (projectId, objectInstanceId, variableName). That's the v3.0 signature and doesn't match ObjState's structure.
    
    Actually, ObjState doesn't currently use ComputeObjectStateId (which is a v3.0 method with a different signature). For the new ObjState, there's NO dedicated ID method added in Plan 02 — only ComputeParamStateId, ComputePropStateId, and ComputeDesignStateId. The ObjState.StateId can be:
    - A simple content hash of ObjectRef + Label
    - Or empty (placeholder — Phase 18 or 19 may finalize the ObjState ID contract)
    
    Per D-15, only ComputeDefStateId→ComputeParamStateId, ComputePropStateId, ComputeDesignStateId, and ComputeObjectInstanceId removal were specified. No new ObjState ID method was added. So for Phase 16, use a straightforward content hash inline:
    - Hash input: ObjectRef + "|" + (Label ?? "") 
    - SHA-256 → 16-char hex, OS_ prefix
    - This is consistent with "deterministic hash" pattern but doesn't require a new ID generator method
    - Alternatively, use Guid.NewGuid().ToString() for now — but that's non-deterministic. Better to reuse the hash pattern.
    
    Use: `DesignStateIdGenerator.ComputeObjectStateId("_", objectRef, label ?? "")` — but that takes 3 params (projectId, objectInstanceId, variableName) which doesn't fit.
    
    Best approach: compute StateId inline in the component using SHA-256 similar to how the old DesignStateComponent had inline ComputeStateId before the ID generator existed. Use `OS_` + first 16 hex chars of SHA-256(ObjectRef + "|" + Label).
    
    f. Create DG.ObjState (the public wrapper) with StateId, ObjectRef, Geometry, Label, CapturedAtUtc = DateTimeOffset.UtcNow
    g. Add to output list
    
    4. da.SetData(0, results)

    #else branch:
    ```csharp
    #else
    namespace DG.Grasshopper.Components;
    public sealed class ObjectStateComponent
    {
    }
    #endif
    ```

    Add DgIcons.ObjectState24 property in DgIcons.cs inside the `#if GRASSHOPPER_SDK` block:
    ```csharp
    public static Bitmap ObjectState24 { get; } = Load("ObjectState24.png");
    ```

    Create ObjectState24.png in Properties folder by copying an existing 24x24 icon (e.g., DesignState24.png) as a visual placeholder. The file must exist as an embedded resource for DgIcons.Load to find it.

    Also check if the #else branch in GhCastingHelpers.cs needs any stub — no, all new Try* methods were added in Plan 03 which already has its #else branch. The component file only adds a new GH_Component subclass which is inherently guarded by #if GRASSHOPPER_SDK.
  </action>
  <verify>
    <automated>dotnet build DG/DG.sln 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    - `dotnet build DG/DG.sln` succeeds
    - DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs exists
    - grep -c "class ObjectStateComponent" DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs returns 2 (one GH_SDK, one else)
    - grep -c "OBJECT STATE" DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs returns 1+ (component display name)
    - grep -c "ObjStateMismatchedListLengths" DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs returns 1 (D-03 guard)
    - grep -c "ObjectState24" DG/src/DG.Grasshopper/DgIcons.cs returns 1
    - DG/src/DG.Grasshopper/Properties/ObjectState24.png exists (as embedded resource)
  </done>
</task>

</tasks>

<artifacts>
| Symbol | Kind | File |
|--------|------|------|
| `DG.Grasshopper.Components.ParameterStateComponent` | GH component (RENAMED from DesignStateComponent) | DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs |
| `DG.Grasshopper.Components.ObjectStateComponent` | GH component (NEW) | DG/src/DG.Grasshopper/Components/ObjectStateComponent.cs |
| `DgIcons.ParameterState24` | static Bitmap (RENAMED from DesignState24) | DG/src/DG.Grasshopper/DgIcons.cs |
| `DgIcons.ObjectState24` | static Bitmap (NEW) | DG/src/DG.Grasshopper/DgIcons.cs |
| `ParameterState24.png` | embedded resource (RENAMED) | DG/src/DG.Grasshopper/Properties/ParameterState24.png |
| `ObjectState24.png` | embedded resource (NEW) | DG/src/DG.Grasshopper/Properties/ObjectState24.png |
</artifacts>

<verification>
- `dotnet build DG/DG.sln` succeeds
- grep confirms old DesignStateComponent.cs removed, ParameterStateComponent.cs with new GUID exists
- ObjStateMismatchedListLengths guard call confirmed in ObjectStateComponent.cs
</verification>

<success_criteria>
- PARAMETER STATE component has new GUID, outputs DG.ParamState, delegates hashing to DesignStateIdGenerator.ComputeParamStateId
- OBJECT STATE component has 3 fixed list inputs with index-mismatch guard, outputs DG.ObjState list
- All GH_SDK guarded code compiles; null stubs in #else branch
- Both icons loadable via embedded resources
</success_criteria>
