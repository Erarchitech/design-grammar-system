---
phase: 18-rules-and-validator-rework
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DG/src/DG.Core/Classification/VariableBinder.cs
  - DG/src/DG.Core/Classification/ClassificationResult.cs
  - DG/src/DG.Grasshopper/Components/ClassificatorComponent.cs
  - DG/src/DG.Grasshopper/DgIcons.cs
  - DG/src/DG.Grasshopper/Properties/Classificator24.png
  - DG/tests/DG.Tests/VariableBinderTests.cs
  - DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs
autonomous: true
requirements:
  - GHVL-02

must_haves:
  truths:
    - ClassificatorComponent.cs no longer exists anywhere in the solution
    - DG.Core.Classification namespace no longer exists (VariableBinder.cs, ClassificationResult.cs deleted)
    - DgIcons.Classificator24 reference removed
    - Classificator24.png file deleted from embedded resources
    - VariableBinderTests.cs no longer exists
    - No `using DG.Core.Classification` references remain anywhere in the solution
    - ParameterStateComponent comments no longer reference CLASSIFICATOR
    - dotnet build DG/DG.sln succeeds after all deletions
  artifacts:
    - (deleted files — no new files created)
  key_links:
    - BindingRow.cs at DG.Core.Models is NOT deleted (RuleEvaluator depends on it, not in DG.Core.Classification)
    - VariableTypeInferrer.cs at DG.Core.Parsing is NOT deleted (stays as-is per D-08)
    - The new DesignStateBindingService replaces the old VariableBinder role — no component fills CLASSIFICATOR's canvas role

user_setup: []
---

<objective>
Delete the CLASSIFICATOR component and all associated code.

Purpose: Per D-09, CLASSIFICATOR is fully purged. The component, its namespace (DG.Core.Classification), its icon, its tests, and all references are removed. The new DesignStateBindingService (Plan 18-01) replaces VariableBinder.BuildBindings logic. Release notes in Phase 20 document the re-wiring path.

Output: Deleted files — ClassificatorComponent.cs, DG.Core.Classification/*, Classificator24.png, VariableBinderTests.cs; modified DgIcons.cs and ParameterStateComponent.cs
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-rules-and-validator-rework/18-CONTEXT.md (D-04, D-09)

@DG/src/DG.Grasshopper/Components/ClassificatorComponent.cs
@DG/src/DG.Core/Classification/VariableBinder.cs
@DG/src/DG.Core/Classification/ClassificationResult.cs
@DG/src/DG.Grasshopper/DgIcons.cs
@DG/src/DG.Grasshopper/Properties/Classificator24.png
@DG/tests/DG.Tests/VariableBinderTests.cs
@DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs
@DG/src/DG.Core/Models/BindingRow.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete DG.Core.Classification namespace — VariableBinder.cs and ClassificationResult.cs</name>

  <read_first>
    @DG/src/DG.Core/Classification/VariableBinder.cs — file to delete
    @DG/src/DG.Core/Classification/ClassificationResult.cs — file to delete
    @DG/src/DG.Core/Models/BindingRow.cs — exists in DG.Core.Models, NOT in DG.Core.Classification — do NOT delete this file per Pitfall 1
  </read_first>

  <files>
    DG/src/DG.Core/Classification/VariableBinder.cs
    DG/src/DG.Core/Classification/ClassificationResult.cs
  </files>

  <action>
    Delete these two files using the Bash tool (rm):
    - `DG/src/DG.Core/Classification/VariableBinder.cs`
    - `DG/src/DG.Core/Classification/ClassificationResult.cs`

    Verify: after deletion, `ls DG/src/DG.Core/Classification/` should show the directory is empty or not exist. If the directory is empty, remove it: `rmdir DG/src/DG.Core/Classification/`.

    Do NOT delete or modify:
    - `DG/src/DG.Core/Models/BindingRow.cs` — this is in DG.Core.Models, NOT DG.Core.Classification. RuleEvaluator, FailingBindingFormatter, and DesignStateBindingService all depend on it.
    - `DG/src/DG.Core/Models/ElementRef.cs` — also not in Classification.
    - `DG/src/DG.Core/Parsing/VariableTypeInferrer.cs` — stays as-is per D-08.
  </action>

  <verify>
    <automated>bash -c "! test -f DG/src/DG.Core/Classification/VariableBinder.cs && ! test -f DG/src/DG.Core/Classification/ClassificationResult.cs && echo 'OK'"</automated>
  </verify>

  <acceptance_criteria>
    - VariableBinder.cs no longer exists at DG/src/DG.Core/Classification/
    - ClassificationResult.cs no longer exists at DG/src/DG.Core/Classification/
    - The Classification directory is deleted (no lingering empty folder)
    - BindingRow.cs at DG/src/DG.Core/Models/ still exists (NOT deleted)
    - VariableTypeInferrer.cs at DG/src/DG.Core/Parsing/ still exists (NOT deleted)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete ClassificatorComponent.cs, remove Classificator24 from DgIcons, and delete Classificator24.png</name>

  <read_first>
    @DG/src/DG.Grasshopper/Components/ClassificatorComponent.cs — file to delete (full file, all 114 lines)
    @DG/src/DG.Grasshopper/DgIcons.cs — line 15: `public static Bitmap Classificator24 { get; } = Load("Classificator24.png");` — line to remove
    @ontology/port-iri-map-V7.md — confirms CLASSIFICATOR is not in the 14-component schema
  </read_first>

  <files>
    DG/src/DG.Grasshopper/Components/ClassificatorComponent.cs
    DG/src/DG.Grasshopper/DgIcons.cs
    DG/src/DG.Grasshopper/Properties/Classificator24.png
  </files>

  <action>
    1. Delete `DG/src/DG.Grasshopper/Components/ClassificatorComponent.cs` using Bash (rm).

    2. Delete `DG/src/DG.Grasshopper/Properties/Classificator24.png` using Bash (rm).

    3. Edit `DG/src/DG.Grasshopper/DgIcons.cs` — remove the line:
       `public static Bitmap Classificator24 { get; } = Load("Classificator24.png");`
       This is at line 15 in the current file. After removal, the remaining icon declarations should be contiguous (no gap or comment placeholder for the deleted line).

    The DG.Grasshopper.csproj uses `EmbeddedResource Include="Properties\*.png"` wildcard — no .csproj change needed. With the .png file deleted, the wildcard simply won't include it.
  </action>

  <verify>
    <automated>bash -c "! test -f DG/src/DG.Grasshopper/Components/ClassificatorComponent.cs && ! test -f DG/src/DG.Grasshopper/Properties/Classificator24.png && echo 'OK'"</automated>
  </verify>

  <acceptance_criteria>
    - ClassificatorComponent.cs no longer exists at DG/src/DG.Grasshopper/Components/
    - Classificator24.png no longer exists at DG/src/DG.Grasshopper/Properties/
    - DgIcons.cs no longer contains `Classificator24` in any form
    - grep -c "Classificator" DG/src/DG.Grasshopper/DgIcons.cs == 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete VariableBinderTests.cs, clean up ParameterStateComponent comments, verify no using references remain</name>

  <read_first>
    @DG/tests/DG.Tests/VariableBinderTests.cs — file to delete (line 1: using DG.Core.Classification)
    @DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs — lines 12 and 22 reference "Classificator" in XML doc comments
  </read_first>

  <files>
    DG/tests/DG.Tests/VariableBinderTests.cs
    DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs
  </files>

  <action>
    1. Delete `DG/tests/DG.Tests/VariableBinderTests.cs` using Bash (rm).

    2. Edit `DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs`:
       - Line 12: Change `attachment to Classificator runs` to `attachment and filtering in Validation Runs`
       - Line 22: Change `Wire the "State" output to Classificator.State and/or Validation Runs.State.` to `Wire the "State" output to Validation Runs.State (or DESIGN STATE for v7.0+ composition).`

    3. Run a grep check across the entire DG/ directory to verify no `using DG.Core.Classification` references remain:
       `grep -rn "using DG.Core.Classification" DG/`
       Expected: zero results (only ClassificatorComponent.cs and VariableBinderTests.cs had it, both now deleted).

    4. Run a build check: `dotnet build DG/DG.sln --no-restore 2>&1` — must succeed with no errors related to missing Classification types.
  </action>

  <verify>
    <automated>bash -c "! test -f DG/tests/DG.Tests/VariableBinderTests.cs && echo 'VBTests deleted'"</automated>
  </verify>

  <acceptance_criteria>
    - VariableBinderTests.cs no longer exists at DG/tests/DG.Tests/
    - ParameterStateComponent.xml-doc-comment no longer references "Classificator"
    - grep -rn "using DG.Core.Classification" DG/ returns zero results
    - grep -rn "Classificator" DG/src/ (excluding git history) returns zero results
    - dotnet build DG/DG.sln (or DG/DG.Grasshopper/) succeeds with no errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure deletion — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Tampering | Accidental deletion of BindingRow.cs | high | mitigate | Verify BindingRow.cs exists at DG.Core.Models AFTER deletion (Task 1 verify step) |
| T-18-SC | Tampering | npm/pip/cargo installs | medium | mitigate | No new packages — build-only; slopcheck not needed |
</threat_model>

<verification>
1. All files deleted — confirmed by file-not-exists checks
2. `grep -rn "Classificator\|DG\.Core\.Classification" DG/src/` returns zero results
3. `dotnet build DG/DG.sln --no-restore` succeeds cleanly
4. BindingRow.cs at DG/src/DG.Core/Models/ is intact (not deleted)
</verification>

<success_criteria>
- CLASSIFICATOR component fully purged per D-09
- DG.Core.Classification namespace deleted — VariableBinder and ClassificationResult gone
- Classificator24 icon + PNG deleted
- VariableBinderTests deleted
- ParameterStateComponent comments updated
- Solution builds clean with zero Classificator/Classification references
- Release notes for Phase 20 will document the removal and re-wiring path
</success_criteria>

## Artifacts this phase produces

### Deleted files
- `DG/src/DG.Core/Classification/VariableBinder.cs` — variable binding logic (replaced by DesignStateBindingService)
- `DG/src/DG.Core/Classification/ClassificationResult.cs` — binding result model
- `DG/src/DG.Grasshopper/Components/ClassificatorComponent.cs` — GH component
- `DG/src/DG.Grasshopper/Properties/Classificator24.png` — component icon
- `DG/tests/DG.Tests/VariableBinderTests.cs` — unit tests for deleted code

### Deleted symbols
- `DG.Core.Classification.VariableBinder` (static class)
- `DG.Core.Classification.ClassificationResult` (sealed class)
- `DG.Grasshopper.Components.ClassificatorComponent` (GH component)
- `DG.Grasshopper.DgIcons.Classificator24` (Bitmap property)

### Modified files
- `DG/src/DG.Grasshopper/DgIcons.cs` — Classificator24 line removed
- `DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs` — comments updated

<output>
Create .planning/phases/18-rules-and-validator-rework/18-03-SUMMARY.md when done
</output>
