---
phase: 06-ui-update-panel-inline-diff-editor
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - graph-viewer/index.html
autonomous: false
requirements:
  - UIST-04
must_haves:
  truths:
    - "After Propose completes, the user sees a diff preview with red deletions and green additions for the current note"
    - "The user can collapse and expand the diff preview panel"
    - "The textarea below the diff is pre-populated with the proposed text and is editable"
    - "Next note / Previous note buttons navigate through notes sequentially"
    - "Skip this note excludes a note from the final Confirm batch"
    - "A summary view shows changed/skipped badges for all notes before Confirm"
    - "Confirm All sends non-skipped notes to the backend and shows success notification"
    - "After Confirm, the panel resets to the Match prompt"
  artifacts:
    - path: "graph-viewer/index.html"
      provides: "Complete Update Knowledge three-step flow"
      contains: "submitConfirm"
  key_links:
    - from: "graph-viewer/index.html (submitConfirm)"
      to: "/data-service/knowledge/update/confirm"
      via: "fetch POST"
      pattern: "knowledge/update/confirm"
---

<objective>
Complete the Update Knowledge flow with the Review view (diff + editor + sequential navigation), Summary view (changed/skipped badges + Confirm All), and Confirm handler with success notification and state reset.

Purpose: Delivers the full three-step update experience — architects review diffs, edit proposed text, and confirm writes.
Output: Complete working Update Knowledge mode in `graph-viewer/index.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-update-panel-inline-diff-editor/06-CONTEXT.md
@.planning/phases/06-ui-update-panel-inline-diff-editor/06-RESEARCH.md
@.planning/phases/06-ui-update-panel-inline-diff-editor/06-UI-SPEC.md
@.planning/phases/06-ui-update-panel-inline-diff-editor/06-01-SUMMARY.md
@graph-viewer/index.html
</context>

<interfaces>
<!-- Backend endpoint for Confirm (Phase 4) -->

POST /knowledge/update/confirm
Request:  { notes: [{ noteId: string, content: string, updatedAt: string }], project: string, prompt: string }
Response: { affectedNoteIds: string[], sessionId: string }
Error:    409 on stale updatedAt (optimistic lock), 413 on content > 100KB

<!-- State available from Plan 01 -->
updateView: "match" | "proposing" | "review" | "summary"
updateDiffs: [{ noteId, title, originalContent, proposedContent, diffHtml, hasChanges, updatedAt }]
currentNoteIndex: number (index into updateDiffs)
editedContents: { [noteId]: string } — initialized to proposedContent per note
skippedNoteIds: string[] — noteIds user clicked Skip on
showDiff: boolean — diff panel toggle
resetUpdateState(): void — resets all update state vars to initial values
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Review view — diff panel, editor, navigation</name>
  <files>graph-viewer/index.html</files>
  <action>
Add the Review view inside the `knowledgeMode === "update"` conditional, rendered when `updateView === "review"`. This is the sequential note-by-note editor per D-05 through D-10.

**Structure (top to bottom):**

1. **"Back to search" button** (per D-16):
   - `className: "secondary-btn"`, `style: { marginBottom: "12px" }`
   - onClick: call `resetUpdateState()` to return to Match view

2. **Note header** — flex row with:
   - Note title: `updateDiffs[currentNoteIndex]?.title` in bold (13px, weight 700)
   - Counter: `"Note " + (currentNoteIndex + 1) + " of " + updateDiffs.length` in muted color (12px, `color: "#98a4bd"`)

3. **If `updateDiffs[currentNoteIndex]?.hasChanges === true`** (per D-05, D-06, D-07):

   a. Collapsible diff toggle (per D-07):
   - Text button: `showDiff ? "Hide diff" : "Show diff"` — 12px, accent color (`#5fd0ff`)
   - onClick: `setShowDiff(prev => !prev)`

   b. Diff preview panel (only when `showDiff` is true):
   - `div` with `className: "diff-preview"` and `dangerouslySetInnerHTML: { __html: updateDiffs[currentNoteIndex]?.diffHtml || "" }`
   - The CSS class `.diff-preview` was added in Plan 01 Task 1

   c. Editable textarea (per D-05, D-08):
   - Reuse `className: "prompt-input"` for styling
   - Value: `editedContents[updateDiffs[currentNoteIndex]?.noteId] || ""`
   - onChange: update `editedContents` for current noteId: `setEditedContents(prev => ({ ...prev, [updateDiffs[currentNoteIndex].noteId]: ev.target.value }))`
   - `style: { minHeight: "120px", resize: "vertical", marginTop: "8px" }`
   - rows: 8

4. **If `updateDiffs[currentNoteIndex]?.hasChanges === false`** (discretion — "no changes" case):
   - "No changes proposed" banner: `div` with `className: "hint"`, `style: { fontStyle: "italic" }`, text "No changes proposed"
   - Textarea still shown with the original content (which equals proposedContent when no changes) — user can manually edit or Skip

5. **Navigation row** (per D-09, D-10) — flex row, `gap: "8px"`, `marginTop: "12px"`:

   a. "Previous note" button:
   - disabled when `currentNoteIndex === 0`
   - onClick: `setCurrentNoteIndex(prev => prev - 1); setShowDiff(true);`

   b. "Next note" / "Review Summary" button:
   - When `currentNoteIndex < updateDiffs.length - 1`: label "Next note", onClick advances index and resets showDiff to true
   - When `currentNoteIndex === updateDiffs.length - 1`: label "Review Summary", onClick transitions to summary view: `setUpdateView("summary")`

   c. "Skip this note" button (per D-10):
   - `className: "secondary-btn"`
   - onClick: add current noteId to `skippedNoteIds` (if not already there), then advance to next note (same logic as Next note — if last note, go to summary)

**IMPORTANT per RESEARCH.md Pitfall 2:** When navigating between notes, reset `showDiff` to `true` so each new note starts with the diff visible.

**IMPORTANT:** Do NOT render all notes simultaneously. Only render the current note at `currentNoteIndex` per D-09.
  </action>
  <verify>
    <automated>docker compose build --no-cache design-grammars; docker compose up -d design-grammars; timeout /t 5; curl -s http://localhost:8080 | Select-String -Pattern "diff-preview|Review Summary|Skip this note|Previous note|Next note|Hide diff|Show diff" -Quiet</automated>
  </verify>
  <done>
    - Review view renders when updateView === "review" with note title + counter header
    - Diff panel shows server-generated HTML with red/green styling per D-06
    - "Show diff" / "Hide diff" toggle collapses/expands the diff panel per D-07
    - Textarea pre-populated with proposedContent per D-08, editable
    - "No changes proposed" banner shown when hasChanges === false
    - Previous/Next note buttons navigate sequentially per D-09
    - "Review Summary" label on last note transitions to summary view
    - "Skip this note" adds noteId to skippedNoteIds and advances per D-10
  </done>
</task>

<task type="auto">
  <name>Task 2: Summary view + Confirm handler + success notification</name>
  <files>graph-viewer/index.html</files>
  <action>
Add the Summary view and Confirm handler to complete the three-step Update flow.

**1. Summary view rendering (when `updateView === "summary"`):**

Per D-11, D-12. Rendered inside the `knowledgeMode === "update"` conditional:

a. "Back to search" button (`.secondary-btn`, `marginBottom: "12px"`) — onClick: `resetUpdateState()`

b. Heading: "Review Changes" (13px, weight 700)

c. Note list — map over `updateDiffs` to show each note's status:
   - Each row is a flex div (`display: "flex"`, `justifyContent: "space-between"`, `alignItems: "center"`, `padding: "8px 0"`)
   - Left: note title (13px, weight 400)
   - Right: status badge:
     - If noteId is in `skippedNoteIds`: span with text "skipped", `color: "#98a4bd"`, `fontSize: "12px"`
     - Otherwise: span with text "changed", `color: "#2fb16f"`, `fontSize: "12px"`, `fontWeight: 600`

d. Non-skipped count: compute `const confirmCount = updateDiffs.filter(d => !skippedNoteIds.includes(d.noteId)).length;`

e. "Confirm All" button:
   - Label: `"Confirm All (" + confirmCount + " notes)"`
   - disabled when `confirmCount === 0` or `knowledgeIsRunning`
   - onClick: `submitConfirm`

f. If all notes skipped (`confirmCount === 0`): show message "All notes skipped — nothing to confirm" (`.hint` class)

**2. submitConfirm handler:**

Per RESEARCH.md Pattern 6 and Pattern 7:

```javascript
const submitConfirm = async () => {
  const notesToConfirm = updateDiffs
    .filter(d => !skippedNoteIds.includes(d.noteId))
    .map(d => ({
      noteId: d.noteId,
      content: editedContents[d.noteId] ?? d.proposedContent,
      updatedAt: d.updatedAt
    }));
  if (notesToConfirm.length === 0) {
    setKnowledgeStatus("All notes skipped — nothing to confirm");
    return;
  }
  setKnowledgeIsRunning(true);
  setKnowledgeStatus("Saving changes...");
  try {
    const base = getDataServiceBaseUrl();
    const res = await fetch(base + "/knowledge/update/confirm", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        notes: notesToConfirm,
        project: project || "default-project",
        prompt: knowledgePromptText.trim()
      })
    });
    const data = await res.json();
    if (!res.ok) {
      if (res.status === 409) {
        setKnowledgeResponseText("Conflict: " + (data?.detail || "Note was modified externally. Reload and retry."));
      } else {
        setKnowledgeResponseText("Error: " + (data?.detail || res.status));
      }
      return;
    }
    const titles = notesToConfirm
      .map(n => updateDiffs.find(d => d.noteId === n.noteId)?.title || n.noteId)
      .join(", ");
    setKnowledgeResponseText("Updated: " + titles);
    setKnowledgeStatus("Done");
    resetUpdateState();
    setKnowledgePromptText("");
  } catch (err) {
    setKnowledgeResponseText("Error: " + err.message);
  } finally {
    setKnowledgeIsRunning(false);
  }
};
```

Key implementation notes:
- Per D-08 and Phase 4 D-09: Send `editedContents[noteId]` (user's edited text), NOT `proposedContent` or `originalContent`
- Per D-12: Single batch confirm call for all non-skipped notes
- Per D-15: Success shows "Updated: [Title 1], [Title 2]" in the response textarea
- Per RESEARCH.md Pitfall 2: After success, `resetUpdateState()` clears ALL update state + `setKnowledgePromptText("")`
- Per RESEARCH.md Pattern 7: Handle 409 with specific conflict message
- Do NOT clear `knowledgePromptText` before Confirm — it's needed for the `prompt` field in the payload

**3. Style the success message (per D-15):**

The success message "Updated: ..." should appear in the response textarea. The textarea already has `readOnly: true`. For green text, you can conditionally apply a style to the response textarea when `knowledgeResponseText` starts with "Updated:":
- `style: { color: knowledgeResponseText.startsWith("Updated:") ? "#2fb16f" : (knowledgeResponseText.startsWith("Conflict:") || knowledgeResponseText.startsWith("Error:") ? "#ff4d4f" : "inherit") }`

Apply this conditional color to the response textarea in the update panel.
  </action>
  <verify>
    <automated>docker compose build --no-cache design-grammars; docker compose up -d design-grammars; timeout /t 5; curl -s http://localhost:8080 | Select-String -Pattern "submitConfirm|Confirm All|Review Changes|skipped|changed" -Quiet</automated>
  </verify>
  <done>
    - Summary view shows all notes with "changed" (green) or "skipped" (muted) badges per D-11
    - "Confirm All (N notes)" button is disabled when all notes are skipped per D-12
    - submitConfirm sends batch payload with editedContents and updatedAt per D-12
    - 409 conflict shows specific error message per RESEARCH.md Pattern 7
    - Success notification shows "Updated: Title1, Title2" in green per D-15
    - Panel resets to Match view after successful Confirm per D-15
    - All update state variables reset after Confirm per RESEARCH.md Pitfall 2
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Update Knowledge flow</name>
  <files>graph-viewer/index.html</files>
  <action>Human verification of the complete three-step Update Knowledge flow.</action>
  <what-built>Complete three-step Update Knowledge flow: Match, Propose, Review, Summary, Confirm</what-built>
  <how-to-verify>
    1. Open http://localhost:8080, log in, open a project with existing knowledge notes
    2. Click Graph Viewer, Specs and Notes tab, select Update Knowledge from dropdown
    3. Match: Type a description and click Search Notes, verify candidate list appears with checkboxes and score badges
    4. Select all/Deselect all: Click toggle, verify all checkboxes change
    5. Empty state: Type a non-matching prompt, verify No matching notes found message
    6. Propose: Check 1-2 notes, click Edit Selected, verify step indicators appear, candidate list dims
    7. Review: After Propose completes, verify diff panel with red/green highlights appears above textarea
    8. Collapse diff: Click Hide diff, diff panel hides; click Show diff, it reappears
    9. Edit textarea: Modify the proposed text in the textarea
    10. Navigation: If 2+ notes selected, click Next note / Previous note to navigate
    11. Skip: Click Skip this note, note should advance
    12. Summary: On last note, click Review Summary, verify changed/skipped badges
    13. Confirm: Click Confirm All, verify success message Updated titles in green
    14. Reset: After confirm, verify panel returns to empty Match state
    15. Back to search: At any point in review/summary, click Back to search, verify reset to Match
  </how-to-verify>
  <verify>Manual walkthrough of all 15 verification steps above</verify>
  <done>All three-step Update Knowledge flow views work end-to-end and all 16 CONTEXT.md decisions verified</done>
  <resume-signal>Type approved or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → data-service | Confirm call sends user-edited content across network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | editedContents sent to Confirm | accept | Server-side MAX_CONTENT_SIZE (100KB) guard already enforced in Phase 4; 413 on violation. No additional client-side guard needed. |
| T-06-05 | Denial of Service | Confirm with many notes | accept | Server processes notes sequentially; max 10 candidates from Match. Low risk. |
| T-06-06 | Repudiation | Updates without audit trail | mitigate | Confirm endpoint creates KnowledgeSession node recording prompt + affected noteIds (already implemented in Phase 4). No client-side action needed. |
| T-06-07 | Information Disclosure | diffHtml XSS | mitigate | Trust assumption documented in Plan 01 T-06-03. If `word_diff_html` does not escape HTML entities in note content, add `html.escape(w)` in app.py. Plan executor should verify this during implementation. |
</threat_model>

<verification>
1. `docker compose build --no-cache design-grammars && docker compose up -d design-grammars`
2. Full manual walkthrough: Match → Propose → Review → Summary → Confirm
3. Verify all 16 decisions (D-01 through D-16) are implemented
4. Verify 409 handling by modifying a note via API between Propose and Confirm (optional edge case)
5. Verify panel reset after successful Confirm
</verification>

<success_criteria>
- Complete three-step Update Knowledge flow works end-to-end (UIST-04 fully satisfied)
- All 16 locked decisions from CONTEXT.md are implemented
- Diff panel renders red/green highlights from backend HTML
- Sequential navigation, skip, and summary work correctly
- Confirm writes to backend and shows success notification
- Panel resets cleanly after Confirm or Back to search
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-update-panel-inline-diff-editor/06-02-SUMMARY.md`
</output>
