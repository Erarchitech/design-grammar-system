---
phase: 821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation
plan: 02
type: execute
wave: 2
depends_on: ["821-01"]
files_modified:
  - dg-reasoner/ontology_export.py
  - dg-reasoner/tests/fixtures/metagraph_fixture.json
  - dg-reasoner/tests/test_ontology_export.py
autonomous: true
requirements: [REAS-05]

must_haves:
  truths:
    - "D-01: `dg-reasoner/ontology_export.py` is a CLEAN implementation of `spec/LPG-OWL-MAPPING.md` (custom Cypher + RDFLib), NOT a port of the throwaway spike; n10s is not imported or referenced anywhere in the reasoning path"
    - "D-14: fidelity is asserted STRUCTURALLY via RDFLib (parse the produced Turtle back, walk each swrl:AtomList and assert atom sequence matches HAS_BODY/HAS_HEAD `order`; assert swrl:argument1/2 match ARG `pos` 1/2) — no golden-file byte diffs"
    - "Success criterion 2: a project-scoped export produces valid Turtle for a real project's OntoGraph + Metagraph, and fidelity tests confirm Atom.ARG.pos and HAS_BODY/HAS_HEAD.order survive translation per Phase 820's mapping spec"
    - "Export scopes by node LABEL, never the `graph` property (Pitfall 1): exported atom count equals an independent label-based count, so the two mistagged R_DOOR_ORIENTATION_V atoms are captured not dropped"
    - "BuiltinAtom arguments use swrl:arguments (rdf:List) universally; binary atoms use swrl:argument1/2; SWRL-incomplete atoms (no ARG edges) are captured as annotated SkippedAtom/SkippedRule, excluded from the swrl:Imp mapping"
    - "D-13/D-15: unit fidelity tests live in `dg-reasoner/tests/` using pytest, run deterministically against a committed fixture, and require NO docker/JRE (the always-on CI tier)"
  artifacts:
    - "dg-reasoner/ontology_export.py"
    - "dg-reasoner/tests/fixtures/metagraph_fixture.json"
    - "dg-reasoner/tests/test_ontology_export.py"
  key_links:
    - "ontology_export.build_graph(session-or-records, project) → rdflib.Graph consumed by Plan 03's reasoning.py and the /reason & /shacl routes"
    - "the committed fixture feeds a session-shim so the same build_graph logic is unit-tested without a live Neo4j"
---

<objective>
Build `dg-reasoner/ontology_export.py` — the production Cypher→RDF translator — clean against
`spec/LPG-OWL-MAPPING.md`, and prove it with structural fidelity tests over a committed fixture.
This is the core REAS deliverable: the label-scoped extraction of a project's OntoGraph +
Metagraph into SWRL RDF, preserving `ARG.pos` (→ swrl:argument1/2 or swrl:arguments) and
`HAS_BODY`/`HAS_HEAD.order` (→ ordered swrl:AtomList). The spike's `export.py` is reference only —
rebuild against the spec.

Purpose: delivers success criterion 2 (project-scoped export + edge-property-reification fidelity).
Plan 03's reasoning and SHACL paths consume `build_graph`'s rdflib.Graph; getting the translation
correct and tested here is what makes the end-to-end round-trip meaningful.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-CONTEXT.md
@.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-RESEARCH.md
@.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files:
- `dg-reasoner/ontology_export.py` — clean translator. Public entry `build_graph(records_or_session, project) -> rdflib.Graph`; helpers for IRI minting (`{base}/project/{project}/{rule|atom|var}/{key}`, base `http://example.org/design-grammar`), `swrl:AtomList` construction (order-preserving), CURIE expansion (`ex:` and `swrlb:` namespaces), XSD datatype mapping, and HermiT-unsupported builtin identification (used by Plan 03).
- `dg-reasoner/tests/fixtures/metagraph_fixture.json` — committed graph-shaped fixture (≥1 rule with a multi-atom body, a BuiltinAtom, both ARG positions, and one deliberately-incomplete atom).
- `dg-reasoner/tests/test_ontology_export.py` — structural fidelity tests (parse produced Turtle back, assert order + argument positions).

New Python symbols (exclude from drift verification — created here):
- Module constants `METAGRAPH_QUERY`, `ONTOGRAPH_QUERY`, `BODY_HEAD_QUERY`, `REFERS_QUERY`, `ARG_QUERY` (label-scoped Cypher)
- Namespaces `EX`, `SWRL`, `SWRLB`, base `http://example.org/design-grammar`
- Functions `build_graph(...)`, `make_atom_list(...)`, `mint(...)`, `expand_iri(...)`, `xsd_datatype(...)` (exact names at executor discretion, matching spec semantics)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Clean Cypher→RDF translator per spec/LPG-OWL-MAPPING.md</name>
  <files>
    - dg-reasoner/ontology_export.py
  </files>
  <read_first>
    - spec/LPG-OWL-MAPPING.md (THE normative contract — node mapping tables, edge-property reification for ARG.pos and HAS_BODY/HAS_HEAD.order, IRI minting strategy, export scoping mandate, BuiltinAtom swrl:arguments rule, SWRL builtin exclusion note)
    - .planning/phases/820-.../spike/export.py (REFERENCE ONLY — the proven mechanics for label-scoped Cypher, make_atom_list, atom_kind mapping, incomplete-atom handling, strip_hermit_unsupported; DO NOT copy verbatim — rebuild clean per spec)
    - .planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-RESEARCH.md (§2.3 translation mechanics, §5 code examples)
    - cypher_template.txt (graph schema v4: HAS_BODY/HAS_HEAD carry `order`, ARG carries `pos`, Atom.type values, Builtin.iri)
    - dg-reasoner/app.py (the module the translator sits beside; reuse its neo4j driver/env pattern — graphify first per repo rule if exploring beyond the driver block)
  </read_first>
  <action>
    Write `dg-reasoner/ontology_export.py`: standalone translator (imports: `from __future__ import annotations`, os, pathlib, urllib.parse.quote, `rdflib`, and — only for the live path — neo4j; keep it importable without a live Neo4j so unit tests can drive `build_graph` with fixture records).

    Define label-scoped Cypher constants exactly per spec §Export Scoping: `METAGRAPH_QUERY` matching `n.project=$project AND (n:Rule OR n:Atom OR n:Var OR n:Literal OR n:Builtin)`; `ONTOGRAPH_QUERY` matching `n.project=$project AND (n:Class OR n:DatatypeProperty OR n:ObjectProperty)`; plus `BODY_HEAD_QUERY` (HAS_BODY|HAS_HEAD with `order`), `REFERS_QUERY` (REFERS_TO targets), `ARG_QUERY` (ARG with `pos`). NEVER scope Metagraph by `graph:'Metagraph'` (Pitfall 1).

    Implement `build_graph(records, project) -> rdflib.Graph` mapping per the spec node tables:
    OntoGraph → owl:Class / owl:DatatypeProperty / owl:ObjectProperty reusing stored `iri`;
    Metagraph → Rule=swrl:Imp (rdfs:label=Rule_Id), Atom.type → swrl:ClassAtom / DatavaluedPropertyAtom / IndividualPropertyAtom / BuiltinAtom with swrl:classPredicate/propertyPredicate/builtin pointing at the REFERS_TO target, Var=swrl:Variable, Literal=typed RDF literal. Encode HAS_BODY→swrl:body and HAS_HEAD→swrl:head as order-preserving `swrl:AtomList` (typed rdf:List first/rest). Encode ARG.pos → swrl:argument1/argument2 for binary atom kinds, and swrl:arguments (rdf:List) UNIVERSALLY for BuiltinAtom. Mint IRIs under base `http://example.org/design-grammar` as `/project/{project}/{rule|atom|var}/{key}` (strip leading `?` from Var names); expand stored CURIEs for `ex:`/`swrlb:`.

    Add the Pitfall-1 guard: compare exported atom count to an independent label-based count and raise AssertionError on mismatch. Capture SWRL-incomplete atoms/rules (missing REFERS_TO or ARG edges) as annotated `SkippedAtom`/`SkippedRule` individuals (kept in the canonical graph, excluded from the swrl:Imp mapping). Expose a `strip_hermit_unsupported(graph) -> int` helper (removes swrl:Imp rules transitively referencing a BuiltinAtom, prunes orphaned Variable/Builtin, returns removed count) for Plan 03's reasoner input. Provide a `main()`/CLI that connects via the env-driven neo4j driver and serializes Turtle for manual export, but keep `build_graph` decoupled from the driver so it is unit-testable.
  </action>
  <verify>
    <automated>test -f dg-reasoner/ontology_export.py && grep -q "n:Rule OR n:Atom" dg-reasoner/ontology_export.py && grep -q "AtomList" dg-reasoner/ontology_export.py && grep -q "swrl" dg-reasoner/ontology_export.py && grep -q "strip_hermit_unsupported" dg-reasoner/ontology_export.py && python -c "import ast,sys; ast.parse(open('dg-reasoner/ontology_export.py').read()); print('ast-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ontology_export.py` defines METAGRAPH_QUERY/ONTOGRAPH_QUERY scoping by label (grep `n:Rule`, `n:Class`) and never filters Metagraph by `graph:'Metagraph'`.
    - `build_graph` maps Rule→swrl:Imp with ordered swrl:AtomList bodies/heads and ARG.pos→swrl:argument1/2 (binary) / swrl:arguments (BuiltinAtom) — verifiable by the Task 2 tests.
    - `strip_hermit_unsupported(graph)` exists and returns an int removed-count (Plan 03 consumes it).
    - No `import n10s`/neosemantics reference anywhere (D-01: n10s is not in the reasoning path).
    - Module parses (ast.parse ok) and `build_graph` is importable without a live Neo4j connection.
  </acceptance_criteria>
  <done>The clean translator implements every normative mapping row and exposes build_graph + strip_hermit_unsupported for downstream reasoning.</done>
</task>

<task type="auto">
  <name>Task 2: Committed fixture + structural fidelity tests (ARG.pos + order survive)</name>
  <files>
    - dg-reasoner/tests/fixtures/metagraph_fixture.json
    - dg-reasoner/tests/test_ontology_export.py
  </files>
  <read_first>
    - dg-reasoner/ontology_export.py (the module under test — its build_graph signature and how it consumes records)
    - spec/LPG-OWL-MAPPING.md (§Edge-Property Reification — the exact expected Turtle shapes for ARG.pos and HAS_BODY/HAS_HEAD.order that the assertions encode)
    - .planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-RESEARCH.md (§6 Validation Architecture — structural-via-RDFLib, no golden bytes; fixture must exercise multi-atom body + BuiltinAtom + both ARG positions)
  </read_first>
  <action>
    Create `dg-reasoner/tests/fixtures/metagraph_fixture.json`: a small graph-shaped fixture (nodes with labels+props, plus HAS_BODY/HAS_HEAD/ARG/REFERS_TO edges) modeling ≥1 real-shaped rule with a multi-atom body (e.g. a ClassAtom + a DatavaluedPropertyAtom + a BuiltinAtom), exercising `ARG.pos` 1 and 2, a BuiltinAtom with its arguments, and ONE deliberately-incomplete atom (no ARG edges) to exercise the SkippedAtom path. Base it on the real `R_BUILDING_MAX_STOREY_9_V` shape from spec/LPG-OWL-MAPPING.md's worked example.

    Create `dg-reasoner/tests/test_ontology_export.py` (pytest, NO docker/JRE): load the fixture, feed it through a lightweight record-shim into `build_graph(..., project="fixture-proj")`, serialize to Turtle, then parse the Turtle back with rdflib and assert STRUCTURALLY (D-14):
    (1) each Rule is a swrl:Imp whose swrl:body swrl:AtomList, walked via rdf:first/rdf:rest, yields atoms in the exact `order` the fixture's HAS_BODY edges specify;
    (2) a binary atom's swrl:argument1/argument2 correspond to ARG `pos` 1/2 (the right Var/Literal at each slot);
    (3) the BuiltinAtom uses swrl:arguments (an rdf:List), not argument1/2;
    (4) the incomplete atom is emitted as a SkippedAtom (annotation type) and excluded from any swrl:Imp body/head;
    (5) the exported-vs-independent atom count guard holds (Pitfall 1 path exercised).
    No golden-file byte comparison — assertions walk the parsed graph.
  </action>
  <verify>
    <automated>test -f dg-reasoner/tests/fixtures/metagraph_fixture.json && python -m pytest dg-reasoner/tests/test_ontology_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `metagraph_fixture.json` contains a multi-atom-body rule, a BuiltinAtom, both ARG positions, and one incomplete atom (grep for `HAS_BODY`, `BuiltinAtom`, and a pos:2 arg).
    - `pytest dg-reasoner/tests/test_ontology_export.py` passes with no docker/neo4j/JRE running.
    - The test parses produced Turtle back with rdflib and asserts atom order matches HAS_BODY/HAS_HEAD `order` AND swrl:argument1/2 match ARG `pos` 1/2 (behavior assertions, not byte diffs).
    - The test asserts the BuiltinAtom uses `swrl:arguments` and the incomplete atom becomes a SkippedAtom.
  </acceptance_criteria>
  <done>Structural fidelity is proven against a committed fixture: ARG.pos and HAS_BODY/HAS_HEAD.order survive the translation, CI-able without docker.</done>
</task>

</tasks>

<verification>
- `pytest dg-reasoner/tests/test_ontology_export.py` green (structural fidelity, no docker).
- Translator scopes by label only; BuiltinAtom uses swrl:arguments; incomplete atoms captured as SkippedAtom.
- `strip_hermit_unsupported` exposed for Plan 03.
- No n10s/neosemantics reference in the reasoning path (D-01).
</verification>

<success_criteria>
Delivers success criterion 2: a project-scoped Cypher→RDF export produces valid Turtle for a
real project's OntoGraph + Metagraph, with fidelity tests confirming Atom.ARG.pos and
HAS_BODY/HAS_HEAD.order survive translation per Phase 820's mapping spec.
</success_criteria>

<output>
Create `.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-02-SUMMARY.md` when done.
</output>
