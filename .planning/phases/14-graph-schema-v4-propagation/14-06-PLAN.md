---
phase: 14-graph-schema-v4-propagation
plan: 06
type: execute
wave: 3
depends_on: ["14-03", "14-04", "14-05"]
files_modified:
  - migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher
autonomous: false
requirements: [SCHM-13]
nyquist_compliant: true

must_haves:
  truths:
    - "The migration renames DesignState kind values to ParamState/ObjState, moves Run-linked DesignStates to graph='ValidGraph', and DELETEs orphaned DesignStates (D-09/D-10)"
    - "The migration first prints a dry-run COUNT of what it will delete, and carries an explicit dev-databases-only guard comment before the destructive DELETE (D-10)"
    - "The D-14 data half renames the ~1169 live nodes from the old layer value to graph='ValidGraph' with the same dry-run + dev-guard safety (D-14.1)"
    - "Executed on the dev DB with before/after node counts captured as SC#5 proof: zero DesignState nodes with the old kind values remain, and zero nodes carry the old layer value"
  artifacts:
    - "migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher"
  key_links:
    - "Runs after 14-05 (code queries ValidGraph) and 14-04 (n8n confirmed v4) so the DB end-state is coherent; consumes 14-03's seed for a meaningful demo"
  prohibitions:
    - "The destructive DELETE must never auto-run against a database presumed production — dev-only guard is mandatory, not optional (D-10)"
    - "Do not rename node labels — only the graph property VALUE (D-14 caveat)"

security_review: required
---

<!-- planner-discipline-allow: DefState -->
<!-- planner-discipline-allow: ObjectState -->
<!-- planner-discipline-allow: ValidationGraph -->

<objective>
Author and execute the one destructive migration of this phase on the dev Neo4j: rename DesignState kind values, move Run-linked DesignStates to the ValidGraph layer, delete orphans (SCHM-13 / D-09 / D-10), AND rename the ~1169 live nodes carrying the old layer value to graph='ValidGraph' (D-14 data half). Both halves live in one dated migration file and both run with dry-run + dev-only guard safety.

Purpose: This is the DB end-state that makes the whole phase coherent — it runs last (after code/config/prompts are v4). It is the highest-risk work in the phase: destructive DELETE + a 1169-node SET against live data.
Output: the migration file + live before/after counts proving SC#5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-graph-schema-v4-propagation/14-CONTEXT.md
@.planning/phases/14-graph-schema-v4-propagation/14-RESEARCH.md
@.planning/phases/14-graph-schema-v4-propagation/14-VALIDATION.md
@migrations/2026-06-23_var_project_merge_key.cypher
@test/seed_designstates.cypher
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the combined kind + ValidGraph-layer migration script</name>
  <files>migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher</files>
  <read_first>
    - migrations/2026-06-23_var_project_merge_key.cypher (the 4-part pattern: header / dry-run audit / migration / verification)
    - 14-RESEARCH.md § "Artifact 9" + "Pattern: Dry-run-then-execute destructive migration" (structure to follow)
    - 14-CONTEXT.md D-08/D-09/D-10/D-14.1
  </read_first>
  <action>
    Create migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher following the existing migration file's 4-part shape (PURPOSE / EXECUTION METHOD via cypher-shell or Neo4j Browser / WARNING dev-only / VERIFICATION). Two clearly-separated sections:
    SECTION A — DesignState kind + layer (SCHM-13, D-09/D-10):
      - Step 0 DRY-RUN (read-only): count DesignState nodes by kind, and count orphans matched by `MATCH (ds:DesignState) WHERE NOT (ds)<-[:VALIDATES]-(:Run) RETURN count(ds) AS orphansToDelete` — print before doing anything.
      - Step 1: rename the two pre-v4 kind values to ParamState / ObjState respectively, and for Run-linked DesignStates SET ds.graph='ValidGraph' (layer move, targets ValidGraph per D-14, NOT the old value).
      - Step 2 (destructive, dev-only guard comment directly above): `MATCH (ds:DesignState) WHERE NOT (ds)<-[:VALIDATES]-(:Run) DETACH DELETE ds`.
    SECTION B — D-14 layer-value consolidation (D-14.1):
      - Dry-run count: `MATCH (n {graph:'<old layer value>'}) RETURN count(n)` (expect ~1169) — print first.
      - Migration: `MATCH (n {graph:'<old layer value>'}) SET n.graph='ValidGraph'` (property value only; node labels unchanged).
    Include a trailing VERIFICATION block (commented, read-only): DesignState kind ∈ old values → expect 0; nodes with the old layer value → expect 0. Add the dev-only guard prose to the header like the existing file. Idempotent/safe-to-re-read where feasible.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; test -f migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher && grep -qi "dry-run" migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher && grep -qi "dev" migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher && grep -q "DETACH DELETE" migrations/2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists with both sections, dry-run counts before each destructive/bulk step, and an explicit dev-only guard.
    - Orphan DELETE uses the (:Run)-[:VALIDATES]->(:DesignState) orphan definition; layer moves target ValidGraph.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Execute the migration live on the dev DB with before/after proof</name>
  <action>Blocking human gate (destructive). After confirming the target is the DEV database, rebuild data-service (picks up 14-05's ValidGraph code), seed the demo data, capture BEFORE counts, run each section's dry-run, execute, then capture AFTER counts as SC#5 proof. Exact steps in how-to-verify.</action>
  <what-built>The combined migration script. This checkpoint executes it live on the dev DB with before/after proof (destructive — requires human confirmation the target is the dev database).</what-built>
  <how-to-verify>
    1. Confirm the target Neo4j is the DEV database, not production (D-10 guard — human confirms).
    2. Pick up 14-05's code: `docker compose up -d --build data-service` so app.py queries ValidGraph before the data flips.
    3. Seed the demo data: run test/seed_designstates.cypher (via cypher-shell or Neo4j Browser). Capture BEFORE counts:
       - `MATCH (ds:DesignState) RETURN ds.kind, count(*)` (expect the two pre-v4 kinds present)
       - `MATCH (n) WHERE n.graph = '<old layer value>' RETURN count(n)` (expect ~1169 + the seeded Metagraph ones as applicable)
    4. Run SECTION A dry-run, review the orphan count, then execute Steps 1-2.
    5. Run SECTION B dry-run (~1169), then execute the SET.
    6. Capture AFTER counts (SC#5 proof):
       - `MATCH (ds:DesignState) WHERE ds.kind IN ['<pre-v4 param kind>','<pre-v4 object kind>'] RETURN count(ds)` → expect 0
       - `MATCH (n {graph:'<old layer value>'}) RETURN count(n)` → expect 0
       - `MATCH (ds:DesignState) WHERE NOT (ds)<-[:VALIDATES]-(:Run) RETURN count(ds)` → expect 0 (no orphans)
    7. Smoke that data-service reads still work post-migration (e.g. GET /data-service/validation/runs/<project> returns without error against ValidGraph).
    Record the before/after numbers in the SUMMARY as SC#5 evidence (a "0 migrated / 0 deleted" result would mean seeding failed — RESEARCH Pitfall 5).
  </how-to-verify>
  <resume-signal>Type "approved" with the before/after counts pasted, or describe what diverged.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live Neo4j dev DB | A manually-run Cypher migration mutates and deletes real nodes; the only guard between "dev" and "prod" is human judgement + the script's guard comment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-MIG | Tampering / Repudiation | 2026-07-03_designstate_kind_and_validgraph_layer_migration.cypher (orphan DETACH DELETE + 1169-node SET) | high | mitigate | Dry-run COUNT printed and reviewed before both the destructive DELETE and the bulk SET; explicit dev-databases-only guard comment above the DELETE (D-10); migration is never invoked by application code (manual cypher-shell/Browser only); human checkpoint confirms the target is the dev DB and records before/after counts as an audit trail (addresses Repudiation) |
| T-14-MIG-B | Tampering | D-14 layer-value SET across ~1169 live nodes | high | mitigate | Same dry-run + dev-guard pattern; renames only the graph property VALUE, never node labels (bounded blast radius); runs after 14-05 so the running data-service already queries ValidGraph (no read/write partition split window in normal operation) |

No package installs this phase (RESEARCH Package Legitimacy Audit: N/A) — no supply-chain (T-*-SC) threat. Cypher-injection and credential-in-JSON are pre-existing, unchanged, and out of scope (RESEARCH Security Domain).
</threat_model>

<verification>
- Migration file present with dry-run + dev-guard + DETACH DELETE + both sections.
- Live dev run: zero old-kind DesignStates, zero orphans, zero old-layer-value nodes after; before/after counts recorded.
- data-service reads succeed post-migration against ValidGraph.
</verification>

<success_criteria>
Running the migration on the dev DB leaves zero DesignState nodes with the pre-v4 kinds, zero orphans, and zero nodes on the old layer value — with before/after counts as proof. Satisfies SCHM-13 and the D-14 data half. Completes the phase's coherent v4 DB end-state.
</success_criteria>

<output>
Create `.planning/phases/14-graph-schema-v4-propagation/14-06-SUMMARY.md` when done.
</output>
