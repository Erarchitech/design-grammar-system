---
phase: 14-graph-schema-v4-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cypher_template.txt
  - training/dataset_schema.json
autonomous: true
requirements: [SCHM-07, SCHM-08]
nyquist_compliant: true

must_haves:
  truths:
    - "cypher_template.txt header declares SCHEMA VERSION v4.0 (D-05 PascalCase Rule props)"
    - "The emitted CYPHER TEMPLATE BLOCK writes r.SWRL for the rule body (was r.text) per D-04"
    - "The GRAPH SCHEMA reference documents Rule props SWRL / RuleName (optional) / RuleDescription (optional, new) per D-04/D-07"
    - "The GRAPH SCHEMA reference documents the three DesignState kinds {ObjState,ParamState,PropState} and Run props {ValidStatus,SendStatus} as document-only, NOT emitted (D-01/D-03)"
    - "dataset_schema.json metadata.version is v4.0 and its metagraph block mirrors the template rule props exactly (SCHM-08)"
  artifacts:
    - "cypher_template.txt (rewritten to v4)"
    - "training/dataset_schema.json (mirrored to v4)"
  key_links:
    - "dataset_schema.json rule props MUST match cypher_template.txt rule props 1:1 — the two are the canonical schema contract mirrored into n8n prompts (14-04)"
  prohibitions:
    - "No stored Run.Status text field is declared anywhere (D-01/D-02) — only ValidStatus + SendStatus"
    - "No DesignState MERGE block remains in the emitted CYPHER TEMPLATE BLOCK (D-03 drops v3 lines 113-116)"
    - "No r.text / r.title write remains in the emitted CYPHER TEMPLATE BLOCK (renamed to SWRL/RuleName per D-04)"

security: "No new threat surface — this plan edits two text/schema-contract files only. See 14-06 threat model (T-14-MIG) for the migration surface."
---

<!-- planner-discipline-allow: DefState -->
<!-- planner-discipline-allow: ObjectState -->

<objective>
Rewrite the two canonical schema-contract files — `cypher_template.txt` and `training/dataset_schema.json` — from v3.1 to v4. This is the source both n8n LLM prompts (14-04) and the v4 reference examples (14-07) mirror, so it lands first.

Purpose: Every downstream artifact that hard-codes the graph schema derives from these two files. Getting the Rule-property rename (D-04/D-05), the DesignState/Run document-only treatment (D-01/D-03), and version bump right here makes the rest of the phase a faithful copy.
Output: `cypher_template.txt` v4.0 and `training/dataset_schema.json` v4.0, mirrored 1:1 on rule props and kinds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-graph-schema-v4-propagation/14-CONTEXT.md
@.planning/phases/14-graph-schema-v4-propagation/14-RESEARCH.md
@ontology/port-iri-map-V7.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite cypher_template.txt to v4</name>
  <files>cypher_template.txt</files>
  <read_first>
    - cypher_template.txt (full — current v3.1, 192 lines)
    - 14-RESEARCH.md § "Artifact 1: cypher_template.txt" (exact before/after line targets)
    - ontology/port-iri-map-V7.md rows for VALIDATOR / VALIDATION GRAPH / RULE DECONSTRUCT (source of SWRL/RuleName/RuleDescription/ValidStatus/SendStatus names)
  </read_first>
  <action>
    Apply the v4 edits from RESEARCH Artifact 1, keeping the diff minimal (do NOT touch the SEMANTIC MAPPING / RULE SEPARATION / IDENTIFIERS prose sections — they carry no renamed tokens):
    1. Line 1 header `(v3)` → `(v4)`; line 3 `SCHEMA VERSION: v3.1` → `SCHEMA VERSION: v4.0`.
    2. GRAPH SCHEMA reference — Rule entry (v3 lines 22-24): rewrite props to `SWRL` (SWRL string), `RuleName` (optional), `RuleDescription` (optional, new in v4), `kind (violation|compliance)`, `project`, `graph='Metagraph'`. Per D-04 (full rename) and D-05 (ontology PascalCase, case-sensitive in Neo4j — deliberate).
    3. GRAPH SCHEMA reference — replace the v3 DesignState entry (lines 49-50) with a clearly-labeled document-only sub-section titled so a reader sees it is NOT emitted by ingest (per D-03). Document: the three kinds {ObjState, ParamState, PropState}; DesignState lives at `graph='ValidGraph'` (D-01/D-14, corrected from v3's Metagraph); `Run` props `ValidStatus` (per-ObjState Boolean list, index-matched) and `SendStatus` (single Boolean) with an explicit note that there is NO stored Status text field (overall pass = AND of ValidStatus, derived at read time, per D-01); and the Run→DesignState relationship `(:Run)-[:VALIDATES]->(:DesignState)` with many-Runs-to-one-DesignState cardinality (Claude's-discretion physical modeling, D-04 deferred it to this phase). State that Run and DesignState are written only by VALIDATOR-on-publish (D-03), not by LLM ingest.
    4. CYPHER TEMPLATE BLOCK `// 2. Metagraph — rule node`: change the `SET r.text = ...` line to `SET r.SWRL = '<SWRL_EXPRESSION>'`. Add two optional trailing SET lines for `r.RuleName` / `r.RuleDescription` with an inline comment that they are optional — the LLM sets them only when the NL rule supplies a name/description; omit otherwise (D-07). Keep `r.kind`, `r.project`, `r.graph='Metagraph'`.
    5. CYPHER TEMPLATE BLOCK `// 2b` DesignState MERGE (v3 lines 113-116): delete the whole `// 2b` comment + MERGE + SET block (D-03 — ingest emits OntoGraph + Metagraph nodes only). Renumber nothing else; sections 3-7 stay.
    6. Update the OUTPUT RULES allowed-node-labels / graph-values lines only if they reference DesignState — they do not in v3 (labels list already excludes it), so leave OUTPUT RULES as-is.
    7. QUALITY CHECKS: add one line asserting the Rule SWRL body property is named `SWRL` (not `text`). Leave the existing "DatatypeProperty display property is SWRL_label" line unchanged (unrelated to the Rule rename).
    Do not introduce any "v1/simplified/for-now" hedging — this is the full v4 contract.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && grep -q "SCHEMA VERSION: v4.0" cypher_template.txt && grep -q "SET r.SWRL" cypher_template.txt && grep -q "RuleDescription" cypher_template.txt && ! grep -qE "^\s*SET r\.text" cypher_template.txt && ! grep -q "// 2b" cypher_template.txt && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Header + version read v4.0; the emitted `// 2` rule MERGE sets `r.SWRL` and shows optional `r.RuleName`/`r.RuleDescription`.
    - The `// 2b` DesignState MERGE block is gone from the emitted CYPHER TEMPLATE BLOCK.
    - A document-only schema reference records the three kinds, `graph='ValidGraph'`, Run ValidStatus/SendStatus (no Status text field), and the Run→DesignState relationship.
    - No `SET r.text` line survives in the emitted block.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mirror training/dataset_schema.json to v4</name>
  <files>training/dataset_schema.json</files>
  <read_first>
    - training/dataset_schema.json (full — current v3.1, ~188 lines; confirm exact JSON shape before editing)
    - 14-RESEARCH.md § "Artifact 2: dataset_schema.json" (field-by-field target)
    - cypher_template.txt (the just-rewritten v4 — this file must mirror it 1:1)
  </read_first>
  <action>
    Make this file mirror the v4 `cypher_template.txt` exactly (SCHM-08 is a literal-parity requirement). Edit ONLY the `metagraph` block and `metadata.version`; leave the dataset-record scaffolding fields (`id`, `layer`, `task`, `rule_kind`, `domain`, `input_nl`, `output_formal`) untouched.
    1. `metagraph.rule_node.props`: rename `text`→`SWRL`, `title`→`RuleName` (optional), add `RuleDescription` (optional, new per D-07); keep `kind`, `project`, `graph`.
    2. Remove the `DesignState` entry from the emission-relevant `metagraph.nodes[]` array and instead add a document-only `metagraph.schema_reference` object (matching cypher_template's document-only treatment, D-03) describing kinds {ObjState,ParamState,PropState}, `graph='ValidGraph'`, and Run props {ValidStatus, SendStatus} (no Status text). Mirror whatever structure Task 1 used so the two files agree.
    3. `metadata.version`: `"v3.1"` → `"v4.0"`.
    Validate the file is still well-formed JSON after editing.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && python -c "import json,sys; d=json.load(open('training/dataset_schema.json')); rp=json.dumps(d.get('metagraph',{}).get('rule_node',{})); sys.exit(0 if ('SWRL' in rp and 'text' not in rp and 'v4' in json.dumps(d.get('metadata',{}))) else 1)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid JSON.
    - `metagraph.rule_node.props` uses `SWRL`/`RuleName`/`RuleDescription`; no `text`/`title` remain there.
    - `metadata.version` is `v4.0`; no `DesignState` node remains in the emission-relevant `nodes[]` (moved to a document-only `schema_reference`).
    - Rule props match `cypher_template.txt` v4 1:1.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
No new files. Two files rewritten in place: `cypher_template.txt` (v4.0) and `training/dataset_schema.json` (v4.0). No new symbols or config keys.
</artifacts_produced>

<verification>
- `grep "SCHEMA VERSION" cypher_template.txt` → v4.0
- Rule props are SWRL / RuleName / RuleDescription in both files (parity).
- No emitted DesignState MERGE; DesignState/Run are document-only.
- No stored Status text field mentioned as an emitted property anywhere.
</verification>

<success_criteria>
Both schema-contract files declare v4, agree on Rule props 1:1, drop the emitted DesignState MERGE, and document (not emit) the three kinds + Run ValidStatus/SendStatus with no Status text field. Satisfies SCHM-07 and SCHM-08.
</success_criteria>

<output>
Create `.planning/phases/14-graph-schema-v4-propagation/14-01-SUMMARY.md` when done.
</output>
