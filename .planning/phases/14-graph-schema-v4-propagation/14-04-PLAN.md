---
phase: 14-graph-schema-v4-propagation
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-03"]
files_modified:
  - n8n/workflows/rules-to-metagraph.json
  - n8n/workflows/graph-query-mcp.json
autonomous: false
requirements: [SCHM-09, SCHM-10]
nyquist_compliant: true

must_haves:
  truths:
    - "rules-to-metagraph.json Build LLM Prompt emits the v4 schema — SWRL not text, DesignState dropped from allowed labels/bullets (D-03/D-04)"
    - "Fetch Existing Entities query coalesces r.SWRL before r.text so the 5 live v3 rules still appear"
    - "Annotate Graph Props backfills RuleDescription (the 4th node RESEARCH found, beyond CONTEXT's named 3)"
    - "graph-query-mcp.json Build Cypher Prompt describes the v4 data model and uses coalesce(r.SWRL, r.text) in its read guidance/examples"
    - "A live rule ingest writes a Rule node with .SWRL, and a live NL query generates Cypher referencing r.SWRL (SC#2/SC#3)"
  artifacts:
    - "n8n/workflows/rules-to-metagraph.json (v4 prompts)"
    - "n8n/workflows/graph-query-mcp.json (v4 prompt)"
  key_links:
    - "The few-shot in Build LLM Prompt must mirror 14-01's v4 cypher_template block (SET r.SWRL) — depends_on 14-01"
    - "Live smoke uses 14-03's test/smoke_*.sh scripts — depends_on 14-03; the n8n re-import gotcha must run before smoke"
  prohibitions:
    - "No coalesce ordering with the old prop first (RESEARCH Pitfall 2 — SWRL before text always)"
    - "Do not edit the underscore-prefixed n8n export files (_active-graph-query.json, _all-workflows-export.json) — they are stale exports, not source"

security: "No new threat surface — LLM prompt-text edits. The Parse LLM Output bracket/quote validator is unchanged and schema-agnostic. See 14-06 threat model (T-14-MIG)."
---

<objective>
Propagate v4 into both n8n LLM workflows — the ingest prompt (`rules-to-metagraph.json`) and the query prompt (`graph-query-mcp.json`) — then prove it with a live ingest + a live query against the running Docker stack. This is the write/query-side of the Rule-property rename and the DesignState-drop.

Purpose: The n8n prompts are hand-mirrored from the v4 schema contract (14-01). Once re-imported, a live rule ingest must generate v4 Cypher (SWRL, no DesignState) and a live NL query must read via SWRL — the SC#2/SC#3 proof.
Output: both workflow JSONs at v4 + a green live smoke pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-graph-schema-v4-propagation/14-CONTEXT.md
@.planning/phases/14-graph-schema-v4-propagation/14-RESEARCH.md
@.planning/phases/14-graph-schema-v4-propagation/14-VALIDATION.md
@cypher_template.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update rules-to-metagraph.json prompts to v4 (SCHM-09)</name>
  <files>n8n/workflows/rules-to-metagraph.json</files>
  <read_first>
    - n8n/workflows/rules-to-metagraph.json (the four nodes: Build LLM Prompt, Fetch Existing Entities, Annotate Graph Props, Parse LLM Output)
    - 14-RESEARCH.md § "Artifact 3" (exact embedded-string targets per node — this is the authoritative edit list)
    - cypher_template.txt (the finalized v4 block the few-shot must mirror)
  </read_first>
  <action>
    Apply RESEARCH Artifact 3's four-node edit list (confirmed canonical file, per README re-import command):
    1. Build LLM Prompt (Function node): bump the schema-constraints marker to (v4); remove DesignState from the allowed-node-labels list and delete the DesignState key/kind bullet (D-03); change the embedded few-shot `SET r.text = ...` to `SET r.SWRL = ...` so it mirrors 14-01's v4 template. The local JS var named text may stay as an identifier, but it must be populated from the SWRL-aliased column the updated Fetch query returns.
    2. Fetch Existing Entities (HTTP node): in its Cypher, change `coalesce(r.text, '') AS text` to `coalesce(r.SWRL, r.text, '') AS text` (backward-compat — the 5 live rules have only r.text).
    3. Annotate Graph Props (HTTP node — the 4th node RESEARCH found): extend the auto-backfill to also set RuleDescription from the raw NL rules_text when absent (mirror the existing lowercase r.description backfill; D-07 optional semantics). 
    4. Parse LLM Output (Function node): no property-name edits required (schema-agnostic MERGE cleanup); optionally note that the LLM should never emit DesignState now (D-03) — not required for SC#2.
    Do NOT edit the underscore-prefixed export files.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -q "SET r.SWRL" n8n/workflows/rules-to-metagraph.json && grep -q "coalesce(r.SWRL, r.text" n8n/workflows/rules-to-metagraph.json && grep -q "RuleDescription" n8n/workflows/rules-to-metagraph.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Build LLM Prompt is v4 (SWRL few-shot, no DesignState label/bullet).
    - Fetch Existing Entities coalesces SWRL before text.
    - Annotate Graph Props backfills RuleDescription.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update graph-query-mcp.json Build Cypher Prompt to v4 (SCHM-10)</name>
  <files>n8n/workflows/graph-query-mcp.json</files>
  <read_first>
    - n8n/workflows/graph-query-mcp.json (Build Cypher Prompt node)
    - 14-RESEARCH.md § "Artifact 4" (exact target lines, all captured)
  </read_first>
  <action>
    Apply RESEARCH Artifact 4's line list: change the data-model marker to (v4 schema); rewrite the Rule properties line to list SWRL (SWRL expression), kind, RuleName, RuleDescription, project (was text/title); change "stored in Rule.text" to "stored in Rule.SWRL"; bump the key-property-names marker to (v4); add a backward-compat guidance line noting some existing rules predate the rename so reads should coalesce(r.SWRL, r.text) for the body and coalesce(r.RuleName, r.title) for the name; change the toLower(r.text) match guidance and the list-all-rules example to the coalesce form; update both Example (with/without project) lines to the coalesce pattern. The upstream db.schema()-style introspection is auto-adapting — do not touch it.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -q "r.SWRL" n8n/workflows/graph-query-mcp.json && grep -q "v4" n8n/workflows/graph-query-mcp.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Prompt describes the v4 data model and uses coalesce(r.SWRL, r.text) in read guidance/examples.
    - No un-coalesced r.text-only read guidance remains.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-import both workflows and run the live SC#2/SC#3 smoke</name>
  <action>Human verification gate. Re-import both edited workflows into n8n (it does not hot-reload edited JSON), warm Ollama, then run the two Wave 0 smoke scripts and confirm v4 (r.SWRL) output. Exact steps in how-to-verify.</action>
  <what-built>Both n8n workflows edited to v4. This checkpoint covers re-import (n8n does NOT hot-reload edited JSON) and the live SC#2/SC#3 smoke.</what-built>
  <how-to-verify>
    1. Re-import both workflows (RESEARCH gotcha — required every edit): inject a top-level "id" into a scratch copy of each file if missing, `docker cp` it into the container, then
       `docker exec -it n8n n8n import:workflow --input=/files/workflows/rules-to-metagraph.json` (and the graph-query-mcp.json equivalent), `n8n update:workflow --active=true`, `docker restart n8n`.
    2. Warm Ollama first (cold start 40-70s is normal, not a failure): `docker exec ollama ollama run llama3.1 "test"`.
    3. Run `bash test/smoke_rules_ingest.sh` — expect the written Rule node to carry .SWRL.
    4. Run `bash test/smoke_graph_query.sh` — expect the generated Cypher to reference r.SWRL.
    5. If either smoke still shows r.text-only output, the old workflow definition is still active — re-check the re-import step.
  </how-to-verify>
  <resume-signal>Type "approved" once both smokes show v4 (r.SWRL) output, or paste the failing output.</resume-signal>
</task>

</tasks>

<verification>
- rules-to-metagraph.json: SWRL few-shot, coalesced Fetch, RuleDescription backfill; no DesignState label.
- graph-query-mcp.json: v4 data model + coalesce read guidance.
- Live: ingest writes .SWRL; query generates r.SWRL Cypher.
</verification>

<success_criteria>
Both n8n workflows emit/describe v4, and a live ingest + live query prove SC#2/SC#3 against the running stack. Satisfies SCHM-09 and SCHM-10.
</success_criteria>

<output>
Create `.planning/phases/14-graph-schema-v4-propagation/14-04-SUMMARY.md` when done.
</output>
