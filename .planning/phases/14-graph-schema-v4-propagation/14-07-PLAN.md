---
phase: 14-graph-schema-v4-propagation
plan: 07
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - training/updated_cypher_reference_examples_v4.cypher
  - test/training_dataset.json
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SCHM-14]
nyquist_compliant: true

must_haves:
  truths:
    - "training/updated_cypher_reference_examples_v4.cypher exists as the v4 successor (new file; v3 kept for greppable history per Claude's Discretion), rewriting r.text to r.SWRL and adding a worked example exercising RuleName/RuleDescription"
    - "test/ fixtures no longer present the old schema as current — either regenerated or annotated with a header noting they are pre-v4 historical/debug reference"
    - "ROADMAP SC#1 and REQUIREMENTS SCHM-07 no longer list a Status text Run property (amended to match D-01/D-02)"
  artifacts:
    - "training/updated_cypher_reference_examples_v4.cypher (new)"
  key_links:
    - "The v4 example must mirror 14-01's finalized cypher_template v4 block — depends_on 14-01"
  prohibitions:
    - "Do not delete training/updated_cypher_reference_examples_v3.cypher — kept for transition-period grep history"
    - "Do not implement a Status text field anywhere — the amendment removes stale wording, it does not add a field (D-02)"

security: "No new threat surface — reference/doc files and two planning-doc amendments. See 14-06 threat model (T-14-MIG)."
---

<!-- planner-discipline-allow: DefState -->
<!-- planner-discipline-allow: ObjectState -->

<objective>
Finish the propagation with documentation hygiene: create the v4 successor of the LLM few-shot reference, clean the `test/` fixtures so no artifact still teaches the old schema, and amend the two stale planning docs (ROADMAP SC#1, REQUIREMENTS SCHM-07) whose "Status text" wording predates the Phase-13 investigation (D-02).

Purpose: SCHM-14 plus the D-02 documentation correction. RESEARCH confirmed no automated test consumes these fixtures, so this is reference-hygiene, not functional risk — keep it small.
Output: new v4 reference file, annotated/regenerated fixtures, amended ROADMAP + REQUIREMENTS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-graph-schema-v4-propagation/14-CONTEXT.md
@.planning/phases/14-graph-schema-v4-propagation/14-RESEARCH.md
@cypher_template.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create training/updated_cypher_reference_examples_v4.cypher (SCHM-14)</name>
  <files>training/updated_cypher_reference_examples_v4.cypher</files>
  <read_first>
    - training/updated_cypher_reference_examples_v3.cypher (the single worked example R_URB_HEIGHT_MAX_75_V)
    - cypher_template.txt (finalized v4 block from 14-01 — the new file must match it)
    - 14-RESEARCH.md § "Artifact 10" (successor-file guidance)
  </read_first>
  <action>
    Create a NEW file training/updated_cypher_reference_examples_v4.cypher (leave the v3 file in place for transition-period grep history). Rewrite the v3 worked example with `r.text` → `r.SWRL`, matching 14-01's v4 CYPHER TEMPLATE BLOCK. Add ONE additional worked example that exercises the full v4 Rule property set (populating RuleName and RuleDescription) since the v3 file never demonstrated the optional-name case. Do NOT include any DesignState MERGE (D-03 — ingest emits OntoGraph + Metagraph only).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; test -f training/updated_cypher_reference_examples_v4.cypher && grep -q "r.SWRL" training/updated_cypher_reference_examples_v4.cypher && grep -q "RuleName" training/updated_cypher_reference_examples_v4.cypher && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - New v4 file exists, uses r.SWRL, demonstrates RuleName/RuleDescription, contains no DesignState MERGE.
    - v3 file untouched.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fixture hygiene — annotate/regenerate test/ artifacts teaching the old schema (SCHM-14)</name>
  <files>test/training_dataset.json</files>
  <read_first>
    - 14-RESEARCH.md § "Artifact 11" (fixture audit table — none reference the old state-kind values; the real v3-ism is old Rule-prop naming; no automated test consumes them)
  </read_first>
  <action>
    RESEARCH confirmed the named fixtures reference the old Rule-prop naming, not the old state-kind values, and no test runner loads them — this is low-effort hygiene. Do the lower-effort option: add a short header/marker to test/training_dataset.json (which carries top-level dataset_version "v3") noting it captures the pre-v4 schema and is retained for historical/debugging reference only, and bump its version marker to indicate superseded-by-v4. Do NOT rewrite every fixture record. If any test/*.json still presents the old schema as current without such a note, add the same one-line note. Confirm (and record in SUMMARY) that no automated test consumes these files, so the task is not over-scoped.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -qi "v4" test/training_dataset.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - test/training_dataset.json carries a pre-v4/superseded marker.
    - SUMMARY records that no automated test consumes the fixtures.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Amend stale "Status text" wording in ROADMAP + REQUIREMENTS (D-02)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 14 Success Criteria (SC#1 lists "Status" as a Run property)
    - .planning/REQUIREMENTS.md SCHM-07 (lists "Status text" as a Run property)
    - 14-CONTEXT.md D-01/D-02 (the resolution: only ValidStatus + SendStatus; no stored Status text)
  </read_first>
  <action>
    Amend both docs so success criteria match the Phase-13 resolution (D-02 — the "Status text" wording is stale, predating the investigation): in ROADMAP SC#1, remove "Status" from the Run-properties list so it reads Run properties (ValidStatus, SendStatus) and, if useful, add a brief parenthetical that overall pass = AND(ValidStatus), no stored Status text field, per Phase 13. In REQUIREMENTS SCHM-07, change "Run properties (ValidStatus Boolean, SendStatus Boolean, Status text)" to remove "Status text", noting D-01/D-14. Use scoped Edits — do not rewrite the files wholesale. Do NOT touch data-service's run.status='completed' concept (unrelated job-lifecycle field).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -q "SendStatus" .planning/REQUIREMENTS.md && ! grep -q "Status text" .planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP SC#1 and REQUIREMENTS SCHM-07 no longer list a Status text Run property.
    - No functional Status field introduced anywhere.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
New file: `training/updated_cypher_reference_examples_v4.cypher`. Edited: `test/training_dataset.json` (superseded marker), `.planning/ROADMAP.md` + `.planning/REQUIREMENTS.md` (stale "Status text" wording removed per D-02). No new symbols/config keys.
</artifacts_produced>

<verification>
- v4 reference file exists (r.SWRL, RuleName/RuleDescription), v3 retained.
- Fixtures annotated; no automated consumer (recorded in SUMMARY).
- ROADMAP/REQUIREMENTS "Status text" wording removed.
</verification>

<success_criteria>
The v4 few-shot reference exists, fixtures no longer teach the old schema, and the stale "Status text" wording is amended to match D-01/D-02. Satisfies SCHM-14 and the D-02 documentation correction.
</success_criteria>

<output>
Create `.planning/phases/14-graph-schema-v4-propagation/14-07-SUMMARY.md` when done.
</output>
