---
phase: 20-e2e-validation-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/e2e-v7.0-checklist.md
  - test/smoke_e2e.sh
  - DG/ (bugfixes only, if bugs found)
  - data-service/ (bugfixes only, if bugs found)
  - docker-compose.yml (bugfixes only, if bugs found)
  - n8n/workflows/ (bugfixes only, if bugs found)
autonomous: false
requirements: [E2E-01]
user_setup:
  - service: docker
    why: "E2E chain requires the full 12+ service Docker stack"
    env_vars:
      - name: SPECKLE_WRITE_TOKEN
        source: "Speckle dashboard -> Personal Access Tokens (optional, for publish verification)"
      - name: SPECKLE_READ_TOKEN
        source: "Speckle dashboard -> Personal Access Tokens (optional, for viewer verification)"
    steps:
      - "Ensure Docker Desktop is running"
      - "Run `docker compose up -d` from project root"
      - "Pull Ollama model: `docker exec ollama ollama pull llama3.1` (if not present)"
      - "Ensure 2 n8n workflows are imported: `docker exec n8n n8n import:workflow --input=/files/workflows/rules-to-metagraph.json` and `docker exec n8n n8n import:workflow --input=/files/workflows/graph-query-mcp.json`"
      - "Ensure n8n workflows are activated in the n8n UI at http://localhost:5678"

must_haves:
  truths:
    - "The full v7.0 pipeline executes end-to-end on live Docker without data loss or component errors"
    - "Both fresh test data and existing project data produce correct results through the entire chain"
    - "Any critical bugs found during E2E are fixed and re-verified"
  artifacts:
    - test/e2e-v7.0-checklist.md
    - test/smoke_e2e.sh
  key_links:
    - "n8n rule-ingest webhook produces valid v4 Cypher against a fresh project"
    - "METAGRAPH returns the ingested rules with correct Objects output"
    - "RULE DECONSTRUCT partitions variables correctly into Objects and DataProperties"
    - "STATE components (OBJECT/PARAMETER/PROPERTY/DESIGN STATE) produce correct state records"
    - "VALIDATOR publishes ValidStatus/SendStatus correctly with v2 statePayloadJson"
    - "VALIDATION GRAPH reads back published Run/Status/DesignState without errors"
    - "PARAMETER REINSTATE applies Parameters and outputs correct StateStatus"
---

<objective>
Validate the full v7.0 DG pipeline end-to-end on live Docker infrastructure, verifying every component in the chain from rule ingest through PARAMETER REINSTATE, and fix any critical bugs found.

Purpose: This is the final quality gate before v7.0 release. Every component built in Phases 13-19 must work together in the actual Docker environment. Bugs found here are fixed as part of this phase -- no separate bug-fix phase.

Output:
- `test/e2e-v7.0-checklist.md` -- Reusable E2E test checklist for the full pipeline
- `test/smoke_e2e.sh` -- Docker-side automation script for curl/Cypher checks
- Fixes to any components that fail during validation
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 20 section: Success Criteria 1, 5)
@.planning/REQUIREMENTS.md (E2E-01)
@.planning/PROJECT.md (project isolation, DB labels, canvas breakage policy)

@.planning/phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-01 through D-04)

@.planning/phases/13-ontology-v7-and-contract-investigation/13-SUMMARY.md (component GUIDs, port IRIs)
@.planning/phases/14-graph-schema-v4-propagation/14-SUMMARY.md (v4 schema state)
@.planning/phases/15-specgraph-runtime-rename/15-SUMMARY.md (SpecGraph labels)
@.planning/phases/17-graph-access-components/17-SUMMARY.md (METAGRAPH, VALIDATION GRAPH outputs)
@.planning/phases/18-rules-and-validator-rework/18-SUMMARY.md (VALIDATOR contract, publish path)
@.planning/phases/19-deconstruct-and-reinstate-components/19-SUMMARY.md (DECONSTRUCT, REINSTATE)

@test/smoke_rules_ingest.sh (existing smoke test pattern to follow)
@test/seed_designstates.cypher (existing fixture pattern)
@data-service/app.py (validation/publish endpoint for Docker-side verification)
@n8n/workflows/rules-to-metagraph.json (v4 ingest workflow)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E validation checklist + Docker-side automation + test fixtures</name>

  <files>
    test/e2e-v7.0-checklist.md
    test/smoke_e2e.sh
    test/fixture_rules_v7.txt
    test/fixture_geometry.json
  </files>

  <read_first>
    test/smoke_rules_ingest.sh (existing smoke test -- pattern for Docker-side curl automation)
    test/seed_designstates.cypher (existing Cypher fixture pattern)
    test/seed_knowledge_nodes.cypher (existing Cypher fixture pattern for project setup)
    .planning/phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-01 through D-04 for checklist structure)
    .planning/ROADMAP.md (Success Criteria 1 for full chain path)
    data-service/app.py (validation/publish and validation/view endpoints for verification)
    n8n/workflows/rules-to-metagraph.json (rule ingest webhook URL and payload format)
  </read_first>

  <action>
    Create three artifacts:

    1. `test/e2e-v7.0-checklist.md`
    - Structured markdown document for both development QA and architect installation verification (per D-01/D-04: checklist IS a release artifact)
    - Section: Prerequisites (Docker running, Ollama model pulled, n8n workflows imported and activated, Speckle tokens set)
    - Section: Fresh Baseline Setup (create new DG project, inject v7 test ontology, set up project-specific Speckle link)
    - Section: Step-by-Step Test Flow -- numbered steps with per-step (a) action, (b) expected output, (c) pass/fail checkbox:
      Step 1: Rule ingest via n8n webhook -- action: POST /webhook/dg/rules-ingest with 3 fixture rules, expected: returns executionId, Cypher creates Rule/Atom/Var/Literal nodes in project set to the fixture project name
      Step 2: METAGRAPH read -- action: wire METAGRAPH in GH or query directly via data-service endpoint, expected: returns Rules list with correct Rule_Id and Objects list with classes referenced by REFERS_TO
      Step 3: RULE DECONSTRUCT partition -- action: feed METAGRAPH Rule output into RULE DECONSTRUCT, expected: Objects output contains only Class/ObjProperty IRIs, DataProperties output contains only DatatypeProperty IRIs, builtins excluded
      Step 4: OBJECT STATE -- action: connect Object + Geometry + Label inputs, expected: outputs ObjState with StateId (OS_ prefix), geometry preserved
      Step 5: PARAMETER STATE -- action: connect Parameters list, expected: outputs ParamState with deterministic StateId (DS_ prefix), parameters indexed correctly
      Step 6: PROPERTY STATE -- action: connect Rule + DataProperty + PropValue, expected: outputs PropState with StateId (PS_ prefix), PropValue typed as DesignStateParameter
      Step 7: DESIGN STATE composition -- action: feed ObjState/ParamState/PropState lists, expected: outputs DesignState with aggregated StateId, 3-part bag composition correct
      Step 8: VALIDATOR publish -- action: connect Rule + DesignState + Run=true + SendValid=true, expected: outputs ValidStatus (list of Booleans per ObjState), RuleName, RuleDescription, SendStatus=true
      Step 9: Neo4j verification of published run -- action: curl POST /db/neo4j/tx/commit with MATCH (r:Run {graph:'ValidGraph', project:'{fixture-project}'}) RETURN r, expected: ValidStatus Boolean array, SendStatus Boolean, statePayloadJson v2 (objStates/paramStates/propStates keys)
      Step 10: VALIDATION GRAPH read-back -- action: data-service GET /validation/view/{fixture-project}, expected: returns Run/Status/DesignState matching what was published
      Step 11: MODEL VIEWER render -- action: open http://localhost:8080/model-viewer/?project={fixture-project}, expected: model viewer loads run with correct state grouping
      Step 12: PARAMETER REINSTATE -- action: connect ParamState to PARAMETER REINSTATE, toggle Reinstate from false to true, expected: Parameters list output matches source, StateStatus (7-value ReStatus) correctly reports each parameter
      Step 13: Existing project smoke test -- repeat Steps 1-12 against a real existing .gh project data, expected: same full chain works with backward-compat tolerance
    - Section: Troubleshooting (per-step failure modes and diagnosis commands)
    - Section: Final Sign-off (all checkboxes checked, no critical bugs remaining)

    2. `test/smoke_e2e.sh`
    - Bash script that automates the Docker-side portions of the checklist (Steps 1, 9, 10)
    - Uses curl to POST to n8n webhooks with Basic auth (reads N8N_USER/N8N_PASSWORD env vars or uses defaults from docker-compose.yml)
    - Uses curl to POST Cypher queries to Neo4j HTTP API at http://localhost:7474/db/neo4j/tx/commit
    - Uses curl to GET/POST data-service endpoints at http://localhost:8000
    - Set of fixture rules: "maximum height of buildings is 75 meters" (R_URB_HEIGHT_MAX_75_V), "minimum area of living units is 28 square meters" (R_RES_AREA_MIN_28_V), "all residential buildings must be at least 10 meters apart" (R_RES_SEPARATION_MIN_10_V) -- per D-02 (2-3 representative rules covering height/numeric, area/numeric, separation/object-property)
    - Script sets project name to "e2e-v7-test-{timestamp}" for isolation per D-02
    - Runs each verification step and prints PASS/FAIL
    - Exits with non-zero on any failure (suitable for CI integration)
    - Follows existing pattern in test/smoke_rules_ingest.sh for structure

    3. Fixture files
    - `test/fixture_rules_v7.txt`: the 3 rules above, one per line (copied into script and exposed as a standalone file for reader reference)
    - `test/fixture_geometry.json`: simple box geometry for test ObjState creation (Rhino common geometry shape in JSON form, matching what OBJECT STATE expects)
  </action>

  <acceptance_criteria>
    - test/e2e-v7.0-checklist.md exists with all sections: Prerequisites, Fresh Baseline Setup, 13-step test flow (numbered with expected outputs per step), Troubleshooting, Final Sign-off
    - test/smoke_e2e.sh exists and is executable (chmod +x)
    - test/fixture_rules_v7.txt contains exactly the 3 rules (height 75m, area 28sqm, separation 10m)
    - test/smoke_e2e.sh uses project name "e2e-v7-test-{timestamp}" for isolation
    - All checklist steps reference concrete endpoint URLs, component names, and expected outputs
  </acceptance_criteria>

  <verify>
    <automated>ls -la test/e2e-v7.0-checklist.md test/smoke_e2e.sh test/fixture_rules_v7.txt && head -5 test/e2e-v7.0-checklist.md | grep -q "Prerequisites" && head -5 test/smoke_e2e.sh | grep -q "#!/usr/bin/env bash" && head -5 test/fixture_rules_v7.txt | grep -q "meters"</automated>
  </verify>

  <done>All three artifacts exist with correct structure. The checklist covers all 13 steps from rule ingest through PARAMETER REINSTATE plus existing-data smoke test. The automation script calls all Docker-side verification endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Execute E2E chain on live Docker, verify all outputs, fix critical bugs inline</name>

  <files>
    DG/src/DG.Core/ (bugfixes only)
    DG/src/DG.Grasshopper/ (bugfixes only)
    data-service/app.py (bugfixes only)
    n8n/workflows/*.json (bugfixes only)
    docker-compose.yml (bugfixes only)
  </files>

  <read_first>
    test/e2e-v7.0-checklist.md (created in Task 1 -- definitive step list)
    test/smoke_e2e.sh (created in Task 1 -- automation reference)
    test/fixture_rules_v7.txt (created in Task 1 -- fixture rules)
    .planning/phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-03: fix bugs inline, D-04: full chain verification)
  </read_first>

  <action>
    Execute the full E2E validation per D-04 (every checkpoint produces correct output, no component errors, no data loss).

    Step sequence:
    1. Ensure Docker stack is running: `docker compose ps` confirms all services healthy (neo4j, n8n, ollama, data-service, design-grammars, speckle-*)
    2. Import n8n workflows if not already active: `docker exec n8n n8n import:workflow --input=/files/workflows/rules-to-metagraph.json --input=/files/workflows/graph-query-mcp.json` -- confirm "Imported" in output
    3. Pull Ollama model if missing: `docker exec ollama ollama pull llama3.1` (skip if already present)
    4. Run the Docker-side automation: `bash test/smoke_e2e.sh` -- verify it prints PASS for all steps
    5. Execute Grasshopper-side checklist manually in Rhino (per D-04: full chain including GH components):
       - Start Rhino 8, load DG plugin
       - Create new GH definition
       - Wire CONNECTOR -> GRAPH DECONSTRUCT -> METAGRAPH -> RULE DECONSTRUCT
       - Wire OBJECT STATE (Object + Geometry + Label) -> DESIGN STATE
       - Wire PARAMETER STATE (sliders/toggles) -> DESIGN STATE
       - Wire PROPERTY STATE (Rule + DataProperty + PropValue) -> DESIGN STATE
       - Wire DESIGN STATE -> VALIDATOR (with Rule input from RULE DECONSTRUCT, Run=true, SendValid=true)
       - Wire VALIDATOR output -> VALIDATION GRAPH
       - Wire VALIDATION GRAPH DesignState -> DESIGN STATE DECONSTRUCT -> OBJECT DECONSTRUCT (round-trip check)
       - Wire PARAMETER REINSTATE from a stored ParamState
       - Verify every output at each step produces expected values
    6. For existing-project smoke test: open an existing .gh file with pre-v7.0 wiring, verify missing-component placeholders appear for CLASSIFICATOR/VALIDATION RUNS/old REINSTATE, verify that re-wired components work correctly
    7. Per D-03: For any critical bug found (component crash, data mismatch, publish failure):
       - Create minimal reproduction case
       - Fix the root cause following existing codebase patterns
       - Re-run the failing step to confirm fix
       - Commit the fix with commit message referencing the step and fix description
       - Re-verify the entire chain from Step 1 to prevent regression
    8. Verify no data loss: check that ALL nodes created by the E2E chain have correct graph='ValidGraph'/'Metagraph'/'OntoGraph' assignment, project filter works, no orphan nodes
    9. Document any deferred minor bugs in the E2E checklist under "Known Issues" section

    Bug-fix guidelines per D-03:
    - Only fix bugs that BREAK the chain (crashes, data loss, wrong outputs)
    - Cosmetic issues, minor label mismatches, or non-functional improvements at Claude's discretion
    - For each fix: git commit separately with message format "fix(20): {component} -- {description of bug}"
    - Do NOT refactor or add features -- this is validation and stabilization only
  </action>

  <acceptance_criteria>
    - All 13 checklist steps pass for fresh fixture data: rule ingest -> METAGRAPH -> RULE DECONSTRUCT -> OBJECT/PARAMETER/PROPERTY STATE -> DESIGN STATE -> VALIDATOR (publish) -> Neo4j verification -> VALIDATION GRAPH read-back -> MODEL VIEWER render -> PARAMETER REINSTATE
    - Existing-project smoke test completes without critical errors
    - Any critical bugs found are FIXED (committed) and the fix does not break any previous step
    - Zero component crashes during any step
    - Neo4j contains published Run with ValidStatus array, SendStatus=true, statePayloadJson with objStates/paramStates/propStates keys
    - test/smoke_e2e.sh exits 0 with all PASS
    - Non-E2E C# tests still pass: `dotnet test DG/tests/DG.Tests/`
  </acceptance_criteria>

  <verify>
    <automated>docker compose ps --format "{{.Names}}" 2>/dev/null | grep -q neo4j && echo "Docker OK" && dotnet test DG/tests/DG.Tests/ --no-build 2>/dev/null | tail -3</automated>
  </verify>

  <done>Full E2E chain verified. All checklist steps pass. Any critical bugs found are fixed and committed. Existing-project smoke test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test runner -> Docker services | All test traffic originates from localhost, targeting local Docker containers -- no external network crossing |
| Grasshopper -> Neo4j | Direct Bolt connection inside the dev environment -- same trust domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | E2E automation script | low | accept | Test uses project-isolated project names -- collateral damage limited to test project |
| T-20-02 | Information Disclosure | E2E checklist (release artifact) | low | accept | Checklist documents wiring topology but not credentials -- safe to publish |
| T-20-03 | Elevation of Privilege | Bugfix code changes | low | mitigate | Bugfixes are minimal and targeted -- scope limited to crash/data-loss fixes per D-03; full non-E2E test suite re-run verifies no regression |
| T-20-SC | Tampering | npm/pip installs | low | accept | No package installs in this plan -- only `dotnet build` and Docker exec commands |
</threat_model>

<verification>
1. `bash test/smoke_e2e.sh` exits 0
2. `dotnet test DG/tests/DG.Tests/` passes all non-E2E tests
3. Human confirms checklist steps pass in Rhino/GH canvas
</verification>

<success_criteria>
- E2E chain verified on live Docker with fresh fixtures (E2E-01)
- All outputs match expected values per D-04
- Any critical bugs fixed and committed
- Existing-project smoke test passes
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `e2e-v7.0-checklist.md` | Document | `test/e2e-v7.0-checklist.md` |
| `smoke_e2e.sh` | Bash script | `test/smoke_e2e.sh` |
| `fixture_rules_v7.txt` | Test fixture | `test/fixture_rules_v7.txt` |
| `fixture_geometry.json` | Test fixture | `test/fixture_geometry.json` |
| Bugfix commits (if any) | Code changes | `DG/src/`, `data-service/`, `n8n/workflows/`, `docker-compose.yml` |

<output>
Create `.planning/phases/20-e2e-validation-and-docs/20-01-SUMMARY.md` when done.
Update `.planning/STATE.md` (current_phase=20, plan=1).
</output>
