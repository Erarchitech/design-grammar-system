---
phase: 13-ontology-v7-and-contract-investigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ontology/V7-INVESTIGATION.md]
autonomous: true
requirements: [ONTO-04]
user_setup: []

must_haves:
  truths:
    - "V7-INVESTIGATION.md records the resolution of conflict (a) ValidStatus(Boolean)-vs-Status(text) as one field named ValidStatus, a per-object Boolean list on Run, with overall pass derived (AND) not stored, and no separate Status text enum"
    - "V7-INVESTIGATION.md records the resolution of conflict (b) DesignState storage layer as graph='ValidGraph' (not Metagraph), written only by VALIDATOR on publish, MERGE'd by StateId+project, one-DesignState-to-many-Runs, with the no-orphan invariant stated as an enforced rule"
    - "V7-INVESTIGATION.md records the resolution of conflict (c) version marker as owl:versionInfo='7.0' with the stale 'Schema version: v3' rdfs:comment removed or rewritten to v7"
    - "V7-INVESTIGATION.md contains a complete V6->V7 rename table mapping every V6 local name/IRI to its V7 successor, including the state trio (ObjectState->ObjState, DefState->ParamState, PropState new), layer hubs (OntoGraph->Ontograph, ValidationGraph->Validgraph, ComputationGraph->Computgraph), ObjectProperty->ObjProperty, DatatypeProperty->DataProperty, ValidationRun->Run, ReinstatementStatus->ReStatus, and the rule-level datatype props ruleText->SWRL, ruleTitle->RuleName, RuleDescription (new)"
    - "The rename table records the OWL-construct collision hazard: &dg;ObjectProperty and &dg;DatatypeProperty (reification classes) must be renamed while owl:ObjectProperty and owl:DatatypeProperty (OWL constructs) must NOT"
  artifacts:
    - path: "ontology/V7-INVESTIGATION.md"
      provides: "Investigation note: three conflict resolutions, final state-kind names, superseded PDF statements, and the authoritative V6->V7 rename table consumed by plans 13-02/13-03"
      contains: "ValidGraph"
      min_lines: 80
  key_links:
    - from: "ontology/V7-INVESTIGATION.md"
      to: ".planning/phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md"
      via: "conflict resolutions transcribe locked decisions D-01..D-11 verbatim (ValidGraph layer, per-object ValidStatus, versionInfo policy)"
      pattern: "ValidStatus"
    - from: "ontology/V7-INVESTIGATION.md"
      to: "ontology/DesignGrammar-V6.owl"
      via: "rename table's V6 column enumerates the actual local names present in the V6 owl (ObjectState, DefState, ValidationRun, ReinstatementStatus, ruleText, ruleTitle, OntoGraph, ValidationGraph, ComputationGraph)"
      pattern: "ObjectState"
---

<objective>
Resolve the three `GH_DesignGrammars.pdf`-internal conflicts and produce the single authoritative V6->V7 rename table that every other Phase 13 plan consumes. This plan locks names; it writes no code and touches no ontology source.

Purpose: Phases 14-20 all build on stable V7 names. If the rename table is wrong or incomplete, every downstream artifact (cypher_template v4, VALIDATION GRAPH, VALIDATOR persistence, Model Viewer) inherits the error. This is the naming authority for the whole milestone.
Output: `ontology/V7-INVESTIGATION.md` — conflict resolutions + final names + the complete rename table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three conflict resolutions into V7-INVESTIGATION.md</name>
  <files>ontology/V7-INVESTIGATION.md</files>
  <read_first>.planning/phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md, ontology/GH_DesignGrammars.pdf, cypher_template.txt</read_first>
  <action>
Create `ontology/V7-INVESTIGATION.md` with a header (title, date, "Source of truth: ontology/GH_DesignGrammars.pdf; decisions locked in 13-CONTEXT.md D-01..D-11") and one section per conflict. Transcribe the CONTEXT.md decisions VERBATIM — do NOT re-open or re-derive them:

- **Conflict (a) — Run status model (resolves CONTEXT D-06/D-07/D-08).** Record: `Run.ValidStatus` is a Neo4j Boolean **list** property, one element per `ObjState` in the validated DesignState, index-matched to ObjState order (D-06). There is NO separate `Status` text enum — the PDF's `ValidStatus`(Boolean at VALIDATOR) vs `Status`(text at VALIDATION GRAPH) is a cosmetic naming inconsistency, unified to one field `ValidStatus` typed as a Boolean list (D-07). Overall pass = `AND(ValidStatus)`, **derived not stored**. `Run.SendStatus` is a single Boolean per Run (D-08). State explicitly that the per-object *population* semantics (all ObjStates vs only rule-target-Class objects) is deferred to Phase 18 — Phase 13 locks only the array **shape** + **index-matching**.
- **Conflict (b) — DesignState storage layer (resolves CONTEXT D-01..D-05).** Record: a DesignState node lives in `graph='ValidGraph'`, corrected from cypher_template v3's `graph='Metagraph'` (cite cypher_template.txt lines 49-50 and 113-116). ValidGraph wins because the PDF's VALIDATION GRAPH component reads DesignState from the ValidGraph handle. The PDF statement "New Design State can only pass to Metagraph through Validator" is **superseded on destination only** — destination becomes ValidGraph, but "only through Validator" is preserved (no direct-insert path). DesignState is written only by VALIDATOR on publish, MERGE'd by StateId+project. Cardinality: one DesignState <-> many Runs. No-orphan invariant: a DesignState exists in ValidGraph only with >=1 Run linked — an ENFORCED rule, not a guideline.
- **Conflict (c) — version marker (resolves CONTEXT D-10).** Record: V7 owl carries `owl:versionInfo = "7.0"`; the stale `rdfs:comment "Schema version: v3"` at DesignGrammar-V6.owl line 46 is removed (or rewritten to "Schema version: v7" — either satisfies the policy). Single source of version truth = `versionInfo`; V6 owl currently also carries `versionInfo 6.1` at line 48.

Add a "Superseded PDF statements" subsection listing each PDF claim that v7.0 overrides (at minimum: DesignState destination = Metagraph; ValidStatus/Status as two fields), each with its replacement.
  </action>
  <verify>grep -c "ValidGraph" ontology/V7-INVESTIGATION.md (>=1); grep -i "no-orphan\|derived not stored\|AND(ValidStatus)" ontology/V7-INVESTIGATION.md</verify>
  <acceptance_criteria>
    - V7-INVESTIGATION.md contains a section resolving each of conflicts (a), (b), (c)
    - The (a) section states "ValidStatus" is a Boolean list index-matched to ObjState and that overall pass is derived (AND), not stored
    - The (b) section states graph='ValidGraph', "only through Validator" preserved, one-DesignState-to-many-Runs, and the no-orphan invariant as enforced
    - The (c) section states versionInfo='7.0' and removal/rewrite of the "Schema version: v3" comment
    - A "Superseded PDF statements" subsection lists each overridden PDF claim with its replacement
  </acceptance_criteria>
  <done>All three conflicts resolved in writing, transcribed from CONTEXT.md D-01..D-11, with a superseded-statements list.</done>
</task>

<task type="auto">
  <name>Task 2: Write the authoritative V6->V7 rename table + final state-kind names</name>
  <files>ontology/V7-INVESTIGATION.md</files>
  <read_first>ontology/DesignGrammar-V6.owl, .planning/REQUIREMENTS.md, .planning/phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md</read_first>
  <action>
Append a "## V6->V7 Rename Table" section — a single markdown table with columns `V6 name | V7 name | Kind (class/objprop/dataprop/individual) | Namespace | Notes`. This table is the authority that 13-02 (rename script) and 13-03 (port->IRI map) consume, so it must be COMPLETE and grep-derived from the actual V6 owl, not guessed. Confirm each V6 source name exists in DesignGrammar-V6.owl before adding its row (approx current counts: ObjectState x35, DefState x19, ValidationRun x21, ReinstatementStatus x27, ruleText x6, ruleTitle x1, &dg;OntoGraph x19, &dgv;ValidationGraph x39, ComputationGraph x58).

Required rows (from ONTO-01/03/04 + CONTEXT):
- **State trio (DesignState subclasses):** ObjectState -> ObjState; DefState -> ParamState; (new) PropState — a DesignState subclass built on the existing propValue/propValueOf properties (Rule + DataProperty + PropValue). Mark PropState as NEW (no V6 source).
- **Layer hubs:** OntoGraph -> Ontograph; ValidationGraph -> Validgraph; ComputationGraph -> Computgraph. Note that Metagraph and SpecGraph are NOT renamed.
- **OntoGraph reification classes:** ObjectProperty -> ObjProperty; DatatypeProperty -> DataProperty. **Notes column MUST flag the collision hazard:** only the `&dg;`-prefixed reification classes are renamed; the OWL constructs `owl:ObjectProperty` / `owl:DatatypeProperty` (element type names) must NOT be touched.
- **Validation:** ValidationRun -> Run; ReinstatementStatus -> ReStatus (covers ReinstatementStatus and ReinstatementStatusValue).
- **New Validgraph datatype properties (Boolean):** SendStatus (single Boolean per Run), ValidStatus (Boolean list per Run). Mark both NEW.
- **Rule-level datatype properties (ONTO-04):** ruleText -> SWRL (ruleText holds the SWRL serialization, e.g. `Building(?b)^hasHeightM(?b,?h)^swrlb:greaterThan(?h,75)->violatesMaxHeight(?b,true)` at V6 owl line 838 — confirm by reading the ruleText def at line 547); ruleTitle -> RuleName (human title, def at line 561); RuleDescription — NEW natural-language description, no V6 source. Add a one-line rationale for this 2->3 mapping.

Close the section with a "## Final state-kind names (locked)" line: `ObjState | ParamState | PropState` — the exact strings phases 14/16 use for DesignState `kind`.
  </action>
  <verify>grep -E "ObjectState.*ObjState|DefState.*ParamState|ValidationRun.*Run|ruleText.*SWRL|ruleTitle.*RuleName" ontology/V7-INVESTIGATION.md; grep -i "owl:ObjectProperty" ontology/V7-INVESTIGATION.md (collision note present)</verify>
  <acceptance_criteria>
    - "## V6->V7 Rename Table" exists with columns V6 name / V7 name / Kind / Namespace / Notes
    - Table rows exist for: ObjectState->ObjState, DefState->ParamState, PropState (NEW), OntoGraph->Ontograph, ValidationGraph->Validgraph, ComputationGraph->Computgraph, ObjectProperty->ObjProperty, DatatypeProperty->DataProperty, ValidationRun->Run, ReinstatementStatus->ReStatus, SendStatus (NEW), ValidStatus (NEW), ruleText->SWRL, ruleTitle->RuleName, RuleDescription (NEW)
    - The ObjProperty/DataProperty rows' Notes flag that owl:ObjectProperty and owl:DatatypeProperty must NOT be renamed
    - A "## Final state-kind names (locked)" line lists exactly ObjState, ParamState, PropState
    - Metagraph and SpecGraph are explicitly noted as NOT renamed
  </acceptance_criteria>
  <done>The complete, grep-verified V6->V7 rename table exists with the state-kind names locked and the OWL-construct collision flagged.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f ontology/V7-INVESTIGATION.md`
- [ ] All three conflicts (a)/(b)/(c) have a resolution section citing the CONTEXT decision IDs
- [ ] The rename table covers every ONTO-01/03/04 rename family and every source name is confirmed present in DesignGrammar-V6.owl
- [ ] No source file was modified — `git status --porcelain ontology/DesignGrammar-V6.owl` is empty
</verification>

<success_criteria>
- All tasks completed
- `ontology/V7-INVESTIGATION.md` resolves conflicts (a)/(b)/(c) and holds the authoritative, complete V6->V7 rename table with final state-kind names locked
- No ontology source files modified (investigation/rename-planning only)
</success_criteria>

<output>
After completion, create `.planning/phases/13-ontology-v7-and-contract-investigation/13-01-SUMMARY.md`
</output>
