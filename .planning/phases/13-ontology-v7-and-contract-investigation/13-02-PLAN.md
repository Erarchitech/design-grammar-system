---
phase: 13-ontology-v7-and-contract-investigation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified: [ontology/apply_v7_rename.py, ontology/DesignGrammar-V7.owl, ontology/V6-to-V7-mapping.md]
autonomous: true
requirements: [ONTO-01, ONTO-02, ONTO-03, ONTO-04]
user_setup: []

must_haves:
  truths:
    - "Running `python ontology/apply_v7_rename.py` from the ontology/ folder exits 0 and prints '[OK] no residual banned tokens'"
    - "DesignGrammar-V7.owl exists and contains ObjState, ParamState, and PropState as rdfs:subClassOf DesignState (the state trio)"
    - "DesignGrammar-V7.owl contains rule-level datatype properties SWRL, RuleName, and RuleDescription with rdfs:domain the Rule class"
    - "DesignGrammar-V7.owl contains Validgraph Boolean datatype properties SendStatus and ValidStatus"
    - "DesignGrammar-V7.owl carries owl:versionInfo '7.0' and no 'Schema version: v3' comment"
    - "DesignGrammar-V6.owl is byte-for-byte unchanged (rename is generative; V6 is the read-only source)"
    - "V6-to-V7-mapping.md lists every renamed IRI as old -> new and is emitted from the script's actual rename operations"
    - "owl:ObjectProperty and owl:DatatypeProperty OWL constructs are intact — only the &dg; reification classes were renamed to ObjProperty/DataProperty"
  artifacts:
    - path: "ontology/apply_v7_rename.py"
      provides: "Idempotent V6->V7 OWL transform following the apply_v6_restructure.py pattern (must_replace/delete_block/reparent helpers + banned-token self-check)"
      contains: "DesignGrammar-V7.owl"
      min_lines: 120
    - path: "ontology/DesignGrammar-V7.owl"
      provides: "The V7 ontology generated from DesignGrammar-V6.owl with all renames, the PropState subclass, rule-level SWRL/RuleName/RuleDescription, and Validgraph SendStatus/ValidStatus"
      contains: "PropState"
      min_lines: 2000
    - path: "ontology/V6-to-V7-mapping.md"
      provides: "Recovery-only old->new IRI mapping for existing publications"
      contains: "ObjState"
      min_lines: 20
  key_links:
    - from: "ontology/apply_v7_rename.py"
      to: "ontology/V7-INVESTIGATION.md"
      via: "the script's rename operations implement the V6->V7 Rename Table from the investigation note exactly"
      pattern: "ObjState|ParamState|Validgraph"
    - from: "ontology/apply_v7_rename.py"
      to: "ontology/DesignGrammar-V6.owl"
      via: "reads SRC = DesignGrammar-V6.owl, writes DST = DesignGrammar-V7.owl (source untouched)"
      pattern: "DesignGrammar-V6.owl"
    - from: "ontology/V6-to-V7-mapping.md"
      to: "ontology/apply_v7_rename.py"
      via: "mapping emitted by the script from its actual rename table so it can never drift from what was applied"
      pattern: "ObjectState|DefState|ValidationRun"
---

<objective>
Generate `DesignGrammar-V7.owl` from `DesignGrammar-V6.owl` via a new `apply_v7_rename.py` that applies the locked V6->V7 rename table, adds the new PropState subclass and the rule/Validgraph datatype properties, and fixes the version marker — with a self-check that fails loudly on any residual old token. Emit the `V6-to-V7-mapping.md` recovery file from the same rename table.

Purpose: This is ONTO-01 — the mechanical realization of the naming contract. Every phase 14+ artifact is validated against these V7 names. The self-check + generative (V6-untouched) approach makes the rename auditable and repeatable.
Output: `ontology/apply_v7_rename.py`, `ontology/DesignGrammar-V7.owl`, `ontology/V6-to-V7-mapping.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md
@ontology/V7-INVESTIGATION.md
@ontology/apply_v6_restructure.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write apply_v7_rename.py following the apply_v6_restructure.py pattern</name>
  <files>ontology/apply_v7_rename.py</files>
  <read_first>ontology/apply_v6_restructure.py, ontology/V7-INVESTIGATION.md, ontology/DesignGrammar-V6.owl</read_first>
  <action>
Create `ontology/apply_v7_rename.py` mirroring `apply_v6_restructure.py`'s structure exactly: module docstring summarizing the V7 delta; `HERE`/`SRC = DesignGrammar-V6.owl`/`DST = DesignGrammar-V7.owl`; the `must_replace(text, old, new, n=None, label="")`, `delete_block`, `del_line`, `reparent` helpers (copy them); a phased sequence of string/regex transforms ordered to avoid collisions; and a SELF-CHECK block at the end that writes DST and asserts zero residual banned tokens (exit 2 on any).

Apply the V6->V7 Rename Table from V7-INVESTIGATION.md. Ordering rules to avoid collisions (mirror how apply_v6 handles KnowledgeGraph->SpecGraph and objectRef):
- **Layer hubs:** rename local names OntoGraph->Ontograph, ValidationGraph->Validgraph, ComputationGraph->Computgraph across entity refs AND element qnames. Metagraph and SpecGraph are NOT renamed.
- **OntoGraph reification classes — COLLISION-CRITICAL:** rename ONLY `&dg;ObjectProperty` -> `&dg;ObjProperty` and `&dg;DatatypeProperty` -> `&dg;DataProperty` (and their `dg:ObjectProperty`/`dg:DatatypeProperty` element qnames). NEVER touch `owl:ObjectProperty` / `owl:DatatypeProperty` (the OWL constructs). Use prefix-anchored replaces (`&dg;`, `dg:`) exactly like apply_v6 does; add a self-check assertion that `owl:ObjectProperty` and `owl:DatatypeProperty` counts are unchanged from SRC.
- **State trio:** ObjectState -> ObjState, DefState -> ParamState (entity refs + qnames + any instance IRIs). Insert a new PropState class as an rdfs:subClassOf DesignState, following the CORE_BLOCK insertion pattern (a raw XML block inserted at an anchor via must_replace). Give PropState an rdfs:label, rdfs:comment (Rule + DataProperty + PropValue composition, building on propValue/propValueOf), and dg:graph/subClassOf consistent with DefState/ObjectState's parent.
- **Validation:** ValidationRun -> Run; ReinstatementStatus -> ReStatus (covers ReinstatementStatusValue too).
- **Rule-level datatype props (ONTO-04):** ruleText -> SWRL, ruleTitle -> RuleName (rename the DatatypeProperty defs at V6 owl lines 547 and 561 and all element usages like `<dgm:ruleText>`). Insert a new RuleDescription DatatypeProperty (domain Rule, range xsd:string, comment: natural-language rule description).
- **New Validgraph Boolean datatype props (ONTO-04):** insert SendStatus (xsd:boolean, single per Run) and ValidStatus (comment MUST state it is a per-object Boolean **list** index-matched to ObjState, overall pass derived AND). Domain = Run; graph = Validgraph.
- **Version marker (ONTO-04 conflict c):** `must_replace("<owl:versionInfo>6.1</owl:versionInfo>", "<owl:versionInfo>7.0</owl:versionInfo>", 1)` and delete/rewrite the `Schema version: v3` comment at line 46.

SELF-CHECK banned list MUST include: `ObjectState`, `DefState`, `ReinstatementStatus`, `ruleText`, `ruleTitle`, `&dg;OntoGraph`, `&dgv;ValidationGraph`, `&dgc;ComputationGraph` (bare, collision-safe anchoring), `Schema version: v3`, `<owl:versionInfo>6.1`. Do NOT ban `SWRL` (legitimately present in imports/atomSwrlLabel) — assert instead that the SWRL datatype property def exists. Keep the script IDEMPOTENT and DELETE nothing from V6 (SRC read-only).
  </action>
  <verify>python -c "import ast; ast.parse(open('ontology/apply_v7_rename.py',encoding='utf-8').read())"</verify>
  <acceptance_criteria>
    - apply_v7_rename.py parses as valid Python and defines SRC=DesignGrammar-V6.owl, DST=DesignGrammar-V7.owl
    - Contains must_replace/delete_block/reparent-style helpers and a final banned-token self-check with sys.exit on residuals
    - The banned list includes ObjectState, DefState, ReinstatementStatus, ruleText, ruleTitle, and the bare layer-hub IRIs
    - Includes an assertion that owl:ObjectProperty / owl:DatatypeProperty counts are preserved
    - Inserts PropState (subclass of DesignState), RuleDescription, SendStatus, ValidStatus blocks
  </acceptance_criteria>
  <done>apply_v7_rename.py is a valid, idempotent, self-checking transform implementing the full rename table.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script, produce DesignGrammar-V7.owl, and verify the result</name>
  <files>ontology/DesignGrammar-V7.owl</files>
  <read_first>ontology/apply_v7_rename.py</read_first>
  <action>
Run `python ontology/apply_v7_rename.py` from the `ontology/` directory. It must exit 0 and print "[OK] no residual banned tokens". If the self-check fails (exit 2) or any `must_replace` raises `[FAIL]`, fix the transform ordering/anchors in apply_v7_rename.py and re-run until clean — do NOT hand-edit DesignGrammar-V7.owl (it is generated output; hand edits would not survive a re-run and would desync from the script).

Then confirm the produced owl with greps:
- `ObjState`, `ParamState`, `PropState` each appear as `rdfs:subClassOf ...DesignState` (state trio).
- `SWRL`, `RuleName`, `RuleDescription` DatatypeProperty defs exist with domain Rule.
- `SendStatus`, `ValidStatus` DatatypeProperty defs exist (ValidStatus comment mentions per-object list).
- `owl:versionInfo` is `7.0`; no `Schema version: v3`.
- Zero occurrences of ObjectState/DefState/ReinstatementStatus/ruleText/ruleTitle.
- `git diff --stat ontology/DesignGrammar-V6.owl` is empty (V6 untouched).
  </action>
  <verify>cd ontology && python apply_v7_rename.py && grep -c "PropState" DesignGrammar-V7.owl && grep -c "ObjectState\|DefState\|ruleText\|ruleTitle" DesignGrammar-V7.owl (must be 0) && git diff --stat DesignGrammar-V6.owl (empty)</verify>
  <acceptance_criteria>
    - `python apply_v7_rename.py` exits 0 and prints "[OK] no residual banned tokens"
    - DesignGrammar-V7.owl contains ObjState, ParamState, PropState as DesignState subclasses
    - DesignGrammar-V7.owl contains SWRL, RuleName, RuleDescription, SendStatus, ValidStatus property defs
    - grep for ObjectState|DefState|ReinstatementStatus|ruleText|ruleTitle in DesignGrammar-V7.owl returns 0
    - owl:versionInfo is 7.0 and no "Schema version: v3" comment remains
    - DesignGrammar-V6.owl is unmodified
  </acceptance_criteria>
  <done>DesignGrammar-V7.owl is generated clean with the state trio, new datatype properties, and correct version marker; V6 untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Emit V6-to-V7-mapping.md from the script's rename table</name>
  <files>ontology/V6-to-V7-mapping.md, ontology/apply_v7_rename.py</files>
  <read_first>ontology/V7-INVESTIGATION.md, ontology/apply_v7_rename.py</read_first>
  <action>
Make `apply_v7_rename.py` also emit `ontology/V6-to-V7-mapping.md` as a side output, built from a single in-script `RENAMES` list of `(v6_name, v7_name, kind, note)` tuples that the transform phases and the mapping writer BOTH consume — so the mapping can never drift from what was actually applied (this mirrors how the investigation note is the decision authority and the script is the implementation). The file is a recovery-only guard for existing publications: a header explaining it maps old publication names to their V7 successors, then a markdown table `Old (V6) | New (V7) | Kind | Note`, one row per rename (state trio, layer hubs, ObjProperty/DataProperty, Run, ReStatus, SWRL/RuleName, plus NEW rows for PropState/RuleDescription/SendStatus/ValidStatus marked "new in V7 — no V6 predecessor"). Re-run the script and confirm the file is written.
  </action>
  <verify>cd ontology && python apply_v7_rename.py && grep -E "ObjectState.*ObjState|ValidationRun.*Run|DefState.*ParamState" V6-to-V7-mapping.md</verify>
  <acceptance_criteria>
    - V6-to-V7-mapping.md is written by apply_v7_rename.py (not hand-authored) from an in-script RENAMES list also used by the transforms
    - Contains a markdown table with rows for every rename incl. ObjectState->ObjState, DefState->ParamState, ValidationRun->Run, ReinstatementStatus->ReStatus, ObjectProperty->ObjProperty, DatatypeProperty->DataProperty, ruleText->SWRL, ruleTitle->RuleName, and layer hubs
    - New-in-V7 entries (PropState, RuleDescription, SendStatus, ValidStatus) are listed and marked as having no V6 predecessor
  </acceptance_criteria>
  <done>V6-to-V7-mapping.md is generated from the script's own rename table, guaranteeing it matches the applied transforms.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ontology && python apply_v7_rename.py` exits 0, prints "[OK] no residual banned tokens", and writes DesignGrammar-V7.owl + V6-to-V7-mapping.md
- [ ] `grep -c "ObjectState\|DefState\|ReinstatementStatus\|ruleText\|ruleTitle" ontology/DesignGrammar-V7.owl` == 0
- [ ] `grep -c "owl:ObjectProperty\|owl:DatatypeProperty" ontology/DesignGrammar-V7.owl` equals the V6 count (constructs preserved)
- [ ] `grep "PropState\|SWRL\|RuleName\|RuleDescription\|SendStatus\|ValidStatus" ontology/DesignGrammar-V7.owl` all present
- [ ] `git diff --stat ontology/DesignGrammar-V6.owl` is empty
</verification>

<success_criteria>
- All tasks completed
- apply_v7_rename.py produces DesignGrammar-V7.owl with the state trio as DesignState subclasses, rule-level SWRL/RuleName/RuleDescription, and Validgraph SendStatus/ValidStatus (Boolean), version 7.0 — satisfying ROADMAP Phase 13 success criterion 1
- V6-to-V7-mapping.md lists every renamed IRI old->new (success criterion 2)
- DesignGrammar-V6.owl unchanged; script is idempotent and self-checking
</success_criteria>

<output>
After completion, create `.planning/phases/13-ontology-v7-and-contract-investigation/13-02-SUMMARY.md`
</output>
