---
phase: 19-deconstruct-and-reinstate-components
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - DG/src/DG.Grasshopper/Components/ParameterReinstateComponent.cs
  - DG/src/DG.Grasshopper/Components/ReinstateComponent.cs
  - DG/tests/DG.Tests/ParameterReinstateComponentTests.cs
autonomous: true
requirements:
  - GHST-07

must_haves:
  truths:
    - PARAMETER REINSTATE has 3 inputs: ParamState (item), Target (item, REQUIRED), Reinstate (Boolean, item, default false)
    - PARAMETER REINSTATE has 3 outputs: Parameters (list, ALL params from ParamState), StateStatus (list, index-matched to Parameters), Status (text item, human-readable summary)
    - Rising-edge trigger (_lastApplyInput initialized to true) prevents auto-fire on first solve
    - Wire-traversal from Target input finds upstream PARAMETER STATE component and resolves sliders/toggles
    - DesignStateReinstatementService.Validate is called for pre-validation (pure logic, all 7 ReStatus values)
    - Deferred value writing via ScheduleSolution (not in SolveInstance -- prevents silent drop)
    - Assembly-mismatch fallback (ReconstructSnapshot/ReconstructParameter) carries forward from old REINSTATE
    - Old ReinstateComponent.cs is removed (replaced by new GUID and new contract)
    - FormatStatus/FormatMessage calls ErrorMessageTemplates (not local method -- moved in Plan 01)
  artifacts:
    - DG/src/DG.Grasshopper/Components/ParameterReinstateComponent.cs
    - DG/tests/DG.Tests/ParameterReinstateComponentTests.cs
  key_links:
    - Old ReinstateComponent.cs is deleted -- ParameterReinstateComponent.cs replaces it with new GUID
    - Wire-traversal searches Input 1 (Target) ONLY per D-02 -- no fallback to Input 0
    - Parameters output uses _latestParamState?.Parameters.ToList() -- always ALL params per D-05
    - StateStatus output is result.Reports.Select(r => r.Status).ToList() -- index-matched to Parameters per D-04
    - _lastApplyInput initialized to true (not false) -- prevents first-solve auto-fire
---

<objective>
Rework the existing REINSTATE component into PARAMETER REINSTATE with a new GUID, required Target input, and new output contract (Parameters + StateStatus + Status). All wire-traversal, rising-edge trigger, deferred write, and assembly-mismatch patterns carry forward from the old component.

Purpose: Architects can re-apply captured parameters from a stored ParamState back to live Grasshopper sliders/toggles, with clear per-parameter status feedback via index-matched lists.

Output: ParameterReinstateComponent.cs replacing ReinstateComponent.cs, with matching tests in DG.Tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Phase 19 decisions
@.planning/phases/19-deconstruct-and-reinstate-components/19-CONTEXT.md
@.planning/phases/19-deconstruct-and-reinstate-components/19-RESEARCH.md

# THE FILE TO ADAPT -- full implementation with all patterns to carry forward
@DG/src/DG.Grasshopper/Components/ReinstateComponent.cs

# New FormatStatus/FormatMessage in ErrorMessageTemplates (from Plan 01)
@DG/src/DG.Core/Services/ErrorMessageTemplates.cs

# DgIcons -- ParameterReinstate24 (from Plan 01)
@DG/src/DG.Grasshopper/DgIcons.cs

# Core models consumed by this component
@DG/src/DG.Core/Models/ParamState.cs
@DG/src/DG.Core/Models/DesignStateParameter.cs
@DG/src/DG.Core/Models/ReinstatementResult.cs
@DG/src/DG.Core/Models/ReinstatementStatus.cs
@DG/src/DG.Core/Models/ReinstatementParameterReport.cs
@DG/src/DG.Core/Models/ResolvedTarget.cs

# Service called for pre-validation
@DG/src/DG.Core/Services/DesignStateReinstatementService.cs

# The PARAMETER STATE component (target of wire-traversal)
@DG/src/DG.Grasshopper/Components/ParameterStateComponent.cs

# Reference: index-matched output contract pattern
@DG/src/DG.Grasshopper/Components/ValidationGraphComponent.cs

# Existing test for the underlying validation service
@DG/tests/DG.Tests/ReinstatementServiceTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ParameterReinstateComponent.cs replacing ReinstateComponent.cs</name>

  <read_first>
    - DG/src/DG.Grasshopper/Components/ReinstateComponent.cs -- FULL source: wire-traversal (FindUpstreamDesignState, FindDesignStateFromInput, ResolveTargets, ResolveSourceInfo), rising-edge trigger (_lastApplyInput), deferred writes (ScheduleWriteValues, WriteToSource), assembly-mismatch fallback (UnwrapSnapshot, UnwrapGhContainer, ReconstructSnapshot, ReconstructParameter, DiagnoseInputType), output helpers (SetOutputs), FormatStatus/FormatMessage
    - DG/src/DG.Core/Services/ErrorMessageTemplates.cs -- FormatStatus, FormatMessage, ReinstateTargetRequired, ReinstateSourceNotFound (from Plan 01)
    - DG/src/DG.Grasshopper/DgIcons.cs -- ParameterReinstate24 (from Plan 01)
    - DG/src/DG.Core/Models/ParamState.cs -- Parameters is Collection<DesignStateParameter>
    - DG/src/DG.Core/Models/DesignStateParameter.cs -- ParameterId, DisplayName, Type enum (Number/Integer/Boolean), NumberValue/IntegerValue/BooleanValue
    - DG/src/DG.Core/Services/DesignStateReinstatementService.cs -- Validate() signature, ReinstatementResult contract
    - DG/src/DG.Core/Models/ReinstatementResult.cs -- Applied, Aborted, Reports
    - DG/src/DG.Core/Models/ResolvedTarget.cs -- ParameterId, Resolution (Resolved/Missing/Ambiguous), TargetType, DomainMin/DomainMax
  </read_first>

  <files>
    DG/src/DG.Grasshopper/Components/ParameterReinstateComponent.cs
    DG/src/DG.Grasshopper/Components/ReinstateComponent.cs
  </files>

  <action>
    Create ParameterReinstateComponent.cs by adapting the full source of ReinstateComponent.cs. The new file replaces the old one entirely. Then DELETE ReinstateComponent.cs (not rename -- new GUID, new contract, clean git history per research recommendation).

    Changes from old REINSTATE to new PARAMETER REINSTATE:

    1. CLASS NAME: `ReinstateComponent` -> `ParameterReinstateComponent`

    2. DISPLAY NAME: "REINSTATE" -> "PARAMETER REINSTATE" (full name in constructor base call)
    3. NICKNAME: "REINSTATE" -> "PARAMREIN" (short name in constructor base call)
    4. DESCRIPTION: Update to describe new contract: "Apply a saved ParamState back to Grasshopper sliders/toggles. Required Target input wired from PARAMETER STATE. Rising-edge Reinstate trigger."
    5. GUID: `D4E2F8A1-C736-4B9D-AE51-2B1F7C9D0E63` -> `8F9D0A1B-2C3D-4E4F-5A5B-6C7D8E9F0A1B`
    6. ICON: `DgIcons.Reinstate24` -> `DgIcons.ParameterReinstate24`

    7. INPUTS (3 inputs, changed from old):
       - Input 0: "ParamState" / "ParamState" / generic item / "ParamState to apply. Wire from DESIGN STATE DECONSTRUCT or PARAMETER STATE State output." (same ParamState input, renamed from "State")
       - Input 1: "Target" / "Target" / generic item / "Required: Wire the State output of a PARAMETER STATE component. REINSTATE walks this wire to discover sliders/toggles." (renamed from "DesignState", now REQUIRED -- no Params.Input[1].Optional = true)
       - Input 2: "Reinstate" / "Reinstate" / Boolean item / "Rising-edge trigger -- fires on false->true transition. Use a Button or Boolean Toggle." (renamed from "Apply", same behavior)

    8. OUTPUTS (3 outputs, completely changed from old):
       - Index 0: GenericParameter "Parameters" / "Parameters" / list / "All captured DesignStateParameters from the input ParamState (D-05). Cross-reference with StateStatus to filter applied vs. blocked."
       - Index 1: GenericParameter "StateStatus" / "StateStatus" / list / "Per-parameter ReinstatementStatus list, index-matched to Parameters (D-04). Same length, same order as Parameters."
       - Index 2: TextParameter "Status" / "Status" / item / "Summary: Applied N parameters / Aborted: M blocked / Unchanged (same state) / Idle (D-06)."

    9. SOLVEINSTANCE logic:
       - Read Input 0 (ParamState): same UnwrapSnapshot logic (preserves assembly-mismatch fallback)
       - Read Input 1 (Target): FindUpstreamDesignState now searches ONLY Params.Input[1] -- the Target input per D-02. NO fallback to Input 0 search. If Target input has no sources, emit Warning using ErrorMessageTemplates.ReinstateTargetRequired() + empty outputs + return.
       - Read Input 2 (Reinstate Boolean): same rising-edge detection. _lastApplyInput = true initialization (prevents first-solve auto-fire per common pitfall 3).
       - Rising edge check: same `isRisingEdge = applyInput && !_lastApplyInput` pattern
       - If no rising edge: SetOutputs with _latestResult and ErrorMessageTemplates.FormatStatus, return
       - Resolve targets: same ResolveTargets logic (but input access uses designStateComponent.Params.Input enumeration -- unchanged)
       - Validate: same `new DesignStateReinstatementService().Validate(...)` call
       - Write values: same ScheduleWriteValues + WriteToSource when result.Applied
       - Blocked parameter surfacing: same Enum-based Warning bubble for MissingTarget/TypeMismatch/AmbiguousTarget/OutOfRange, using ErrorMessageTemplates.ReinstatementBlocked()
       - Store _latestResult = result
       - SetOutputs with result and ErrorMessageTemplates.FormatStatus(result)
       - Message = ErrorMessageTemplates.FormatMessage(result)

    10. SetOutputs (changed to match new output contract):
        - Index 0 (Parameters): `da.SetDataList(0, _latestParamState?.Parameters.ToList() ?? new List<DesignStateParameter>())` -- ALL params per D-05
        - Index 1 (StateStatus): `da.SetDataList(1, result?.Reports.Select(r => r.Status).ToList() ?? new List<ReinstatementStatus>())` -- index-matched per D-04
        - Index 2 (Status): `da.SetData(2, statusText)` -- summary text from ErrorMessageTemplates.FormatStatus

    11. ALL helper methods carry forward identically (internal implementation unchanged):
        - UnwrapSnapshot, UnwrapGhContainer, ReconstructSnapshot, ReconstructParameter, DiagnoseInputType -- full assembly-mismatch fallback
        - FindUpstreamDesignState, FindDesignStateFromInput -- but modified to search ONLY Input 1 (Target) per D-02
        - ResolveTargets, ResolveSourceInfo -- unchanged (enumerates PARAMETER STATE component inputs)
        - ScheduleWriteValues, WriteToSource -- unchanged (deferred write via ScheduleSolution, slider/toggle/param_* value write)
        - Note: Type alias `using CoreDesignStateParameter = DG.Core.Models.DesignStateParameter;` carries forward unchanged

    12. FormatStatus and FormatMessage are REMOVED from the component -- they now live in ErrorMessageTemplates (created in Plan 01). The component calls `ErrorMessageTemplates.FormatStatus(result)` and `ErrorMessageTemplates.FormatMessage(result)`.

    13. GRASSHOPPER_SDK guard and #else stub: same pattern as old component.

    After ParameterReinstateComponent.cs is created and builds successfully, DELETE the old ReinstateComponent.cs file. Use `git rm` or `rm` to remove it, then verify that NO remaining file references ReinstateComponent (GUID D4E2F8A1 or the class name ReinstateComponent).
  </action>

  <verify>
    <automated>dotnet build DG/DG.sln -c Release 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - ParameterReinstateComponent.cs compiles without errors
    - Old ReinstateComponent.cs is deleted (confirmed by file not found)
    - ComponentGuid is `8F9D0A1B-2C3D-4E4F-5A5B-6C7D8E9F0A1B`
    - All 3 inputs registered: ParamState (0), Target (1, REQUIRED, no Optional flag), Reinstate (2, Boolean default false)
    - All 3 outputs: Parameters (list), StateStatus (list), Status (text)
    - No FormatStatus/FormatMessage local methods in ParameterReinstateComponent.cs (calls ErrorMessageTemplates)
    - Wire-traversal searches Input 1 (Target) only -- no fallback to Input 0
    - _lastApplyInput initialized to true
    - `grep -c "D4E2F8A1" DG/src/**/*.cs` returns 0 (old GUID purged from all source)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create ParameterReinstateComponentTests</name>

  <read_first>
    - DG/tests/DG.Tests/ReinstatementServiceTests.cs -- existing validation service tests (12 tests) -- pattern reference for test construction
    - DG/tests/DG.Tests/ErrorMessageTemplateTests.cs -- FormatStatus/FormatMessage tests (created in Plan 01)
    - DG/src/DG.Core/Models/ParamState.cs -- Parameters collection, StateId
    - DG/src/DG.Core/Models/DesignStateParameter.cs -- typed parameter model
    - DG/src/DG.Core/Models/ReinstatementStatus.cs -- 7-value enum
    - DG/src/DG.Core/Models/ReinstatementResult.cs -- Applied, Aborted, Reports
    - DG/src/DG.Core/Models/ReinstatementParameterReport.cs -- ParameterId, DisplayName, Status, Detail
  </read_first>

  <files>
    DG/tests/DG.Tests/ParameterReinstateComponentTests.cs
  </files>

  <action>
    Create ParameterReinstateComponentTests.cs in DG/tests/DG.Tests/. Since DG.Tests references DG.Core only (not DG.Grasshopper), tests cover the component's data contract and logic at the Core model level. The 12 existing ReinstatementServiceTests already cover the validation logic -- these tests focus on the component's output contract and helpers.

    Test cases:

    1. `ParametersOutput_ContainsAllCapturedParams()` -- creates a ParamState with 3 DesignStateParameter items (one Number, one Integer, one Boolean). Verifies that `paramState.Parameters.Count == 3` and that all three items are present by ParameterId. Documents D-05: ALL parameters output, not just applied.

    2. `StateStatus_IndexMatchedToParameters()` -- creates a ReinstatementResult with 3 Reports (Applied, TypeMismatch, MissingTarget). Creates a matching 3-item ParamState.Parameters. Verifies `result.Reports.Count == paramState.Parameters.Count`. Verifies `paramState.Parameters[i].ParameterId == result.Reports[i].ParameterId` for each index. Documents D-04: index-matched contract.

    3. `ReinstatementStatus_EnumHasSevenValues()` -- uses `Enum.GetValues<ReinstatementStatus>()` to verify all 7 values exist: Applied, MissingTarget, TypeMismatch, AmbiguousTarget, OutOfRange, Unchanged, WouldApply. Ensures the enum contract is stable.

    4. `UnwrapSnapshot_NullInput_ReturnsNull()` -- tests the null-guard path of the unwrap logic. Creates a null input and verifies it maps to null (at the Core level, this is a null-coalescing test that validates the unwrap path contracts).

    5. `ReconstructSnapshot_AssemblyMismatchHandling()` -- tests the reflection-based reconstruction path that handles cross-assembly ParamState objects. Creates a ParamState with Parameters, serializes to the structure ReconstructSnapshot would consume (property names and types), and verifies the same ParameterId/DisplayName/Type/NumberValue/IntegerValue/BooleanValue round-trip through a mock reconstruction. Use the existing ReconstructSnapshot/ReconstructParameter pattern to verify Core ParamState properties are accessible by name.

    6. `ReinstateTargetRequired_ErrorContract()` -- verifies ErrorMessageTemplates.ReinstateTargetRequired() contains "PARAMETER REINSTATE", "Target input", and "required". Verifies ErrorMessageTemplates.ReinstateSourceNotFound() contains "PARAMETER REINSTATE" and "upstream".

    7. `FormatStatus_And_FormatMessage_Contract()` -- verifies that ErrorMessageTemplates.FormatStatus and FormatMessage exist and return non-empty strings for each branch (dependencies already covered by Plan 01's ErrorMessageTemplateTests -- this test confirms the methods are accessible from the test project).

    Follow existing test conventions: Assert.Contains for keyword verification, Assert.NotEmpty, Assert.Equal for counts, inline object construction.
  </action>

  <verify>
    <automated>dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~ParameterReinstate" --no-restore 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - ParameterReinstateComponentTests.cs compiles without errors
    - All 7 test cases pass green
    - Test names are descriptive and reference the requirement/D-IDs they verify
    - Test for StateStatus index-matching explicitly verifies list-length parity and per-index ParameterId matching
    - Test for 7-value enum verifies all members -- not a subset
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

PARAMETER REINSTATE operates entirely within the Grasshopper document process space. Its deferred value writes (ScheduleSolution) write to in-memory GH slider/toggle components in the same document -- no network, no DB, no external I/O.

| Boundary | Description |
|----------|-------------|
| (none) | All data is in-process. ParamState is an in-memory .NET object. Slider/toggle writes are local GH API calls. |

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages. All dependencies are in-project. |
</threat_model>

<verification>
1. `dotnet build DG/DG.sln -c Release` succeeds (verifies new component compiles, old file removed)
2. `dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~ParameterReinstate"` passes (7 tests)
3. `dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~ReinstatementService"` passes (12 existing tests still green -- no regression)
4. Verification gate: `grep -c "D4E2F8A1" DG/src/**/*.cs` returns 0 -- old REINSTATE GUID purged
5. `grep -c "ReinstateComponent" DG/src/**/*.cs` returns 0 -- old component class name purged
</verification>

<success_criteria>
- ParameterReinstateComponent.cs compiles with all 3 inputs + 3 outputs matching D-01 through D-06
- Old ReinstateComponent.cs deleted -- zero remaining references to GUID D4E2F8A1 or class ReinstateComponent
- Rising-edge trigger properly initialized (_lastApplyInput = true)
- All wire-traversal, deferred write, assembly-mismatch patterns carried forward
- FormatStatus/FormatMessage called from ErrorMessageTemplates (not local methods)
- 7 ParameterReinstateComponentTests pass green
- 12 existing ReinstatementServiceTests still pass (no regression)
</success_criteria>

## Artifacts This Phase Produces

### New file paths
- `DG/src/DG.Grasshopper/Components/ParameterReinstateComponent.cs`
- `DG/tests/DG.Tests/ParameterReinstateComponentTests.cs`

### Deleted file paths
- `DG/src/DG.Grasshopper/Components/ReinstateComponent.cs`

### New class
- `DG.Grasshopper.Components.ParameterReinstateComponent` (sealed, extends GH_Component, ComponentGuid = `8F9D0A1B-2C3D-4E4F-5A5B-6C7D8E9F0A1B`)

### New test class
- `DG.Tests.ParameterReinstateComponentTests` (sealed, 7 test methods)

### Component port contract
PARAMETER REINSTATE:
- Input 0: ParamState (generic item) -- the stored parameter values to apply
- Input 1: Target (generic item, REQUIRED) -- wired from PARAMETER STATE's State output for wire-traversal target discovery
- Input 2: Reinstate (Boolean item, default false) -- rising-edge trigger
- Output 0: Parameters (generic list) -- ALL DesignStateParameters from ParamState (D-05)
- Output 1: StateStatus (generic list) -- ReinstatementStatus list index-matched to Parameters (D-04)
- Output 2: Status (text item) -- summary text via ErrorMessageTemplates.FormatStatus (D-06)

### GUID allocation
- Old REINSTATE GUID `D4E2F8A1-C736-4B9D-AE51-2B1F7C9D0E63` -- removed
- New PARAMETER REINSTATE GUID `8F9D0A1B-2C3D-4E4F-5A5B-6C7D8E9F0A1B`

<output>
Create `.planning/phases/19-deconstruct-and-reinstate-components/19-03-SUMMARY.md` when done.
</output>
