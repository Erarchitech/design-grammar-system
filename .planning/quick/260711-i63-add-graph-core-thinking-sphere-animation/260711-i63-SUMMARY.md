---
phase: quick-260711-i63
plan: 01
subsystem: ui
tags: [canvas, graph-viewer, animation, react, higgsfield]

requires: []
provides:
  - Thinking-core video composite + procedural-glow fallback subsystem in graphEngine
  - Core-to-node stream + per-node emergence subsystem in graphEngine
  - GraphScreen turn-lifecycle wiring (Ingest/Query/Edit) for the thinking sphere and streams
affects: [graph-viewer]

tech-stack:
  added: []
  patterns:
    - "Offscreen HTMLVideoElement composited onto canvas 2D via globalCompositeOperation='lighter' over a local dark radial backdrop, with a procedural fallback gated on video-ready/failed flags"
    - "Intensity-hold pattern: engine state (_think) tracks the max of an explicit start/stop target and a derived floor from live async work (_streams.length), so a dependent visual doesn't fully fade while related work is still in flight"
    - "neoId-diffed stream/emergence system: snapshotNodeIds() before mutation, emitNewNodeStreams(prevIds) after reload, keyed by stable Neo4j node id rather than array index"

key-files:
  created: []
  modified:
    - ui-v2/src/graph/graphEngine.js
    - ui-v2/src/screens/GraphScreen.jsx

key-decisions:
  - "Guarded updateThinking()'s call into updateStreams()/drawThinkingCore()'s call into drawStreams() with existence checks so Task 1's commit doesn't crash tick() before Task 2 adds those methods (defensive sequencing within a single-file, multi-task plan)"
  - "Stream particle perpendicular wobble amplitude scaled by a local coreScale = base*0.05 (not specified numerically in the plan) to keep filament width proportional to the datascape's current zoom/fit scale"
  - "Stream head/tail fade curves (growIn on arrival, tailFade on retract) implemented as straightforward linear ramps since the plan described the desired behavior but not an exact formula"

requirements-completed: [I63-CORE-SPHERE, I63-NODE-STREAMS]

coverage:
  - id: D1
    description: "Thinking sphere appears at the datascape core on prompt submission and fades in/out via updateThinking(dt), compositing dg-think-sphere.mp4 (screen/lighter over a local dark backdrop) with a procedural-glow fallback when the video is unavailable"
    requirement: "I63-CORE-SPHERE"
    verification:
      - kind: manual_procedural
        ref: "npm --prefix ui-v2 run dev → Graph screen → submit any Ingest/Query/Edit prompt and observe the core sphere fade in/out"
        status: unknown
    human_judgment: true
    rationale: "Visual/animation correctness (glow appearance, fade timing, video vs. fallback rendering) requires a human to actually watch the canvas; no automated visual regression harness exists in this repo."
  - id: D2
    description: "On Ingest/Edit, one wispy stream travels from the core's rim to each newly created node's live orbit position, and the node emerges (fade + scale) as its stream arrives; pre-existing nodes render unchanged"
    requirement: "I63-NODE-STREAMS"
    verification:
      - kind: manual_procedural
        ref: "npm --prefix ui-v2 run dev → Graph screen → Ingest a rule → observe filaments radiate from the core to new Rule/Atom/Var/Literal nodes"
        status: unknown
    human_judgment: true
    rationale: "Requires visually confirming filament trajectories track rotating/panning nodes correctly and that emergence timing reads as intended; not automatable without a browser-driven visual test."

duration: ~35min
completed: 2026-07-11
status: complete
---

# Quick Task 260711-i63: Graph-Core Thinking-Sphere Animation Summary

**Added a wisp-video thinking-core overlay and core-to-node particle streams to the Graph screen's canvas engine, wired into the Ingest/Query/Edit prompt-turn lifecycle.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-07-11T10:27:34Z
- **Tasks:** 3/3 completed
- **Files modified:** 2 (`ui-v2/src/graph/graphEngine.js`, `ui-v2/src/screens/GraphScreen.jsx`) + 1 pre-existing asset added to git (`ui-v2/public/dg-think-sphere.mp4`)

## Accomplishments

- `GraphEngine` gained a thinking-core subsystem: `startThinking()`/`stopThinking()` public API, an intensity ramp (`updateThinking(dt)`) driven from `tick()`, and `drawThinkingCore()` which composites the pre-baked `dg-think-sphere.mp4` loop (white wisps, screen/`lighter` blend) over a local dark radial backdrop — with a pulsing procedural-glow fallback when the video is missing/failed.
- `GraphEngine` gained a core→node stream + node-emergence subsystem: `snapshotNodeIds()`/`emitNewNodeStreams(prevIds)` diff rings by stable `neoId` to find newly created nodes, `updateStreams(dt)` advances each filament's head/tail and drives per-node emergence, and `drawStreams()` renders monochrome ink particles pouring from the sphere's rim to each target's live (rotation/pan-tracked) orbit position.
- Continuity requirement satisfied: while streams are alive, `updateThinking()` holds effective intensity at `Math.max(target, 0.6)` even after `stopThinking()`, and streams originate at `base*0.24` from the core (the sphere's rim, not its exact center) — so streams visibly pour out of a still-glowing sphere and the sphere only fully fades after the last stream lands.
- `GraphScreen.runTurn()` wired the whole thing into the existing turn lifecycle: snapshot node ids before mutation, `startThinking()` right after `setBusy(true)` (covers all three modes — Ingest/Query/Edit), `emitNewNodeStreams(prevIds)` immediately after `loadGraph()` on mutating turns only, and `stopThinking()` in the `finally` block (covers both success and error paths).
- Pre-existing nodes render byte-identical to before: an absent `_emerge[neoId]` key resolves to `emK = 1`, leaving radius/alpha untouched.

## Task Commits

Each task was committed atomically:

1. **Task 1: Thinking-core video composite with procedural fallback + start/stop API** - `84febef` (feat)
2. **Task 2: Core-to-node streams + per-node emergence system** - `d625612` (feat)
3. **Task 3: Wire thinking-core + streams into the GraphScreen prompt-turn lifecycle** - `d1e3483` (feat)

_Note: Task 1's commit also includes the pre-existing `ui-v2/public/dg-think-sphere.mp4` asset (per the executor's asset note — it was untracked and is a feature asset, not regenerated or re-encoded by this plan)._

## Files Created/Modified

- `ui-v2/src/graph/graphEngine.js` - Adds thinking-core state/API (`startThinking`/`stopThinking`/`updateThinking`/`drawThinkingCore`/`_initThinkVideo`) and the stream/emergence subsystem (`snapshotNodeIds`/`emitNewNodeStreams`/`updateStreams`/`drawStreams`), plus per-node emergence scaling in both the active-layer and dimmed inactive-layer node draw loops, plus `setData()` reset of streams/emergence.
- `ui-v2/src/screens/GraphScreen.jsx` - Adds the four call sites in `runTurn()`: snapshot before mutation, `startThinking()` on submit, `emitNewNodeStreams()` after `loadGraph()` on mutating turns, `stopThinking()` in `finally`.
- `ui-v2/public/dg-think-sphere.mp4` - Pre-existing Higgsfield-generated seamless-loop core sphere asset (960×960, 4.0s, 24fps, silent H.264), now tracked in git.

## Decisions Made

- Guarded `updateThinking()`'s call into `updateStreams()` and `drawThinkingCore()`'s call into `drawStreams()` with `if (this.methodName)` existence checks. The plan's Task 1 instructions reference Task 2's not-yet-written methods; the guard keeps `tick()` from throwing if the engine is ever exercised between the two commits, with zero behavior change once both methods exist (which they do, at the end of this plan).
- `coreScale = base * 0.05` chosen locally in `drawStreams()` to scale the perpendicular particle wobble — the plan's formula referenced a `coreScale` term without defining its value; this keeps filament width visually proportional to the datascape's current zoom/fit.
- Stream particle fade curves (`growIn` on head-arrival, `tailFade` on retract past `p=1`) implemented as simple linear ramps over the windows the plan described qualitatively ("fade in near the moving head and out as the tail retracts") rather than an exact specified formula.

## Deviations from Plan

None — plan executed as written, plus the two locked mid-flight requirements (seamless-loop-only playback via `video.loop = true`, and the continuous sphere→streams rim-origin/intensity-hold transition) were implemented exactly as specified in the plan's task instructions. The three implementation-detail decisions above (guard clauses, `coreScale` value, fade curve shape) fill gaps the plan intentionally left to the executor's discretion rather than contradicting any plan instruction — not tracked as Rule 1-4 deviations.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. The `dg-think-sphere.mp4` asset was already present in `ui-v2/public/` and is now committed to git.

## Next Phase Readiness

- The Graph screen's canvas engine now exposes a stable public API (`startThinking`, `stopThinking`, `snapshotNodeIds`, `emitNewNodeStreams`) that other screens/flows could reuse for similar "processing" affordances if needed later.
- Manual visual verification (human-check items in the plan) has not been performed in this session — no browser/dev-server was driven. `npm run build` passed cleanly after every task; a human should run `npm --prefix ui-v2 run dev` and exercise Ingest/Query/Edit at least once to confirm the visual read (sphere glow, wisp compositing, stream trajectories, emergence timing) before this quick task is considered fully verified end-to-end.

---
*Quick task: 260711-i63*
*Completed: 2026-07-11*

## Self-Check: PASSED

- FOUND: ui-v2/src/graph/graphEngine.js
- FOUND: ui-v2/src/screens/GraphScreen.jsx
- FOUND: ui-v2/public/dg-think-sphere.mp4
- FOUND: .planning/quick/260711-i63-add-graph-core-thinking-sphere-animation/260711-i63-SUMMARY.md
- FOUND commit: 84febef
- FOUND commit: d625612
- FOUND commit: d1e3483
