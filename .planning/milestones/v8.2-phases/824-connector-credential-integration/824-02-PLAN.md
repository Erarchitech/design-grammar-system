---
phase: 824-connector-credential-integration
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - DG/src/DG.Grasshopper/Components/ConnectorComponent.cs
  - DG/README.md
autonomous: true
requirements: [CONNG-01, CONNG-02]
must_haves:
  truths:
    - "ConnectorComponent registers a new optional 'DataServiceUrl' text input defaulting to http://localhost:8000 and a new optional platform-token text input, BOTH appended after the existing Connect input (new indices 6 and 7)"
    - "The four Neo4j inputs (Neo4jURI/Neo4jUser/Neo4jPassword/Database), the PROJECT NAME input, the Connect input, the two outputs, and the component GUID 24E78A17-4533-44E7-8931-1224A70A1B36 are all unchanged, so saved .gh canvases keep their wiring (source assertions)"
    - "On Connect=true with a non-empty token, SolveInstance calls the DG.Core ConnectorHeartbeatClient and surfaces the outcome via AddRuntimeMessage — GH_RuntimeMessageLevel.Error with ConnectorTokenRejected() on Rejected, GH_RuntimeMessageLevel.Warning with ConnectorHeartbeatUnreachable(url) on Unreachable — and reflects the auth state in the Message label (e.g. 'Auth OK'/'Auth failed')"
    - "On an empty token the component makes NO heartbeat call and behaves exactly as before (bolt-only), preserving backward compatibility"
    - "dotnet build DG/DG.sln -c Release succeeds"
  prohibitions:
    - statement: "An invalid/revoked/expired token never gates the bolt connection or suppresses output — da.SetData(0, connection) and da.SetData(1, project) run regardless of the heartbeat outcome"
      status: resolved
      verification: "Source review of SolveInstance: the two da.SetData calls are not nested inside an auth-outcome conditional; IsConnected is derived from the bolt result only"
    - statement: "The raw token is never written into ConnectionInfo (the Database output), Message, StatusMessage, or any AddRuntimeMessage/log string"
      status: resolved
      verification: "grep -i 'token|credential' DG/src/DG.Core/Models/ConnectionInfo.cs is empty; in ConnectorComponent.cs the token variable is passed only to the heartbeat client and never interpolated into Message/StatusMessage/AddRuntimeMessage"
    - statement: "BuildRequestKey does not embed the raw token verbatim — it uses a SHA-256 hash of the token"
      status: resolved
      verification: "Source assertion: BuildRequestKey references a hash of the token (Convert.ToHexString(SHA256...) or a HashToken helper), not the raw token string"
    - statement: "The token input's internalised (persistent) data is cleared during solve so the .gh file never serializes the raw token"
      status: resolved
      verification: "Source assertion: SolveInstance clears the token param's PersistentData (GH_PersistentParam<GH_String>.PersistentData.Clear()) after reading it"
  artifacts:
    - "ConnectorComponent inputs: DataServiceUrl (idx 6), platform token (idx 7); heartbeat integration + secrecy handling"
    - "DG/README.md CONNECTOR contract updated with the two additive inputs"
  key_links:
    - "ConnectorComponent consumes DG.Core.Data.ConnectorHeartbeatClient + DG.Core.Services.ErrorMessageTemplates.ConnectorTokenRejected/ConnectorHeartbeatUnreachable from Plan 824-01 (depends_on 01)"
---

<objective>
Wire the platform-credential feature into the `CONNECTOR` Grasshopper component additively: two new optional inputs (`DataServiceUrl`, platform `Token`) appended after `Connect`, a heartbeat call on connect when a token is present, in-canvas Error/Warning feedback via `AddRuntimeMessage` + the Plan-01 templates, and token-secrecy handling (never persisted to the `.gh`, output, status, or logs; hashed in the request key). The existing four Neo4j inputs, PROJECT NAME, Connect, both outputs, and the component GUID stay byte-for-byte unchanged so every saved canvas keeps working.

Purpose: delivers CONNG-01 (additive token input, GUID + existing ports preserved) and the in-canvas half of CONNG-02 (Error/Warning runtime messages), building on Plan 824-01's DG.Core client and templates. The token authenticates the heartbeat ONLY — it never gates or replaces the raw-Neo4j bolt connection (locked PROJECT.md decision).

Output: edited ConnectorComponent.cs + a DG/README.md port-doc update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/824-connector-credential-integration/824-CONTEXT.md
@DG/src/DG.Grasshopper/Components/ConnectorComponent.cs
@DG/src/DG.Grasshopper/Components/ValidatorComponent.cs
@DG/src/DG.Core/Models/ConnectionInfo.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append DataServiceUrl + platform Token inputs (GUID + existing ports unchanged)</name>
  <files>DG/src/DG.Grasshopper/Components/ConnectorComponent.cs</files>
  <read_first>
    - DG/src/DG.Grasshopper/Components/ConnectorComponent.cs (RegisterInputParams currently registers indices 0-5: Neo4jURI, Neo4jUser, Neo4jPassword, Database, PROJECT NAME, Connect; GUID = new("24E78A17-4533-44E7-8931-1224A70A1B36"))
    - DG/src/DG.Grasshopper/Components/ValidatorComponent.cs (its DataServiceUrl input: AddTextParameter("DataServiceUrl","DataServiceUrl","DG data-service base URL", GH_ParamAccess.item, "http://localhost:8000"))
  </read_first>
  <action>
    In RegisterInputParams, AFTER the existing `Connect` parameter (keep indices 0-5 exactly as-is), append two parameters so they become index 6 and 7: index 6 `AddTextParameter("DataServiceUrl", "DataServiceUrl", "DG data-service base URL for platform-token auth", GH_ParamAccess.item, "http://localhost:8000")`; index 7 `AddTextParameter("Platform Token", "Token", "Optional platform credential (dgc_… token minted from the Connectors screen, Grasshopper connector). Wire from a Panel — never internalised.", GH_ParamAccess.item, "")`. Immediately after registering each, mark them optional: set `pManager[6].Optional = true;` and `pManager[7].Optional = true;`. Do NOT change the existing 6 input registrations, the 2 output registrations, or ComponentGuid. Do not reorder anything.
  </action>
  <verify>
    <automated>grep -q '24E78A17-4533-44E7-8931-1224A70A1B36' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q '"DataServiceUrl"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q '"Platform Token"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q 'Optional = true' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q '"Neo4jURI"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q '"PROJECT NAME"' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; echo INPUTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - RegisterInputParams registers exactly 8 inputs: original 6 (unchanged text/order) + DataServiceUrl (idx 6) + Platform Token (idx 7), both marked Optional
    - ComponentGuid literal still equals 24E78A17-4533-44E7-8931-1224A70A1B36
    - The two output params and their order are unchanged
  </acceptance_criteria>
  <done>CONNECTOR exposes the two additive optional inputs with the existing ports and GUID untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Call the heartbeat on connect and surface Error/Warning feedback</name>
  <files>DG/src/DG.Grasshopper/Components/ConnectorComponent.cs</files>
  <read_first>
    - DG/src/DG.Grasshopper/Components/ConnectorComponent.cs (SolveInstance reads da.GetData(0..5); async connect via _connectorService.TryConnectAsync + StartConnection/ScheduleSolution/ExpireSolution; Message label pattern)
    - DG/src/DG.Core/Data/ConnectorHeartbeatClient.cs (Plan 01 — CheckAsync(dataServiceUrl, token, ct) → HeartbeatResult{Outcome,Status})
    - DG/src/DG.Core/Services/ErrorMessageTemplates.cs (Plan 01 — ConnectorTokenRejected(), ConnectorHeartbeatUnreachable(url))
  </read_first>
  <action>
    Add a field `private readonly IConnectorHeartbeatClient _heartbeatClient = new ConnectorHeartbeatClient();` and a field holding the latest auth outcome, e.g. `private HeartbeatResult _latestAuth = new(HeartbeatOutcome.NotAttempted, null);`. In SolveInstance read the two new inputs into locals: `var dataServiceUrl = "http://localhost:8000";` `var token = string.Empty;` then `da.GetData(6, ref dataServiceUrl); da.GetData(7, ref token);`. Fold the heartbeat into the existing async connect flow: when `connect` is true, extend the awaited work so that after the bolt `TryConnectAsync` completes it ALSO awaits `_heartbeatClient.CheckAsync(dataServiceUrl, token, ct)` when the token is non-empty (empty token ⇒ skip the call and leave Outcome NotAttempted). Keep the bolt ConnectionInfo and the HeartbeatResult as two separate results (do NOT put the token or the HeartbeatResult inside ConnectionInfo). When the task completes in SolveInstance, set `_latestAuth` and map it: `Rejected` ⇒ `AddRuntimeMessage(GH_RuntimeMessageLevel.Error, ErrorMessageTemplates.ConnectorTokenRejected());` `Unreachable` ⇒ `AddRuntimeMessage(GH_RuntimeMessageLevel.Warning, ErrorMessageTemplates.ConnectorHeartbeatUnreachable(dataServiceUrl));` `Authenticated` and `NotAttempted` ⇒ no runtime message. Append an auth suffix to the existing `Message` when a token was supplied: Authenticated ⇒ " · Auth OK"; Rejected ⇒ " · Auth failed"; Unreachable ⇒ " · Auth unreachable"; NotAttempted ⇒ no suffix. CRITICAL: leave the two `da.SetData(0, _latestConnection)` / `da.SetData(1, project)` calls exactly where they are — outputs are emitted regardless of the auth outcome (the token never gates bolt).
  </action>
  <verify>
    <automated>grep -q 'ConnectorHeartbeatClient' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q 'CheckAsync' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q 'ConnectorTokenRejected' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q 'ConnectorHeartbeatUnreachable' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q 'GH_RuntimeMessageLevel.Error' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q 'GH_RuntimeMessageLevel.Warning' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; echo HEARTBEAT_WIRED_OK</automated>
  </verify>
  <acceptance_criteria>
    - SolveInstance reads inputs 6 (DataServiceUrl) and 7 (Token) and calls _heartbeatClient.CheckAsync only when the token is non-empty
    - Rejected → AddRuntimeMessage(Error, ConnectorTokenRejected()); Unreachable → AddRuntimeMessage(Warning, ConnectorHeartbeatUnreachable(url)); Authenticated/NotAttempted → no message
    - Message carries an auth suffix only when a token was supplied; da.SetData for both outputs runs unconditionally (auth never gates output)
  </acceptance_criteria>
  <done>CONNECTOR authenticates the heartbeat on connect and gives clear in-canvas Error/Warning feedback without ever blocking the bolt output.</done>
</task>

<task type="auto">
  <name>Task 3: Token secrecy — hash in request key, scrub persistent data, keep token off all outputs</name>
  <files>DG/src/DG.Grasshopper/Components/ConnectorComponent.cs</files>
  <read_first>
    - DG/src/DG.Grasshopper/Components/ConnectorComponent.cs (BuildRequestKey currently concatenates Uri|User|Password|Database|Project; WithStatus builds ConnectionInfo; Message/StatusMessage strings)
    - .planning/phases/824-connector-credential-integration/824-CONTEXT.md (Token Secrecy decisions: hash in request key; scrub PersistentData; never in output/Message/logs)
    - DG/src/DG.Core/Models/ConnectionInfo.cs (must remain token-free)
  </read_first>
  <action>
    (a) Request key: extend BuildRequestKey to include the token as a SHA-256 hash, not the raw value — add a private static `HashToken(string token)` helper returning `Convert.ToHexString(System.Security.Cryptography.SHA256.HashData(System.Text.Encoding.UTF8.GetBytes(token ?? string.Empty)))` and append its result to the key so a token change re-triggers a connect without the raw secret entering the key. (b) Persistent-data scrub: in SolveInstance, after `da.GetData(7, ref token)`, if the token input carries internalised data, clear it — `if (Params.Input[7] is Grasshopper.Kernel.GH_PersistentParam<Grasshopper.Kernel.Types.GH_String> tokenParam && tokenParam.PersistentData.DataCount > 0) { tokenParam.PersistentData.Clear(); AddRuntimeMessage(GH_RuntimeMessageLevel.Remark, "CONNECTOR: platform token was internalised and has been cleared for security — wire it from a Panel instead."); }` so the .gh never serializes the raw token. (c) Confirm the token never reaches ConnectionInfo, Message, StatusMessage, or any AddRuntimeMessage string — it is passed only to `_heartbeatClient.CheckAsync` and to `HashToken`. Do NOT add a token field to ConnectionInfo.
  </action>
  <verify>
    <automated>grep -Eq 'SHA256|HashToken' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; grep -q 'PersistentData' DG/src/DG.Grasshopper/Components/ConnectorComponent.cs &amp;&amp; ! grep -Eiq 'token|credential' DG/src/DG.Core/Models/ConnectionInfo.cs &amp;&amp; echo SECRECY_OK</automated>
  </verify>
  <acceptance_criteria>
    - BuildRequestKey incorporates a SHA-256 hash of the token (via HashToken), never the raw token string
    - SolveInstance clears the token input's PersistentData when internalised data is present
    - ConnectionInfo.cs contains no token/credential field; the token variable is never interpolated into Message/StatusMessage/AddRuntimeMessage text
  </acceptance_criteria>
  <done>The raw token is confined to the heartbeat call and its hash — never persisted to the .gh, an output, the status label, or a log.</done>
</task>

<task type="auto">
  <name>Task 4: Update DG/README.md CONNECTOR contract and build the solution</name>
  <files>DG/README.md</files>
  <read_first>
    - DG/README.md (the CONNECTOR component contract / port listing — around the Grasshopper Component Contracts section that lists CONNECTOR inputs)
  </read_first>
  <action>
    In DG/README.md, update the CONNECTOR component contract to document the two new additive optional inputs — `DataServiceUrl` (DG data-service base URL, default http://localhost:8000) and `Platform Token` (nickname Token; optional dgc_ credential minted from the Connectors screen's Grasshopper connector; wire from a Panel, never internalised; used only to authenticate a data-service heartbeat and never persisted). Note explicitly that the existing Neo4j inputs and the component GUID are unchanged and that an invalid token surfaces an in-canvas error but does not block the bolt connection. Then build the whole solution to confirm DG.Core (Plan 01) and DG.Grasshopper compile together.
  </action>
  <verify>
    <automated>grep -q 'DataServiceUrl' DG/README.md &amp;&amp; grep -Eq 'Platform Token|Token' DG/README.md &amp;&amp; cd DG &amp;&amp; dotnet build DG.sln -c Release --nologo 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - DG/README.md documents the DataServiceUrl and Platform Token inputs and states the GUID/existing ports are unchanged
    - dotnet build DG/DG.sln -c Release succeeds (both DG.Core and DG.Grasshopper compile)
  </acceptance_criteria>
  <done>The component contract is documented and the full solution builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .gh canvas file → shared/versioned team storage | The token must never be serialized into the canvas; internalised values are scrubbed |
| Grasshopper plugin → data-service | Token relayed as a Bearer credential for the heartbeat only (bolt auth unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-824-04 | Information Disclosure | token baked into a shared .gh file | high | mitigate | Token read from a wired Panel (volatile, unsaved); internalised persistent data is cleared each solve (Task 3); token never enters ConnectionInfo/Message/logs |
| T-824-05 | Elevation of Privilege | invalid token silently grants access | medium | mitigate | Heartbeat is advisory feedback only; a Rejected token raises a visible Error and never gates the bolt path, so failure is never silent (Task 2) |
| T-824-06 | Tampering | additive inputs break saved canvases (GUID/index drift) | medium | mitigate | New inputs are appended at indices 6-7; existing indices 0-5, outputs, and the GUID are unchanged (Task 1 verify) |
</threat_model>

<verification>
- All automated sentinels return (INPUTS_OK, HEARTBEAT_WIRED_OK, SECRECY_OK) and dotnet build DG/DG.sln -c Release succeeds
- Manual review: the two da.SetData output calls run regardless of the heartbeat outcome; the token variable flows only into _heartbeatClient.CheckAsync and HashToken; ConnectionInfo remains token-free
- Cross-check with Plan 01: ConnectorHeartbeatClient, ConnectorTokenRejected(), ConnectorHeartbeatUnreachable(url) all resolve at compile time (depends_on 01)
</verification>

<success_criteria>
CONNECTOR gains a platform-token heartbeat with clear in-canvas Error/Warning feedback, additively (GUID + existing ports preserved, saved canvases keep working), the token never gates the bolt connection, and the raw token never lands in the .gh file, an output, the status label, or a log.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 824-02)

- **Edited:** DG/src/DG.Grasshopper/Components/ConnectorComponent.cs — new inputs `DataServiceUrl` (idx 6) + `Platform Token` (idx 7); fields `_heartbeatClient`, `_latestAuth`; private `HashToken(string)` helper; heartbeat integration in SolveInstance; persistent-data scrub
  - **New symbols:** `HashToken(string)` (private), the two new GH input params
- **Edited:** DG/README.md — CONNECTOR contract documents the two additive inputs
</artifacts>

<output>
Create `.planning/phases/824-connector-credential-integration/824-02-SUMMARY.md` when done
</output>
