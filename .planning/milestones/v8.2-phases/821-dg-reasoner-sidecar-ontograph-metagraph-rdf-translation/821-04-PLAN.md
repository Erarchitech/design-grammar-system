---
phase: 821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation
plan: 04
type: execute
wave: 4
depends_on: ["821-03"]
files_modified:
  - data-service/app.py
  - dg-reasoner/tests/test_roundtrip_integration.py
autonomous: true
requirements: [REAS-05]

must_haves:
  truths:
    - "D-06: a thin proxy route in data-service (`POST /reasoner/consistency`) forwards `{project}` to `http://dg-reasoner:8000/reason/consistency` (DG_REASONER_URL) using httpx with a SHORT connect/read timeout, so a sidecar hang never blocks the Speckle-publish/validation hot path"
    - "D-12: the sidecar stays internal-only — data-service's proxy is the only thing that reaches it (no nginx route, no host port); reachability (success criterion 3) is satisfied via the proxy"
    - "Success criterion 4: a REAL ontology export round-trips through Cypher export → RDFLib graph → Owlready2 → HermiT WITHOUT error against live docker Neo4j (v8-ui-smoke, 16 rules), proving the pipeline end-to-end — not a toy fixture"
    - "D-16: the live round-trip asserts drift-immunity — the two known-mistagged v8-ui-smoke atoms (R_DOOR_ORIENTATION_V_A1/_A2, carrying graph:'OntoGraph') MUST appear in the export, turning 820's data-quality finding into a regression guard for the label-scoped-export mandate"
    - "D-13/D-15: the integration round-trip lives in `dg-reasoner/tests/` using pytest, is marked `@pytest.mark.integration`, and is skipped when Neo4j is unreachable"
    - "Isolation: the proxy's short timeout means a paused/hung sidecar returns a fast proxy error, and a concurrent data-service validation/Speckle call is unaffected (success criterion 1 isolation, verifiable)"
  artifacts:
    - "data-service/app.py (POST /reasoner/consistency proxy route)"
    - "dg-reasoner/tests/test_roundtrip_integration.py"
  key_links:
    - "data-service POST /reasoner/consistency --httpx--> dg-reasoner:8000/reason/consistency (DG_REASONER_URL from Plan 01 compose env)"
    - "integration test → live docker neo4j (v8-ui-smoke) → sidecar /reason/consistency → Owlready2/HermiT round-trip verdict + drift-immunity assertion"
---

<objective>
Add the thin data-service proxy route (`POST /reasoner/consistency` → the sidecar, D-06) with a
short httpx timeout so the sidecar is reachable from data-service without ever blocking its hot
path, and prove the whole pipeline with a live integration round-trip: real `v8-ui-smoke` data →
Cypher export → RDFLib → hybrid union → Owlready2 → HermiT, completing without error, with the
drift-immunity assertion (D-16) pinning the two known-mistagged atoms.

Purpose: closes success criterion 3 (routes reachable from data-service) and success criterion 4
(real end-to-end round-trip, not a toy fixture), plus demonstrates the criterion-1 isolation
property. This is the final plan — after it, the phase goal is met.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-CONTEXT.md
@.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-RESEARCH.md
@.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-03-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New/modified:
- `data-service/app.py` — new `POST /reasoner/consistency` proxy route beside the existing
  `/reasoner/settings` (L1135-1158). Reads `DG_REASONER_URL` (default `http://dg-reasoner:8000`),
  forwards `{project}` via httpx with a short timeout, maps sidecar timeout/unreachable to a clean
  502/504 without raising into the hot path. (No new dependency — httpx already in data-service.)
- `dg-reasoner/tests/test_roundtrip_integration.py` — `@pytest.mark.integration` live round-trip
  against docker Neo4j (v8-ui-smoke) through the sidecar's `/reason/consistency`, with the D-16
  drift-immunity assertion.

New symbols (exclude from drift verification — created here):
- data-service route `POST /reasoner/consistency`; env read `DG_REASONER_URL`; a small `ReasonerConsistencyRequest {project}` pydantic model
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Thin data-service → sidecar proxy route with short timeout (D-06)</name>
  <files>
    - data-service/app.py
  </files>
  <read_first>
    - data-service/app.py (lines 1-70 for the httpx/env pattern and lines 1131-1158 for the existing /reasoner/settings routes this lands beside; graphify first per repo rule if exploring beyond these ranges)
    - .planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-RESEARCH.md (§2.7 proxy — short connect/read timeout, DG_REASONER_URL, internal-only)
    - .planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-CONTEXT.md (D-06 reachability criterion, D-12 internal-only)
  </read_first>
  <action>
    In `data-service/app.py`, add a `POST /reasoner/consistency` route beside the existing
    `/reasoner/settings` handlers. Read `DG_REASONER_URL = os.getenv("DG_REASONER_URL",
    "http://dg-reasoner:8000")` (the compose env set in Plan 01). Accept a pydantic model
    `ReasonerConsistencyRequest {project: str}` (optionally `engine: str = "hermit"` pass-through).
    Forward the payload to `{DG_REASONER_URL}/reason/consistency` using httpx with an EXPLICIT short
    timeout (e.g. `httpx.Timeout(connect=2.0, read=<a few seconds beyond the sidecar's own timeout or
    a deliberately short hot-path bound>, write=2.0, pool=2.0)` — the key property: a sidecar hang
    surfaces as a fast, caught error, never a hang in data-service). On `httpx.TimeoutException` or
    `httpx.ConnectError`, return a clean JSON error with status 504/502 (do NOT let it raise into the
    request path). On success, pass the sidecar's JSON body + status through. Use a sync or async
    httpx client consistent with the surrounding data-service style. Do NOT add an nginx route (the
    sidecar stays internal-only, D-12).
  </action>
  <verify>
    <automated>grep -q "/reasoner/consistency" data-service/app.py && grep -q "DG_REASONER_URL" data-service/app.py && grep -Eq "httpx|Timeout" data-service/app.py && python -c "import ast; ast.parse(open('data-service/app.py').read()); print('ast-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `data-service/app.py` defines `POST /reasoner/consistency` reading `DG_REASONER_URL` and forwarding via httpx with an explicit short timeout.
    - A sidecar timeout/connect error is caught and returned as a clean 5xx JSON (grep for the timeout/except handling) — it never propagates as an unhandled exception into the hot path.
    - No new dependency added to data-service/requirements.txt (httpx already present).
    - `data-service/app.py` still parses (ast.parse ok).
  </acceptance_criteria>
  <done>data-service reaches the sidecar through a thin, short-timeout proxy that can never block the Speckle-publish/validation hot path (D-06/D-12).</done>
</task>

<task type="auto">
  <name>Task 2: Live integration round-trip + drift-immunity regression guard (D-16)</name>
  <files>
    - dg-reasoner/tests/test_roundtrip_integration.py
  </files>
  <read_first>
    - dg-reasoner/reasoning.py (run_consistency — the pipeline under test)
    - dg-reasoner/ontology_export.py (build_graph — the export whose atom capture the drift assertion checks)
    - dg-reasoner/tests/conftest.py (the integration marker + neo4j-availability skip helper from Plan 01)
    - .planning/phases/820-.../820-DECISION.md (the two mistagged atoms R_DOOR_ORIENTATION_V_A1/_A2 carry graph:'OntoGraph'; v8-ui-smoke has 16 Rule nodes — the richest project)
    - .planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-RESEARCH.md (§6 Validation Architecture — integration tier; §4 Pitfall 1/2b drift-immunity)
  </read_first>
  <action>
    Create `dg-reasoner/tests/test_roundtrip_integration.py`, marked `@pytest.mark.integration` and
    skipped when Neo4j is unreachable (conftest helper). Against live docker Neo4j targeting
    `v8-ui-smoke`:
    (1) Round-trip proof (success criterion 4): call `reasoning.run_consistency("v8-ui-smoke")` (or
    hit the sidecar's `POST /reason/consistency` over HTTP if the test runs inside the compose
    network) and assert it completes WITHOUT raising — the full Cypher export → RDFLib → hybrid union
    → Owlready2 → HermiT pipeline returns a well-formed dict with `consistent` set (true/false, not
    error) and a numeric `stripped_builtin_rules` (v8-ui-smoke is builtin-heavy, so > 0 is expected).
    (2) Drift-immunity (D-16): call `ontology_export.build_graph` for `v8-ui-smoke` and assert the two
    known-mistagged atoms `R_DOOR_ORIENTATION_V_A1` and `R_DOOR_ORIENTATION_V_A2` ARE present in the
    export (as minted atom IRIs — mapped or as SkippedAtom), proving label-scoping captured them where
    graph-property scoping would have dropped them. This is the regression guard.
    Keep the test resilient: skip (not fail) when neo4j/JRE unavailable; assert real behavior when up.
  </action>
  <verify>
    <automated>test -f dg-reasoner/tests/test_roundtrip_integration.py && grep -q "pytest.mark.integration" dg-reasoner/tests/test_roundtrip_integration.py && grep -q "R_DOOR_ORIENTATION_V" dg-reasoner/tests/test_roundtrip_integration.py && grep -Eq "run_consistency|/reason/consistency" dg-reasoner/tests/test_roundtrip_integration.py && python -c "import ast; ast.parse(open('dg-reasoner/tests/test_roundtrip_integration.py').read()); print('ast-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The test is `@pytest.mark.integration` and skips cleanly when Neo4j is unreachable.
    - When the stack is up, it exercises the full pipeline via `run_consistency`/the sidecar route and asserts completion WITHOUT error against real v8-ui-smoke data (not a fixture).
    - It asserts both `R_DOOR_ORIENTATION_V_A1` and `R_DOOR_ORIENTATION_V_A2` appear in the export (D-16 drift-immunity regression guard).
    - `stripped_builtin_rules` is asserted numeric/> 0 for the builtin-heavy v8-ui-smoke corpus.
  </acceptance_criteria>
  <done>The real end-to-end round-trip is proven against live data and the label-scoping mandate is locked in as a regression guard.</done>
</task>

</tasks>

<verification>
- `data-service/app.py` exposes `POST /reasoner/consistency` proxying to DG_REASONER_URL with a short httpx timeout; sidecar hang → fast caught 5xx, hot path unaffected.
- `pytest dg-reasoner/tests/test_roundtrip_integration.py -m integration` (stack up) proves the real round-trip completes without error and pins the two mistagged atoms (D-16).
- Full suite: `pytest dg-reasoner/tests` green with the stack up; `pytest dg-reasoner/tests -m "not integration"` green always.
- Sidecar remains internal-only (no nginx route, no host port); reachable only via the proxy.
</verification>

<success_criteria>
Closes success criterion 3 (routes reachable from data-service via the proxy) and success
criterion 4 (a real ontology round-trips export → RDFLib → Owlready2 → HermiT without error),
and demonstrates the criterion-1 isolation property (short-timeout proxy). Phase goal met.
</success_criteria>

<output>
Create `.planning/phases/821-dg-reasoner-sidecar-ontograph-metagraph-rdf-translation/821-04-SUMMARY.md` when done.
</output>
