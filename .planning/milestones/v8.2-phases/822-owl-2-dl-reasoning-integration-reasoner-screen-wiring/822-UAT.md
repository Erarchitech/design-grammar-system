---
status: passed
phase: 822-owl-2-dl-reasoning-integration-reasoner-screen-wiring
source: [822-VERIFICATION.md]
started: 2026-07-12T13:30:00.000Z
updated: 2026-07-13
completed: 2026-07-13
---

## Current Test

None — all three tests executed and passed on 2026-07-13 against the live docker stack
(v8.1/v8.2 gap-closure session). Method notes in `## Execution Notes` below.

## Tests

### 1. Real timeout → Unknown, never blocks (criterion 3 — highest priority)
expected: Force a genuine reasoner hang — either lower DG_REASONER_TIMEOUT_SECONDS to a value shorter than a real HermiT run against a non-trivial project, or otherwise cause the subprocess to genuinely exceed the bound — then click Run check and time the response. The request must return at (or shortly after) the bounded ceiling, never hang indefinitely; the sidecar's subprocess is actually killed (os.killpg); the UI shows the achromatic "Inconclusive" verdict, never green/red, never a stuck spinner.
result: **PASS** (2026-07-13). `DG_REASONER_TIMEOUT_SECONDS` temporarily set 90→1 in docker-compose.yml, `dg-reasoner` recreated (env confirmed `1` via `docker exec printenv`). Run check clicked against project `v8-ui-smoke` (real run needs ~1.5–2s, so 1s forces a genuine expiry). Verdict rendered **within 1.5s of the click** (timeout ceiling 1s + transport overhead): "– Inconclusive — the reasoner timed out before finishing. Try again." with the achromatic **Inconclusive** badge — never green/red, no stuck spinner, Run button re-enabled. Sidecar log shows the real HermiT java subprocess launched then the route returning **504 Gateway Timeout** (`POST /reason/consistency HTTP/1.1" 504`) — the `_reason_with_timeout` kill path fired for real, not a monkeypatched return. DOM re-probed at t+4s/t+8s/t+15s: verdict stable, no late flip. Compose value restored to 90, container recreated, follow-up live run returned `consistent:true` in 1999ms.

### 2. Inconsistent verdict renders correctly in the live UI
expected: Run the check against a project/fixture with a genuine unsatisfiable class (or temporarily add a disjoint-class violation to a test project), click Run check, observe the rendered result: "Schema inconsistent — N unsatisfiable class(es)." with a bulleted list of human labels only (no raw IRI), signal-red badge "Inconsistent".
result: **PASS** (2026-07-13). The Phase 820 spike's seeded contradiction (`ex:SlidingDoor rdfs:subClassOf ex:Door, ex:Window` against the curated `ex:Door owl:disjointWith ex:Window`) was temporarily appended to `ontology/dg-disjointness.ttl` (volume-mounted; `_hybrid_union` parses it per request, no restart needed). Run check → rendered: **"Schema inconsistent — 1 unsatisfiable class."** + **Inconsistent** badge + the human label **"SlidingDoor"** only — no raw `http://example.org/design-grammar/ex#` IRI anywhere in the card. Overlay then restored byte-identical (git-clean) and re-run → **"✓ Schema consistent"** — which also proves criterion 4 (re-run reflects updated ontology state, no cached/stale answer).

### 3. Full click-through of Run/Cancel and the Consistent verdict
expected: Click "Run check" on the HermiT card, observe spinner+elapsed timer+disabled Run button during the run, then the "✓ Schema consistent" verdict with badge and "Last checked" timestamp; separately, start a run and click Cancel mid-flight, confirm "× Run cancelled — no result." renders distinctly (not as Unknown) and the Run button re-enables.
result: **PASS** (2026-07-13). Timed DOM captures during a live run: at t+400ms the card showed **"Running… 0s"** elapsed timer, a **Cancel** button, and the Run button `disabled: true`; Cancel clicked mid-flight at t+400ms → by t+1.2s the card showed **"× Run cancelled — no result."** (distinct wording from the Inconclusive/timeout verdict) with the Run button re-enabled. A separate uncancelled run rendered **"✓ Schema consistent — no unsatisfiable classes found."** + **Consistent** badge + **"Last checked just now"**.

## Summary

total: 3
passed: 3
issues: 1 (fixed in-session)
pending: 0
skipped: 0
blocked: 0

## Gaps

None remaining. One cosmetic defect found and fixed during execution:

- **Stale v8.1 intro copy** — the Reasoner screen header still read "both reasoners are
  shown as placeholders until their integration is elaborated", contradicting the
  integrated HermiT card below it. Fixed in `ui-v2/src/screens/ReasonerScreen.jsx`
  (2026-07-13): copy now states HermiT runs a live schema-consistency check and only
  "integration pending" entries leave validation behavior unchanged.

## Execution Notes

- Executed via the in-app browser against the live stack (`localhost:8080`, all
  containers up). The automation environment renders zero animation frames, so the
  landing-ring canvas (RAF-driven) could not be used for navigation; the Reasoner layer
  was activated by dispatching the App's own `region`/`project` state setters, and all
  observations are DOM-level assertions of the rendered card (text, badge, button
  disabled state) plus network/log evidence. The screens themselves are plain React DOM
  and behaved identically to manual navigation.
- Project under test: `v8-ui-smoke` (the stack's seeded verification project).
- All fixtures reverted: `ontology/dg-disjointness.ttl` and `docker-compose.yml` both
  git-clean afterwards; `dg-reasoner` back on the 90s ceiling with a passing live run.
