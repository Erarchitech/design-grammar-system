---
phase: 820-reasoning-stack-architecture-decision-ontograph-axiom-scopin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/LPG-OWL-MAPPING.md
autonomous: true
requirements: [REAS-04]

must_haves:
  truths:
    - "spec/LPG-OWL-MAPPING.md normatively maps every Metagraph node/relationship to the W3C SWRL RDF vocabulary (swrl:Imp, swrl:body/head, swrl:ClassAtom, swrl:DatavaluedPropertyAtom, swrl:IndividualPropertyAtom, swrl:BuiltinAtom) — success criterion 2"
    - "The spec covers edge-property reification: ARG.pos → swrl:argument1/argument2 for binary atoms and swrl:arguments rdf:List for BuiltinAtom; HAS_BODY/HAS_HEAD.order → rdf:List (swrl:AtomList) position (REAS-04's explicit reification target)"
    - "The spec's IRI strategy reuses stored iri for Class/DatatypeProperty/ObjectProperty/Builtin and mints deterministic project-scoped IRIs for Rule/Atom/Var, keeping ex: (dynamic domain vocab) separate from the dg/dgm/dgv/dgs meta-schema namespaces"
    - "The spec normatively mandates label-scoped export (never {project, graph}) and owl:AllDifferent UNA handling for future ABox translation"
    - "The BuiltinAtom mapping row reflects the real per-builtin arity of v8-ui-smoke's 8 builtins (verified by live Cypher, not assumed)"
  artifacts:
    - "spec/LPG-OWL-MAPPING.md"
  key_links:
    - "spec/LPG-OWL-MAPPING.md cross-references spec/DATABASE.md § Graph Separation as the authoritative node/property list (link, do not duplicate)"
    - "The spec is the normative contract Phase 821's ontology_export.py implements against"
---

<objective>
Author the durable `spec/LPG-OWL-MAPPING.md` — the normative contract for translating DG's Neo4j labelled-property graph into RDF/OWL. It must cover, for OntoGraph and Metagraph (normative) plus a ValidGraph sketch (informative): the SWRL RDF vocabulary mapping, edge-property reification (ARG.pos, HAS_BODY/HAS_HEAD.order), the IRI-minting strategy, the label-scoping requirement, and OWL's no-unique-name-assumption handling.

Purpose: This is the reviewable contract Phase 821's real translator builds against before a line of it is written. Getting the reification and namespace-split right here is what prevents silent data loss (ARG.pos / order) downstream.

Output: `spec/LPG-OWL-MAPPING.md`, sitting next to `spec/DATABASE.md`, cross-referencing it as the authoritative node/property source.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-CONTEXT.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New file:
- `spec/LPG-OWL-MAPPING.md` — normative LPG→OWL mapping spec (OntoGraph + Metagraph normative; ValidGraph informative)

New normative conventions the spec introduces (referenced downstream by Phase 821, exclude from drift verification):
- SWRL RDF vocabulary namespace `http://www.w3.org/2003/11/swrl#` applied to Metagraph
- IRI-minting path scheme: `{base}/project/{project}/rule/{Rule_Id}`, `/atom/{Atom_Id}`, `/var/{name}` under base `http://example.org/design-grammar`
- The `ex:` (dynamic per-project domain vocabulary) vs `dg/dgm/dgv/dgs` (static meta-schema) namespace split, unioned only at reasoning time
- Label-scoped export requirement (`MATCH (n:Rule|Atom|Var|Literal|Builtin {project})` / `MATCH (n:Class|DatatypeProperty|ObjectProperty {project})`), superseding `graph`-property scoping
- `owl:AllDifferent` / `owl:distinctMembers` UNA convention for future ABox exports
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Metagraph mapping core — SWRL vocabulary table, edge-property reification, IRI minting (with live builtin-arity verification)</name>
  <files>
    - spec/LPG-OWL-MAPPING.md
  </files>
  <read_first>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md (§ Architecture Pattern 1 — the full DG→SWRL RDF term mapping table, verbatim source; § Pattern 2 — IRI minting; § Common Pitfall 1 — label scoping; § Common Pitfall 3 + § Open Questions 1 — per-builtin arity must be verified, not assumed; § Live OntoGraph Characterization — the 8 builtins and the HAS_BODY order sample)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-CONTEXT.md (§ LPG→OWL Mapping Spec decisions — standard W3C SWRL vocabulary, IRI strategy, no custom reification vocabulary)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md (§ spec/LPG-OWL-MAPPING.md — mirror DATABASE.md's "bold label — one-liner — fenced example" shape, using Turtle blocks; keep next to DATABASE.md and cross-reference, do not duplicate)
    - spec/DATABASE.md (§ Graph Separation table — the authoritative node/label/property list this spec must stay consistent with and link back to; read the Metagraph rows)
    - cypher_template.txt (worked SWRL example showing Rule/Atom/Var/Literal/Builtin and HAS_BODY/HAS_HEAD order + ARG pos)
    - training/dataset_schema.json (per-builtin definitions — cross-check the arity of the 8 live builtins greaterThan/lessThan/or/equalTo/continuousFromTo/fillet/notEqual/towards)
  </read_first>
  <action>
    Create spec/LPG-OWL-MAPPING.md with a top-level `## Overview` stating scope up front (normative for OntoGraph + Metagraph, informative for ValidGraph), mirroring how DATABASE.md's Overview states graph separation. Cross-reference spec/DATABASE.md § Graph Separation as the authoritative node/property list — link to it, do not re-list the labels; this spec only ADDS the RDF-mapping columns.

    Write the Metagraph mapping section as a table mapping every Metagraph node/relationship to the W3C SWRL RDF vocabulary (namespace http://www.w3.org/2003/11/swrl#), reproducing RESEARCH § Pattern 1's table: Rule→swrl:Imp (with Rule.kind/RuleName/RuleDescription as DG-namespaced annotation properties, no SWRL equivalent); (Rule)-[HAS_BODY{order}]->(Atom)→swrl:body pointing to a swrl:AtomList (rdf:List, order preserved as first/rest position); (Rule)-[HAS_HEAD{order}]->(Atom)→swrl:head→swrl:AtomList; Atom{type:'ClassAtom'}→swrl:ClassAtom (swrl:classPredicate ← REFERS_TO owl:Class); Atom{type:'DataPropertyAtom'}→swrl:DatavaluedPropertyAtom; Atom{type:'ObjectPropertyAtom'}→swrl:IndividualPropertyAtom (cover normatively even though v8-ui-smoke has 0 ObjectProperty nodes — schema-legal per cypher_template.txt); Atom{type:'BuiltinAtom'}→swrl:BuiltinAtom (swrl:builtin ← REFERS_TO; swrl:arguments → rdf:List); Var→swrl:Variable; Literal→plain typed RDF literal (no reification).

    Explicitly document edge-property reification as its own subsection (this is REAS-04's named requirement): ARG.pos → swrl:argument1 / swrl:argument2 for binary atom kinds (Class/DataProperty/ObjectProperty atoms), and swrl:arguments (rdf:List, pos = list position) for BuiltinAtom; HAS_BODY/HAS_HEAD.order → implicit rdf:List position in the swrl:AtomList. State that BuiltinAtom MUST use the rdf:List form universally (strictly more general, costs nothing for binary builtins) rather than assuming argument1/2.

    Before finalizing the BuiltinAtom row, run the live arity query against Neo4j (read connection creds from env, do not hardcode): `MATCH (a:Atom {type:'BuiltinAtom', project:'v8-ui-smoke'})-[r:ARG]->() RETURN a.iri, count(r) AS arity` and cross-check against training/dataset_schema.json (Open Question 1). Record the observed arities in the BuiltinAtom subsection so the row reflects verified data, resolving whether continuousFromTo/towards need >2 args.

    Document the IRI-minting strategy (RESEARCH § Pattern 2) as a subsection: reuse the stored `iri` for Class/DatatypeProperty/ObjectProperty/Builtin; mint deterministic IRIs for Rule/Atom/Var under base http://example.org/design-grammar with project-scoped segments /project/{project}/rule/{Rule_Id}, /atom/{Atom_Id}, /var/{name-without-leading-?}; Literal is not minted (becomes a literal). State explicitly that `ex:` denotes per-project DYNAMICALLY GENERATED domain vocabulary, a separate namespace from the meta-schema's dg/dgm/dgv/dgs, unioned only at reasoning time (quote DesignGrammar-V7.owl's own disclaimer that domain classes are not part of that ontology).

    Add a normative "Export scoping" subsection mandating label-based Cypher scoping (MATCH by n:Rule|Atom|Var|Literal|Builtin for Metagraph, n:Class|DatatypeProperty|ObjectProperty for OntoGraph) and forbidding {project, graph}-property scoping, citing the confirmed v8-ui-smoke mistagged-atom finding (Pitfall 1). Use Turtle fenced blocks for illustrative examples (e.g. the swrl:Imp example already drafted in RESEARCH § Pattern 1).
  </action>
  <verify>
    <automated>test -s spec/LPG-OWL-MAPPING.md && grep -q "swrl:Imp" spec/LPG-OWL-MAPPING.md && grep -q "swrl:AtomList" spec/LPG-OWL-MAPPING.md && grep -q "swrl:argument1" spec/LPG-OWL-MAPPING.md && grep -q "swrl:arguments" spec/LPG-OWL-MAPPING.md && grep -qi "HAS_BODY" spec/LPG-OWL-MAPPING.md && grep -qi "ARG" spec/LPG-OWL-MAPPING.md</automated>
  </verify>
  <acceptance_criteria>
    - `spec/LPG-OWL-MAPPING.md` exists with an `## Overview` declaring normative (OntoGraph+Metagraph) vs informative (ValidGraph) scope.
    - Metagraph section maps Rule→`swrl:Imp`, ClassAtom→`swrl:ClassAtom`, DataPropertyAtom→`swrl:DatavaluedPropertyAtom`, ObjectPropertyAtom→`swrl:IndividualPropertyAtom`, BuiltinAtom→`swrl:BuiltinAtom`, Var→`swrl:Variable` (grep each SWRL term).
    - An edge-property reification subsection maps `ARG.pos` to `swrl:argument1`/`swrl:argument2` (binary) and `swrl:arguments` rdf:List (builtin), and `HAS_BODY`/`HAS_HEAD` `order` to rdf:List/`swrl:AtomList` position (grep `argument1`, `arguments`, `AtomList`).
    - The BuiltinAtom subsection records the observed per-builtin arity from the live Cypher query (names at least continuousFromTo and towards with their verified arg counts).
    - The IRI subsection contains the three minting path segments (`/rule/`, `/atom/`, `/var/`) and states the `ex:` vs meta-schema namespace split.
    - An export-scoping subsection mandates label-based scoping and forbids `graph`-property scoping (grep for `label` and a reference to the mistagged-atom finding).
    - Cross-references `spec/DATABASE.md` rather than duplicating its node/property list (grep `DATABASE.md`).
  </acceptance_criteria>
  <done>The normative OntoGraph+Metagraph mapping is complete with verified builtin arity, reification rules, IRI scheme, and label-scoping requirement.</done>
</task>

<task type="auto">
  <name>Task 2: OntoGraph mapping, UNA handling, and informative ValidGraph sketch</name>
  <files>
    - spec/LPG-OWL-MAPPING.md
  </files>
  <read_first>
    - spec/LPG-OWL-MAPPING.md (the file created in Task 1 — read current state before extending)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md (§ Architecture Pattern 3 — UNA handling with owl:AllDifferent/owl:distinctMembers, forward-looking for Phase 823 ABox; § Anti-Patterns — ex: namespace conflation)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-CONTEXT.md (§ LPG→OWL Mapping Spec — informative ValidGraph section marked as Phase 823 input; UNA mandate)
    - spec/DATABASE.md (§ ValidGraph / DesignState / Run rows — the source shape for the informative ValidGraph sketch; and the OntoGraph rows)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md (§ ValidGraph informative sketch analog: DATABASE.md ValidGraph section)
  </read_first>
  <action>
    Extend spec/LPG-OWL-MAPPING.md with an OntoGraph mapping section: Class→owl:Class, DatatypeProperty→owl:DatatypeProperty, ObjectProperty→owl:ObjectProperty, each reusing the node's stored `iri` verbatim. State that the live OntoGraph is flat — confirmed zero SUBCLASS_OF/DOMAIN/RANGE/DISJOINT_WITH relationship types across the whole database — so real subsumption/disjointness axioms come only from the static DesignGrammar-V7.owl TBox in the hybrid union, not from the dynamic export.

    Add a normative "Unique Name Assumption (UNA)" subsection (RESEARCH § Pattern 3): even though Phase 820's own spike is TBox-only and mints no individuals, the spec MUST require, for when Phase 823's ValidGraph→RDF (ABox) translation lands, emitting one owl:AllDifferent per project export batch with owl:distinctMembers listing every minted named individual, so DL reasoners (which do not assume UNA) don't merge instances Neo4j treats as distinct. Document this as a normative requirement on the translator with an illustrative owl:AllDifferent block.

    Add an INFORMATIVE ValidGraph section (explicitly marked informative and "Phase 823 input", not normative): a sketch of how DesignState (kinds ObjState/ParamState/PropState) and Run (ValidStatus Boolean list, SendStatus) would map to RDF individuals, mirroring DATABASE.md's ValidGraph description. Keep it a forward-looking sketch, not a finished contract — Phase 823 owns finalizing it.

    Add a closing "Consistency & Propagation" note pointing to the schema-change propagation list (CLAUDE.md) so a future schema change knows to revisit this spec, and reiterating that this spec is normative input to Phase 821's translator.
  </action>
  <verify>
    <automated>grep -q "owl:Class" spec/LPG-OWL-MAPPING.md && grep -q "owl:AllDifferent" spec/LPG-OWL-MAPPING.md && grep -qi "distinctMembers" spec/LPG-OWL-MAPPING.md && grep -qi "informative" spec/LPG-OWL-MAPPING.md && grep -qi "ValidGraph" spec/LPG-OWL-MAPPING.md</automated>
  </verify>
  <acceptance_criteria>
    - OntoGraph section maps Class/DatatypeProperty/ObjectProperty to `owl:Class`/`owl:DatatypeProperty`/`owl:ObjectProperty` reusing stored `iri` (grep `owl:Class`, `owl:DatatypeProperty`, `owl:ObjectProperty`).
    - OntoGraph section states the live graph is flat (zero axiom-bearing relationship types) and that TBox axioms come from DesignGrammar-V7.owl.
    - A UNA subsection normatively requires `owl:AllDifferent` / `owl:distinctMembers` for future ABox exports (grep both terms).
    - A ValidGraph section exists, is explicitly labelled informative / Phase 823 input, and sketches DesignState (ObjState/ParamState/PropState) and Run → RDF.
    - A propagation note references the schema-change propagation requirement.
  </acceptance_criteria>
  <done>The spec is complete: OntoGraph mapping, normative UNA handling, and the informative ValidGraph sketch are all present, closing REAS-04's mapping-spec deliverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arity Cypher query → live Neo4j | Read-only query; dev credential must come from env, not be hardcoded in any committed note |
| spec/LPG-OWL-MAPPING.md → git repo | Committed doc; illustrative examples must not embed real client project data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-820-03 | Information Disclosure | spec/LPG-OWL-MAPPING.md examples | low | accept | Examples are illustrative/schema-derived (swrl:Imp sample from RESEARCH), not live client rule bodies; target verification data is v8-ui-smoke QA data |
| T-820-04 | Information Disclosure | ad-hoc arity Cypher query credentials | low | mitigate | Run the arity query with credentials from env vars (same NEO4J_* convention as the spike), never write the literal password into the spec or any committed note |
</threat_model>

<verification>
- `spec/LPG-OWL-MAPPING.md` covers, in one file: SWRL Metagraph mapping, ARG.pos + HAS_BODY/HAS_HEAD.order reification, IRI minting, label-scoping, OntoGraph mapping, UNA handling, and an informative ValidGraph sketch.
- BuiltinAtom arity reflects a live Cypher check, not an assumption.
- The file cross-references DATABASE.md rather than duplicating it.
</verification>

<success_criteria>
Delivers REAS-04 success criterion 2: a written LPG→OWL mapping spec covering edge-property reification for Atom.ARG.pos and Rule.HAS_BODY/HAS_HEAD.order, reviewable before Phase 821 implements the translator against it.
</success_criteria>

<output>
Create `.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-02-SUMMARY.md` when done.
</output>
