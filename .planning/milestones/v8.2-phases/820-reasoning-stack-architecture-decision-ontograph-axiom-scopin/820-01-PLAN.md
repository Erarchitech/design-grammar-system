---
phase: 820-reasoning-stack-architecture-decision-ontograph-axiom-scopin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/requirements.txt
  - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/export.py
  - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/run_naive.py
  - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/run_hybrid.py
  - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/README.md
  - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/
autonomous: false
requirements: [REAS-04]

must_haves:
  truths:
    - "Running run_naive.py against v8-ui-smoke's exported OntoGraph reports zero inconsistent classes (documents the trivial-consistency false positive) — success criterion 3, per D-hybrid two-part spike proof (a)"
    - "Running run_hybrid.py with curated ex:Door owl:disjointWith ex:Window plus ex:SlidingDoor rdfs:subClassOf both reports ex:SlidingDoor as unsatisfiable — the non-trivial result the naive export missed (two-part spike proof (b))"
    - "export.py Cypher scopes by node label, so the two mistagged R_DOOR_ORIENTATION_V atoms (graph:'OntoGraph') are captured, not dropped (Pitfall 1)"
    - "rdflib-parsed axiom counts for DesignGrammar-V7.owl (subClassOf/domain/range/disjointWith) are captured as authoritative evidence, not grep guesses (Pitfall 2)"
    - "The spike is reproducible from README.md alone — env vars plus JRE-17 throwaway container"
  artifacts:
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/requirements.txt"
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/export.py"
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/run_naive.py"
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/run_hybrid.py"
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/README.md"
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/naive_result.txt"
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/hybrid_result.txt"
    - ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/axiom_counts.txt"
  key_links:
    - "export.py label-scoped Cypher → RDFLib Turtle (naive_export.ttl / hybrid_export.ttl) consumed by run_naive.py and run_hybrid.py"
    - "Owlready2 sync_reasoner() shells out to the bundled HermiT JAR — requires JRE 17 (throwaway container, no host install)"
    - "Neo4j credentials read from NEO4J_URI/NEO4J_USER/NEO4J_PASSWORD env vars, never hardcoded (spike/ is committed to git)"
---

<objective>
Build the throwaway spike that empirically proves the axiom-scoping decision: (a) a naive flat export of live OntoGraph data reasons as trivially "consistent" (the false positive), and (b) the chosen hybrid approach — static TBox + live export + one curated disjointness axiom + a seeded TBox contradiction — makes HermiT flag a genuinely unsatisfiable class. Capture both reasoner outputs plus authoritative axiom counts as evidence for 820-DECISION.md (Plan 03).

Purpose: This spike is the defensible evidence behind REAS-04's hybrid decision. Without it, "the schema is too empty to fail" is an assertion, not a demonstration. It also validates the label-scoped export discipline (Pitfall 1) that Phase 821's real exporter inherits.

Output: A `spike/` subfolder (pinned requirements.txt, export.py, run_naive.py, run_hybrid.py, README.md) and captured evidence under `spike/output/`. The spike is explicitly throwaway — NOT the seed of dg-reasoner (Phase 821 builds clean against the spec).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-CONTEXT.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md
@.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files (all throwaway spike, under the phase `spike/` subfolder):
- `spike/requirements.txt` — pinned: `owlready2==0.51`, `rdflib==7.6.0`, `neo4j==6.2.0`
- `spike/export.py` — Neo4j → RDFLib Turtle exporter (label-scoped)
- `spike/run_naive.py` — part (a): naive export → HermiT → expect consistent
- `spike/run_hybrid.py` — part (b): hybrid + seeded contradiction → expect unsatisfiable
- `spike/README.md` — reproduction steps (env vars, JRE-17 container, run order)
- `spike/output/naive_export.ttl`, `spike/output/hybrid_export.ttl` — exported RDF
- `spike/output/naive_result.txt`, `spike/output/hybrid_result.txt` — reasoner output evidence
- `spike/output/axiom_counts.txt` — rdflib-parsed TBox axiom counts

New Python symbols in `export.py` (not pre-existing — exclude from drift verification):
- Module constants `METAGRAPH_QUERY`, `ONTOGRAPH_QUERY`
- Env reads `NEO4J_URI` (default `bolt://neo4j:7687`), `NEO4J_USER` (default `neo4j`), `NEO4J_PASSWORD` (default `12345678`), `PROJECT` (default `v8-ui-smoke`)
- Functions such as `build_rdf_graph(...)`, `export_naive(...)`, `export_hybrid(...)`, `count_tbox_axioms(...)` (exact names at executor discretion)

New IRI-minting conventions introduced by this spike (base `http://example.org/design-grammar`):
- `{base}/project/{project}/rule/{Rule_Id}`, `{base}/project/{project}/atom/{Atom_Id}`, `{base}/project/{project}/var/{name-without-leading-?}`

New spike-only seeded axioms (demonstration constructs, not production schema):
- `ex:Door owl:disjointWith ex:Window`, `ex:SlidingDoor rdfs:subClassOf ex:Door, ex:Window`
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify spike dependencies before install</name>
  <files>.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/requirements.txt</files>
  <read_first>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md (§ Package Legitimacy Audit — all four packages flagged SUS by the seam, manually Approved; the downloads signal is genuinely unavailable, so a pre-install sanity check is required per RESEARCH)
  </read_first>
  <action>Blocking-human gate — no code is written until this passes. Pause before any `pip install` of the three spike dependencies (owlready2==0.51, rdflib==7.6.0, neo4j==6.2.0) and have a human confirm each package's legitimacy on pypi.org per the steps in how-to-verify below. This gate is never auto-approved (T-820-SC); the executor must wait for an explicit "approved" signal before Task 2's install runs.</action>
  <what-built>
    No code is written before this gate. The spike is about to `pip install` three packages into a throwaway venv/container: `owlready2==0.51`, `rdflib==7.6.0`, `neo4j==6.2.0`. RESEARCH.md's Package Legitimacy Audit corrected all four seam `SUS` flags to Approved via manual PyPI release-history cross-check, but the automated downloads signal was unavailable — treat as procedural caution, not a legitimacy doubt.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/owlready2/ — confirm 0.51 is a real published release with multi-year release history (first release 2017), upstream repo referenced (github.com/pwin/owlready2 or bitbucket.org/jibalamy/owlready2).
    2. Open https://pypi.org/project/rdflib/ — confirm 7.6.0, official RDFLib org (github.com/RDFLib/rdflib), 16-year history.
    3. Open https://pypi.org/project/neo4j/ — confirm 6.2.0, official Neo4j vendor driver (already a pinned production dependency in data-service/requirements.txt).
    4. Confirm none are typosquats (exact names, not `owlready`/`rdf-lib`/`neo4j-driver`).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the spike's pip install, or name any package to withhold and re-check.</resume-signal>
  <acceptance_criteria>
    - Human has confirmed all three package names resolve to legitimate, established PyPI projects at the pinned versions before any install runs.
    - This gate is blocking-human and is never auto-approved (T-820-SC).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human gate; no automated command. Downstream install tasks must not run until "approved".</automated>
    <human-check>Package legitimacy confirmed on pypi.org for owlready2==0.51, rdflib==7.6.0, neo4j==6.2.0.</human-check>
  </verify>
  <done>Human approves the three pinned packages; spike install unblocked.</done>
</task>

<task type="auto">
  <name>Task 2: Spike environment + label-scoped Neo4j→RDFLib exporter + authoritative axiom counts</name>
  <files>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/requirements.txt
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/export.py
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/README.md
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/naive_export.ttl
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/axiom_counts.txt
  </files>
  <read_first>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md (§ Code Examples "Correct export scoping" gives the literal METAGRAPH_QUERY / ONTOGRAPH_QUERY; § Architecture Patterns 1 & 2 give the SWRL mapping table and IRI-minting strategy; § Live OntoGraph Characterization gives the target project facts; § Common Pitfalls 1 & 2; § Environment Availability gives the JRE-container fallback; § Standard Stack → Installation gives the exact requirements.txt)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-PATTERNS.md (§ spike/export.py analog: data-service/app.py driver-init + env-var pattern; § spike/requirements.txt and § spike/README.md guidance)
    - data-service/app.py (lines 1-67 only — the neo4j GraphDatabase.driver init and NEO4J_URI/USER/PASSWORD os.getenv pattern to reuse verbatim; graphify first per repo rule if exploring beyond these lines)
    - cypher_template.txt (Metagraph/OntoGraph schema shape the export must stay consistent with: HAS_BODY/HAS_HEAD carry order, ARG carries pos)
    - ontology/DesignGrammar-V7.owl (the static TBox to axiom-count with rdflib; xml:base is http://example.org/design-grammar)
  </read_first>
  <action>
    Write spike/requirements.txt with exactly the three pinned lines owlready2==0.51, rdflib==7.6.0, neo4j==6.2.0 (throwaway reproducibility — pin all three, deviating from data-service's unpinned convention per RESEARCH).

    Write spike/export.py as a standalone script (no FastAPI/pydantic imports — only `from __future__ import annotations`, os, pathlib, the neo4j `GraphDatabase`, and rdflib). Read connection settings from os.getenv with the exact names and dev-safe defaults used by data-service/app.py: NEO4J_URI default `bolt://neo4j:7687`, NEO4J_USER default `neo4j`, NEO4J_PASSWORD default `12345678`. NEVER inline the literal password anywhere except as the os.getenv default (T-820-01). Accept the target project from a PROJECT env var or argv, defaulting to `v8-ui-smoke` (the project with the most rules — 16 — per RESEARCH's discretion rule).

    Define two module constants METAGRAPH_QUERY and ONTOGRAPH_QUERY scoped by node LABEL, never by the `graph` property: METAGRAPH_QUERY matches nodes where n.project=$project AND (n:Rule OR n:Atom OR n:Var OR n:Literal OR n:Builtin); ONTOGRAPH_QUERY matches n.project=$project AND (n:Class OR n:DatatypeProperty OR n:ObjectProperty). This label-scoping is the Pitfall 1 fix — v8-ui-smoke's two R_DOOR_ORIENTATION_V atoms (A1 ClassAtom, A2 DatatypeProperty-typed) carry graph:'OntoGraph' and would be silently dropped by any {project, graph:'Metagraph'} query.

    Build one in-memory RDFLib Graph and serialize to spike/output/naive_export.ttl. Map OntoGraph nodes to owl:Class / owl:DatatypeProperty / owl:ObjectProperty reusing each node's stored `iri` verbatim (these are `ex:`-prefixed dynamically-generated domain terms, a namespace SEPARATE from DesignGrammar-V7.owl's dg/dgm/dgv/dgs — do not conflate). Map Metagraph per the SWRL RDF vocabulary in RESEARCH § Pattern 1: Rule→swrl:Imp; HAS_BODY→swrl:body and HAS_HEAD→swrl:head each as an ordered rdf:List (swrl:AtomList) whose first/rest chain preserves the edge `order` property; Atom by type→swrl:ClassAtom / swrl:DatavaluedPropertyAtom / swrl:IndividualPropertyAtom / swrl:BuiltinAtom; ARG.pos→swrl:argument1/swrl:argument2 for binary atom kinds and swrl:arguments (rdf:List) for BuiltinAtom; Var→swrl:Variable; Literal→a typed RDF literal (not a minted individual). Mint deterministic IRIs under base http://example.org/design-grammar with project-scoped path segments /project/{project}/rule/{Rule_Id}, /atom/{Atom_Id}, /var/{name-stripped-of-leading-?}.

    Add a TBox-counting routine that parses ontology/DesignGrammar-V7.owl with rdflib and writes authoritative triple counts to spike/output/axiom_counts.txt for RDFS.subClassOf, RDFS.domain, RDFS.range, and OWL.disjointWith (Pitfall 2: rdflib parse, NOT grep — expect disjointWith == 0, which is exactly why curated disjointness is needed in Task 3's hybrid).

    Write spike/README.md documenting: the three NEO4J_* env vars (never hardcode the password); that no JRE exists on the host so the spike runs inside a throwaway container built from `python:3.11-slim` + `apt-get install -y openjdk-17-jre-headless`, attached to the running docker-compose network so `bolt://neo4j:7687` resolves (document how to find the network via `docker network ls` and the fallback `bolt://host.docker.internal:7687`); the exact run order (pip install -r requirements.txt, then python export.py, then run_naive.py && run_hybrid.py); and that the spike is throwaway, NOT the seed of dg-reasoner. Use RESEARCH § Environment Availability and § Validation Architecture "Quick run command" as the content source.

    Run the exporter against v8-ui-smoke inside the JRE-equipped throwaway container (or a host venv for the export step, which needs no JRE) so naive_export.ttl and axiom_counts.txt are produced as real artifacts.
  </action>
  <verify>
    <automated>test -s ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/naive_export.ttl" && test -s ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/axiom_counts.txt" && grep -qi "disjointWith" ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/axiom_counts.txt" && grep -q "swrl:Imp" ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/naive_export.ttl"</automated>
  </verify>
  <acceptance_criteria>
    - `spike/requirements.txt` contains exactly `owlready2==0.51`, `rdflib==7.6.0`, `neo4j==6.2.0` (grep each).
    - `spike/export.py` reads NEO4J_PASSWORD via os.getenv (grep `os.getenv("NEO4J_PASSWORD"`) and contains no bare assignment of the literal password to a variable outside an os.getenv default.
    - `spike/export.py` defines METAGRAPH_QUERY and ONTOGRAPH_QUERY that filter on node labels (grep `n:Rule` and `n:Class`) and do NOT filter Metagraph by `graph:'Metagraph'` (the query must survive the mistagged R_DOOR_ORIENTATION_V atoms).
    - `spike/output/naive_export.ttl` exists, is non-empty, and contains `swrl:Imp` (at least one Rule mapped) — confirms the SWRL vocabulary mapping ran against real data.
    - `spike/output/axiom_counts.txt` reports an owl:disjointWith count of 0 for DesignGrammar-V7.owl (the empirical justification for curated disjointness).
    - `spike/README.md` documents the three NEO4J_* env vars and the JRE-17 throwaway-container reproduction path.
  </acceptance_criteria>
  <done>Exporter produces label-scoped Turtle from live v8-ui-smoke and rdflib-parsed axiom counts; README makes the run reproducible; no credential is hardcoded.</done>
</task>

<task type="auto">
  <name>Task 3: Two-part reasoner proof — naive (trivially consistent) vs hybrid + seeded contradiction (unsatisfiable)</name>
  <files>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/run_naive.py
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/run_hybrid.py
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/hybrid_export.ttl
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/naive_result.txt
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/hybrid_result.txt
  </files>
  <read_first>
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-RESEARCH.md (§ Code Examples "Owlready2 spike skeleton (part a)" and "(part b)" — the literal script bodies to start from; § Common Pitfall 4 — keep the contradiction TBox-only, no ABox/individuals; § Architecture Patterns diagram showing naive vs hybrid load sets)
    - .planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/export.py (the exporter written in Task 2 — run_hybrid may reuse its build routine to produce hybrid_export.ttl)
    - data-service/reasoner.py (only for the recognized reasoner-id vocabulary — HermiT is the default engine id; no settings-persistence machinery is needed in the spike)
  </read_first>
  <action>
    Write spike/run_naive.py: load spike/output/naive_export.ttl via owlready2 get_ontology(...).load(), run sync_reasoner() (HermiT — the reasoner.py default engine id), then write the result of list(onto.inconsistent_classes()) to spike/output/naive_result.txt and print it. Expected outcome: an empty list — the documented trivial-consistency false positive (naive export has no axioms connecting the flat domain terms, so nothing can be unsatisfiable). Start from the RESEARCH § Code Examples part-(a) skeleton.

    Produce the hybrid export TTL (spike/output/hybrid_export.ttl) = static ontology/DesignGrammar-V7.owl UNION the live v8-ui-smoke OntoGraph/Metagraph export PLUS a curated disjointness axiom `ex:Door owl:disjointWith ex:Window` PLUS a seeded contradiction asserting `ex:SlidingDoor rdfs:subClassOf ex:Door` and `ex:SlidingDoor rdfs:subClassOf ex:Window`. This is a PURE TBox construction using two of v8-ui-smoke's real domain classes — mint NO individuals, add NO ABox assertions (Pitfall 4; matches the milestone's TBox-only, rule-authoring-time reasoning scope). Build this either by extending export.py with a hybrid flag or inline in run_hybrid.py.

    Write spike/run_hybrid.py: load hybrid_export.ttl via owlready2, run sync_reasoner(), then write list(onto.inconsistent_classes()) to spike/output/hybrid_result.txt and print it. Expected outcome: ex:SlidingDoor appears as unsatisfiable/inconsistent — the non-trivial result the naive export missed, because SlidingDoor is forced under two disjoint parents. Start from the RESEARCH § Code Examples part-(b) skeleton.

    Run both scripts inside the JRE-17 throwaway container (Owlready2 shells out to the bundled HermiT JAR, which needs the JRE) so naive_result.txt and hybrid_result.txt are captured as real before/after evidence for 820-DECISION.md.
  </action>
  <verify>
    <automated>test -s ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/naive_result.txt" && test -s ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/hybrid_result.txt" && grep -qi "SlidingDoor" ".planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/spike/output/hybrid_result.txt"</automated>
  </verify>
  <acceptance_criteria>
    - `spike/run_naive.py` and `spike/run_hybrid.py` both call `sync_reasoner()` and write `onto.inconsistent_classes()` to their respective output files (grep `sync_reasoner` and `inconsistent_classes` in each).
    - `spike/output/naive_result.txt` records an empty inconsistent-classes result (the trivial-consistency false positive) — behavior assertion: naive reasoning finds nothing unsatisfiable.
    - `spike/output/hybrid_result.txt` names `SlidingDoor` as an unsatisfiable/inconsistent class — behavior assertion: the hybrid + seeded contradiction is caught by HermiT.
    - `spike/output/hybrid_export.ttl` contains both `owl:disjointWith` and `SlidingDoor` (grep) — confirms the curated axiom and the seeded contradiction are present in the reasoned graph.
    - The contradiction is TBox-only: `hybrid_export.ttl` introduces no `owl:NamedIndividual` beyond what DesignGrammar-V7.owl already declares (Pitfall 4).
  </acceptance_criteria>
  <done>Both reasoner runs execute against real data and produce the expected before/after contrast (naive: consistent; hybrid: SlidingDoor unsatisfiable), captured as evidence files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike script → live Neo4j | Dev credential crosses here; the script is committed to git, so the credential must never be inlined |
| spike/ + output/ → git repo | Committed artifacts could leak secrets or non-test-project rule bodies |
| pip → PyPI | Third-party packages installed into the spike environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-820-01 | Information Disclosure | spike/export.py Neo4j connection | high | mitigate | Read NEO4J_URI/NEO4J_USER/NEO4J_PASSWORD from os.getenv (dev-safe default only); document the three env vars in spike/README.md; never inline the literal password (RESEARCH § Security Domain V6) |
| T-820-02 | Information Disclosure | spike/output/*.ttl committed rule bodies | low | accept | Spike targets v8-ui-smoke (QA/smoke-test data per STATE.md), not a real client project; README notes the same caution applies if the spike is later re-run against a real project |
| T-820-SC | Tampering | pip installs (owlready2, rdflib, neo4j) | high | mitigate | RESEARCH § Package Legitimacy Audit + Task 1 blocking-human checkpoint verifying each on pypi.org before install; pinned versions in requirements.txt |
</threat_model>

<verification>
- `spike/output/naive_result.txt` shows no inconsistent classes; `spike/output/hybrid_result.txt` names SlidingDoor — the two-part proof contrast holds.
- `spike/output/axiom_counts.txt` shows owl:disjointWith == 0 in DesignGrammar-V7.owl (rdflib-parsed).
- No credential literal in any committed spike file except as an os.getenv default.
- README reproduces the run end-to-end (env vars + JRE-17 container).
</verification>

<success_criteria>
Spike proves REAS-04 success criterion 3: a naive export of a real project's live OntoGraph trivially reports "consistent", and the chosen hybrid scoping avoids that false positive by catching a seeded unsatisfiability. Evidence files exist for Plan 03 to cite.
</success_criteria>

<output>
Create `.planning/phases/820-reasoning-stack-architecture-decision-ontograph-axiom-scopin/820-01-SUMMARY.md` when done.
</output>
