---
phase: 823-shacl-validation-layer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/RULE-PARTITION-POLICY.md
  - spec/LPG-OWL-MAPPING.md
  - spec/DATABASE.md
  - CLAUDE.md
  - README.md
  - .github/copilot-instructions.md
autonomous: true
requirements: [SHCL-01]
must_haves:
  truths:
    - "A documented policy states which rule categories the SWRL VALIDATOR owns vs. which SHACL owns, with a 'what belongs where' decision table"
    - "The policy prescribes re-homing a mis-categorized rule, never merging or overriding verdicts (single-authoring precedence)"
    - "The LPG-OWL-MAPPING ValidGraph sketch is updated to reflect the now-implemented ABox export + UNA obligation"
    - "shaclReportJson on ValidationRun is documented where DATABASE.md covers ValidGraph, and the schema-change-propagation checklist references the new policy"
  artifacts:
    - "spec/RULE-PARTITION-POLICY.md (new)"
    - "cross-references added in spec/LPG-OWL-MAPPING.md and CLAUDE.md; shaclReportJson documented in spec/DATABASE.md"
  key_links:
    - "Policy is cross-referenced from LPG-OWL-MAPPING.md and CLAUDE.md schema-change-propagation checklist (D-11)"
---

<objective>
Author the rule-partition/precedence policy (D-11/D-12/D-13/D-14) as a new spec/RULE-PARTITION-POLICY.md, and propagate the two documentation obligations this phase creates: (1) mark the spec/LPG-OWL-MAPPING.md ValidGraph-to-RDF sketch as now-implemented and its owl:AllDifferent UNA obligation as satisfied by Phase 823 (D-04/D-05), and (2) document the new shaclReportJson run-record property plus the new policy in the schema-change-propagation surfaces (spec/DATABASE.md, CLAUDE.md).

Purpose: Success Criterion 2 of the phase requires a documented partition policy so no business rule is authored twice with disagreeing verdicts. This plan is pure documentation and has zero code dependency — it runs in Wave 1 in parallel with the ABox exporter.

Output: spec/RULE-PARTITION-POLICY.md plus scoped edits to spec/LPG-OWL-MAPPING.md, spec/DATABASE.md, and CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/823-shacl-validation-layer/823-CONTEXT.md
@.planning/phases/823-shacl-validation-layer/823-RESEARCH.md
@spec/LPG-OWL-MAPPING.md
@spec/DATABASE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author spec/RULE-PARTITION-POLICY.md (partition line, decision table, precedence, enforcement)</name>
  <files>spec/RULE-PARTITION-POLICY.md</files>
  <read_first>
    - spec/LPG-OWL-MAPPING.md (structure/tone to match: heading style, tables, Informative/Normative labeling)
    - .planning/phases/823-shacl-validation-layer/823-CONTEXT.md D-11/D-12/D-13/D-14 (the exact partition line, precedence principle, and deferred-linter note)
    - .planning/REQUIREMENTS.md (SHCL-01/SHCL-02 wording + the Out-of-Scope "Replacing the SWRL VALIDATOR with SHACL" row for framing)
  </read_first>
  <action>
    Create spec/RULE-PARTITION-POLICY.md. State the partition line (D-12): the SWRL VALIDATOR owns architect-authored design-compliance rules (quantitative geometry/parameter constraints from NL ingestion — the metagraph Rule corpus); SHACL owns structural data-integrity of instance data (schema conformance of DesignState/Run/Rule structure). Include a "What belongs where" decision table mapping future rule categories (examples: max building height, required-property-present, enum membership, referential resolvability, value-range-from-regulation) to SWRL vs SHACL with a one-line rationale each. Document precedence (D-13): single-authoring principle — no business rule exists in both systems, so verdict disagreement can only mean mis-categorization; the remedy is re-homing the rule to its correct system, never merging or overriding verdicts; for design compliance SWRL is authoritative, for data integrity SHACL is authoritative. Document enforcement (D-14): documentation + review discipline this phase; an automated shape-vs-rule overlap linter is explicitly deferred as a named future item, not a promise. Add a short "How SHACL findings surface" pointer to the severity mapping (violation/warning/info to red/orange/yellow) and the ErrorMessageTemplates What+Where+How-to-fix house style, cross-referencing the shapes file ontology/dg-shapes.ttl.
  </action>
  <verify>
    <automated>test -f spec/RULE-PARTITION-POLICY.md && grep -qi "SWRL" spec/RULE-PARTITION-POLICY.md && grep -qi "SHACL" spec/RULE-PARTITION-POLICY.md && grep -Eqi "re-hom|rehom" spec/RULE-PARTITION-POLICY.md && echo PARTITION_OK</automated>
  </verify>
  <acceptance_criteria>
    - spec/RULE-PARTITION-POLICY.md exists and contains a decision table (a markdown table contrasting SWRL vs SHACL ownership)
    - The doc contains the precedence rule text naming re-homing (not verdict-merging) as the remedy for disagreement
    - The doc names the deferred overlap linter explicitly as out-of-scope-this-phase (D-14)
    - grep for "dg-shapes.ttl" in spec/RULE-PARTITION-POLICY.md succeeds (points to the shapes artifact)
  </acceptance_criteria>
  <done>spec/RULE-PARTITION-POLICY.md fully states the partition line, decision table, single-authoring precedence, and deferred-linter enforcement note.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference the policy + document shaclReportJson and the implemented ABox/UNA</name>
  <files>spec/LPG-OWL-MAPPING.md, spec/DATABASE.md, CLAUDE.md, README.md, .github/copilot-instructions.md</files>
  <read_first>
    - spec/LPG-OWL-MAPPING.md (the ValidGraph-to-RDF Sketch section ~L321 labeled Informative, and the UNA section ~L299 that says "future ABox exports" — both need a Phase-823-implemented status note + a cross-ref to RULE-PARTITION-POLICY.md)
    - spec/DATABASE.md (locate the ValidGraph / ValidationRun coverage where rulesJson/statePayloadJson-style run-record properties are described)
    - CLAUDE.md (the "Schema Change Propagation" checklist paragraph listing files to update on schema changes)
    - README.md ~L200 (existing statePayloadJson sibling-property documentation — shaclReportJson extends the same list)
    - .github/copilot-instructions.md ~L28 (same statePayloadJson sibling-property pattern)
  </read_first>
  <action>
    In spec/LPG-OWL-MAPPING.md: add a status note to the "ValidGraph to RDF Sketch" section stating it is implemented in Phase 823 by dg-reasoner/valid_graph_export.py (D-04), and update the UNA section's "future ABox exports" language to record that Phase 823 now emits owl:AllDifferent over ValidGraph + Metagraph individuals (D-05); add a cross-reference line to spec/RULE-PARTITION-POLICY.md (D-11). In spec/DATABASE.md: document the new run-record property shaclReportJson on the ValidationRun node (ValidGraph) — a JSON string holding the per-run SHACL report envelope with status, conforms, results, and counts, sibling to rulesJson/statePayloadJson, absent on pre-823 runs. In CLAUDE.md: add spec/RULE-PARTITION-POLICY.md as a cross-referenced policy and add shaclReportJson (and ontology/dg-shapes.ttl) to the Schema Change Propagation checklist so future schema edits keep them in sync (D-11). In README.md and .github/copilot-instructions.md: extend the existing statePayloadJson sibling-property documentation (README.md ~L200, copilot-instructions.md ~L28) with a one-line mention of shaclReportJson (per-run SHACL report envelope JSON string, absent on pre-823 runs).
  </action>
  <verify>
    <automated>grep -qi "RULE-PARTITION-POLICY" spec/LPG-OWL-MAPPING.md && grep -qi "RULE-PARTITION-POLICY" CLAUDE.md && grep -qi "shaclReportJson" spec/DATABASE.md && grep -qi "shaclReportJson" CLAUDE.md && grep -qi "shaclReportJson" README.md && grep -qi "shaclReportJson" .github/copilot-instructions.md && echo PROPAGATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - spec/LPG-OWL-MAPPING.md ValidGraph sketch carries a Phase-823-implemented status note and a cross-reference to RULE-PARTITION-POLICY.md
    - spec/LPG-OWL-MAPPING.md UNA section reflects that owl:AllDifferent is now emitted (no longer "future")
    - spec/DATABASE.md documents shaclReportJson on the ValidationRun node as a JSON-string run-record property
    - CLAUDE.md schema-change-propagation checklist references both RULE-PARTITION-POLICY.md and shaclReportJson
    - README.md and .github/copilot-instructions.md mention shaclReportJson alongside their existing statePayloadJson documentation
  </acceptance_criteria>
  <done>The mapping spec, DATABASE.md, CLAUDE.md, README.md, and copilot-instructions.md all reflect the implemented ABox/UNA, the new shaclReportJson property, and the partition policy cross-reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → engineering practice | Policy governs where rules are authored; no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-823-09 | Repudiation | undocumented partition leading to double-authored rules | low | mitigate | The policy itself is the mitigation — a version-controlled, cross-referenced decision record + review discipline (D-14) |
| T-823-SC | Tampering | package installs | low | accept | Documentation-only plan; no packages, no code, no supply-chain surface |
</threat_model>

<verification>
- Both automated grep gates return their sentinel (PARTITION_OK, PROPAGATION_OK)
- Manual review: the decision table covers at least one example that maps to SWRL and at least one that maps to SHACL, with rationale
</verification>

<success_criteria>
A version-controlled rule-partition/precedence policy exists and is cross-referenced from the mapping spec and CLAUDE.md; the ValidGraph ABox + UNA obligations are recorded as implemented; shaclReportJson is documented as a ValidationRun property.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 823-04)

- **New file:** spec/RULE-PARTITION-POLICY.md (partition line + decision table + precedence + deferred-linter note)
- **Doc updates:** spec/LPG-OWL-MAPPING.md (ValidGraph sketch marked implemented; UNA marked emitted; policy cross-ref); spec/DATABASE.md (shaclReportJson ValidationRun property documented); CLAUDE.md (schema-change-propagation checklist gains RULE-PARTITION-POLICY.md + shaclReportJson + dg-shapes.ttl)
</artifacts>

<output>
Create `.planning/phases/823-shacl-validation-layer/823-04-SUMMARY.md` when done
</output>
