---
phase: 01-cloud-llm-connector
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - n8n/workflows/rules-to-metagraph.json
  - n8n/workflows/graph-query-mcp.json
  - n8n/workflows/spec-ingest.json
  - n8n/workflows/spec-query.json
  - n8n/workflows/spec-update.json
autonomous: true
requirements:
  - LLMC-04
  - LLMC-05
user_setup: []
must_haves:
  truths:
    - "[D-05] All HTTP nodes are direct HTTP nodes -- no n8n Function node middleware added (n8n stays thin)"
    - "[D-06] Big-bang migration: all 5 workflows rewired in a single plan (not one-at-a-time)"
    - "[D-07] Each workflow sends `{provider: null, model: null}` -- gateway resolves active provider from settings"
    - "No n8n workflow contains a direct `http://ollama:11434` URL in any HTTP request node"
    - "All 5 workflows call `http://data-service:8000/llm/generate` as the LLM endpoint"
    - "Response parsing extracts text from the gateway envelope field `.text` instead of Ollama-specific `.response`"
    - "With no key configured and n8n sending null provider, the gateway routes to Ollama -- identical behavior to pre-v9.0 (LLMC-05)"
    - "Switching providers in the UI takes effect immediately with no n8n workflow edits"
  artifacts:
    - n8n/workflows/rules-to-metagraph.json (node ~87 URL + body + parse Function node updated)
    - n8n/workflows/graph-query-mcp.json (2 Ollama nodes ~92 and ~153 URL + body + downstream parse updated)
    - n8n/workflows/spec-ingest.json (node ~87 URL + body + any downstream parse updated)
    - n8n/workflows/spec-query.json (node ~123 URL + body + any downstream parse updated)
    - n8n/workflows/spec-update.json (node ~99 URL + body + any downstream parse updated)
  key_links:
    - "6 HTTP nodes (not 5) across 5 workflows must be rewired -- graph-query-mcp has 2 nodes"
    - "Parse LLM Output function nodes read from `.response` (Ollama) -- must change to `.text` (gateway envelope)"
    - "Ollama `keep_alive`, `options.temperature`, `options.num_predict` params are no longer sent to gateway -- gateway adapters set provider-appropriate defaults"
---

<objective>
Rewire all 5 n8n workflows (6 Ollama HTTP nodes total) to call the new LLM gateway at `http://data-service:8000/llm/generate` instead of calling `http://ollama:11434/api/generate` directly.

Purpose: D-06 mandates big-bang migration -- all workflows rewired in a single plan. The gateway is designed as a drop-in replacement: URL swap + body reformat + response field rename. n8n sends `{provider: null, model: null}` per D-07, letting the gateway resolve the active provider from settings. This makes provider switching a UI-only operation with no workflow edits.

Output:
- All 5 n8n workflow JSON files modified with updated HTTP node URLs, request body formats, and response parsing logic.
- grep for `ollama:11434` in n8n/workflows/ returns zero matches.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v9.0-phases/01-cloud-llm-connector/01-CONTEXT.md
@.planning/milestones/v9.0-phases/01-cloud-llm-connector/01-RESEARCH.md
@n8n/workflows/rules-to-metagraph.json
@n8n/workflows/graph-query-mcp.json
@n8n/workflows/spec-ingest.json
@n8n/workflows/spec-query.json
@n8n/workflows/spec-update.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire rules-to-metagraph.json and graph-query-mcp.json (4 Ollama nodes)</name>

  <files>
    n8n/workflows/rules-to-metagraph.json
    n8n/workflows/graph-query-mcp.json
  </files>

  <read_first>
    - n8n/workflows/rules-to-metagraph.json (nodes ~32-48 Set Input Defaults for ollama_url/ollama_model/ollama_keep_alive variables, node ~87 "Ollama Generate" HTTP node with bodyParametersJson, node ~106 "Parse LLM Output" Function node with response extraction from `$json.response`)
    - n8n/workflows/graph-query-mcp.json (node ~90 first Ollama HTTP for Cypher generation, node ~151 second Ollama HTTP for answer generation, Function nodes downstream of each that parse the response)
    - .planning/milestones/v9.0-phases/01-cloud-llm-connector/01-RESEARCH.md (Common Pitfalls 3 and 4: response field name change from .response to .text, 6 nodes total not 5)
  </read_first>

  <action>
    Apply these changes to both workflow files. Each change is a JSON field edit within the workflow's node parameters. Use `Edit` tool for each targeted replacement.

    **rules-to-metagraph.json changes:**

    1. Node "Ollama Generate" (id "4", near position [20,0], current url field at line ~88):
       - **URL change:** Replace
         `"url": "={{$items(\"Build LLM Prompt\")[0].json.ollama_url}}/api/generate"`
         with
         `"url": "http://data-service:8000/llm/generate"`
       - **Body format change (D-07):** Replace the `bodyParametersJson` expression which currently sends Ollama-specific fields (model, prompt, stream, keep_alive, options, stop). The new body expression sends only the gateway contract fields:
         `"bodyParametersJson": "={{ { \"provider\": null, \"model\": null, \"prompt\": $items(\"Build LLM Prompt\")[0].json.prompt, \"system\": null } }}"`
       - Remove the `"options"` sub-object (no longer contains `timeout` at this level) -- the timeout field stays in the `"options"` list at the node level (already present: `"timeout": 900000`).

    2. Node "Parse LLM Output" (id "5", near position [260,0], Function node at line ~107):
       - **Response field change:** In the `functionCode` string, the variable extraction logic currently reads `$json.response`. Change any reference from `$json.response` to `$json.text` when extracting the LLM output text from the raw response.
       - Specifically, find the line:
         `const raw = $json.response !== undefined ? $json.response : ($json.body !== undefined ? $json.body : $json);`
         Change to:
         `const raw = $json.text !== undefined ? $json.text : ($json.body !== undefined ? $json.body : $json);`
       - Find the line:
         `} else if (raw && typeof raw === 'object' && typeof raw.response === 'string') {`
         Change to:
         `} else if (raw && typeof raw === 'object' && typeof raw.text === 'string') {`
       - The rest of the Cypher extraction logic (bracket nesting, MERGE consolidation, etc.) remains unchanged -- it operates on the extracted text string.

    3. No other nodes in this workflow need modification. The "Set Input Defaults" node still sets ollama_url/ollama_model variables but they are no longer consumed -- they can be left as dead variables (no workflow breakage, just unused). The "Prepare Graph Payload" and "Execute LLM Cypher" nodes are unchanged.

    **graph-query-mcp.json changes** (2 Ollama nodes):

    4. First Ollama node (Cypher generation, near position ~[180,480], id varies -- search for `url` containing `ollama_url`/api/generate):
       - **URL change:** Replace
         `"url": "={{$items(\"Build Cypher Prompt\")[0].json.ollama_url}}/api/generate"`
         with
         `"url": "http://data-service:8000/llm/generate"`
       - **Body format change:** Replace the bodyParametersJson Ollama format with:
         `"bodyParametersJson": "={{ { \"provider\": null, \"model\": null, \"prompt\": $items(\"Build Cypher Prompt\")[0].json.prompt, \"system\": null } }}"`
       - Find any downstream Function node that reads the response from this node and change `$json.response` to `$json.text`.

    5. Second Ollama node (Answer generation, near position ~[180,1040]):
       - **URL change:** Replace
         `"url": "={{$items(\"Build Answer Prompt\")[0].json.ollama_url}}/api/generate"`
         with
         `"url": "http://data-service:8000/llm/generate"`
       - **Body format change:** Replace the bodyParametersJson with:
         `"bodyParametersJson": "={{ { \"provider\": null, \"model\": null, \"prompt\": $items(\"Build Answer Prompt\")[0].json.prompt, \"system\": null } }}"`
       - Find any downstream Function node that reads the response and change response field extraction to use `$json.text`.

    6. The "Set Input Defaults" node in this workflow (near position ~[180,280]) sets `ollama_url`, `ollama_model`, `ollama_keep_alive` -- these can remain as unused variables.

    **Important notes for all changes:**
    - n8n JSON workflow format uses `"={{ expression }}"` for dynamic expressions. The escape sequence `\"` is used inside JSON strings. Preserve exact n8n expression syntax.
    - The `bodyParametersJson` field contains a complete expression string. Find the existing one and replace it entirely -- do NOT try to edit it piece by piece (risk of malformed JSON).
    - Each workflow has a `"nodes"` array. Locate nodes by `"name"` field (e.g., `"name": "Ollama Generate"`, `"name": "Parse LLM Output"`) rather than position.
    - After changing URL and body, verify the `"jsonParameters": true` flag remains set on the HTTP node.
    - Verify `"requestMethod": "POST"` remains on the HTTP node.
  </action>

  <verify>
    <automated>grep -c "ollama:11434" n8n/workflows/rules-to-metagraph.json n8n/workflows/graph-query-mcp.json 2>&1 | grep -v ":0$" ; if [ $? -eq 1 ]; then echo "PASS: no ollama:11434 refs remain in these 2 workflows"; else echo "FAIL: ollama:11434 refs still present"; fi</automated>
  </verify>

  <acceptance_criteria>
    - `grep "ollama:11434" n8n/workflows/rules-to-metagraph.json` returns zero matches
    - `grep "ollama:11434" n8n/workflows/graph-query-mcp.json` returns zero matches
    - `grep "data-service:8000/llm/generate" n8n/workflows/rules-to-metagraph.json` returns exactly 1 match (the HTTP node URL)
    - `grep "data-service:8000/llm/generate" n8n/workflows/graph-query-mcp.json` returns exactly 2 matches (both HTTP node URLs)
    - `grep 'provider.*null.*model.*null' n8n/workflows/rules-to-metagraph.json | wc -l` returns 1 (body contains provider:null, model:null)
    - `grep 'provider.*null.*model.*null' n8n/workflows/graph-query-mcp.json | wc -l` returns 2 (both bodies)
    - In rules-to-metagraph.json Parse LLM Output node functionCode: `$json.text` appears instead of `$json.response` in the response extraction logic
    - Both JSON files remain valid (parseable as JSON) -- verify with `python -c "import json; json.load(open('n8n/workflows/rules-to-metagraph.json')); json.load(open('n8n/workflows/graph-query-mcp.json'))"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewire spec-ingest.json, spec-query.json, and spec-update.json (2 Ollama nodes)</name>

  <files>
    n8n/workflows/spec-ingest.json
    n8n/workflows/spec-query.json
    n8n/workflows/spec-update.json
  </files>

  <read_first>
    - n8n/workflows/spec-ingest.json (node ~87 with url "http://ollama:11434/api/generate" -- single HTTP node, check for downstream Function nodes that read the response)
    - n8n/workflows/spec-query.json (node ~123 with url "http://ollama:11434/api/generate")
    - n8n/workflows/spec-update.json (node ~99 with url "http://ollama:11434/api/generate")
  </read_first>

  <action>
    Apply the same URL + body + response parsing changes to the 3 spec workflows. Each has exactly 1 Ollama HTTP node.

    **For each of the 3 workflow files** (spec-ingest.json, spec-query.json, spec-update.json):

    1. **URL change:** Find the HTTP node with `"url": "http://ollama:11434/api/generate"`. Replace it with:
       `"url": "http://data-service:8000/llm/generate"`

    2. **Body format change:** Find the `bodyParametersJson` in the same node. Replace the Ollama-format body with the gateway contract format:
       `"bodyParametersJson": "={{ { \"provider\": null, \"model\": null, \"prompt\": $json.prompt, \"system\": $json.system } }}"`

       Note: The spec workflows may use `$json.prompt` directly (simpler workflows) or reference items from preceding nodes like `$items("NodeName")`. Inspect each workflow's existing body expression to determine the correct prompt field reference. Preserve the same item reference pattern -- just wrap it in the new key structure.

       Example transformations:
       - If current body has `"prompt": $json.prompt`, new body has `"prompt": $json.prompt` inside the gateway envelope.
       - If Ollama-specific fields like `"stream"`, `"keep_alive"`, `"options"`, `"stop"` exist, remove them entirely -- the gateway adapters handle these.

    3. **Response parsing update:** After the HTTP node, find any Function node or downstream node that parses the LLM response. The response now comes from the gateway as `{text, provider, model, usage}` instead of `{model, response, done, ...}`.
       - If a downstream Function node extracts `$json.response`, change to `$json.text`.
       - If it extracts `$json.model` for model identification, update per below.
       - If the downstream node expects the raw Ollama response structure (e.g., checking `done`, `eval_count`), restructure the logic to read from the gateway envelope.

       **For each workflow, check the specific downstream consumption pattern:**

       **spec-ingest.json:** After the HTTP node, there should be a Function or Set node that extracts generation text. Look at the output format the workflow uses upstream. Change any `$json.response` reference to `$json.text`.

       **spec-query.json:** Same pattern. Look for the node immediately after the HTTP call that processes the response. Update response field references.

       **spec-update.json:** Same pattern.

    4. **Preservation rules:**
       - Keep `"jsonParameters": true` on the HTTP node unchanged.
       - Keep `"requestMethod": "POST"` unchanged.
       - Keep `"options"` timeout and other HTTP-level settings unchanged (e.g., `"timeout": 300000`).
       - Do NOT add new Function nodes or middleware -- per D-05, n8n stays thin with direct HTTP nodes.
       - The `"authentication"` property on the HTTP node should be removed if present (or set to "none") -- the gateway does not require n8n-side auth; provider auth is handled inside the gateway.

    **JSON editing strategy:**
    - These are smaller, simpler workflows than rules-to-metagraph. Read each file completely first, then apply targeted Edit replacements for the URL string, the bodyParametersJson string, and any Function node code that references `.response`.
    - For the bodyParametersJson replacement in particular: the expression is a JSON string within a JSON file. Find the exact existing `"bodyParametersJson"` line and replace it. Use the Edit tool with exact old_string matching the current value.
  </action>

  <verify>
    <automated>grep -c "ollama:11434" n8n/workflows/spec-ingest.json n8n/workflows/spec-query.json n8n/workflows/spec-update.json 2&gt;&amp;1 | grep -v ":0$" ; if [ $? -eq 1 ]; then echo "PASS"; else echo "FAIL -- ollama:11434 still present"; fi; for f in n8n/workflows/spec-*.json; do python -c "import json; json.load(open('$f')); print('valid')"; done</automated>
  </verify>

  <acceptance_criteria>
    - `grep "ollama:11434" n8n/workflows/spec-ingest.json` returns zero matches
    - `grep "ollama:11434" n8n/workflows/spec-query.json` returns zero matches
    - `grep "ollama:11434" n8n/workflows/spec-update.json` returns zero matches
    - `grep "data-service:8000/llm/generate" n8n/workflows/spec-ingest.json` returns exactly 1 match
    - `grep "data-service:8000/llm/generate" n8n/workflows/spec-query.json` returns exactly 1 match
    - `grep "data-service:8000/llm/generate" n8n/workflows/spec-update.json` returns exactly 1 match
    - Each workflow's HTTP node bodyParametersJson contains `"provider\": null` and `"model\": null`
    - Each workflow sends `prompt` field (not Ollama-specific fields like "stream", "keep_alive", "options")
    - All 3 JSON files are valid JSON (python -c "import json; json.load(open('file'))" succeeds)
    - Any downstream Function node reads `$json.text` instead of `$json.response`
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts This Phase Produces

All modified workflow files in this plan:

### n8n/workflows/rules-to-metagraph.json

| Change | Node | Description |
|--------|------|-------------|
| URL | "Ollama Generate" (HTTP node) | `ollama_url`/api/generate -> `http://data-service:8000/llm/generate` |
| Body | "Ollama Generate" (HTTP node) | Ollama-format body replaced with `{provider, model, prompt, system}` gateway contract |
| Response field | "Parse LLM Output" (Function node) | `$json.response` -> `$json.text` for text extraction; `raw.response` -> `raw.text` in fallback path |
| Response field | "Ollama Generate" -> downstream flow | Generated text now in `$json.text` instead of `$json.response` |

### n8n/workflows/graph-query-mcp.json

| Change | Node | Description |
|--------|------|-------------|
| URL | Cypher generation HTTP node | `ollama_url`/api/generate -> `http://data-service:8000/llm/generate` |
| URL | Answer generation HTTP node | `ollama_url`/api/generate -> `http://data-service:8000/llm/generate` |
| Body | Both HTTP nodes | Ollama-format body replaced with gateway contract |
| Response field | Downstream Function nodes | `$json.response` -> `$json.text` for any response parsing |

### n8n/workflows/spec-ingest.json

| Change | Node | Description |
|--------|------|-------------|
| URL | HTTP node | `http://ollama:11434/api/generate` -> `http://data-service:8000/llm/generate` |
| Body | HTTP node | Ollama format -> gateway contract |
| Response | Downstream node | `$json.response` -> `$json.text` |

### n8n/workflows/spec-query.json

| Change | Node | Description |
|--------|------|-------------|
| URL | HTTP node | Same URL/body/response changes as spec-ingest.json |

### n8n/workflows/spec-update.json

| Change | Node | Description |
|--------|------|-------------|
| URL | HTTP node | Same URL/body/response changes as spec-ingest.json |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n -> data-service (http) | n8n calls gateway HTTP endpoint -- no auth between containers (Docker internal network) |
| n8n workflow JSON files | Workflow definitions stored on disk with no LLM credentials -- only gateway URL reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | n8n workflow execution logs | MEDIUM | accept | n8n may log request/response bodies from the gateway HTTP node. Gateway responses never contain raw API keys (only `{text, provider, model, usage}`). The `provider: null` tag sent by n8n contains no credentials. |
| T-02-02 | Tampering | n8n workflow JSON file edits | LOW | accept | Workflow files are version-controlled; changes are reviewed before merge. Only the URL, body, and response field references are modified -- no new Function node code that could introduce logic errors. |
| T-02-SC | Tampering | n8n workflow node configuration | MEDIUM | accept | Workflow files reference the internal Docker service name `data-service:8000` which is only accessible within the Docker network. No external exposure. |
</threat_model>

<verification>
## Verification

1. `grep -r "ollama:11434" n8n/workflows/ | wc -l` -- returns 0 (zero remaining direct Ollama calls)
2. `grep -r "data-service:8000/llm/generate" n8n/workflows/ | wc -l` -- returns 6 (one per Ollama node replaced)
3. `python -c "import json; [json.load(open(f'n8n/workflows/{f}')) for f in ['rules-to-metagraph.json','graph-query-mcp.json','spec-ingest.json','spec-query.json','spec-update.json']]; print('All 5 workflows valid JSON')"`
4. In each workflow: verify the HTTP node URL references `data-service:8000/llm/generate` and body contains `provider` and `model` keys
5. In rules-to-metagraph.json: verify the Parse LLM Output function node reads `$json.text` instead of `$json.response`
</verification>

<success_criteria>
## Success Criteria

1. `grep -c "ollama:11434" n8n/workflows/*.json` returns "0" for every workflow file
2. `grep -c "data-service:8000/llm/generate" n8n/workflows/*.json` returns the node count per file (1 for most, 2 for graph-query-mcp)
3. Each HTTP node body carries `{provider: null, model: null}` per D-07 -- gateway resolves active provider from settings
4. Response parsing nodes extract generation text from `.text` instead of `.response`
5. All 5 workflow JSON files remain structurally valid (parseable as JSON)
6. The 3 spec workflows are consistent in their body format and response parsing patterns
</success_criteria>

<output>
Create `.planning/milestones/v9.0-phases/01-cloud-llm-connector/01-02-SUMMARY.md` when done
</output>