---
phase: 14-graph-schema-v4-propagation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DG/src/DG.Core/Data/Neo4jRuleRepository.cs
  - test/smoke_rules_ingest.sh
  - test/smoke_graph_query.sh
  - test/seed_designstates.cypher
autonomous: true
requirements: [SCHM-09, SCHM-10, SCHM-13]
nyquist_compliant: true

must_haves:
  truths:
    - "Neo4jRuleRepository RulesQuery coalesces new-before-old on all three renamed props (SWRL, RuleName, RuleDescription) so both v4 and the 5 live v3 Rule nodes read correctly (D-06)"
    - "The C# unit suite still passes after the coalesce edit"
    - "test/smoke_rules_ingest.sh POSTs a live rule and asserts the written Neo4j Rule node carries .SWRL (Wave 0 harness for SCHM-09)"
    - "test/smoke_graph_query.sh POSTs a NL query and asserts the generated Cypher references r.SWRL not r.text (Wave 0 harness for SCHM-10)"
    - "test/seed_designstates.cypher inserts a realistic mix of v3-kind DesignStates — some Run-linked, at least one orphaned (Wave 0 precondition for SCHM-13)"
  artifacts:
    - "DG/src/DG.Core/Data/Neo4jRuleRepository.cs (extended coalesce)"
    - "test/smoke_rules_ingest.sh"
    - "test/smoke_graph_query.sh"
    - "test/seed_designstates.cypher"
  key_links:
    - "The two smoke scripts are consumed by 14-04's live-smoke verify; the seed script is consumed by 14-06's migration demo — they MUST exist before those waves run"
  prohibitions:
    - "Coalesce argument order must be new-before-old (SWRL before swrl before text) — never old-before-new (RESEARCH Pitfall 2)"

security: "No new threat surface — one read-side C# patch plus three test/harness scripts. The seed script intentionally creates v3-kind nodes for the migration demo; it is dev-only test fixture data. See 14-06 threat model (T-14-MIG)."
---

<objective>
Deliver the interim read-side backward-compat patch (D-06) and the three Nyquist Wave 0 scripts the later waves depend on. This plan builds the verification harness so SCHM-09 (live ingest), SCHM-10 (live query), and SCHM-13 (migration demo) become live-provable, and keeps current Grasshopper validation reads alive under the SWRL/RuleName rename.

Purpose: Under the full Rule-property rename, a new v4 rule has `r.SWRL` but no `r.text`; the existing lowercase coalesce would read empty for new rules. Extending the coalesce (D-06) keeps interim reads working before Phases 17/18 rebuild the readers. The Wave 0 scripts exist here so downstream waves are not blocked.
Output: patched `Neo4jRuleRepository.cs` (compiles, tests green) + three executable `test/` scripts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-CONTEXT.md
@.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-RESEARCH.md
@.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Neo4jRuleRepository coalesce for the renamed Rule props (D-06)</name>
  <files>DG/src/DG.Core/Data/Neo4jRuleRepository.cs</files>
  <read_first>
    - DG/src/DG.Core/Data/Neo4jRuleRepository.cs (the RulesQuery constant, lines ~23-35)
    - 14-RESEARCH.md § "Artifact 8: Neo4jRuleRepository.cs" (exact line-accurate targets)
  </read_first>
  <action>
    In the RulesQuery Cypher constant, extend the three coalesce chains, always listing the new PascalCase property first (D-06):
    - name line (~27): `coalesce(r.title, r.name, r.Rule_Id)` becomes `coalesce(r.RuleName, r.title, r.name, r.Rule_Id)`
    - swrl line (~31): `coalesce(r.swrl, r.text, '')` becomes `coalesce(r.SWRL, r.swrl, r.text, '')`
    - description line (~28): `coalesce(r.description, '')` becomes `coalesce(r.RuleDescription, r.description, '')` (RESEARCH-recommended additive third line, completes the same backward-compat contract enabled by the n8n RuleDescription backfill)
    Do not change the returned column aliases (name/swrl/description) — downstream C# mapping is unchanged. This is a compiled change: after editing, a `dotnet build` of DG.Core is implied.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -q "coalesce(r.SWRL, r.swrl, r.text" DG/src/DG.Core/Data/Neo4jRuleRepository.cs && grep -q "coalesce(r.RuleName, r.title" DG/src/DG.Core/Data/Neo4jRuleRepository.cs && echo PASS</automated>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; DOTNET_ROLL_FORWARD=LatestMajor dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~Neo4jRuleRepositoryVariableKindTests"</automated>
  </verify>
  <acceptance_criteria>
    - All three coalesce chains list the new PascalCase property first.
    - Filtered xUnit suite passes (DOTNET_ROLL_FORWARD=LatestMajor per STATE.md — net9.0 runtime absent, SDK 10 installed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the two live-smoke scripts (Wave 0 harness for SCHM-09 / SCHM-10)</name>
  <files>test/smoke_rules_ingest.sh, test/smoke_graph_query.sh</files>
  <read_first>
    - 14-VALIDATION.md § "Wave 0 Requirements" + "Per-Task Verification Map" (the exact curl targets and assertions)
    - 14-RESEARCH.md § "Runtime State Inventory" (n8n import:workflow gotcha; Ollama cold-start latency Pitfall 3)
  </read_first>
  <action>
    Create two bash scripts (style consistent with existing test/test_phase04_update_flow.sh).
    smoke_rules_ingest.sh: POST a rule to http://localhost:8080/n8n/webhook/dg/rules-ingest with body {"rules_text":"Maximum height of each Warehouse must be 40 m.","project_name":"phase14-smoke"}; allow a long timeout / retry for Ollama cold start (40-70s, not a failure); then query Neo4j at /neo4j/db/neo4j/tx/commit for the written Rule node and assert it carries a non-empty .SWRL property. Exit non-zero with a clear message if .SWRL is absent (the tell that the old workflow definition is still active — see re-import note below).
    smoke_graph_query.sh: POST {"prompt_text":"List all rules with their SWRL text","project_name":"phase14-smoke"} to /n8n/webhook/dg/graph-query and assert the generated Cypher in the response references r.SWRL (and not exclusively r.text). 
    Add a header comment in BOTH scripts documenting the mandatory n8n re-import step these scripts assume has run: `docker exec -it n8n n8n import:workflow --input=/files/workflows/<file>.json` — and the top-level `id`-field workaround (inject an id into a scratch copy, docker cp it in, `n8n update:workflow --active=true`, `docker restart n8n`) per RESEARCH. These scripts VERIFY v4; they do not perform the re-import.
    Make both scripts executable (chmod +x).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; test -f test/smoke_rules_ingest.sh && test -f test/smoke_graph_query.sh && grep -q "rules-ingest" test/smoke_rules_ingest.sh && grep -q "graph-query" test/smoke_graph_query.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both scripts exist, are executable, and target the correct webhook endpoints.
    - Each asserts on r.SWRL presence in the ingested node / generated query.
    - Both carry the n8n re-import gotcha header comment.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create the DesignState seeding script (Wave 0 precondition for SCHM-13)</name>
  <files>test/seed_designstates.cypher</files>
  <read_first>
    - 14-RESEARCH.md § "Runtime State Inventory" (dev DB holds ZERO DesignState nodes — confirmed live) + Pitfall 5
    - migrations/2026-06-23_var_project_merge_key.cypher (header/comment style to mirror)
  </read_first>
  <action>
    Create a Cypher seed script that inserts a realistic mix of pre-v4 DesignState nodes so the 14-06 migration exercises rename + layer-move + orphan-delete on non-trivial data (dev DB currently has zero). Include a header comment (PURPOSE / EXECUTION METHOD via cypher-shell or Neo4j Browser / dev-databases-only) mirroring the existing migration file. Seed, all at graph='Metagraph', project 'phase14-smoke':
    - 2-3 DesignState nodes with the pre-v4 parameter-kind value, each linked from a fake Run via `(:Run)-[:VALIDATES]->(:DesignState)`.
    - 1-2 DesignState nodes with the pre-v4 object-kind value, Run-linked.
    - at least ONE orphaned DesignState (a pre-v4-kind node with NO incoming Run link) so the orphan-DELETE path (D-10) has a target.
    Use the same kind literals the v3 template used (the two pre-v4 state kinds) so the migration's rename step has real input. Include a trailing VERIFICATION comment query counting seeded nodes.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; test -f test/seed_designstates.cypher && grep -q "DesignState" test/seed_designstates.cypher && grep -q "VALIDATES" test/seed_designstates.cypher && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Seed file exists, creates Run-linked and at least one orphaned DesignState at graph='Metagraph'.
    - Uses the pre-v4 kind literals and the (:Run)-[:VALIDATES]->(:DesignState) relationship.
    - Carries a dev-only header comment mirroring the migration-file pattern.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
New files: `test/smoke_rules_ingest.sh`, `test/smoke_graph_query.sh`, `test/seed_designstates.cypher`. Edited: `DG/src/DG.Core/Data/Neo4jRuleRepository.cs` (extended coalesce, no new symbols). Convention introduced: the Run→DesignState relationship type `:VALIDATES` (direction Run→DesignState), reused by 14-01 schema doc and 14-06 migration.
</artifacts_produced>

<verification>
- Coalesce chains new-before-old on SWRL/RuleName/RuleDescription; filtered xUnit green.
- Three Wave 0 scripts present and targeting the right endpoints/labels.
</verification>

<success_criteria>
The read-side survives the rename (D-06) and the three Nyquist Wave 0 scripts exist before Waves 2/3 consume them. Delivers the live-verification harness for SCHM-09/SCHM-10/SCHM-13.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-03-SUMMARY.md` when done.
</output>
