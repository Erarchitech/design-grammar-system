---
phase: 14-graph-schema-v4-propagation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - data-service/app.py
  - DG/src/DG.Core/Services/ValidationRunsQueryService.cs
  - DG/tests/DG.Tests/E2E/DesignStateValidationFlowTests.cs
autonomous: true
requirements: [SCHM-12]
nyquist_compliant: true

must_haves:
  truths:
    - "data-service store_validation_run adds run.ValidStatus and run.SendStatus alongside the existing run.status='completed' (which is untouched — job lifecycle, NOT the removed pass/fail Status per D-01/D-02)"
    - "get_validation_run and list_validation_runs RETURN clauses surface run.ValidStatus / run.SendStatus"
    - "The runtime layer literal is renamed ValidGraph everywhere in code: data-service VALIDATION_GRAPH constant, C# ValidationRunsQueryService constant, and the 5 E2E-test occurrences (D-14 code half)"
  artifacts:
    - "data-service/app.py (ValidStatus/SendStatus props + layer-literal rename)"
    - "DG/src/DG.Core/Services/ValidationRunsQueryService.cs (layer-literal rename)"
    - "DG/tests/DG.Tests/E2E/DesignStateValidationFlowTests.cs (5 literal renames)"
  key_links:
    - "This is the CODE half of D-14; the DATA half (1169-node migration) is 14-06, which depends on this plan so code queries ValidGraph before data flips to it"
  prohibitions:
    - "Do not delete or repurpose run.status='completed' (RESEARCH Pitfall 4 — distinct from the removed Run.Status text field)"
    - "No stale runtime layer literal (the old ValidationGraph value) remains in data-service/, DG/src, or DG/tests code"
    - "Do not compute full per-ObjState index-matched ValidStatus population here — that is Phase 18 (GHVL-05); this plan does schema presence only"

security: "No new threat surface at the code level — const renames + additive properties. The coordinated data migration and its risk live in 14-06 (T-14-MIG). This plan's verification is static (grep + build), NOT a live data query — the live before/after proof belongs to 14-06."
---

<!-- planner-discipline-allow: ValidationGraph -->

<objective>
Align the data-service (`app.py`) with the v4 Run schema — add `ValidStatus`/`SendStatus` as schema-present properties — and perform the CODE half of D-14: rename the runtime layer literal from the old ValidationGraph value to `ValidGraph` across `app.py`, the C# `ValidationRunsQueryService`, and the E2E test. The DATA half (migrating 1169 live nodes) is 14-06, which depends on this plan.

Purpose: app.py has zero `:Rule` queries (confirmed), so SCHM-12's real scope is the Run validation properties plus the D-14 literal consolidation so future DesignState writes (Phase 18) land in the same partition as Run.
Output: app.py + two C# files updated; verified statically (grep + build). Live before/after data proof is deferred to 14-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-CONTEXT.md
@.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-RESEARCH.md
@ontology/port-iri-map-V7.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ValidStatus/SendStatus to the data-service Run write and reads (SCHM-12)</name>
  <files>data-service/app.py</files>
  <read_first>
    - data-service/app.py lines 380-620 (store_validation_run, get_validation_run, list_validation_runs) + line 41 (VALIDATION_GRAPH const)
    - 14-RESEARCH.md § "Artifact 7: app.py" (exact scope, Pitfall 4, Open Question 2 defaults)
  </read_first>
  <action>
    Per D-01 / RESEARCH Artifact 7 — schema presence only, no Phase-18 binding logic:
    1. In store_validation_run, add two new properties to the ValidationRun MERGE/SET: `run.SendStatus` set true on a successful publish (directly knowable today), and `run.ValidStatus` as schema-present (an empty list default or best-effort from the already-available passed/failed data — do NOT build per-ObjState index matching; that is Phase 18). Leave `run.status = 'completed'` EXACTLY as-is (job lifecycle, not the removed pass/fail Status — RESEARCH Pitfall 4).
    2. In get_validation_run and list_validation_runs, add `run.ValidStatus AS validStatus, run.SendStatus AS sendStatus` to the RETURN clauses so the read side surfaces the new fields.
    3. Update the prose comment blocks (~lines 161-164, 526-529) that mention the old v3 state-kind vocabulary to the v4 kind names, for doc accuracy.
    Add a SUMMARY note that full per-ObjState ValidStatus population is Phase 18 (GHVL-05).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -q "ValidStatus" data-service/app.py && grep -q "SendStatus" data-service/app.py && grep -q "run.status = 'completed'" data-service/app.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - store_validation_run sets ValidStatus + SendStatus; run.status='completed' untouched.
    - get_validation_run and list_validation_runs RETURN the two new fields.
    - Old-kind vocabulary comments updated to v4 names.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename the runtime layer literal to ValidGraph in code (D-14 code half)</name>
  <files>data-service/app.py, DG/src/DG.Core/Services/ValidationRunsQueryService.cs, DG/tests/DG.Tests/E2E/DesignStateValidationFlowTests.cs</files>
  <read_first>
    - 14-CONTEXT.md D-14 (the exact three code locations + line numbers)
    - 14-RESEARCH.md § "Critical Finding" (RESOLVED — Option B / D-14) and "Artifact 7"
  </read_first>
  <action>
    Rename the runtime graph-property literal from the old ValidationGraph value to `ValidGraph` (D-14, Option B) — the VALUE string only, NOT any node label:
    1. data-service/app.py line ~41: the `VALIDATION_GRAPH` constant value becomes `"ValidGraph"`.
    2. DG/src/DG.Core/Services/ValidationRunsQueryService.cs line ~15: the private const value becomes `"ValidGraph"`.
    3. DG/tests/DG.Tests/E2E/DesignStateValidationFlowTests.cs: the 5 hardcoded graph-literal occurrences (lines ~78, 127, 187, 204, 221) become `ValidGraph`.
    Do NOT rename the ValidationRun / ValidationEntity / IntegrationConfig node LABELS (D-14 caveat — label renames are out of scope). After this, no code under data-service/, DG/src, or DG/tests should contain the old layer-value literal. This is coordinated with the 14-06 data migration (which flips the 1169 live nodes and restarts the service); this plan only changes source. A `dotnet build` of DG.Core + DG.Tests is implied.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -rq "ValidGraph" data-service/app.py DG/src/DG.Core/Services/ValidationRunsQueryService.cs && ! grep -rIl "ValidationGraph" data-service/app.py DG/src/DG.Core/Services/ValidationRunsQueryService.cs DG/tests/DG.Tests/E2E/DesignStateValidationFlowTests.cs && echo PASS</automated>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; DOTNET_ROLL_FORWARD=LatestMajor dotnet build DG/src/DG.Core/DG.Core.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - All three files use the ValidGraph literal; zero occurrences of the old layer value remain in code.
    - Node labels unchanged; DG.Core builds clean.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- app.py: ValidStatus/SendStatus present in write + reads; run.status='completed' intact.
- Layer literal is ValidGraph across the three code files; old value gone; DG.Core builds.
- No live data query here — 14-06 owns the coordinated restart + migration + before/after proof.
</verification>

<success_criteria>
data-service carries the v4 Run validation properties and the runtime layer literal is renamed to ValidGraph in code, ready for 14-06 to migrate the data. Satisfies SCHM-12 and the code half of D-14.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-05-SUMMARY.md` when done.
</output>
