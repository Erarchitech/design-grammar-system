---
phase: 14-graph-schema-v4-propagation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - graph-viewer/config.template.js
  - graph-viewer/index.html
autonomous: false
requirements: [SCHM-11]
nyquist_compliant: true

must_haves:
  truths:
    - "NeoVis visGroups renders three state kinds — ParamState (blue), ObjState (orange), PropState (new mint-teal) — keyed by kind VALUE (D-12)"
    - "config.template.js has a single DatatypeProperty entry; the duplicate DataProperty entries in labels + visGroups are gone (D-11)"
    - "index.html hardcoded rule-list Cypher (line ~1576) reads coalesce(r.SWRL, r.text, '') so the 5 live v3-shaped Rule nodes still display (D-13)"
  artifacts:
    - "graph-viewer/config.template.js (reconciled labels/visGroups + PropState color)"
    - "graph-viewer/index.html (rule-list Cypher coalesce)"
  key_links:
    - "visGroups is keyed by kind VALUES (DesignState uses group:'kind'), not by label — the three new keys must be the kind strings, not 'DesignState'"
  prohibitions:
    - "No old state-kind visGroups keys remain in graph-viewer/ (the two renamed-from kinds must be fully replaced by ParamState/ObjState)"
    - "No DataProperty label/visGroups entry remains (reconciled to DatatypeProperty per D-11)"

security: "No new threat surface — client config + one hardcoded read query. See 14-06 threat model (T-14-MIG)."
---

<!-- planner-discipline-allow: DefState -->
<!-- planner-discipline-allow: ObjectState -->
<!-- planner-discipline-allow: DataProperty -->

<objective>
Bring the NeoVis visualization config (`graph-viewer/config.template.js`) and the one hardcoded client Cypher read (`graph-viewer/index.html`) to v4: three state-kind colors, the DatatypeProperty/DataProperty duplicate reconciled, and the rule-list query coalescing the renamed SWRL property.

Purpose: The graph-viewer is the human-facing check that v4 nodes render correctly. Reconciling the duplicate label (D-11) and adding the PropState color (D-12) closes SCHM-11's visual gap.
Output: v4 `config.template.js` and `index.html` with a live browser color check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-CONTEXT.md
@.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-RESEARCH.md
@ontology/port-iri-map-V7.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile NeoVis config.template.js labels, visGroups, and state-kind colors</name>
  <files>graph-viewer/config.template.js</files>
  <read_first>
    - graph-viewer/config.template.js (full, 61 lines — current DefState/ObjectState/DataProperty entries)
    - 14-RESEARCH.md § "Artifact 5: config.template.js" (exact line targets + PropState hex recommendation)
    - graph-viewer/entrypoint.sh (confirm the sed injection only rewrites connection-string keys, NOT labels/visGroups — RESEARCH gotcha)
  </read_first>
  <action>
    Edit only the `labels` and `visGroups` blocks; leave connection-string keys and `relationships` untouched. Per D-11 and D-12:
    1. `labels` block: delete the redundant `DataProperty: { label: "SWRL_label" }` line (line ~22). Keep the `DatatypeProperty` line — the DB keeps the `DatatypeProperty` node label; no node carries the removed label.
    2. `visGroups` block: delete the redundant `DataProperty` color entry (line ~45).
    3. `visGroups`: replace the two old state-kind keys with the v4 kind values — rename the blue-keyed entry to `ParamState` (keep colors `#a8d8ea`/`#6fb3cf`, inherited from the old blue per D-12) and the orange-keyed entry to `ObjState` (keep colors `#f4a261`/`#d4823a`, inherited from the old orange).
    4. Add a new `PropState: { color: { background: "#7bcfc0", border: "#4fa696" } }` entry — a mint-teal chosen (Claude's Discretion D-12) to sit distinct from Class green `#78c38a` and KnowledgeNote teal `#4ecdc4`.
    5. Update the explanatory comment above the state-kind entries so it names the v4 kind trio (ParamState / ObjState / PropState) and still explains that DesignState uses `group:"kind"` so these keys are kind VALUES, not labels. Do NOT leave either old kind name in the comment text (a repo grep gate asserts zero occurrences in graph-viewer/).
    Note: `labels.DesignState { label: "StateId", group: "kind" }` stays unchanged — the group-by-kind mechanism already supports three kinds without edit.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -q "ParamState" graph-viewer/config.template.js && grep -q "PropState" graph-viewer/config.template.js && grep -q "7bcfc0" graph-viewer/config.template.js && grep -vq "DataProperty" graph-viewer/config.template.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - visGroups contains ParamState, ObjState, PropState keyed by kind value; PropState uses #7bcfc0.
    - No DataProperty entry remains in labels or visGroups; DatatypeProperty stays.
    - grep for the two old kind names in graph-viewer/config.template.js returns zero.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Coalesce the renamed SWRL property in index.html rule-list Cypher</name>
  <files>graph-viewer/index.html</files>
  <read_first>
    - graph-viewer/index.html around lines 1437-1600 (the buildCypher + executeCypher rule-list query)
    - 14-RESEARCH.md § "Artifact 6: index.html" (only line ~1576 needs a change; line 1448 needs none)
  </read_first>
  <action>
    Locate the hardcoded rule-list query near line 1576 that currently returns `coalesce(r.text, '') AS text`. Change it to `coalesce(r.SWRL, r.text, '') AS text` — new property first, old property fallback, per the phase-wide backward-compat coalesce pattern (D-13). This keeps the 5 live v3-shaped Rule nodes (which have only `.text`) displaying while new v4 nodes surface their `.SWRL`. Do NOT touch the `buildCypher("MetaGraph")` NeoVis view builder (line ~1448 — it matches Rule subgraphs, references no renamed prop) or the KnowledgeGraph/OntoGraph branches (Phase 15 / out of scope). Verify coalesce argument order is new-before-old (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system"; grep -q "coalesce(r.SWRL, r.text" graph-viewer/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The rule-list query returns coalesce(r.SWRL, r.text, '') with new property first.
    - No other index.html Cypher was altered.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser check — three state-kind colors + rule-list survive the rename</name>
  <action>Human verification gate. Rebuild the UI with --no-cache, hard-refresh the browser, and confirm the three state-kind colors render distinctly, no DataProperty group artifact appears, and existing v3 rules still list. Exact steps in how-to-verify.</action>
  <what-built>Rebuilt the graph-viewer container with the v4 NeoVis config (three state-kind colors, DataProperty duplicate removed) and the coalesced rule-list query.</what-built>
  <how-to-verify>
    1. Rebuild UI (Docker layer cache gotcha — --no-cache required):
       `docker compose build --no-cache design-grammars && docker compose up -d design-grammars`
    2. Hard-refresh http://localhost:8080 (Ctrl+Shift+R) or open incognito to defeat browser cache.
    3. Load a graph view that includes DesignState nodes of each kind (if none exist yet in the dev DB, this can be confirmed after 14-06 seeds them, or by temporarily inspecting the config legend). Confirm ParamState renders blue, ObjState orange, PropState mint-teal, and no DataProperty group artifact appears.
    4. Confirm the rule list still shows SWRL text for the 5 existing v3 rules (they have only r.text).
  </how-to-verify>
  <resume-signal>Type "approved" or describe color/legend/rule-list issues.</resume-signal>
</task>

</tasks>

<artifacts_produced>
No new files. Two files edited: `graph-viewer/config.template.js` (adds one new visGroups key `PropState` with hex `#7bcfc0`/`#4fa696`) and `graph-viewer/index.html`. New config value introduced: the PropState mint-teal color pair.
</artifacts_produced>

<verification>
- config.template.js: ParamState/ObjState/PropState present; no DataProperty; PropState hex #7bcfc0.
- index.html: rule-list Cypher uses coalesce(r.SWRL, r.text, '').
- Browser: three kind colors distinct; existing v3 rules still list.
</verification>

<success_criteria>
NeoVis renders all three state kinds with distinct colors, the DatatypeProperty/DataProperty duplicate is reconciled, and the client rule-list read survives the SWRL rename. Satisfies SCHM-11.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/14-graph-schema-v4-propagation/14-02-SUMMARY.md` when done.
</output>
