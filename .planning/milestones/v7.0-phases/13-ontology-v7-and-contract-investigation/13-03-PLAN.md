---
phase: 13-ontology-v7-and-contract-investigation
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified: [ontology/port-iri-map-V7.md]
autonomous: true
requirements: [ONTO-06]
user_setup: []

must_haves:
  truths:
    - "port-iri-map-V7.md contains a single master table with columns Component, Port, Direction, V7 IRI, Layer, Notes"
    - "All 14 GH_DesignGrammars components are present: CONNECTOR, GRAPH DECONSTRUCT, METAGRAPH, ONTOGRAPH, VALIDATION GRAPH, OBJECT STATE, PARAMETER STATE, PROPERTY STATE, DESIGN STATE, DESIGN STATE DECONSTRUCT, OBJECT DECONSTRUCT, PARAMETER REINSTATE, RULE DECONSTRUCT, VALIDATOR"
    - "Every output port (and every IRI-carrying input) of all 14 components resolves to a V7 IRI — zero unmapped references"
    - "Every V7 IRI cited in the map exists in DesignGrammar-V7.owl (or is explicitly annotated as a runtime-DB label with no ontology IRI, per the ontology<->DB documentation policy)"
    - "CLASSIFICATOR and VALIDATION RUNS do not appear as current components"
  artifacts:
    - path: "ontology/port-iri-map-V7.md"
      provides: "The ONTO-06 component-port -> ontology-IRI contract for all 14 V7 components; successor of GH-mapping.png; greppable by phases 14/17/18"
      contains: "V7 IRI"
      min_lines: 60
  key_links:
    - from: "ontology/port-iri-map-V7.md"
      to: "ontology/DesignGrammar-V7.owl"
      via: "each port's V7 IRI is grep-checked to resolve to an entity in the generated V7 owl"
      pattern: "Ontograph|Validgraph|ObjState|Run"
    - from: "ontology/port-iri-map-V7.md"
      to: "ontology/GH_DesignGrammars.pdf"
      via: "the component/port inventory is transcribed from the 14-component PDF schema (source of truth for ports)"
      pattern: "VALIDATOR|VALIDATION GRAPH"
    - from: "ontology/port-iri-map-V7.md"
      to: "ontology/V7-INVESTIGATION.md"
      via: "IRI names come from the locked V6->V7 rename table; DesignState/Run/ValidStatus wiring follows conflicts (a)/(b)"
      pattern: "ValidStatus|DesignState"
---

<objective>
Produce `ontology/port-iri-map-V7.md` — the machine-greppable master table mapping every port of all 14 GH_DesignGrammars components to its V7 ontology IRI and layer. Replaces the non-machine-readable `GH-mapping.png`.

Purpose: This is ONTO-06 and the last piece of the locked contract. Phases 14 (schema), 17 (graph-access components), and 18 (validator) grep this file to wire component ports to the right IRIs. "Zero unmapped output ports" is a Phase 13 success criterion — a missing mapping here becomes a runtime wiring bug three phases later.
Output: `ontology/port-iri-map-V7.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v7.0-phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md
@ontology/V7-INVESTIGATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 14-component port -> V7 IRI master table</name>
  <files>ontology/port-iri-map-V7.md</files>
  <read_first>ontology/GH_DesignGrammars.pdf, ontology/V7-INVESTIGATION.md, ontology/DesignGrammar-V7.owl, .planning/REQUIREMENTS.md</read_first>
  <action>
Create `ontology/port-iri-map-V7.md`: a header (title; "Successor of GH-mapping.png"; "Source of truth for ports: ontology/GH_DesignGrammars.pdf; IRI names: ontology/V7-INVESTIGATION.md rename table"; note that per PROJECT.md the Neo4j DB keeps existing labels and this file is where V7 IRIs meet DB labels), then a SINGLE master markdown table:

`Component | Port | Direction (in/out) | V7 IRI | Layer | Notes`

Read the PDF for each component's exact port list. Cover all 14 components (grouped by section but in one table), mapping every OUTPUT port and every input that carries an IRI/handle:
- **Graph access (GHGA):** CONNECTOR (out: Database handle), GRAPH DECONSTRUCT (out: Metagraph/Ontograph/ValidGraph/SpecGraph handles), METAGRAPH (out: Rules, Objects), ONTOGRAPH (out: Class, ObjProperties, DataProperties), VALIDATION GRAPH (out: Run, Status/ValidStatus, DesignState).
- **State (GHST):** OBJECT STATE (out: ObjState), PARAMETER STATE (out: ParamState), PROPERTY STATE (out: PropState), DESIGN STATE (out: DesignState), DESIGN STATE DECONSTRUCT (out: ObjState/ParamState/PropState lists), OBJECT DECONSTRUCT (out: Object/Geometry/Label), PARAMETER REINSTATE (out: Parameters, StateStatus/ReStatus).
- **Rules & validation (GHVL):** RULE DECONSTRUCT (out: Objects, DataProperties, Rule, SWRL, RuleName, RuleDescription), VALIDATOR (out: ValidStatus, RuleName, RuleDescription, SendStatus + non-overlapping extras).

Use the LOCKED V7 names from the rename table for every IRI (Ontograph/Validgraph/Computgraph, ObjProperty, DataProperty, Run, ObjState/ParamState/PropState, ReStatus, SWRL/RuleName/RuleDescription, SendStatus/ValidStatus). For VALIDATION GRAPH's Status output, map it to the unified `ValidStatus` per conflict (a) and note "read-back of Run.ValidStatus (per-object Boolean list)". For any port whose value is a runtime-only DB construct with no ontology IRI (e.g. a Neo4j Database handle, or DB-only labels not renamed in the ontology), put the concrete DB label in the V7 IRI column and mark Notes "runtime/DB — no ontology IRI (ontology<->DB documented, not enforced)". No port may be left blank — "zero unmapped references" (ROADMAP SC3).
  </action>
  <verify>grep -c "|" ontology/port-iri-map-V7.md (table rows present); for each component name, grep -c returns >=1</verify>
  <acceptance_criteria>
    - port-iri-map-V7.md has one master table with columns Component / Port / Direction / V7 IRI / Layer / Notes
    - All 14 component names appear (CONNECTOR ... VALIDATOR) and CLASSIFICATOR/VALIDATION RUNS do not appear as current components
    - Every output port of every component has a non-empty V7 IRI cell (runtime/DB ports annotated, not blank)
    - IRI names match the locked rename table (Ontograph/Validgraph, ObjProperty, DataProperty, Run, ObjState/ParamState/PropState, ReStatus, SWRL/RuleName/RuleDescription, SendStatus/ValidStatus)
    - VALIDATION GRAPH Status maps to ValidStatus with a per-object-Boolean-list note
  </acceptance_criteria>
  <done>The master table covers all 14 components with every output port mapped to a V7 IRI or annotated DB label.</done>
</task>

<task type="auto">
  <name>Task 2: Verify every ontology IRI in the map resolves in DesignGrammar-V7.owl</name>
  <files>ontology/port-iri-map-V7.md</files>
  <read_first>ontology/port-iri-map-V7.md, ontology/DesignGrammar-V7.owl</read_first>
  <action>
For every row whose V7 IRI is an ontology entity (not a runtime/DB-annotated one), confirm the local name resolves in `DesignGrammar-V7.owl` (grep the owl for the class/property local name). Any IRI that does not resolve is either (i) a typo against the rename table — fix the map, or (ii) a genuinely missing ontology entity that 13-02 should have created — STOP and flag it as a 13-02 gap (do not paper over it in the map). Add a short "## Resolution check" footer to port-iri-map-V7.md summarizing: N ontology IRIs, all resolved in DesignGrammar-V7.owl; M runtime/DB ports annotated. This footer is the evidence for ROADMAP SC3 ("every output port resolves to a V7 IRI — zero unmapped references").
  </action>
  <verify>Spot-grep 6+ distinct IRIs from the map against DesignGrammar-V7.owl (e.g. Ontograph, Validgraph, ObjState, ParamState, PropState, Run, ValidStatus) — each returns >=1</verify>
  <acceptance_criteria>
    - Every ontology-IRI row's local name is found in DesignGrammar-V7.owl
    - Rows that cannot resolve are corrected (typo) or escalated as a 13-02 gap — none silently left unresolved
    - A "## Resolution check" footer records the counts (ontology IRIs resolved / runtime-DB ports annotated)
  </acceptance_criteria>
  <done>Every ontology IRI in the port map is confirmed to exist in DesignGrammar-V7.owl; the resolution check is recorded.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f ontology/port-iri-map-V7.md`
- [ ] All 14 component names present; no CLASSIFICATOR/VALIDATION RUNS as current
- [ ] No output-port row has an empty V7 IRI cell
- [ ] Spot-checked ontology IRIs (Ontograph, Validgraph, ObjState, ParamState, PropState, Run, ValidStatus) all resolve in DesignGrammar-V7.owl
- [ ] "## Resolution check" footer present with counts
</verification>

<success_criteria>
- All tasks completed
- ontology/port-iri-map-V7.md maps every output port of all 14 components to a V7 IRI (or annotated DB label) with zero unmapped references — satisfying ROADMAP Phase 13 success criteria 3 (port map) and ONTO-06
- Every ontology IRI in the map resolves in DesignGrammar-V7.owl
</success_criteria>

<output>
After completion, create `.planning/milestones/v7.0-phases/13-ontology-v7-and-contract-investigation/13-03-SUMMARY.md`
</output>
