---
phase: 13-ontology-v7-and-contract-investigation
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified: [ontology/apply_v7_extensions.py, ontology/DesignGrammar-BOT-extension-V7.owl, ontology/DesignGrammar-Topologic-extension-V7.owl, ontology/DesignGrammar-standards-extension-V7.owl, ontology/catalog-v001-V7.xml, ontology/make_export_v7.py, ontology/export_to_markdown_v7.py, ontology/make_docs_v7.py, ontology/DesignGrammar-V7.md]
autonomous: true
requirements: [ONTO-05]
user_setup: []

must_haves:
  truths:
    - "Running `python ontology/apply_v7_extensions.py` produces V7 versions of the three extension files (BOT, Topologic, standards facade) with clean self-checks"
    - "catalog-v001-V7.xml maps the DG ontology IRIs to the -V7 physical files and preserves the seven external vocabulary imports"
    - "DesignGrammar-V7.md is regenerated from DesignGrammar-V7.owl and references V7 names/paths (no -V6 paths, no stale layer names)"
    - "All V7 owl files (core + 3 extensions) parse as well-formed XML"
  artifacts:
    - path: "ontology/apply_v7_extensions.py"
      provides: "Extension/facade regen following apply_v6_extensions.py — V7 IRI/name updates + version bump 6.0->7.0"
      contains: "extension-V7.owl"
      min_lines: 60
    - path: "ontology/catalog-v001-V7.xml"
      provides: "OASIS XML catalog mapping DG ontology IRIs to -V7 files plus external imports"
      contains: "DesignGrammar-V7.owl"
      min_lines: 30
    - path: "ontology/DesignGrammar-V7.md"
      provides: "Regenerated human-readable V7 ontology documentation"
      contains: "V7"
      min_lines: 500
  key_links:
    - from: "ontology/catalog-v001-V7.xml"
      to: "ontology/DesignGrammar-V7.owl"
      via: "the DG-core catalog <uri> entry points at DesignGrammar-V7.owl"
      pattern: "DesignGrammar-V7.owl"
    - from: "ontology/apply_v7_extensions.py"
      to: "ontology/DesignGrammar-BOT-extension-V6.owl"
      via: "reads the V6 extension files as source, writes -V7 successors (V6 untouched)"
      pattern: "extension-V6.owl"
    - from: "ontology/DesignGrammar-V7.md"
      to: "ontology/DesignGrammar-V7.owl"
      via: "export_to_markdown_v7.py parses DesignGrammar-V7.owl to render the markdown"
      pattern: "DesignGrammar-V7"
---

<objective>
Regenerate the V7 companion artifacts around `DesignGrammar-V7.owl`: the three extension/facade owl files, the OASIS catalog, and the human-readable markdown docs — each by porting its `*_v6` generator to a `*_v7` generator (never hand-editing generated output).

Purpose: ONTO-05. The ontology bundle must load and parse as a set — Protege/OWL-API resolves imports through the catalog, and the facade carries the cross-vocabulary alignment axioms. This closes Phase 13's "load/parse without errors" success criterion (SC4).
Output: `apply_v7_extensions.py` + 3 `-V7` extension owls, `catalog-v001-V7.xml`, `make_export_v7.py` + `export_to_markdown_v7.py`, `make_docs_v7.py`, `DesignGrammar-V7.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v7.0-phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md
@ontology/V7-INVESTIGATION.md
@ontology/apply_v6_extensions.py
@ontology/make_export_v6.py
@ontology/catalog-v001-V6.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port apply_v6_extensions.py to apply_v7_extensions.py and regenerate the 3 facades</name>
  <files>ontology/apply_v7_extensions.py, ontology/DesignGrammar-BOT-extension-V7.owl, ontology/DesignGrammar-Topologic-extension-V7.owl, ontology/DesignGrammar-standards-extension-V7.owl</files>
  <read_first>ontology/apply_v6_extensions.py, ontology/V7-INVESTIGATION.md, ontology/DesignGrammar-standards-extension-V6.owl</read_first>
  <action>
Create `ontology/apply_v7_extensions.py` mirroring `apply_v6_extensions.py`: read the three `-V6` extension files as SRC, write `-V7` successors, keep the `check()`/`rm_lines()` helpers and the residual-token self-check. Apply the V7 deltas from the rename table that actually occur in these facade files (they reference far fewer entities than the core owl — only apply renames whose source token is present):
- Version bump: `<owl:versionInfo>6.0</owl:versionInfo>` -> `<owl:versionInfo>7.0</owl:versionInfo>` in all three.
- In the standards facade's alignment axioms, rename any referenced entity that changed: ValidationRun->Run, ValidationGraph->Validgraph, and (if present) DatatypeProperty->DataProperty / ObjectProperty->ObjProperty — using the SAME `&dg;`/`dgv:` prefix-anchored, owl-construct-safe replaces as the core script (never touch owl:ObjectProperty/owl:DatatypeProperty). Update prose mentions of renamed layer hubs.
- BOT/Topologic anchor on `dg:Topology` (unchanged in V7) — likely only the version bump applies; confirm by grepping each source for renamed tokens before adding a transform, and keep each `check()` banned-list asserting no stale V6-only token survives.
Run the script; it must write all three `-V7` files and print its clean-exit line.
  </action>
  <verify>cd ontology && python apply_v7_extensions.py && ls DesignGrammar-*-extension-V7.owl | wc -l (==3) && grep -L "versionInfo>6.0" DesignGrammar-*-extension-V7.owl</verify>
  <acceptance_criteria>
    - apply_v7_extensions.py parses as valid Python and reads -V6 extensions / writes -V7 extensions
    - All three DesignGrammar-{BOT,Topologic,standards}-extension-V7.owl are produced
    - Each V7 extension carries versionInfo 7.0 and no renamed-away token (per its check() banned list)
    - owl:ObjectProperty / owl:DatatypeProperty constructs are untouched in the facades
  </acceptance_criteria>
  <done>The three V7 extension/facade owls are generated cleanly by apply_v7_extensions.py.</done>
</task>

<task type="auto">
  <name>Task 2: Write catalog-v001-V7.xml</name>
  <files>ontology/catalog-v001-V7.xml</files>
  <read_first>ontology/catalog-v001-V6.xml</read_first>
  <action>
Create `ontology/catalog-v001-V7.xml` by copying `catalog-v001-V6.xml`'s structure and retargeting the four DG `<uri>` entries to the `-V7` physical files: DesignGrammar-V7.owl (DG-core), DesignGrammar-standards-extension-V7.owl (DG-standards), DesignGrammar-BOT-extension-V7.owl (DG-BOT), DesignGrammar-Topologic-extension-V7.owl (DG-Topologic). Keep the seven external vocabulary `<uri>` imports (BOT, SWRL, PROV-O, SOSA, SHACL, SKOS, DCTERMS, GEOSPARQL) byte-identical. Update the header comment to say "v7.0 bundle" and note the single physical file DesignGrammar-V7.owl holds all sub-namespaces. Leave catalog-v001-V6.xml and catalog-v001.xml untouched (use one catalog at a time).
  </action>
  <verify>python -c "import xml.dom.minidom as m; m.parse('ontology/catalog-v001-V7.xml')" && grep -c "\-V7.owl" ontology/catalog-v001-V7.xml (==4)</verify>
  <acceptance_criteria>
    - catalog-v001-V7.xml is well-formed XML
    - The four DG <uri> entries point at DesignGrammar-V7.owl and the three -extension-V7.owl files
    - The seven external vocabulary imports are preserved
    - catalog-v001-V6.xml is unmodified
  </acceptance_criteria>
  <done>catalog-v001-V7.xml resolves the V7 bundle and preserves external imports.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate DesignGrammar-V7.md via make_export_v7.py / make_docs_v7.py</name>
  <files>ontology/make_export_v7.py, ontology/export_to_markdown_v7.py, ontology/make_docs_v7.py, ontology/DesignGrammar-V7.md</files>
  <read_first>ontology/make_export_v6.py, ontology/make_docs_v6.py, ontology/export_to_markdown_v6.py</read_first>
  <action>
Port the doc-generation chain to V7, following make_export_v6.py's approach of generating the exporter from the prior exporter:
1. `make_export_v7.py`: generate `export_to_markdown_v7.py` from `export_to_markdown_v6.py`, applying V7 changes — file paths DesignGrammar-V6.owl->DesignGrammar-V7.owl and DesignGrammar-V6.md->DesignGrammar-V7.md, plus any layer-name/label strings that changed (OntoGraph->Ontograph, ValidationGraph->Validgraph, ComputationGraph->Computgraph in LAYER_NAMES/prose). Only rename tokens that are present.
2. Run `python make_export_v7.py` to write export_to_markdown_v7.py.
3. `make_docs_v7.py`: port make_docs_v6.py to invoke export_to_markdown_v7.py and write DesignGrammar-V7.md. Run it.
4. Confirm DesignGrammar-V7.md is produced from DesignGrammar-V7.owl, references V7 names, and contains no `-V6` path or stale layer-hub name.
Do NOT hand-edit DesignGrammar-V7.md (generated). If the exporter errors on a V7 construct (e.g. the new PropState class or list-typed ValidStatus), fix export_to_markdown_v7.py, not the output.
  </action>
  <verify>cd ontology && python make_export_v7.py && python make_docs_v7.py && test -f DesignGrammar-V7.md && grep -c "DesignGrammar-V6\|<owl:versionInfo>6" DesignGrammar-V7.md (==0)</verify>
  <acceptance_criteria>
    - export_to_markdown_v7.py is generated by make_export_v7.py and targets DesignGrammar-V7.owl / DesignGrammar-V7.md
    - DesignGrammar-V7.md is generated by make_docs_v7.py from DesignGrammar-V7.owl
    - DesignGrammar-V7.md contains no "-V6" paths and no stale layer-hub names (OntoGraph/ValidationGraph/ComputationGraph)
    - DesignGrammar-V7.md reflects the state trio (ObjState/ParamState/PropState) and rule-level SWRL/RuleName/RuleDescription
  </acceptance_criteria>
  <done>DesignGrammar-V7.md is regenerated from the V7 owl via the ported doc chain, free of V6 residue.</done>
</task>

<task type="auto">
  <name>Task 4: Parse-check the full V7 bundle</name>
  <files>ontology/DesignGrammar-V7.owl</files>
  <read_first>ontology/catalog-v001-V7.xml</read_first>
  <action>
Confirm every V7 owl file (DesignGrammar-V7.owl + the three -extension-V7.owl) is well-formed XML and the catalog is well-formed, using a stdlib XML parse (no external OWL library needed — matches the project's "SWRL parsing uses bespoke regex, not vendor OWL libraries" stance). This is the concrete stand-in for ROADMAP SC4 "load/parse without errors" in an environment without a running reasoner. Record the parse results in the SUMMARY.
  </action>
  <verify>for f in ontology/DesignGrammar-V7.owl ontology/DesignGrammar-*-extension-V7.owl ontology/catalog-v001-V7.xml; do python -c "import sys,xml.dom.minidom as m; m.parse('$f'); print('OK', '$f')"; done</verify>
  <acceptance_criteria>
    - DesignGrammar-V7.owl and all three -extension-V7.owl files parse as well-formed XML
    - catalog-v001-V7.xml parses as well-formed XML
    - Any parse failure is fixed at its generator (apply_v7_rename.py / apply_v7_extensions.py), not by hand-editing output
  </acceptance_criteria>
  <done>The whole V7 bundle parses cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ontology && python apply_v7_extensions.py` writes 3 -V7 extension owls with clean self-checks
- [ ] `catalog-v001-V7.xml` well-formed; 4 DG entries point at -V7 files; 7 external imports preserved
- [ ] `python make_export_v7.py && python make_docs_v7.py` regenerate DesignGrammar-V7.md with no -V6 residue
- [ ] All V7 owl files + catalog parse as well-formed XML
- [ ] V6 files (DesignGrammar-V6.owl, -extension-V6.owl, catalog-v001-V6.xml) all unmodified
</verification>

<success_criteria>
- All tasks completed
- V7 extension facades, catalog-v001-V7.xml, and regenerated DesignGrammar-V7.md all load/parse without errors — satisfying ROADMAP Phase 13 success criterion 4 and ONTO-05
- All generators are ported (no hand-edited generated output); all V6 sources untouched
</success_criteria>

<output>
After completion, create `.planning/milestones/v7.0-phases/13-ontology-v7-and-contract-investigation/13-04-SUMMARY.md`
</output>
