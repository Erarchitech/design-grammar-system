---
phase: 16-dg-core-state-models-and-state-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DG/src/DG.Core/Models/ObjState.cs
  - DG/src/DG.Core/Models/PropState.cs
  - DG/src/DG.Core/Models/DesignState.cs
  - DG/src/DG.Core/Models/DesignStateSnapshot.cs
  - DG/src/DG.Core/Models/ParamState.cs
  - DG/src/DG.Core/Models/DefState.cs
  - DG/src/DG.Core/Models/ObjectState.cs
  - DG/src/DG.Core/Models/ObjectInstance.cs
  - DG/tests/DG.Tests/ObjStateModelTests.cs
  - DG/tests/DG.Tests/ParamStateModelTests.cs
  - DG/tests/DG.Tests/PropStateModelTests.cs
  - DG/tests/DG.Tests/DesignStateModelTests.cs
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
user_setup: []

must_haves:
  truths:
    - Four model classes (ObjState, ParamState, PropState, DesignState) compile in DG.Core.Models
    - ParamState carries forward DesignStateSnapshot fields (StateId, CapturedAtUtc, Parameters collection) per D-14
    - DesignState aggregates ObjState/ParamState/PropState as three independent lists per D-01/D-02
    - DesignState.StateId is not computed here (handled by DesignStateIdGenerator in Plan 02)
    - DefState.cs, ObjectState.cs, ObjectInstance.cs deleted with zero remaining references in DG.Core and DG.Grasshopper
  artifacts:
    - DG/src/DG.Core/Models/ObjState.cs
    - DG/src/DG.Core/Models/PropState.cs
    - DG/src/DG.Core/Models/DesignState.cs
    - DG/src/DG.Core/Models/DesignStateSnapshot.cs (renamed to ParamState.cs)
    - DG/src/DG.Core/Models/DefState.cs (DELETED)
    - DG/src/DG.Core/Models/ObjectState.cs (DELETED)
    - DG/src/DG.Core/Models/ObjectInstance.cs (DELETED)
  key_links:
    - ObjState.Geometry field typed as object? (in-process handle, NOT serialized per PROJECT.md)
    - PropState.PropValue reuses DesignStateParameter typed-nullable pattern per D-08
    - DesignState lists are independent bags per D-02 — no cross-index alignment validation
---

<objective>
Create the four new DG.Core model classes (ObjState, ParamState, PropState, DesignState), rename DesignStateSnapshot to ParamState, and delete the unused v3.0 scaffolding (DefState, ObjectState, ObjectInstance). Write unit tests for all four models.

Purpose: Establish the data contracts that every downstream plan in Phase 16 serializes, wraps, and captures via GH components. These models are pure logic (no GH SDK dependency) and multi-target net7.0+net9.0.

Output:
- ObjState.cs, ParamState.cs, PropState.cs, DesignState.cs
- DefState.cs, ObjectState.cs, ObjectInstance.cs deleted
- DesignStateSnapshot.cs removed (replaced by ParamState.cs)
- 4 test files (ObjStateModelTests, ParamStateModelTests, PropStateModelTests, DesignStateModelTests)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/16-dg-core-state-models-and-state-components/16-CONTEXT.md
@.planning/milestones/v7.0-phases/16-dg-core-state-models-and-state-components/16-RESEARCH.md

# Source files to read for patterns
@DG/src/DG.Core/Models/DesignStateSnapshot.cs
@DG/src/DG.Core/Models/ElementRef.cs
@DG/src/DG.Core/Models/DesignStateParameter.cs
@DG/src/DG.Core/Models/DefState.cs
@DG/src/DG.Core/Models/ObjectState.cs
@DG/src/DG.Core/Models/ObjectInstance.cs
@DG/src/DG.Core/Services/DesignStateIdGenerator.cs
@DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs
@DG/tests/DG.Tests/DesignStateJsonSerializerTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ObjState, PropState, and DesignState model classes</name>
  <files>
    DG/src/DG.Core/Models/ObjState.cs
    DG/src/DG.Core/Models/PropState.cs
    DG/src/DG.Core/Models/DesignState.cs
  </files>
  <read_first>
    DG/src/DG.Core/Models/ElementRef.cs — ObjState wraps ObjectRef (user-supplied string, NOT geometry-hash), Geometry (object? in-process handle), and Label (user-supplied display label) — distinct from ElementRef's DgEntityId/Geometry/DisplayName
    DG/src/DG.Core/Models/DesignStateParameter.cs — typed nullable value pattern reused as PropValue
    DG/src/DG.Core/Models/DesignStateSnapshot.cs — ParamState source (StateId, CapturedAtUtc, Parameters)
    16-RESEARCH.md — Code Examples section for all three models
    16-CONTEXT.md — D-01, D-02, D-04, D-08, D-09, D-10, D-11
  </read_first>
  <action>
    Create three files in DG.Core.Models namespace.

    ObjState.cs:
    - Properties: StateId (string, empty default), ObjectRef (string, empty default — user-supplied identifier per PROJECT.md), Geometry (object?, in-process handle NOT serialized), Label (string?), CapturedAtUtc (DateTimeOffset)
    - All init-only auto-properties, no constructor logic
    - StateId prefix OS_ (existing convention, ComputeObjectStateId already exists with OS_ prefix)
    - Geometry follows ElementRef pattern — typed as object? for Rhino/GH in-process handle, excluded from serialization per Pitfall 2

    PropState.cs:
    - Properties: StateId (string, empty default, PS_ prefix per D-11), RuleIri (string, empty default — plain string IRI ref per D-09), DataPropertyIri (string, empty default — plain string IRI per D-09), PropValue (DesignStateParameter, nullable — reuses typed Number/Integer/Boolean pattern per D-08)
    - All init-only auto-properties
    - PropValue field type is DesignStateParameter (NOT a new wrapper class per Research Assumption A1)
    - Rule-scoped per D-10: one Rule + one DataProperty + one value only

    DesignState.cs:
    - Properties: StateId (string, empty default, DS_ prefix per D-04), ObjStates (List&lt;ObjState&gt;, init default new()), ParamStates (List&lt;ParamState&gt;, init default new()), PropStates (List&lt;PropState&gt;, init default new()), CapturedAtUtc (DateTimeOffset)
    - All init-only auto-properties
    - Three lists are independent bags per D-02 — no cross-index alignment validation in this model
    - StateId is NOT computed in the model (DesignStateIdGenerator.ComputeDesignStateId handles that)
    - Internal list ordering preserves wiring order per research recommendation (Claude's discretion)
    - Do NOT add length validation, cross-list alignment checks, or hash computation to this model

    All three files follow DG.Core conventions:
    - File-scoped namespace (DG.Core.Models;)
    - No GRASSHOPPER_SDK conditional — these are pure logic in DG.Core
    - Sealed modifier NOT used (downstream phases may need inheritance per existing pattern — Rule, Variable, ElementRef in DG.Core.Models are unsealed)
  </action>
  <verify>
    <automated>dotnet build DG/DG.sln 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    - `dotnet build DG/DG.sln` succeeds with no errors
    - ObjState.cs compiles with 5 init-only auto-properties (StateId, ObjectRef, Geometry, Label, CapturedAtUtc)
    - PropState.cs compiles with 4 init-only auto-properties (StateId, RuleIri, DataPropertyIri, PropValue)
    - DesignState.cs compiles with 5 init-only auto-properties (StateId, ObjStates, ParamStates, PropStates, CapturedAtUtc)
    - No GRASSHOPPER_SDK or GH dependency in any of the three files
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename DesignStateSnapshot to ParamState and delete DefState, ObjectState, ObjectInstance scaffolding</name>
  <files>
    DG/src/DG.Core/Models/DesignStateSnapshot.cs
    DG/src/DG.Core/Models/ParamState.cs
    DG/src/DG.Core/Models/DefState.cs
    DG/src/DG.Core/Models/ObjectState.cs
    DG/src/DG.Core/Models/ObjectInstance.cs
  </files>
  <read_first>
    DG/src/DG.Core/Models/DesignStateSnapshot.cs — exact current state (3 init-only auto-properties)
    DG/src/DG.Core/Models/DefState.cs — zero call sites confirmed, delete
    DG/src/DG.Core/Models/ObjectState.cs — zero call sites confirmed, delete
    DG/src/DG.Core/Models/ObjectInstance.cs — zero call sites confirmed, delete
    16-CONTEXT.md — D-12 (delete), D-14 (rename DesignStateSnapshot → ParamState)
  </read_first>
  <action>
    Two operations:

    1. Rename DesignStateSnapshot → ParamState:
    - Create DG/src/DG.Core/Models/ParamState.cs with class name ParamState, same namespace DG.Core.Models
    - Keep identical properties: StateId (string, init, empty default), CapturedAtUtc (DateTimeOffset, init), Parameters (Collection&lt;DesignStateParameter&gt; with getter-only init in constructor or default new())
    - Add using System.Collections.ObjectModel; — same as DesignStateSnapshot.cs
    - Main body identical to DesignStateSnapshot — no behavior changes, pure rename per D-14
    - Delete DG/src/DG.Core/Models/DesignStateSnapshot.cs (remove the old file)

    2. Delete three scaffolding files:
    - Delete DG/src/DG.Core/Models/DefState.cs — purely a class definition with StateId/CapturedAtUtc/Parameters
    - Delete DG/src/DG.Core/Models/ObjectState.cs — purely a class definition with 6 properties
    - Delete DG/src/DG.Core/Models/ObjectInstance.cs — purely a class definition with InstanceId/ProjectId/VariableName/CreatedAtUtc
    - These have zero call sites outside their own file, the ID generator, and tests (confirmed by research) — no compilation cleanup needed outside this plan. The downstream plans (02 serializer, 05-06 components) will handle reference updates.
    - Do NOT touch VariableBinder, ClassificatorComponent, or any Phase 18 code per cross-phase note
  </action>
  <verify>
    <automated>dotnet build DG/DG.sln 2>&1 | Select-String -Pattern "error" -NotMatch | Select-String -Pattern "Build succeeded"</automated>
  </verify>
  <done>
    - DG/src/DG.Core/Models/ParamState.cs exists with class name ParamState, same fields as DesignStateSnapshot
    - DG/src/DG.Core/Models/DesignStateSnapshot.cs no longer exists
    - DG/src/DG.Core/Models/DefState.cs no longer exists
    - DG/src/DG.Core/Models/ObjectState.cs no longer exists
    - DG/src/DG.Core/Models/ObjectInstance.cs no longer exists
    - `dotnet build DG/DG.sln` succeeds (any remaining references to deleted types in downstream code are handled by subsequent plans)
  </done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for all four model classes</name>
  <files>
    DG/tests/DG.Tests/ObjStateModelTests.cs
    DG/tests/DG.Tests/ParamStateModelTests.cs
    DG/tests/DG.Tests/PropStateModelTests.cs
    DG/tests/DG.Tests/DesignStateModelTests.cs
  </files>
  <read_first>
    DG/tests/DG.Tests/DesignStateJsonSerializerTests.cs — test patterns (xUnit Facts, AAA style)
    DG/tests/DG.Tests/DesignStateIdGeneratorTests.cs — test patterns for deterministic ID testing
    DG/src/DG.Core/Models/DesignStateParameter.cs — typed param creation pattern
    DG/src/DG.Core/Services/DesignStateIdGenerator.cs — existing ComputeObjectStateId test calls
  </read_first>
  <action>
    Create four test files in DG.Tests namespace. Follow existing xUnit patterns from DG.Tests (namespace DG.Tests, sealed class, [Fact] attributes).

    ObjStateModelTests.cs:
    - ObjState_ShouldSetProperties_ThroughInitOnlySetters — create ObjState with init syntax, verify all 5 properties
    - ObjState_ShouldHaveEmptyDefaults — verify default StateId is "", ObjectRef is "", Geometry is null, Label is null
    - ObjState_Geometry_ShouldBeObjectType — verify Geometry property type is object? (not a specific serializable type)
    - Use init-only property syntax (no constructors to test)

    ParamStateModelTests.cs:
    - ParamState_ShouldSetProperties_ThroughInitOnlySetters — create with StateId and CapturedAtUtc
    - ParamState_ShouldHaveEmptyStateIdByDefault — default StateId is ""
    - ParamState_ShouldAcceptParameters — add DesignStateParameter instances, verify Count
    - ParamState_ShouldPreserveDesignStateSnapshotContract — confirm same field set as original DesignStateSnapshot (StateId, CapturedAtUtc, Collection&lt;DesignStateParameter&gt; Parameters)

    PropStateModelTests.cs:
    - PropState_ShouldSetProperties_ThroughInitOnlySetters — create with all 4 properties
    - PropState_ShouldAcceptPropValue_AsDesignStateParameter — set PropValue with typed DesignStateParameter (Number/Integer/Boolean)
    - PropState_ShouldAcceptIriStrings — set RuleIri and DataPropertyIri as plain string IRIs (e.g. "dgm:Rule_R_URB_HEIGHT_MAX_75_V" and "dg:hasHeight")
    - PropState_ShouldHaveEmptyStateIdByDefault — default StateId is ""

    DesignStateModelTests.cs:
    - DesignState_ShouldSetProperties_ThroughInitOnlySetters — create with all properties
    - DesignState_ShouldAcceptMultipleObjStates — add ObjState instances to ObjStates list
    - DesignState_ShouldAcceptMultipleParamStates — add ParamState instances to ParamStates list
    - DesignState_ShouldAcceptMultiplePropStates — add PropState instances to PropStates list
    - DesignState_ThreeLists_ShouldBeIndependent — add items to one list doesn't affect other lists per D-02
    - DesignState_ShouldHaveEmptyStateIdByDefault — default StateId is ""
  </action>
  <verify>
    <automated>dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~ObjState|FullyQualifiedName~ParamState|FullyQualifiedName~PropState|FullyQualifiedName~DesignState" 2>&1</automated>
  </verify>
  <done>
    - `dotnet test DG/tests/DG.Tests/ --filter "FullyQualifiedName~ObjState|FullyQualifiedName~ParamState|FullyQualifiedName~PropState|FullyQualifiedName~DesignState"` passes (all tests green)
    - ObjStateModelTests.cs has 3+ facts
    - ParamStateModelTests.cs has 4+ facts
    - PropStateModelTests.cs has 4+ facts
    - DesignStateModelTests.cs has 5+ facts
    - All test files follow existing DG.Tests conventions (namespace DG.Tests, sealed class, [Fact] attributes)
  </done>
</task>

</tasks>

<artifacts>
| Symbol | Kind | File |
|--------|------|------|
| `DG.Core.Models.ObjState` | class | DG/src/DG.Core/Models/ObjState.cs |
| `DG.Core.Models.ParamState` | class | DG/src/DG.Core/Models/ParamState.cs (renamed from DesignStateSnapshot) |
| `DG.Core.Models.PropState` | class | DG/src/DG.Core/Models/PropState.cs |
| `DG.Core.Models.DesignState` | class | DG/src/DG.Core/Models/DesignState.cs |
| `DG.Core.Models.DefState` | class — DELETED | DG/src/DG.Core/Models/DefState.cs |
| `DG.Core.Models.ObjectState` | class — DELETED | DG/src/DG.Core/Models/ObjectState.cs |
| `DG.Core.Models.ObjectInstance` | class — DELETED | DG/src/DG.Core/Models/ObjectInstance.cs |
| `DG.Core.Models.DesignStateSnapshot` | class — DELETED | DG/src/DG.Core/Models/DesignStateSnapshot.cs |
| ObjStateModelTests | test class | DG/tests/DG.Tests/ObjStateModelTests.cs |
| ParamStateModelTests | test class | DG/tests/DG.Tests/ParamStateModelTests.cs |
| PropStateModelTests | test class | DG/tests/DG.Tests/PropStateModelTests.cs |
| DesignStateModelTests | test class | DG/tests/DG.Tests/DesignStateModelTests.cs |
</artifacts>

<verification>
- `dotnet build DG/DG.sln` succeeds after all three tasks
- `dotnet test DG/tests/DG.Tests/ --filter "ObjState|ParamState|PropState|DesignState"` passes
- grep -c "class DefState" DG/src/DG.Core/ should return 0
- grep -c "class ObjectState" DG/src/DG.Core/ should return 0
- grep -c "class ObjectInstance" DG/src/DG.Core/ should return 0
- grep -c "class DesignStateSnapshot" DG/src/DG.Core/ should return 0
- grep -c "class ParamState" DG/src/DG.Core/Models/ParamState.cs should return 1
</verification>

<success_criteria>
- Four model classes compile in DG.Core
- ParamState exists as ParamState (not DesignStateSnapshot)
- DefState, ObjectState, ObjectInstance, DesignStateSnapshot no longer exist in DG/src/DG.Core
- 15+ unit tests across 4 test files all pass
</success_criteria>
