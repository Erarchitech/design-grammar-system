---
phase: 20-e2e-validation-and-docs
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - DG_OBSIDIAN/atlas/Graph schema v3 is the canonical data model.md
  - DG_OBSIDIAN/atlas/Neo4j stores ontology and metagraph in a single database.md
  - DG_OBSIDIAN/00-home/index.md
  - DG_OBSIDIAN/00-home/Current priorities.md
  - DG_OBSIDIAN/archive/ (multiple moved files)
  - DG_OBSIDIAN/sessions/YYYY-MM-DD Phase 20 E2E Validation and Docs.md
  - .planning/REQUIREMENTS.md
  - scripts/refresh_graphify.sh (executed, not modified)
autonomous: false
requirements: [E2E-04]
user_setup:
  - service: graphify
    why: "Knowledge graph regeneration"
    steps:
      - "Ensure graphify-out/.graphify_python exists (run `/graphify` in Claude Code if not)"
      - "Ensure Bash is available for `scripts/refresh_graphify.sh`"

must_haves:
  truths:
    - "All stale vault notes for deleted/renamed components are moved to DG_OBSIDIAN/archive/"
    - "The 'Graph schema v3 is the canonical data model' atlas note is rewritten to teach v4 (title updated, content accurate)"
    - "The 00-home/index.md links to archive for old notes and reflects current v7.0 state"
    - "Graphify knowledge graph is regenerated -- community notes no longer describe CLASSIFICATOR/VALIDATION RUNS or v3.0-phase plans as current"
    - "Current priorities.md marks v7.0 complete and v4.0 BOT Ontology Bridge as next milestone"
    - "REQUIREMENTS.md has all 39 v7.0 requirements checked complete"
  artifacts:
    - DG_OBSIDIAN/archive/ (stale notes moved)
    - DG_OBSIDIAN/atlas/Graph schema v4 is the canonical data model.md (rewritten)
    - DG_OBSIDIAN/atlas/Neo4j stores ontology and metagraph in a single database.md (updated)
    - DG_OBSIDIAN/00-home/index.md (updated)
    - DG_OBSIDIAN/00-home/Current priorities.md (updated)
    - DG_OBSIDIAN/sessions/2026-07-05 Phase 20 E2E Validation and Docs.md (new)
    - .planning/REQUIREMENTS.md (all 39 requirements checked)
    - graphify-out/graph.json (regenerated)
  key_links:
    - "Graphify regeneration is the FINAL step -- captures codebase after ALL Phase 20 changes committed"
    - "Session protocol creates session note at end, documenting everything Phase 20 accomplished"
    - "REQUIREMENTS.md traceability must show all 39 v7.0 requirements as complete"
---

<objective>
Archive stale knowledge vault notes for deleted/renamed components, refresh the graphify knowledge graph, and update vault structure to reflect v7.0 completion. Mark all v7.0 requirements complete in REQUIREMENTS.md.

Purpose: The DG_OBSIDIAN vault is the persistent project memory. After v7.0, notes teaching v3 schema, describing deleted components (CLASSIFICATOR, VALIDATION RUNS), or referencing old phase plans must be archived or updated. The graphify knowledge graph must be regenerated to match the current codebase. REQUIREMENTS.md must reflect milestone completion with full traceability.

Output:
- DG_OBSIDIAN/archive/ populated with stale notes
- Updated atlas notes (v3->v4 schema)
- Updated vault index and priorities
- Regenerated graphify graph
- Session note documenting Phase 20
- REQUIREMENTS.md with all 39 v7.0 requirements checked complete
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 20 section: Success Criteria 4, 5)
@.planning/REQUIREMENTS.md (E2E-04, traceability table)

@.planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-13 through D-15)

# Obsidian vault state
@DG_OBSIDIAN/00-home/index.md (current state -- update links after archive)
@DG_OBSIDIAN/00-home/Current priorities.md (current state -- update for milestone completion)

# Atlas notes to update
@DG_OBSIDIAN/atlas/Graph schema v3 is the canonical data model.md (rewrite to v4)
@DG_OBSIDIAN/atlas/Neo4j stores ontology and metagraph in a single database.md (add ValidGraph/DesignState/Run)

# Session protocol reference
@CLAUDE.md (Obsidian Knowledge Vault section -- session protocol format)
@.planning/ROADMAP.md (next milestone: v4.0 BOT Ontology Bridge)

# Graphify refresh script
@scripts/refresh_graphify.sh (execute -- do not modify)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive stale knowledge vault notes and update current notes</name>

  <files>
    DG_OBSIDIAN/archive/.gitkeep
    DG_OBSIDIAN/atlas/Graph schema v3 is the canonical data model.md
    DG_OBSIDIAN/atlas/Neo4j stores ontology and metagraph in a single database.md
    DG_OBSIDIAN/00-home/index.md
  </files>

  <read_first>
    DG_OBSIDIAN/00-home/index.md (current vault index -- must understand link structure before archive)
    DG_OBSIDIAN/atlas/Graph schema v3 is the canonical data model.md (will be renamed + rewritten)
    DG_OBSIDIAN/atlas/Neo4j stores ontology and metagraph in a single database.md (will be updated)
    .planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-13: archive stale notes, update in-place where appropriate)
    .planning/REQUIREMENTS.md (v7.0 traceability for all 39 requirements)
    ontology/DesignGrammar-V7.owl (canonical names for atlas note accuracy)
    ontology/V7-INVESTIGATION.md (conflict resolutions for atlas note context)
  </read_first>

  <action>
    Perform D-13 (archive stale notes, update current in-place) vault housekeeping.

    Step 1: Identify stale notes for archive
    Scan all DG_OBSIDIAN subdirectories (atlas/, knowledge/business, knowledge/debugging, knowledge/decisions, knowledge/integrations, knowledge/patterns, knowledge/publications, graphify/communities) for notes whose PRIMARY subject is a deleted/renamed component or v3.0-phase plan.
    
    Notes to MOVE to archive/ (mv command via Bash):
    - Any note primarily about CLASSIFICATOR component (not just mentioning it in passing)
    - Any note primarily about VALIDATION RUNS component (the old component, not the new VALIDATION GRAPH)
    - Any note primarily about the old REINSTATE component behavior
    - Any note primarily about v3.0-phase plans (phases 8-12) as current work
    - Any note that describes only v3 schema content without v4 update
    
    Notes to UPDATE in-place (not archive -- edit content):
    - `atlas/Graph schema v3 is the canonical data model.md` -> rename to `Graph schema v4 is the canonical data model.md` and rewrite content (see below)
    - `atlas/Neo4j stores ontology and metagraph in a single database.md` -> add ValidGraph layer, DesignState/Run entries, update graph separation table
    - Any note that mentions old component names in passing but whose primary subject is still current (e.g., a pattern note that once referenced CLASSIFICATOR but the pattern itself applies to the new flow) -> update the references in-place

    Step 2: Rewrite `atlas/Graph schema v4 is the canonical data model.md`
    - Rename from "Graph schema v3 is the canonical data model.md" to "Graph schema v4 is the canonical data model.md"
    - Frontmatter: update tags (v3->v4), update date to 2026-07-05, remove the [!warning] supersession block (v4 is the current schema)
    - Content: Replace all v3 schema content with v4:
      - Schema Files list: ALL files are now v4 (cypher_template.txt v4, dataset_schema.json v4, updated_cypher_reference_examples_v3.cypher -> v4 successor)
      - Add DesignState kind {ObjState, ParamState, PropState} to schema description
      - Add Run properties (ValidStatus Boolean list, SendStatus Boolean) -- note: no stored Status text field
      - Add rule-level properties (SWRL replaces text, RuleName replaces title, RuleDescription is optional)
      - Keep: Rule ID format, Atom ID format, IRI Prefixes, Semantic Mapping (Mandatory Propagation list
      - Update Mandatory Propagation list to include data-service app.py, .github/copilot-instructions.md, README.md, spec/DATABASE.md
      - Add link to releases notes: "See docs/RELEASE-NOTES-v7.0.md for the full component re-wiring guide"
      - Keep graphify:connections section (will be regenerated by graphify in Task 2)
      - Add note: "Schema v3 migration history: see the v3->v4 migration notes in spec/DATABASE.md"

    Step 3: Update `atlas/Neo4j stores ontology and metagraph in a single database.md`
    - Graph Separation table: ADD rows for ValidGraph (DesignState, Run, IntegrationConfig, ValidationEntity) and SpecGraph (SpecNote, SpecTag, SpecSession, SpecClass)
    - Rename the "ValidationGraph" label reference in the original table to "ValidGraph"
    - Canonical Node Labels (v4) section: ADD DesignState (kind: ObjState/ParamState/PropState, key: StateId, graph: ValidGraph), Run (key: Run_Id, properties: ValidStatus Boolean list, SendStatus Boolean, graph: ValidGraph)
    - Update section title to "Canonical Node Labels (v4)"
    - Add DesignState ID prefix documentation: OS_ (ObjState), DS_ (ParamState), PS_ (PropState)
    - Add relationship: HAS_STATE (DesignState -> state nodes, read-side composition)
    - Update v3 Migration Notes to be titled "Schema Migration History (v3->v4, complete)" with both v2->v3 and v3->v4 migrations listed

    Step 4: Update `00-home/index.md`
    - Atlas section: Update link from "[[Graph schema v3 is the canonical data model]]" to "[[Graph schema v4 is the canonical data model]]"
    - Add archive section or inline note pointing to `DG_OBSIDIAN/archive/` for old component notes
    - Decisions section: Keep all existing decision links (they remain valid historical records) -- add a note that deleted component decisions are in the archive
    - Session log: Add link to [[sessions/2026-07-05 Phase 20 E2E Validation and Docs]]
    - Update date tag to 2026-07-05

    Step 5: Create archive directory structure
    - Ensure DG_OBSIDIAN/archive/ exists
    - Create DG_OBSIDIAN/archive/.gitkeep so the directory is tracked
    - After moving files, update any cross-references in vault notes that pointed to archived notes

    Note on graphify/communities notes: These are regenerated by refresh_graphify.sh in Task 2. Do NOT manually archive them -- the regeneration will replace stale community notes automatically.
  </action>

  <acceptance_criteria>
    - DG_OBSIDIAN/archive/ directory exists with moved stale notes
    - DG_OBSIDIAN/atlas/ has "Graph schema v4 is the canonical data model.md" (not v3)
    - DG_OBSIDIAN/atlas/Neo4j stores ontology and metagraph in a single database.md has ValidGraph + SpecGraph + DesignState + Run documentation
    - DG_OBSIDIAN/00-home/index.md links to v4 atlas note, has archive reference, has Phase 20 session link
    - atlas "Graph schema v3 is the canonical data model.md" no longer exists (either renamed or archived)
  </acceptance_criteria>

  <verify>
    <automated>ls DG_OBSIDIAN/archive/.gitkeep 2>/dev/null && ls DG_OBSIDIAN/atlas/"Graph schema v4 is the canonical data model.md" 2>/dev/null && test ! -f DG_OBSIDIAN/atlas/"Graph schema v3 is the canonical data model.md"</automated>
  </verify>

  <done>Stale notes archived. Atlas schema note renamed and rewritten to v4. Database atlas note updated with ValidGraph/DesignState/Run. Vault index updated with archive link and v4 references.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate graphify, update vault priorities + session note, finalize REQUIREMENTS.md</name>

  <files>
    DG_OBSIDIAN/graphify/communities/ (regenerated by refresh_graphify.sh)
    DG_OBSIDIAN/00-home/Current priorities.md
    DG_OBSIDIAN/00-home/index.md
    DG_OBSIDIAN/sessions/2026-07-05 Phase 20 E2E Validation and Docs.md
    .planning/REQUIREMENTS.md
  </files>

  <read_first>
    scripts/refresh_graphify.sh (execute -- do NOT modify the script itself)
    DG_OBSIDIAN/00-home/Current priorities.md (current state -- update for milestone completion)
    .planning/REQUIREMENTS.md (traceability table for all 39 requirements)
    .planning/ROADMAP.md (next milestone v4.0 BOT Ontology Bridge for priorities)
    CLAUDE.md (Obsidian Knowledge Vault section -- session protocol, session note template)
    .planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-14: full graphify regeneration, D-15: update vault + session + priorities)
  </read_first>

  <action>
    Perform D-14 (full graphify regeneration) and D-15 (update vault index + session + priorities), plus finalize REQUIREMENTS.md traceability.

    Step 1: Graphify regeneration per D-14
    - Run from project root: `bash scripts/refresh_graphify.sh`
    - This regenerates DG_OBSIDIAN/graphify/communities/ notes from the current graph.json
    - Since all stale source notes have been archived/updated in Task 1, the regenerated community notes will reflect the current v7.0 codebase structure
    - Verify no ERROR in script output. If error occurs (e.g., graphify-out/.graphify_python not found), diagnose and fix before proceeding
    - After regeneration, spot-check 2-3 community notes to confirm they no longer reference CLASSIFICATOR, VALIDATION RUNS, or v3.0-phase plans as current

    Step 2: Update Current priorities.md per D-15
    - Set Active section: Replace Phase 20 context reference with a note that v7.0 is complete. Add v4.0 BOT Ontology Bridge as the next active milestone. Keep the migration pending note for `migrations/2026-06-23_var_project_merge_key.cypher` (still needs to run against live Neo4j).
    - Set Upcoming section: Keep v4.0 BOT Ontology Bridge as first upcoming item. Remove or rephase v7.0 items.
    - Keep Completed Recently section: Add Phase 20 execution as the most recent completed item. Format matches existing pattern: "**v7.0 Phase 20 executed** -- 2026-07-05. 3/3 plans across 3 waves, all E2E-01..04 requirements satisfied. Created E2E checklist + Docker automation, release notes, updated repo/AI docs, refreshed DG_OBSIDIAN + graphify. See [[sessions/2026-07-05 Phase 20 E2E Validation and Docs|session]]."
    - Keep infoboxes (v3.0 superseded, .NET SDK status, worktree config) -- still relevant context

    Step 3: Create session note per D-15 (follows CLAUDE.md session protocol)
    - Create file: `DG_OBSIDIAN/sessions/2026-07-05 Phase 20 E2E Validation and Docs.md`
    - Frontmatter: tags [session, phase20, v7], date 2026-07-05
    - Content:
      - Summary of Phase 20 work (E2E validation, release notes, docs update, DG_OBSIDIAN refresh)
      - Key decisions referenced (D-01 through D-15)
      - Plans executed (20-01, 20-02, 20-03)
      - Files created/modified
      - Current state: v7.0 milestone complete

    Step 4: Update index.md after graphify regeneration
    - If refresh_graphify.sh created new community notes, update the graphify section in index.md
    - Remove or update any stale graphify community links

    Step 5: Finalize REQUIREMENTS.md per Success Criterion 5
    - Mark all 39 v7.0 requirements as [x] (complete):
      - ONTO-01 through ONTO-06 (Phase 13)
      - SCHM-07 through SCHM-14 (Phase 14) -- including SCHM-11 and SCHM-12 if not already checked
      - SPEC-01 through SPEC-04 (Phase 15)
      - CORE-01 through CORE-05 (Phase 16)
      - GHST-01 through GHST-04 (Phase 16)
      - GHGA-01 through GHGA-05 (Phase 17)
      - GHVL-01 through GHVL-06 (Phase 18)
      - GHST-05 through GHST-07 (Phase 19)
      - E2E-01 through E2E-04 (Phase 20)
    - Update traceability table to show all rows as "Complete" status
    - Update coverage count if needed (verify 39/39)
    - Add a line: "*Milestone v7.0 completed 2026-07-05 -- all 39 requirements satisfied.*"

    Skip Step 6 from CLAUDE.md protocol (design decision creation) if no new decisions were made during Phase 20 -- D-01..D-15 were captured during context discussion and already logged. Skip Step 4 (debugging note creation) if no bugs requiring permanent note were found.
  </action>

  <acceptance_criteria>
    - `bash scripts/refresh_graphify.sh` exits 0 and regenerates community notes
    - Spot-check of 2 regenerated community notes shows no stale CLASSIFICATOR/VALIDATION RUNS references as current
    - DG_OBSIDIAN/00-home/Current priorities.md: v7.0 Phase 20 listed under Completed Recently, v4.0 BOT Ontology Bridge listed as next active
    - DG_OBSIDIAN/sessions/2026-07-05 Phase 20 E2E Validation and Docs.md exists with correct frontmatter and content
    - .planning/REQUIREMENTS.md: All 39 v7.0 requirements checked [x], traceability table shows all Complete, coverage 39/39
  </acceptance_criteria>

  <verify>
    <automated>bash scripts/refresh_graphify.sh 2>&1 | tail -3 && grep -c "\[x\]" .planning/REQUIREMENTS.md | head -1 && grep -c "Completed Recently.*Phase 20" DG_OBSIDIAN/00-home/Current\ priorities.md 2>/dev/null; echo "VAULT_OK=$?"</automated>
  </verify>

  <done>Graphify regenerated. Vault priorities updated for v7.0 completion. Session note created. REQUIREMENTS.md shows all 39 v7.0 requirements complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graphify regeneration -> file system | refresh_graphify.sh reads graph.json and writes community notes -- local file operation, no external network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-07 | Tampering | Vault note archiving | low | mitigate | Archived notes preserved in DG_OBSIDIAN/archive/ -- no deletion, only move; git tracks all changes |
| T-20-08 | Denial of Service | refresh_graphify.sh failure | low | mitigate | Script checks for graphify-out/.graphify_python and graph.json before running; error message guides diagnosis |
| T-20-09 | Repudiation | REQUIREMENTS.md traceability | low | mitigate | Each checked requirement references its completing phase; git log provides immutable record |
| T-20-SC | Tampering | npm/pip installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
1. `bash scripts/refresh_graphify.sh` exits 0
2. All 39 requirements checked in REQUIREMENTS.md
3. Vault index, priorities, and session note updated
4. Atlas notes teach v4, not v3
</verification>

<success_criteria>
- DG_OBSIDIAN stale notes archived, current notes updated to v4 (E2E-04)
- Graphify regenerated -- community notes match current codebase (E2E-04)
- All 39 v7.0 requirements checked complete in REQUIREMENTS.md (Success Criterion 5)
- Session note created documenting Phase 20 work
- Current priorities.md transitions to v4.0 BOT Ontology Bridge as next milestone
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| Archive notes | Moved files | `DG_OBSIDIAN/archive/*` |
| `Graph schema v4 is the canonical data model.md` | Renamed + rewritten atlas note | `DG_OBSIDIAN/atlas/Graph schema v4 is the canonical data model.md` |
| Vault index (updated) | Document | `DG_OBSIDIAN/00-home/index.md` |
| Current priorities (updated) | Document | `DG_OBSIDIAN/00-home/Current priorities.md` |
| Phase 20 session note | Document | `DG_OBSIDIAN/sessions/2026-07-05 Phase 20 E2E Validation and Docs.md` |
| REQUIREMENTS.md (finalized) | Document | `.planning/REQUIREMENTS.md` |
| Graphify community notes (regenerated) | Multiple files | `DG_OBSIDIAN/graphify/communities/*.md` |

<output>
Create `.planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-03-SUMMARY.md` when done.
Create `.planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-SUMMARY.md` (phase-level wrap-up summary documenting all 3 plans).
Update `.planning/STATE.md` (current_phase=20, plan=3, status=completed).
</output>
