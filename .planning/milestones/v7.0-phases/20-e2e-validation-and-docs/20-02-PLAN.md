---
phase: 20-e2e-validation-and-docs
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - docs/RELEASE-NOTES-v7.0.md
  - CLAUDE.md
  - .github/copilot-instructions.md
  - spec/DATABASE.md
  - README.md
autonomous: false
requirements: [E2E-02, E2E-03]
user_setup: []

must_haves:
  truths:
    - "docs/RELEASE-NOTES-v7.0.md documents every breaking change with per-component re-wiring guide, ASCII wiring diagrams, and upgrade instructions"
    - "CLAUDE.md teaches schema v4, not v3, and has updated Current Priorities, Known Gotchas, and Tech Stack"
    - ".github/copilot-instructions.md describes v4 schema and 14-component architecture -- zero v3 references presented as current"
    - "spec/DATABASE.md documents ValidGraph, DesignState kinds, Run properties in v4"
    - "README.md references 14 components and current milestone status"
    - "SC#3 grep gate passes: `grep -ri \"schema v3|v3 schema\"` over target docs returns zero hits presenting v3 as current"
  artifacts:
    - docs/RELEASE-NOTES-v7.0.md
    - CLAUDE.md (updated)
    - .github/copilot-instructions.md (rewritten)
    - spec/DATABASE.md (updated)
    - README.md (updated)
  key_links:
    - "Release notes ASCII diagrams accurately match GH_DesignGrammars.pdf wiring"
    - "copilot-instructions.md v4 schema section exactly matches cypher_template.txt v4 and DesignGrammar-V7.owl"
    - "grep gate is the mechanical proof that no doc teaches v3 as current"
---

<objective>
Document every v7.0 breaking change for canvas migration in release notes, and update all repo/AI-assistant documentation to reflect schema v4 and the 14-component set.

Purpose: Architects upgrading from v2.0 need clear re-wiring instructions (release notes). AI coding assistants need accurate schema and component documentation (repo docs). Both ensure the v7.0 milestone is usable and maintainable.

Output:
- `docs/RELEASE-NOTES-v7.0.md` -- Per-component breakage documentation with ASCII wiring diagrams
- Updated `CLAUDE.md`, `.github/copilot-instructions.md`, `spec/DATABASE.md`, `README.md`
- SC#3 grep gate verification
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 20 section: Success Criteria 2, 3)
@.planning/REQUIREMENTS.md (E2E-02, E2E-03)

@.planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-05 through D-12)

# Phase summarys for release notes accuracy -- read concrete decisions, not summaries
@.planning/milestones/v7.0-phases/13-ontology-v7-and-contract-investigation/13-CONTEXT.md (port-IRI contract, component GUIDs)
@.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-CONTEXT.md (cross-phase notes for release notes -- ValidStatus contract, CLASSIFICATOR purge reasoning)
@.planning/milestones/v7.0-phases/19-deconstruct-and-reinstate-components/19-CONTEXT.md (cross-phase notes for release notes -- REINSTATE rework, DECONSTRUCT Warning contract)

# Ontology source of truth for doc accuracy
@ontology/port-iri-map-V7.md (per-port IRI mapping for all 14 components -- MUST read for release notes accuracy)
@ontology/DesignGrammar-V7.owl (V7 ontology -- source of truth for all names)
@ontology/V7-INVESTIGATION.md (conflict resolutions for reference)
@ontology/GH_DesignGrammars.pdf (14-component wiring diagrams for ASCII diagram reference)

# Docs to be modified (read current state before editing)
@CLAUDE.md
@.github/copilot-instructions.md
@spec/DATABASE.md
@README.md

# Existing release notes pattern
@docs/GSD_Guide_ClaudeCode.pdf (existing docs format reference -- Phase 20 follows established markdown docs convention)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v7.0 release notes with per-component breakage guide and ASCII wiring diagrams</name>

  <files>
    docs/RELEASE-NOTES-v7.0.md
  </files>

  <read_first>
    ontology/port-iri-map-V7.md (14 component IRI mappings -- source of truth for port names)
    ontology/DesignGrammar-V7.owl (class/property names for accuracy)
    ontology/GH_DesignGrammars.pdf (wiring diagrams -- source for ASCII wire patterns)
    .planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-CONTEXT.md (cross-phase notes with release notes content)
    .planning/milestones/v7.0-phases/19-deconstruct-and-reinstate-components/19-CONTEXT.md (cross-phase notes with release notes content)
    .planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-05 through D-08 for structure requirements)
  </read_first>

  <action>
    Create `docs/RELEASE-NOTES-v7.0.md` per D-05 (standalone file), D-06 (full re-wiring guide per changed/removed component), D-07 (sections: Breaking Changes, New Features, Upgrade Guide), D-08 (ASCII wiring diagrams).

    Document structure (mandatory sections):

    1. **Title and version** -- "# Release Notes: DG Plugin v7.0", date, brief one-paragraph summary of what v7.0 is (14-component set, ontology-aligned ports, 3-part DesignState composition)

    2. **Breaking Changes** -- Per-component re-wiring guide. For EACH affected component (CLASSIFICATOR, VALIDATION RUNS, old REINSTATE, old DESIGN STATE, CONNECTOR, RULE DECONSTRUCT, VALIDATOR):
       a. Component name (old) -> (new/existence)
       b. What broke -- precise description of the incompatibility
       c. ASCII wiring diagram showing old wiring -> new wiring
       d. Port-level mapping (old input/output -> new input/output)
       e. GUID change if applicable

       Per-component content:

       **CLASSIFICATOR (removed)** -- Old: CLASSIFICATOR accepted Rule + Variables/Objects/Properties inputs and output DefState. New: Use OBJECT STATE (captures Object+Geometry+Label into ObjState) + PROPERTY STATE (captures Rule+DataProperty+PropValue into PropState) + DESIGN STATE (composes all three state types). ASCII diagram:
       ```
       Before:  METAGRAPH -> RULE DECONSTRUCT -> CLASSIFICATOR -> VALIDATOR
       After:   METAGRAPH -> RULE DECONSTRUCT -> OBJECT STATE -> DESIGN STATE -> VALIDATOR
                                          \-> PROPERTY STATE -/
       ```
       Port mapping: CLASSIFICATOR Rule input -> RULE DECONSTRUCT Rule output; CLASSIFICATOR Objects/Properties -> OBJECT STATE Object + PROPERTY STATE Rule+DataProperty+PropValue; CLASSIFICATOR DefState output -> DESIGN STATE DesignState output

       **VALIDATION RUNS (replaced by VALIDATION GRAPH)** -- Old GUID A7F2C3E1 removed. New GUID 95fc9d32-307e-41fd-a158-bfae49a3dc2a. New component reads from ValidGraph handle. Outputs Run/Status/DesignState instead of single ValidationRuns list. ASCII diagram:
       ```
       Before:  CONNECTOR -> VALIDATION RUNS -> [ValidationRuns list]
       After:   CONNECTOR -> GRAPH DECONSTRUCT -> VALIDATION GRAPH -> Run + Status + DesignState
       ```

       **REINSTATE (replaced by PARAMETER REINSTATE)** -- Old GUID replaced. Old REINSTATE auto-fired on every solve. New PARAMETER REINSTATE requires Target input (Input 1), uses rising-edge trigger on Reinstate input (false->true), Parameters output returns ALL params from ParamState, StateStatus index-matched to Parameters. ASCII diagram:
       ```
       Before:  ParamState -> REINSTATE -> Parameters + Status
       After:   ParamState -> PARAMETER REINSTATE -> Parameters + StateStatus + Status
                 Target (Input 1) -/
       ```
       Port changes: Target input added (required, Input 1); StateStatus output added (7-value ReStatus index-matched); old Status output preserved

       **DESIGN STATE (old) -> PARAMETER STATE + OBJECT STATE + PROPERTY STATE + DESIGN STATE (new)** -- Old monolithic DESIGN STATE component is split into three capture components and one composition component:
       - OBJECT STATE: captures Object (GH geometry reference) + Geometry (rendering hint) + Label string into ObjState (OS_ prefix)
       - PARAMETER STATE: captures Parameters list (sliders, toggles) into ParamState (DS_ prefix, deterministic StateId)
       - PROPERTY STATE: captures Rule + DataProperty + PropValue into PropState (PS_ prefix)
       - DESIGN STATE (new): composes multiple ObjState + ParamState + PropState inputs into a single DesignState with aggregated StateId
       ASCII diagram:
       ```
       Before:  Object -> DESIGN STATE -> DesignState (DefState)
                Params -/
       After:   Object + Geometry + Label -> OBJECT STATE -> ObjState -> DESIGN STATE -> DesignState
                Parameters -> PARAMETER STATE -> ParamState -/
                Rule + DataProperty + PropValue -> PROPERTY STATE -> PropState -/
       ```

       **CONNECTOR (renamed from Connection)** -- Input port renames: ServerURI -> Neo4jURI, User -> Neo4jUser, Password -> Neo4jPassword, DbName input added (preserved). Output renamed from Connection to Database handle. ASCII diagram:
       ```
       Before:  CONNECTOR (ServerURI, User, Password) -> Connection (Handle)
       After:   CONNECTOR (Neo4jURI, Neo4jUser, Neo4jPassword, DbName, Project) -> Database (Handle)
       ```

       **RULE DECONSTRUCT (extended)** -- New outputs: Objects (IRIs of Classes/ObjProperties referenced by rule atoms via REFERS_TO), DataProperties (DatatypeProperty IRIs). Old Variables/VariableName outputs removed (replaced by Objects + DataProperties partition). ASCII diagram:
       ```
       Before:  RULE DECONSTRUCT -> Variables + VariableName + Rule + SWRL + RuleName + RuleDescription
       After:   RULE DECONSTRUCT -> Objects + DataProperties + Rule + SWRL + RuleName + RuleDescription
       ```

       **VALIDATOR (reworked)** -- New inputs: DesignState (replaces old DefState from CLASSIFICATOR), Run (Boolean -- triggers validation), SendValid (Boolean -- triggers publish). Old CLASSIFICATOR input removed. Outputs: ValidStatus (list of Booleans per ObjState, index-matched), RuleName, RuleDescription, SendStatus. Non-overlapping extras preserved: DataServiceUrl input, Report/FailingBindings/ValidationRunId/ModelViewerUrl outputs. ASCII diagram:
       ```
       Before:  Rule + DefState + DataServiceUrl -> VALIDATOR -> ValidStatus + FailingBindings + Report + ValidationRunId + ModelViewerUrl
       After:   Rule + DesignState + Run + SendValid + DataServiceUrl -> VALIDATOR -> ValidStatus + RuleName + RuleDescription + SendStatus + FailingBindings + Report + ValidationRunId + ModelViewerUrl
       ```

    3. **New Features** section:
       - 3-part state composition (ObjState/ParamState/PropState -> DesignState)
       - SpecGraph layer (KnowledgeGraph runtime rename)
       - Ontology-aligned port names matching DesignGrammar-V7.owl IRIs
       - Full 14-component architecture covering all graph layers (Metagraph, OntoGraph, ValidGraph, SpecGraph)
       - GRAPH DECONSTRUCT (single Database -> 4 layer handles)
       - ONTOGRAPH (Class/ObjProperties/DataProperties from OntoGraph)
       - DESIGN STATE DECONSTRUCT + OBJECT DECONSTRUCT for state decomposition

    4. **Upgrade Guide** section:
       - Step 1: Install new .gha (DG v7.0 plugin -- replaces existing DG.gha)
       - Step 2: Run DB migrations (kind rename: DefState->ParamState, ObjectState->ObjState; ValidGraph layer assignment for DesignState; SpecGraph rename if not already run)
       - Step 3: Open existing .gh file -- missing-component placeholders appear for CLASSIFICATOR, VALIDATION RUNS, old REINSTATE
       - Step 4: Re-wire per Breaking Changes section above (refer to per-component ASCII diagrams)
       - Step 5: Verify full chain works (refer to test/e2e-v7.0-checklist.md)

    5. **Appendix: GUID Reference** -- Table of old->new GUIDs for all changed components

    6. **Appendix: Port IRI Reference** -- Link to port-iri-map-V7.md (the canonical per-port IRI document for developers)
  </action>

  <acceptance_criteria>
    - docs/RELEASE-NOTES-v7.0.md exists with all sections: Breaking Changes (per-component with ASCII diagrams), New Features, Upgrade Guide, GUID appendix, Port IRI appendix
    - Every affected component documented: CLASSIFICATOR removal path, VALIDATION RUNS->VALIDATION GRAPH, REINSTATE->PARAMETER REINSTATE, DESIGN STATE split, CONNECTOR port renames, RULE DECONSTRUCT extended, VALIDATOR reworked
    - Each breaking change has an ASCII wiring diagram showing old wiring -> new wiring
    - GUIDs match Phase 17/18/19 final values (95fc9d32-307e-41fd-a158-bfae49a3dc2a for VALIDATION GRAPH, no old A7F2C3E1 references outside the old->new mapping)
  </acceptance_criteria>

  <verify>
    <automated>grep -c "Breaking Changes" docs/RELEASE-NOTES-v7.0.md && grep -c "CLASSIFICATOR" docs/RELEASE-NOTES-v7.0.md && grep -c "VALIDATION GRAPH" docs/RELEASE-NOTES-v7.0.md && grep -c "PARAMETER REINSTATE" docs/RELEASE-NOTES-v7.0.md && grep -c "Before:" docs/RELEASE-NOTES-v7.0.md | head -1 && grep -c "Upgrade Guide" docs/RELEASE-NOTES-v7.0.md | head -1</automated>
  </verify>

  <done>docs/RELEASE-NOTES-v7.0.md exists with per-component breakage guide, ASCII wiring diagrams for all changed components, new features summary, and upgrade instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Update repo/AI-assistant docs to schema v4 and 14-component set</name>

  <files>
    CLAUDE.md
    .github/copilot-instructions.md
    spec/DATABASE.md
    README.md
  </files>

  <read_first>
    CLAUDE.md (current state -- sections to update: Graph Schema v3, Schema Change Propagation, Known Gotchas, Tech Stack Summary, Current Priorities)
    .github/copilot-instructions.md (current state -- full rewrite needed per D-10: every section teaches v3)
    spec/DATABASE.md (current state -- entire doc teaches v3 labels/graphs)
    README.md (current state -- component count, milestone status, data model conventions)
    .planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-09, D-10, D-11: specific update directives per doc)
    ontology/DesignGrammar-V7.owl (canonical class/property names for all doc content)
    ontology/V7-INVESTIGATION.md (conflict resolutions for reference)
    .planning/ROADMAP.md (v7.0 and next milestone references for Current Priorities)
  </read_first>

  <action>
    Update four documents per D-09 (targeted update per doc), D-10 (copilot-instructions.md full rewrite), D-11 (CLAUDE.md schema+factual sections). Do NOT do full rewrites of CLAUDE.md, spec/DATABASE.md, or README.md -- preserve institutional knowledge that remains accurate.

    **CLAUDE.md updates (D-11):**
    1. "## Graph Schema v3" section -> Replace entire section title to "## Graph Schema v4" and content:
       - Source of truth line: "**Single source of truth:** `training/dataset_schema.json` + `cypher_template.txt`"
       - Node Labels table: Keep existing OntoGraph/Metagraph rows (Class, DatatypeProperty, ObjectProperty, Rule, Atom, Var, Literal, Builtin). Add three DesignState `kind` values (ObjState, ParamState, PropState). Add Run node (ValidGraph, key `Run_Id`, display `Run_Id`). Update labels table with: `ValidGraph: DesignState (kind: ObjState/ParamState/PropState), Run (ValidStatus Boolean list, SendStatus Boolean), IntegrationConfig, ValidationEntity`
       - Relationships: Keep HAS_BODY, HAS_HEAD, REFERS_TO, ARG. Add `HAS_STATE: DesignState -> ObjState/ParamState/PropState` (read-side composition)
       - Schema Change Propagation list: ADD `data-service/ app.py Cypher`, `.github/copilot-instructions.md`, `README.md`, `spec/DATABASE.md` to the list of artifacts to update
    2. "## Current Priorities" section: REPLACE with Phase 20 (E2E Validation and Docs) as active and v7.0 completion as recent. Add "v4.0 BOT Ontology Bridge" as the next milestone. Remove Phase 14/Phase 15 from priorities (those are complete).
    3. "## Known Gotchas" section: ADD gotchas for v7.0: "Canvas breakage -- old .gh files referencing CLASSIFICATOR/VALIDATION RUNS/old REINSTATE show missing-component placeholders. Re-wire per docs/RELEASE-NOTES-v7.0.md." and "Old component GUIDs in saved .gh files -- opening a v2.0 canvas without updating GUID mapping file causes missing components."
    4. "## Tech Stack Summary" section: Update component count from 5 to 14. No other changes needed.

    **`.github/copilot-instructions.md` full rewrite (D-10):**
    - Replace entire file content. Structure:
    ```
    # Copilot Instructions for design-grammar-system

    ## Overview
    [Brief overview -- Docker pipeline, LLM Cypher generation, DG Grasshopper plugin. Same first paragraph style but replace "v3 schema" with "v4 schema"]

    ## Current canonical graph model (v4)
    Use the v4 schema as the source of truth for all code changes, prompts, generated Cypher, and UI labels.

    ### Graph separation
    - `graph = 'OntoGraph'` for ontology terms.
    - `graph = 'Metagraph'` for rules, atoms, variables, literals, and builtins.
    - `graph = 'ValidGraph'` for validation runs, design states, and integration config.
    - `graph = 'SpecGraph'` for project spec storage (notes, tags, sessions).
    - Every persisted node must include `project` and `graph`.
    - DesignState persisted with `graph = 'ValidGraph'`, `kind` in {ObjState, ParamState, PropState}.

    ### Canonical node labels and properties
    [Same v3 content but add:]
    - `DesignState`: key `StateId`, kind {ObjState, ParamState, PropState}, statePayloadJson v2
    - `Run`: key `Run_Id`, `ValidStatus` (Boolean list per ObjState), `SendStatus` (single Boolean)
    - Rule properties: `Rule_Id` (key), `SWRL`, `RuleName`, `RuleDescription`

    ### Canonical relationships
    [Keep HAS_BODY, HAS_HEAD, REFERS_TO, ARG. Add:]
    - `HAS_STATE`: DesignState -> state nodes (read-side)

    ## Component Architecture (14 components)
    [New section -- list all 14 components with their role and wiring flow:]
    - CONNECTOR: Database layer handle output
    - GRAPH DECONSTRUCT: Splits Database into Metagraph/OntoGraph/ValidGraph/SpecGraph handles
    - METAGRAPH: Reads Rules + Objects from Metagraph handle
    - ONTOGRAPH: Reads Class/ObjProperties/DataProperties from OntoGraph handle
    - VALIDATION GRAPH: Reads Run/Status/DesignState from ValidGraph handle
    - RULE DECONSTRUCT: Partitions rule into Objects + DataProperties outputs
    - OBJECT STATE: Captures Object+Geometry+Label into ObjState
    - PARAMETER STATE: Captures Parameters list into ParamState
    - PROPERTY STATE: Captures Rule+DataProperty+PropValue into PropState
    - DESIGN STATE: Composes ObjState/ParamState/PropState into DesignState
    - DESIGN STATE DECONSTRUCT: Splits DesignState into ObjState/ParamState/PropState
    - OBJECT DECONSTRUCT: Splits ObjState into Object/Geometry/Label
    - PARAMETER REINSTATE: Applies ParamState to canvas with rising-edge trigger
    - VALIDATOR: Evaluates Rule+DesignState, publishes validations

    ### Mandatory dependency propagation for schema v4
    [Keep same list but update title to v4. ADD: data-service Cypher, copilot-instructions.md, README.md, spec/DATABASE.md to the propagation list.]

    ### Required property migrations (already complete -- v7.0 milestone)
    [Keep existing migration notes but preface with "v7.0 completed these migrations:" instead of presenting them as pending.]

    [Keep: Architecture (Docker services, UI application, page flow, components, project-scoped graph isolation, Pencil design files), LLM integration, Reference files, Files to inspect, Deployment notes, Obsidian Knowledge Vault sections -- update only schema version references from v3 to v4.]
    ```

    **`spec/DATABASE.md` updates (D-09):**
    1. Title: "Database Schema (Neo4j Graph v4)"
    2. Graph Separation table: ADD `ValidGraph` row (DesignState, Run, IntegrationConfig, ValidationEntity -- validation metadata and run state). Change `ValidationGraph` to `ValidGraph` in existing row (the IntegrationConfig/ValidationEntity rows). ADD `SpecGraph` row (SpecNote, SpecTag, SpecSession, SpecClass -- project spec storage).
    3. Section: "DesignState (ValidGraph)" -- new subsection documenting:
       - `kind` = ObjState | ParamState | PropState on DesignState nodes
       - `statePayloadJson` = v2 JSON envelope with objStates/paramStates/propStates keys
       - Each kind carries a StateId with prefix: OS_ (ObjState), DS_ (ParamState), PS_ (PropState)
    4. Section: "Run (ValidGraph)" -- new subsection documenting:
       - `Run_Id`, `ValidStatus` (Boolean list per ObjState), `SendStatus` (Boolean)
       - `statePayloadJson` projection for read-back
    5. Canonical Node Labels table: ADD DesignState and Run entries. ADD `kind` property for DesignState.
    6. Schema Source Files: Add `data-service/app.py`, `graph-viewer/config.template.js` to the list.
    7. Update existing v3 migration notes to be historical (preface with "v3->v4 migration (complete)").

    **`README.md` updates (D-09):**
    1. Quick start Step 7: Replace "CLASSIFICATOR" reference with OBJECT STATE + PROPERTY STATE + DESIGN STATE flow. Update component references to match 14-component set.
    2. Data model conventions section: Replace graph separation table (currently only OntoGraph + Metagraph) with full 4-graph table (add ValidGraph, SpecGraph). Add DesignState kind and Run properties.
    3. Grasshopper add-in scaffold section: Update component list to all 14 components. Remove CLASSIFICATOR from the list. Add new components: GRAPH DECONSTRUCT, ONTOGRAPH, VALIDATION GRAPH, OBJECT STATE, PROPERTY STATE, DESIGN STATE, DESIGN STATE DECONSTRUCT, OBJECT DECONSTRUCT.
    4. Add link to docs/RELEASE-NOTES-v7.0.md in the Grasshopper section for upgrade instructions.
    5. Update the milestone/status reference if any.
  </action>

  <acceptance_criteria>
    - CLAUDE.md: "## Graph Schema v4" section exists (not v3), Node Labels table includes DesignState (kind ObjState/ParamState/PropState) and Run (ValidStatus/SendStatus), Schema Change Propagation list includes data-service, copilot-instructions.md, README.md, spec/DATABASE.md, Known Gotchas includes v7.0 canvas breakage and old GUID gotchas, Current Priorities reflects v7.0 completion + v4.0 BOT Ontology Bridge as next
    - .github/copilot-instructions.md: File has no v3 schema references presenting as current. Has "Current canonical graph model (v4)" section. Has "Component Architecture (14 components)" section listing all 14 with roles and wiring flow.
    - spec/DATABASE.md: Title says "Neo4j Graph v4". Graph Separation table includes ValidGraph (with DesignState, Run, IntegrationConfig, ValidationEntity rows) and SpecGraph. Has DesignState and Run subsections.
    - README.md: Grasshopper section lists all 14 components (not 5), CLASSIFICATOR not mentioned, links to docs/RELEASE-NOTES-v7.0.md
  </acceptance_criteria>

  <verify>
    <automated>grep -q "Graph Schema v4" CLAUDE.md && grep -q "Component Architecture" .github/copilot-instructions.md && grep -c "DesignState" .github/copilot-instructions.md | head -1 && grep -q "v4" spec/DATABASE.md && grep -c "OBJECT STATE\|PROPERTY STATE\|DESIGN STATE\|VALIDATION GRAPH" README.md | head -1</automated>
  </verify>

  <done>All four documents updated to reflect schema v4 and 14-component set. CLAUDE.md has accurate current priorities and gotchas. copilot-instructions.md fully rewritten per D-10. spec/DATABASE.md and README.md updated per D-09.</done>
</task>

<task type="auto">
  <name>Task 3: Run SC#3 grep gate and manual doc review</name>

  <files>
    CLAUDE.md
    .github/copilot-instructions.md
    spec/DATABASE.md
    README.md
    docs/RELEASE-NOTES-v7.0.md
  </files>

  <read_first>
    .planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-CONTEXT.md (D-12: SC#3 grep gate + manual review)
    .planning/ROADMAP.md (Success Criterion 3: grep gate definition)
  </read_first>

  <action>
    Perform the SC#3 grep gate and manual review per D-12.

    Step 1: Run the grep gate:
    ```
    grep -ri "schema v3\|v3 schema" CLAUDE.md .github/copilot-instructions.md spec/ README.md docs/RELEASE-NOTES-v7.0.md
    ```
    - Expected output: zero matching lines OR only lines that explicitly describe v3 in past tense ("was the canonical schema in v3.0", "v3 migration is complete")
    - If any line presents v3 as current (e.g., "v3 schema is the source of truth", "use the v3 schema"), it must be fixed to v4 or marked as historical

    Step 2: Manual review of each doc for:
    - Stale component names: replace "ValidationRunsComponent", "ClassificatorComponent", "ReinstateComponent", "Connection" with current v7.0 names
    - Stale label names: replace "ValidationGraph" (old) with "ValidGraph" where referring to the runtime graph property value
    - Stale property names: replace "text" (old Rule property) with "SWRL", "title" (old) with "RuleName"
    - Stale kind values: replace "DefState", "ObjectState" with current kind values
    - Any reference to schema v3 that should be v4
    - Component count accuracy (should reference 14 components, not 5 or 8)

    Step 3: Fix any issues found:
    - For each issue: Edit the affected file to replace stale content with current v7.0 content
    - Re-run the grep gate after fixes to confirm SC#3 passes
    - Do NOT change institutional knowledge that references v3 as historical context (e.g., migration notes) -- only fix content that presents v3 as current

    The gate is HARD per D-12: if any remaining grep hit presents v3 as current, the phase is not done.
  </action>

  <acceptance_criteria>
    - `grep -ri "schema v3\|v3 schema" CLAUDE.md .github/copilot-instructions.md spec/ README.md docs/RELEASE-NOTES-v7.0.md` returns zero lines that present v3 as current
    - Manual review finds no stale component names, label names, property names, or component count errors in any of the 5 docs
    - Any issues found have been fixed via Edit
  </acceptance_criteria>

  <verify>
    <automated>grep -rEin "schema v3|v3 schema" CLAUDE.md .github/copilot-instructions.md spec/ README.md docs/RELEASE-NOTES-v7.0.md 2>/dev/null; echo "EXIT_CODE=$?"</automated>
  </verify>

  <done>SC#3 grep gate passes. Manual review confirms all 5 docs are clean of stale v3 or v2.0 references. Any issues found are fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> readers | Release notes and repo docs are public-facing -- factual accuracy is critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Information Disclosure | Release notes | low | accept | Documents wiring topology and port mappings, not credentials or internal implementation details |
| T-20-05 | Tampering | copilot-instructions.md | medium | mitigate | Full rewrite cross-referenced against DesignGrammar-V7.owl and port-iri-map-V7.md for factual accuracy; manual review (Task 3) catches drift |
| T-20-06 | Spoofing | Docs v3->v4 edits | low | mitigate | SC#3 grep gate mechanically asserts no v3-as-current content survives; manual review catches semantic errors grep cannot find |
| T-20-SC | Tampering | npm/pip installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
1. `grep -ri "schema v3\|v3 schema" CLAUDE.md .github/copilot-instructions.md spec/ README.md docs/RELEASE-NOTES-v7.0.md` returns zero lines presenting v3 as current (SC#3)
2. Manual review passes for all 5 docs
3. All 15 locked decisions (D-05 through D-12) have corresponding content
</verification>

<success_criteria>
- docs/RELEASE-NOTES-v7.0.md created with per-component breakage guide + ASCII wiring diagrams (E2E-02)
- CLAUDE.md, copilot-instructions.md, spec/DATABASE.md, README.md updated to v4 + 14-component set (E2E-03)
- SC#3 grep gate passes with zero current-v3 references
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `RELEASE-NOTES-v7.0.md` | Document | `docs/RELEASE-NOTES-v7.0.md` |
| CLAUDE.md (updated) | Document | `CLAUDE.md` |
| copilot-instructions.md (rewritten) | Document | `.github/copilot-instructions.md` |
| DATABASE.md (updated) | Document | `spec/DATABASE.md` |
| README.md (updated) | Document | `README.md` |

<output>
Create `.planning/milestones/v7.0-phases/20-e2e-validation-and-docs/20-02-SUMMARY.md` when done.
Update `.planning/STATE.md` (current_phase=20, plan=2).
</output>
