---
phase: 18-rules-and-validator-rework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs
autonomous: true
requirements:
  - GHVL-01

must_haves:
  truths:
    - RULE DECONSTRUCT outputs Objects (Generic list) and DataProperties (Generic list) instead of Variables/VariableName
    - Partition uses VariableTypeInferrer.Infer() per D-08 to classify variables
    - Builtin-only variables are excluded from both Objects and DataProperties outputs per D-07
    - Variables with no REFERS_TO link produce an error message on the component per D-06
    - ExpectedOutputNames = ["Rule","Objects","DataProperties","SWRL","RuleName","RuleDescription"]
    - Same output count (6) preserved — no canvas layout crash
  artifacts:
    - DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs (modified)
  key_links:
    - Objects output feeds downstream consumers (OBJECT STATE Class input, DESIGN STATE ObjState input)
    - Rule passthrough feeds VALIDATOR Rule input
    - VariableTypeInferrer.Infer() in DG.Core.Parsing stays unchanged (D-08)

user_setup: []
---

<objective>
Extend RULE DECONSTRUCT to partition rule variables into Objects and DataProperties outputs.

Purpose: The component replaces the flat all-variables list with a type-aware partition. Architects see which variables are Objects (geometry entities) and which are DataProperties (quantitative properties). Builtins are hidden. This is the input-side counterpart to DesignStateBindingService — the VALIDATOR uses this partitioning to know how to bind values.

Output: Modified RuleDeconstructComponent.cs
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-CONTEXT.md (D-06, D-07, D-08 — edge case decisions)

@DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs
@DG/src/DG.Core/Parsing/VariableTypeInferrer.cs
@DG/src/DG.Core/Models/Variable.cs
@ontology/port-iri-map-V7.md — RULE DECONSTRUCT port contract
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ExpectedOutputNames and output registration to Objects/DataProperties</name>

  <read_first>
    @DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs — lines 13-21 for ExpectedOutputNames, lines 38-45 for RegisterOutputParams, lines 163-221 for EnsureOutputLayout
    @ontology/port-iri-map-V7.md — lines 64-70 for RULE DECONSTRUCT port contract: Objects (out, dg:Object), DataProperties (out, dg:DataProperty), Rule (out), SWRL (out), RuleName (out), RuleDescription (out)
  </read_first>

  <files>
    DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs
  </files>

  <action>
    Modify ExpectedOutputNames array at line 13-21 from:
    `"Rule", "Variables", "VariableName", "SWRL", "RuleName", "RuleDescription"`
    to:
    `"Rule", "Objects", "DataProperties", "SWRL", "RuleName", "RuleDescription"`

    The array stays 6 elements — the count is unchanged so `HasExpectedOutputLayout` still passes on existing canvases (same port count prevents crash).

    Modify RegisterOutputParams at lines 38-45:
    - Output index 1: change from `AddGenericParameter("Variables", "Variables", "Unique DG.Variable list", GH_ParamAccess.list)` to `AddGenericParameter("Objects", "Objects", "Object-type variables from the rule (each maps to an ObjState)", GH_ParamAccess.list)`
    - Output index 2: change from `AddTextParameter("VariableName", "VariableName", "Variable names without '?' prefix", GH_ParamAccess.list)` to `AddGenericParameter("DataProperties", "DataProperties", "DataProperty-type variables from the rule (each maps to a PropState)", GH_ParamAccess.list)`
    - Outputs 0, 3, 4, 5 unchanged (Rule, SWRL, RuleName, RuleDescription)

    Modify EnsureOutputLayout at lines 181-197 to match the new output definitions (same pattern, updated names and descriptions). Same Param_GenericObject for Objects (not Param_String — Objects output carries DG.Variable objects, same as old Variables output). Same Param_GenericObject for DataProperties (not Param_String — DataProperties output carries DG.Variable objects).

    Do NOT change the output type from list to item — both Objects and DataProperties are GH_ParamAccess.list.
  </action>

  <verify>
    <automated>grep -c "Objects" DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs && grep -c "DataProperties" DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs</automated>
  </verify>

  <acceptance_criteria>
    - ExpectedOutputNames = `["Rule","Objects","DataProperties","SWRL","RuleName","RuleDescription"]`
    - ExpectedOutputNames.Length == 6 (same count as before)
    - RegisterOutputParams registers index 1 as "Objects" (Generic, list) and index 2 as "DataProperties" (Generic, list)
    - EnsureOutputLayout creates matching output definitions
    - grep -c "Variables" on the file shows only the removed occurrences are gone (Variables array removed from ExpectedOutputNames and RegisterOutputParams)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add variable partitioning logic in SolveInstance</name>

  <read_first>
    @DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs — lines 60-161 for current SolveInstance logic
    @DG/src/DG.Core/Parsing/VariableTypeInferrer.cs — Infer(Rule, string) returns VariableKind? (Object/Property/null)
    @..planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-CONTEXT.md — D-06 (REFERS_TO error), D-07 (Builtin exclusion), D-08 (VariableTypeInferrer stays)
  </read_first>

  <files>
    DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs
  </files>

  <action>
    After the existing variable collection logic in SolveInstance (after line ~102 where all variables are collected into the variablesByName dictionary), add a partition step.

    Remove lines 104-121 that build `variables` list and `publicVars`/`variableNames` lists — these are the old Variables/VariableName output logic.

    Replace with:

    1. Classify each variable from variablesByName using VariableTypeInferrer.Infer(rule, variable.Name):
       - VariableKind.Object → add to objects list (as DG.Variable)
       - VariableKind.Property → add to dataProperties list (as DG.Variable)
       - null → check if variable is Builtin-only: scan rule.BodyAtoms + rule.HeadAtoms for BuiltinAtom where the variable appears in any Arg. If found ONLY in BuiltinAtom args and not in ClassAtom/ObjectPropertyAtom/DataPropertyAtom: exclude per D-07 (skip, don't add to either list). If found in regular atoms but Infer returned null (unclassifiable): add runtime message at GH_RuntimeMessageLevel.Error per D-06 — use message format: $"Variable '?{variable.Name}' could not be classified: no REFERS_TO link found. Ensure the variable appears in a ClassAtom or DataPropertyAtom."

    2. Set outputs:
       - da.SetDataList(1, objectsPublic) — Objects output (replaces old Variables)
       - da.SetDataList(2, dataPropertiesPublic) — DataProperties output (replaces old VariableName)

    3. Update the component Message:
       - Format: `"{objects.Count} obj, {dataProperties.Count} prop"` (instead of old `"{publicVars.Count} vars"`)
       - If there are unclassified variables: append `, {unclassifiedCount} unclassified`

    Keep existing logic for:
    - Rule passthrough at da.SetData(0, publicRule) (unchanged)
    - SWRL at da.SetData(3, rule.Swrl) (unchanged)
    - RuleName at da.SetData(4, rule.Name) (unchanged)
    - RuleDescription at da.SetData(5, rule.Description) (unchanged)
    - All existing Rule building (lines 123-153 from publicRule) — keep as-is

    Do NOT change:
    - The ComponentGuid {1D5AD920-C5DB-4C07-BF66-BA4E2526E6D4}
    - The Icon reference
    - The AddToDocument, Read overrides

    Do NOT add any new `using` for DG.Core.Classification — VariableTypeInferrer is already in DG.Core.Parsing and imported via the existing `using DG.Core.Parsing` at line 3.
  </action>

  <verify>
    <automated>$b = dotnet build DG/src/DG.Grasshopper/ --no-restore 2>&1; if ($LASTEXITCODE -ne 0) { throw "Build failed, exit code: $LASTEXITCODE" }; $b</automated>
  </verify>

  <acceptance_criteria>
    - SolveInstance classifies each variable via VariableTypeInferrer.Infer(rule, variable.Name)
    - Object variables appear in "Objects" output (list), not "Variables" output
    - Property variables appear in "DataProperties" output (list), not "VariableName" output
    - Builtin-only variables are excluded from both outputs per D-07
    - Unclassifiable variables (Infer returns null, not Builtin-only) produce a runtime error message and add to the component's error count
    - Component Message shows partition counts: "2 obj, 3 prop"
    - Old "Variables" and "VariableName" output setting code removed
    - dotnet build DG/src/DG.Grasshopper/ succeeds with no errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rule input -> RULE DECONSTRUCT | Untrusted Rule object from database/metagraph; variable classification must not crash on malformed atoms |
| Variable classification -> downstream | Partition output feeds OBJECT STATE and VALIDATOR; misclassification causes binding failures downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Tampering | VariableTypeInferrer.Infer() | low | accept | VariableTypeInferrer is pure logic from Phase 7 (D-08); malformed Rule atoms return null which triggers D-06 error |
| T-18-04 | Tampering | Builtin atom elimination | low | accept | If a variable is misclassified as builtin, it silently disappears from both outputs — user sees "0 obj, 0 prop" and can inspect the rule. Accept per D-07 |
</threat_model>

<verification>
1. `dotnet build DG/src/DG.Grasshopper/ --no-restore` succeeds
2. ExpectedOutputNames array has exactly 6 elements: ["Rule","Objects","DataProperties","SWRL","RuleName","RuleDescription"]
3. No "Variables" or "VariableName" remain in ExpectedOutputNames or RegisterOutputParams
4. SolveInstance uses VariableTypeInferrer.Infer() for partitioning
5. D-06: unclassifiable vars produce error message (not silent skip)
6. D-07: Builtin-only vars excluded from both outputs
</verification>

<success_criteria>
- RULE DECONSTRUCT partitions variables by type per GHVL-01
- Objects output shows Object-type variables, DataProperties output shows Property-type variables
- VariableTypeInferrer used as-is (D-08)
- All edge cases handled per D-06 (unclassified error), D-07 (Builtin exclusion)
- Component compiles without errors
</success_criteria>

## Artifacts this phase produces

### Modified files
- `DG/src/DG.Grasshopper/Components/RuleDeconstructComponent.cs` — output names changed, variable partition added

### Deleted surfaces from old component
- "Variables" output removed
- "VariableName" output removed

<output>
Create .planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-02-SUMMARY.md when done
</output>
