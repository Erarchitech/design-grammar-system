---
phase: 18-rules-and-validator-rework
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - data-service/app.py
  - data-service/tests/test_validation_runs_state.py
autonomous: true
requirements:
  - GHVL-06

must_haves:
  truths:
    - _project_state_summary detects v2 statePayloadJson via root "version" field and counts state elements from objStates/paramStates/propStates arrays
    - v1 payloads (no "version" field) still produce correct summary from "parameters" array — backward compatible
    - Return shape is identical for v1 and v2: {stateId, capturedAtUtc, parameterCount}
    - Null/malformed/empty payloads return None (never raise)
    - All existing tests pass after the adaptation
    - New tests cover v2 envelope (all 3 kinds, empty arrays, missing fields), v1 fallback, and malformed input
  artifacts:
    - data-service/tests/test_validation_runs_state.py (extended)
  key_links:
    - _project_state_summary output is consumed by list_validation_runs which feeds the Model Viewer
    - useValidationRunsGrouping.js reads run.state.stateId for group-by-State — this field path is preserved
    - The publish path (Plan 18-05) will store statePayloadJson v2; this read-side must handle it

user_setup: []
---

<objective>
Adapt the Model Viewer read-side to handle v2 statePayloadJson while keeping v1 tolerance.

Purpose: The Model Viewer must continue working against v7.0 data. Runs published with v1 ParamState-only payloads must still render (backward compat). Runs published with the new v2 3-part DesignState payload must also render correctly. The `_project_state_summary` function detects the version and counts appropriately. The return shape is identical for both versions — no JS changes needed for basic operation.

Output: Modified data-service/app.py _project_state_summary + extended Python tests
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-CONTEXT.md
@.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-RESEARCH.md (v2 payload detection pattern)
@.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-UI-SPEC.md (data contract, test cases)

@data-service/app.py — _project_state_summary (lines 534-558), list_validation_runs (lines 561-605)
@data-service/tests/test_validation_runs_state.py — existing tests (9 test functions)

@graph-viewer/model-viewer/src/useValidationRunsGrouping.js — deriveStateKey reads run.state.stateId (preserved)
@graph-viewer/model-viewer/src/ValidationRunsStrip.jsx — renders group.groupLabel (preserved)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt _project_state_summary in data-service/app.py for v2 envelope detection</name>

  <read_first>
    @data-service/app.py — lines 534-558 for current _project_state_summary implementation
    @.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-RESEARCH.md — lines 502-541 for v2 detection pattern (version field check, objStates/paramStates/propStates count)
    @.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-UI-SPEC.md — lines 49-73 for contract spec
  </read_first>

  <files>
    data-service/app.py
  </files>

  <action>
    Edit _project_state_summary at data-service/app.py (lines 534-558).

    Current implementation (lines 552-558):
    ```python
    parameters = parsed.get("parameters")
    parameter_count = len(parameters) if isinstance(parameters, list) else 0
    return {
        "stateId": parsed.get("stateId") or "",
        "capturedAtUtc": parsed.get("capturedAtUtc"),
        "parameterCount": parameter_count,
    }
    ```

    Replace with version-aware logic:

    1. Check `version = parsed.get("version")`
    2. If `version == "2"`:
       - Count: `len(parsed.get("objStates") or []) + len(parsed.get("paramStates") or []) + len(parsed.get("propStates") or [])`
       - Return: `{"stateId": parsed.get("stateId") or "", "capturedAtUtc": parsed.get("capturedAtUtc"), "parameterCount": <total count>}`
    3. Else (v1 fallback):
       - Unchanged existing logic: count from `parsed.get("parameters")` list

    The return shape dictionary keys must be identical for both paths: `stateId`, `capturedAtUtc`, `parameterCount`.

    Keep all existing guards:
    - `if not state_payload_json: return None`
    - Try/except json.JSONDecodeError, ValueError, TypeError, RecursionError → return None
    - `if not isinstance(parsed, dict): return None`

    Do NOT change:
    - The function signature `def _project_state_summary(state_payload_json: str | None) -> dict[str, Any] | None:`
    - Any other code in app.py outside this function
    - The list_validation_runs function (it already calls _project_state_summary at line 602)
  </action>

  <verify>
    <automated>cd data-service && python -m pytest tests/test_validation_runs_state.py -v 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - _project_state_summary checks `parsed.get("version")` for v2 detection
    - v2 path counts from objStates + paramStates + propStates arrays (each with len() guard)
    - v1 fallback path uses existing "parameters" array logic (unchanged)
    - Return shape for v2: {"stateId": str, "capturedAtUtc": str|None, "parameterCount": int}
    - Return shape for v1: {"stateId": str, "capturedAtUtc": str|None, "parameterCount": int}
    - Both paths return identical dict keys
    - All existing tests (9 tests) pass after the change
    - null/malformed input returns None (never raises)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend Python tests with v2 envelope test cases</name>

  <read_first>
    @data-service/tests/test_validation_runs_state.py — existing test patterns, imports, and test functions
    @.planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-UI-SPEC.md — lines 176-193 for the test contract (5 v2-specific tests)
  </read_first>

  <files>
    data-service/tests/test_validation_runs_state.py
  </files>

  <action>
    Append 5 new test functions to data-service/tests/test_validation_runs_state.py (after the last existing test at line 82).

    Import `from app import _project_state_summary` is already at line 7 — no import change needed.

    Test 1 — `test_v2_envelope_all_three_kinds`:
    Input: `{"version":"2","stateId":"DS_1","capturedAtUtc":"2026-07-04T10:00:00.0000000Z","objStates":[{}],"paramStates":[{},{}],"propStates":[{},{},{}]}`
    Expected: `{"stateId":"DS_1","capturedAtUtc":"2026-07-04T10:00:00.0000000Z","parameterCount":6}`

    Test 2 — `test_v2_envelope_empty_arrays`:
    Input: `{"version":"2","stateId":"DS_2","capturedAtUtc":"2026-07-04T10:00:00Z","objStates":[],"paramStates":[],"propStates":[]}`
    Expected: `{"stateId":"DS_2","capturedAtUtc":"2026-07-04T10:00:00Z","parameterCount":0}`

    Test 3 — `test_v2_missing_arrays_defaults_to_zero`:
    Input: `{"version":"2","stateId":"DS_3"}`
    Expected: `{"stateId":"DS_3","capturedAtUtc":None,"parameterCount":0}`

    Test 4 — `test_v1_fallback_no_version_field`:
    Input: `{"stateId":"S-1","parameters":[{"parameterId":"p1"},{"parameterId":"p2"}]}`
    Expected: `{"stateId":"S-1","capturedAtUtc":None,"parameterCount":2}`

    Test 5 — `test_v2_incompatible_version_falls_back`:
    Input: `{"version":"1","stateId":"S-2","parameters":[{"parameterId":"p1"}]}`
    Expected: `{"stateId":"S-2","capturedAtUtc":None,"parameterCount":1}` — version "1" is not "2", so falls back to parameters array

    All existing 9 tests must still pass. Run with: `cd data-service && python -m pytest tests/test_validation_runs_state.py -v`
  </action>

  <verify>
    <automated>cd data-service && python -m pytest tests/test_validation_runs_state.py -v 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - 5 new test functions appended to test_validation_runs_state.py
    - test_v2_envelope_all_three_kinds covers 3 arrays with different element counts
    - test_v2_envelope_empty_arrays covers empty arrays → parameterCount 0
    - test_v2_missing_arrays_defaults_to_zero covers missing arrays → parameterCount 0
    - test_v1_fallback_no_version_field covers v1 backward compatibility
    - test_v2_incompatible_version_falls_back covers version != "2" fallthrough
    - All 14 tests (9 existing + 5 new) pass with `pytest tests/test_validation_runs_state.py -v`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statePayloadJson string -> _project_state_summary | Untrusted JSON from Neo4j database; must handle non-dict, malformed, null safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06 | DoS | _project_state_summary JSON parsing | low | accept | Existing try/except guards for JSONDecodeError, ValueError, TypeError, RecursionError return None on any failure |
</threat_model>

<verification>
1. `cd data-service && python -m pytest tests/test_validation_runs_state.py -v` — all 14 tests pass
2. `_project_state_summary` never raises on any input (tested with garbage, null, empty, arrays, etc.)
3. Return shape identical for v1 and v2 — confirmed by assertion shape checks in tests
</verification>

<success_criteria>
- _project_state_summary handles v2 statePayloadJson with version detection
- v1 backward compatibility preserved — existing v1 payloads produce correct summaries
- All existing tests pass; 5 new tests cover v2 scenarios
- Model Viewer continues to group runs by `state.stateId` (field path preserved)
- No JS changes required for basic operation (UI-SPEC confirms useValidationRunsGrouping.js and ValidationRunsStrip.jsx are unchanged for basic compatibility)
</success_criteria>

## Artifacts this phase produces

### Modified files
- `data-service/app.py` — `_project_state_summary` function adapted for v2 envelope detection
- `data-service/tests/test_validation_runs_state.py` — 5 new test functions added

### Functional change
- `_project_state_summary` now detects `version == "2"` and counts from `objStates + paramStates + propStates` instead of `parameters`

<output>
Create .planning/milestones/v7.0-phases/18-rules-and-validator-rework/18-04-SUMMARY.md when done
</output>
