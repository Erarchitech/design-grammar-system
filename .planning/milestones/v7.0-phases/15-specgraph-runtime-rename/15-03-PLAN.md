---
phase: 15-specgraph-runtime-rename
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/workflows/knowledge-ingest.json
  - n8n/workflows/spec-ingest.json
  - n8n/workflows/knowledge-query.json
  - n8n/workflows/spec-query.json
  - n8n/workflows/knowledge-update.json
  - n8n/workflows/spec-update.json
  - n8n/workflows/_active-graph-query.json
  - n8n/workflows/_all-workflows-export.json
autonomous: true
requirements: [SPEC-03]
nyquist_compliant: true

must_haves:
  truths:
    - "n8n/workflows/knowledge-ingest.json is renamed spec-ingest.json with workflow name 'Knowledge Ingest'->'Spec Ingest' and node display name 'Write KnowledgeSession'->'Write SpecSession' (D-02 — internal workflow name + node display names switch to Spec*, no stale Knowledge* in the n8n UI/code), and all inline Cypher labels Knowledge*->Spec* + graph:'KnowledgeGraph'->'SpecGraph' (SPEC-03)"
    - "n8n/workflows/knowledge-query.json is renamed spec-query.json with the same class of edits (name, node rename, inline Cypher)"
    - "n8n/workflows/knowledge-update.json is renamed spec-update.json with workflow name 'knowledge-update'->'spec-update' and any Knowledge*-token node display name updated (it has no inline Knowledge* Cypher — it delegates to data-service)"
    - "Every renamed node's new name is reflected in the workflow's connections object — no connections entry references a node name that no longer exists (landmine L-1)"
    - "The two generated export files n8n/workflows/_active-graph-query.json and _all-workflows-export.json are DELETED (D-05)"
    - "All three workflows remain valid JSON after editing"
  artifacts:
    - "n8n/workflows/spec-ingest.json (renamed from knowledge-ingest.json)"
    - "n8n/workflows/spec-query.json (renamed from knowledge-query.json)"
    - "n8n/workflows/spec-update.json (renamed from knowledge-update.json)"
  key_links:
    - "Inline Cypher Spec* labels + graph:'SpecGraph' MUST match the 15-01 migration + 15-02 data-service end-state 1:1"
  prohibitions:
    - "Do NOT change any webhook `path` value — dg/knowledge-ingest, dg/knowledge-query, dg/knowledge-update are preserved so the UI + nginx proxy keep resolving (SPEC-02 URL-compat principle, RESEARCH § Scope Boundaries)"
    - "Do NOT edit the export files' content instead of deleting — they are non-canonical generated snapshots (D-05); the canonical files are the source of truth"

security: "No new threat surface — these are workflow definition files edited/deleted on disk. The only externally reachable identifiers (webhook paths) are deliberately preserved. Deleting the two export snapshots removes stale copies, reducing drift, not adding risk."
---

<objective>
Propagate the Spec* rename through the three canonical n8n knowledge workflows — file names, workflow `name`, node display names (with their `connections` references), and inline Cypher labels — while preserving the webhook `path` values the UI depends on. Delete the two non-canonical export snapshots that would otherwise carry stale Knowledge* references past this phase (SC#4).

Purpose: n8n writes SpecNote/SpecSession via inline Cypher (ingest/query) and via data-service (update); after this plan the live webhooks operate on Spec* labels with unchanged URLs.
Output: `spec-ingest.json`, `spec-query.json`, `spec-update.json`; two export files removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-CONTEXT.md
@.planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-RESEARCH.md
@.planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename + rewrite knowledge-ingest.json -> spec-ingest.json</name>
  <files>n8n/workflows/knowledge-ingest.json, n8n/workflows/spec-ingest.json</files>
  <read_first>
    - n8n/workflows/knowledge-ingest.json (FULL — top-level `name`, `nodes[]` with `name` + inline Cypher in parameters, `connections` object keyed by node name)
    - .planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-RESEARCH.md § "Execution Landmines" (L-1 connections)
  </read_first>
  <action>
    `git mv n8n/workflows/knowledge-ingest.json n8n/workflows/spec-ingest.json`. Then edit content: top-level `"name": "Knowledge Ingest"` -> `"Spec Ingest"`; node `"name": "Write KnowledgeSession"` -> `"Write SpecSession"` AND update every occurrence of that node name inside the `connections` object (both as a key and as a `"node"` value) so the graph stays connected (L-1); inline Cypher in node `parameters` — every `KnowledgeNote`/`KnowledgeTag`/`KnowledgeSession`/`KnowledgeClass` -> `Spec*` and every `graph:'KnowledgeGraph'` / `'KnowledgeGraph'` -> `'SpecGraph'`. PRESERVE the webhook node's `"path"` value (dg/knowledge-ingest) and `webhookId` unchanged.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && f=n8n/workflows/spec-ingest.json && test -f "$f" && test ! -f n8n/workflows/knowledge-ingest.json && python -m json.tool "$f" >/dev/null && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' "$f")" && grep -q "dg/knowledge-ingest" "$f" && python -c "import json,sys; d=json.load(open('$f')); names={n['name'] for n in d['nodes']}; c=d.get('connections',{}); ok=all(k in names for k in c) and all(t['node'] in names for outs in c.values() for arr in outs.values() for lst in arr for t in lst); sys.exit(0 if ok else 1)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - spec-ingest.json exists, knowledge-ingest.json gone, file parses as JSON.
    - Zero Knowledge* graph-layer tokens; workflow name "Spec Ingest"; node "Write SpecSession".
    - Webhook path dg/knowledge-ingest preserved.
    - connections integrity: every connection references an existing node name (no dangling refs — L-1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rename + rewrite knowledge-query.json -> spec-query.json</name>
  <files>n8n/workflows/knowledge-query.json, n8n/workflows/spec-query.json</files>
  <read_first>
    - n8n/workflows/knowledge-query.json (FULL — same structure as ingest: name, nodes, inline Cypher, connections)
    - .planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-RESEARCH.md § "Execution Landmines"
  </read_first>
  <action>
    `git mv n8n/workflows/knowledge-query.json n8n/workflows/spec-query.json`. Edit: `"name": "Knowledge Query"` -> `"Spec Query"`; node `"Write KnowledgeSession"` -> `"Write SpecSession"` with matching `connections` updates (L-1); inline Cypher Knowledge*->Spec* and graph literal 'KnowledgeGraph'->'SpecGraph'. PRESERVE webhook `"path"` (dg/knowledge-query) and webhookId.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && f=n8n/workflows/spec-query.json && test -f "$f" && test ! -f n8n/workflows/knowledge-query.json && python -m json.tool "$f" >/dev/null && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' "$f")" && grep -q "dg/knowledge-query" "$f" && python -c "import json,sys; d=json.load(open('$f')); names={n['name'] for n in d['nodes']}; c=d.get('connections',{}); ok=all(k in names for k in c) and all(t['node'] in names for outs in c.values() for arr in outs.values() for lst in arr for t in lst); sys.exit(0 if ok else 1)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - spec-query.json exists, knowledge-query.json gone, valid JSON.
    - Zero Knowledge* graph-layer tokens; workflow name "Spec Query"; node "Write SpecSession".
    - Webhook path dg/knowledge-query preserved; connections integrity holds (L-1).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rename knowledge-update.json -> spec-update.json</name>
  <files>n8n/workflows/knowledge-update.json, n8n/workflows/spec-update.json</files>
  <read_first>
    - n8n/workflows/knowledge-update.json (FULL — 8 nodes, webhook path dg/knowledge-update, delegates writes to data-service; RESEARCH confirms 0 inline Knowledge* label Cypher)
    - .planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-RESEARCH.md § "knowledge-update.json — active vs. deprecated"
  </read_first>
  <action>
    `git mv n8n/workflows/knowledge-update.json n8n/workflows/spec-update.json`. Edit: top-level `"name": "knowledge-update"` -> `"spec-update"`. Scan node `name` fields for any Knowledge* token (e.g. a "Write KnowledgeSession"-style node) and rename to Spec* WITH matching `connections` updates (L-1) if present. PRESERVE the webhook `"path"` (dg/knowledge-update) and webhookId. If a node references a data-service `/knowledge/update/...` URL, leave that URL unchanged (SPEC-02).
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && f=n8n/workflows/spec-update.json && test -f "$f" && test ! -f n8n/workflows/knowledge-update.json && python -m json.tool "$f" >/dev/null && test -z "$(grep -in 'KnowledgeGraph\|KnowledgeNote\|KnowledgeTag\|KnowledgeSession\|KnowledgeClass' "$f")" && grep -q "dg/knowledge-update" "$f" && grep -q '"name": "spec-update"' "$f" && python -c "import json,sys; d=json.load(open('$f')); names={n['name'] for n in d['nodes']}; c=d.get('connections',{}); ok=all(k in names for k in c) and all(t['node'] in names for outs in c.values() for arr in outs.values() for lst in arr for t in lst); sys.exit(0 if ok else 1)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - spec-update.json exists, knowledge-update.json gone, valid JSON.
    - Workflow name "spec-update"; zero Knowledge* graph-layer tokens.
    - Webhook path dg/knowledge-update + any /knowledge/update/ data-service URL preserved; connections integrity holds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Delete the two generated export snapshots</name>
  <files>n8n/workflows/_active-graph-query.json, n8n/workflows/_all-workflows-export.json</files>
  <read_first>
    - .planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-CONTEXT.md D-05 (why these are deleted, not edited)
  </read_first>
  <action>
    `git rm n8n/workflows/_active-graph-query.json n8n/workflows/_all-workflows-export.json`. These are non-canonical generated exports; the canonical workflows (spec-ingest/spec-query/spec-update/rules-to-metagraph/graph-query-mcp) are the source of truth. Do not recreate them.
  </action>
  <verify>
    <automated>cd "C:/Users/Admin/source/repos/design-grammar-system" && test ! -f n8n/workflows/_active-graph-query.json && test ! -f n8n/workflows/_all-workflows-export.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both export files are removed from the working tree and staged for deletion.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
Renamed files: spec-ingest.json, spec-query.json, spec-update.json (from knowledge-*.json). Deleted: _active-graph-query.json, _all-workflows-export.json. No new webhook paths or workflow IDs — URL-facing identifiers preserved.
</artifacts_produced>

<verification>
- All three spec-*.json parse as JSON, are Knowledge*-clean (graph-layer tokens), and pass the connections-integrity check (L-1).
- Webhook paths dg/knowledge-* preserved in all three.
- Both export snapshots deleted.
</verification>

<success_criteria>
SPEC-03 satisfied: the three canonical n8n knowledge workflows are renamed and operate on Spec* labels with intact node graphs and preserved webhook URLs; the two stale export snapshots are gone so they cannot re-introduce Knowledge* references (SC#4).
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/15-specgraph-runtime-rename/15-03-SUMMARY.md` when done.
</output>
