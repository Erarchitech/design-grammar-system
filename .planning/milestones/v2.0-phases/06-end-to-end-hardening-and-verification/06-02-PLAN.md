---
phase: 06-end-to-end-hardening-and-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data-service/app.py
  - data-service/tests/test_error_responses.py
  - graph-viewer/model-viewer/src/App.jsx
autonomous: true
requirements:
  - INTG-03

must_haves:
  truths:
    - "Data-service validation error responses return structured JSON with error, hint, and code fields"
    - "Model-viewer handles fetch failures for validation endpoints with retry affordance"
    - "Pytest tests verify structured error body shape for at least 3 validation error scenarios"
  artifacts:
    - path: "data-service/app.py"
      provides: "Structured error response helper and updated validation endpoint error handling"
      contains: "_structured_error_response"
    - path: "data-service/tests/test_error_responses.py"
      provides: "Pytest tests for structured error response bodies"
      contains: "test_"
    - path: "graph-viewer/model-viewer/src/App.jsx"
      provides: "Extended error handling for validation fetch paths"
      contains: "runsError"
  key_links:
    - from: "data-service/tests/test_error_responses.py"
      to: "data-service/app.py"
      via: "TestClient calling validation endpoints"
      pattern: "client\\.(get|post|delete)"
    - from: "graph-viewer/model-viewer/src/App.jsx"
      to: "data-service/app.py"
      via: "fetch calls to /data-service/validation/* endpoints"
      pattern: "fetch.*validation"
---

<objective>
Add structured error responses to data-service validation endpoints and extend model-viewer error handling to cover additional fetch failure paths. Closes INTG-03 for Python and frontend surfaces.

Purpose: Actionable error messages from data-service HTTP responses and viewer error states per D-05, D-06.
Output: Structured error helper in data-service, pytest coverage, extended viewer error handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-end-to-end-hardening-and-verification/06-CONTEXT.md
@.planning/phases/05-model-viewer-grouping-by-rule-state/05-01-SUMMARY.md
@.planning/phases/05-model-viewer-grouping-by-rule-state/05-02-SUMMARY.md

<interfaces>
<!-- Key existing patterns the executor needs. -->

From data-service/app.py — existing _project_state_summary pattern (defensive, never raises):
```python
def _project_state_summary(state_payload_json: str | None) -> dict[str, Any] | None:
    # Returns compact dict or None. Never raises.
```

From data-service/app.py — existing HTTPException patterns:
```python
raise HTTPException(status_code=404, detail="No Speckle integration config found for this DG project.")
raise HTTPException(status_code=400, detail=str(exc))
raise HTTPException(status_code=500, detail=f"Validation publish failed: {exc}")
```

From graph-viewer/model-viewer/src/ValidationRunsStrip.jsx — existing error retry pattern:
```jsx
{runsError && React.createElement('div', {className: 'mv-error-retry'}, ...)}
// "Could not load grouped runs. Try again." with retry button
```

From data-service/tests/conftest.py:
```python
import pytest
```

From data-service/tests/test_validation_runs_state.py — existing test pattern (uses TestClient).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured error response helper to data-service + update validation endpoints</name>
  <files>data-service/app.py</files>
  <read_first>
    - data-service/app.py (full file — understand all existing HTTPException calls and the _project_state_summary pattern)
    - data-service/tests/test_validation_runs_state.py (existing test patterns for test setup)
  </read_first>
  <action>
    **Per D-05, D-06:** Add a helper function `_structured_error_response(error: str, hint: str, code: str, status_code: int = 500) -> HTTPException` to data-service/app.py that returns an HTTPException with `detail` set to a JSON-serializable dict:
    ```python
    def _structured_error_response(error: str, hint: str, code: str, status_code: int = 500) -> HTTPException:
        return HTTPException(
            status_code=status_code,
            detail={"error": error, "hint": hint, "code": code},
        )
    ```

    Place this function near `_project_state_summary` (around line 509) as a sibling private helper.

    **Update validation-related HTTPException calls** to use the structured helper. Target these endpoints:

    1. `POST /validation/publish` (line 789+):
       - "No Speckle integration config found" → `_structured_error_response("No Speckle project configuration found.", "Open the project's Speckle Settings card on the DG home page and save your Speckle project ID.", "SPECKLE_CONFIG_MISSING", 404)`
       - "Speckle write token is not configured" → `_structured_error_response("Speckle write token not configured.", "Save it in the DG home page Speckle Settings card or set SPECKLE_WRITE_TOKEN on data-service.", "SPECKLE_TOKEN_MISSING", 500)`
       - `SpeckleValidationError` catch → `_structured_error_response(f"Validation publish failed: {exc}", "Check that Speckle server is reachable and project ID is correct.", "PUBLISH_VALIDATION_ERROR", 400)`
       - Generic exception catch → `_structured_error_response(f"Validation publish failed: {exc}", "Check data-service logs for details.", "PUBLISH_INTERNAL_ERROR", 500)`

    2. `DELETE /validation/run/{project}/{run_id}` (line 870+):
       - "Validation run not found" → `_structured_error_response("Validation run not found.", "Verify the run ID exists for this project.", "RUN_NOT_FOUND", 404)`
       - "missing Speckle identifiers" → `_structured_error_response("Validation run is missing Speckle identifiers required for deletion.", "This run may have been created before Speckle integration was configured.", "SPECKLE_IDS_MISSING", 500)`

    3. `GET /validation/runs/{project}` (line 865+):
       - Keep as-is if no error paths exist, or wrap any potential Neo4j query failures

    **Do NOT change non-validation endpoints** (knowledge, MCP, folder endpoints). Scope discipline — only validation surfaces per D-05.
  </action>
  <verify>
    <automated>cd data-service ; python -c "from app import _structured_error_response; r = _structured_error_response('test', 'hint', 'CODE', 400); print(r.detail)"</automated>
  </verify>
  <acceptance_criteria>
    - data-service/app.py contains `def _structured_error_response(`
    - data-service/app.py contains `"error":` and `"hint":` and `"code":` in the helper
    - At least 4 HTTPException raises in validation endpoints use `_structured_error_response` instead of bare `detail=` strings
    - Non-validation endpoints (knowledge, MCP, folder) remain unchanged
  </acceptance_criteria>
  <done>Data-service validation endpoints return structured {error, hint, code} JSON error bodies instead of bare strings.</done>
</task>

<task type="auto">
  <name>Task 2: Pytest for error responses + model-viewer error path extensions</name>
  <files>data-service/tests/test_error_responses.py, graph-viewer/model-viewer/src/App.jsx</files>
  <read_first>
    - data-service/app.py (updated file from Task 1 — verify helper exists)
    - data-service/tests/test_validation_runs_state.py (existing test patterns for TestClient setup)
    - data-service/tests/conftest.py (fixture infrastructure)
    - graph-viewer/model-viewer/src/App.jsx (current error handling patterns, look for fetch calls that lack error handling)
    - graph-viewer/model-viewer/src/ValidationRunsStrip.jsx (runsError retry pattern to replicate)
  </read_first>
  <action>
    **Pytest (D-08):** Create `data-service/tests/test_error_responses.py` using FastAPI TestClient:

    ```python
    from fastapi.testclient import TestClient
    from app import app

    client = TestClient(app)
    ```

    Test scenarios:
    1. `test_publish_validation_missing_config` — POST `/validation/publish` with a non-existent project. Assert response status 404 and body contains keys `error`, `hint`, `code`.
    2. `test_delete_run_not_found` — DELETE `/validation/run/fake-project/fake-run-id`. Assert 404 and structured body.
    3. `test_error_response_shape` — Call any endpoint known to error. Assert the response JSON has exactly the 3 keys: `error` (str), `hint` (str), `code` (str). No extra keys.

    **Model-viewer (D-05):** In `graph-viewer/model-viewer/src/App.jsx`, find fetch calls to validation endpoints that currently lack error handling. Extend the existing `runsError` pattern from `ValidationRunsStrip.jsx`:

    - If `loadRunDetails` or similar fetch functions exist without try/catch + error state, add error handling following the pattern:
      ```javascript
      try {
        const res = await fetch(url);
        if (!res.ok) {
          const body = await res.json().catch(() => ({}));
          setError(body.hint || body.error || 'Request failed. Try again.');
          return;
        }
        // ... success path
      } catch (err) {
        setError('Could not reach server. Try again.');
      }
      ```
    - If error states already exist for all validation fetch paths (likely per Phase 5 work), just verify the pattern reads the structured `hint` field from error responses and displays it. If the current code reads `detail` (FastAPI default field), update to prefer `hint` falling back to `error` falling back to `detail`.

    **Scope note:** Only touch validation-related fetch calls in App.jsx. Do not modify Speckle viewer, graph viewer, or other fetch paths.
  </action>
  <verify>
    <automated>cd data-service ; python -m pytest tests/test_error_responses.py -v</automated>
  </verify>
  <acceptance_criteria>
    - data-service/tests/test_error_responses.py exists with at least 3 test functions
    - `python -m pytest tests/test_error_responses.py` exits 0
    - Each test asserts presence of `error`, `hint`, `code` keys in response JSON
    - App.jsx fetch error handling reads `hint` or `error` field from response body (grep confirms `body.hint` or `body.error` or `.hint` appears)
  </acceptance_criteria>
  <done>Pytest tests verify structured error bodies from validation endpoints. Model-viewer error handling reads structured error responses and displays actionable hints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → data-service | HTTP error responses cross trust boundary — must not leak internal stack traces |
| data-service → Browser | Structured error JSON consumed by frontend JavaScript |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | I (Information Disclosure) | _structured_error_response | mitigate | Error messages include user-facing hints only. Exception stack traces are NOT included in `error` or `hint` fields. For `PUBLISH_INTERNAL_ERROR`, use generic message without `repr(exc)`. Log full exception server-side. |
| T-06-04 | I (Information Disclosure) | App.jsx error display | accept | Frontend displays `hint` field which is controlled by the server. No raw exception text shown to user. |
</threat_model>

<verification>
- `python -m pytest data-service/tests/ -v` exits 0 (all existing + new tests pass)
- grep confirms `_structured_error_response` is called in at least 4 places in app.py
- App.jsx error handling reads structured fields from response body
</verification>

<success_criteria>
Data-service returns structured {error, hint, code} JSON for validation errors. Pytest tests verify the shape. Model-viewer reads hint/error from structured responses. No internal stack traces leak to the client.
</success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-hardening-and-verification/06-02-SUMMARY.md`
</output>
