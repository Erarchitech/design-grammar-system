---
phase: 07-ui-session-history-panel-neovis-knowledge-view
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - graph-viewer/index.html
autonomous: false
requirements: [INFR-04]

must_haves:
  truths:
    - "KnowledgeNote nodes render in teal (#4ecdc4) in the graph viewer when Specs&Notes tab is active"
    - "KnowledgeTag nodes render in yellow (#ffe66d), distinct from SWRL metagraph nodes"
    - "KnowledgeSession and KnowledgeClass nodes have distinct colors (purple, pink)"
    - "Graph View dropdown remains 2-option (OntoGraph / MetaGraph) and only visible in DesignRules tab"
  artifacts:
    - path: "graph-viewer/config.template.js"
      provides: "KnowledgeGraph node visual configuration"
      contains: "KnowledgeNote"
  key_links:
    - from: "graph-viewer/index.html (activeTab useEffect)"
      to: "buildCypher('KnowledgeGraph')"
      via: "auto-switch on Specs&Notes tab activation"
      pattern: "buildCypher.*KnowledgeGraph"
---

<objective>
Verify NeoVis KnowledgeGraph visual distinction and perform end-to-end human verification of the complete Session History panel.

Purpose: Confirm that KnowledgeGraph nodes are visually distinct in the graph viewer and that the entire Phase 7 UI works correctly end-to-end — session history panel, restore, filtering, accordion, and NeoVis colors.
Output: Verified Phase 7 UI passing all acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.1-phases/07-ui-session-history-panel-neovis-knowledge-view/07-CONTEXT.md
@.planning/milestones/v1.1-phases/07-ui-session-history-panel-neovis-knowledge-view/07-UI-SPEC.md
@.planning/milestones/v1.1-phases/07-ui-session-history-panel-neovis-knowledge-view/07-01-SUMMARY.md

<interfaces>
<!-- NeoVis config already has KnowledgeGraph colors (config.template.js lines 51-54) -->
```javascript
visGroups: {
  KnowledgeNote: { color: { background: "#4ecdc4", border: "#2fa89f" } },
  KnowledgeTag: { color: { background: "#ffe66d", border: "#d4bf3a" } },
  KnowledgeSession: { color: { background: "#a78bfa", border: "#7c5fcf" } },
  KnowledgeClass: { color: { background: "#f472b6", border: "#db2777" } }
}
```

<!-- Auto-switch behavior (index.html line 2770) -->
```javascript
if (activeTab === "specs-notes") {
  const q = buildCypher("KnowledgeGraph");
  setCypher(q);
  run(q);
}
```

<!-- Graph View dropdown only in DesignRules tab — per Phase 5 D-07 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NeoVis KnowledgeGraph config verification + automated checks</name>
  <files>graph-viewer/index.html, graph-viewer/config.template.js</files>
  <read_first>
    - graph-viewer/config.template.js (lines 29-54 — knowledge node labels + visGroup colors)
    - graph-viewer/index.html (lines 2770-2779 — activeTab useEffect with buildCypher KnowledgeGraph)
    - graph-viewer/index.html (lines 2896-2908 — tab toggle rendering)
  </read_first>
  <action>
Verify (do NOT modify unless broken) the following are present and correct:

1. **config.template.js labels** — confirm these entries exist:
   - `KnowledgeNote: { label: "title" }`
   - `KnowledgeTag: { label: "name" }`
   - `KnowledgeSession: { label: "mode" }`
   - `KnowledgeClass: { label: "label" }`

2. **config.template.js visGroups** — confirm these color entries exist:
   - `KnowledgeNote: { color: { background: "#4ecdc4", border: "#2fa89f" } }`
   - `KnowledgeTag: { color: { background: "#ffe66d", border: "#d4bf3a" } }`
   - `KnowledgeSession: { color: { background: "#a78bfa", border: "#7c5fcf" } }`
   - `KnowledgeClass: { color: { background: "#f472b6", border: "#db2777" } }`

3. **index.html auto-switch** — confirm `buildCypher("KnowledgeGraph")` is called when `activeTab === "specs-notes"`.

4. **index.html Graph View dropdown** — confirm the dropdown with OntoGraph/MetaGraph options is rendered inside the DesignRules tab block (`activeTab === "design-rules"`), NOT in the Specs&Notes tab block.

If all 4 checks pass: report PASS, no code changes needed. If any check fails: fix the specific issue.
  </action>
  <verify>
    <automated>findstr /C:"KnowledgeNote" graph-viewer\config.template.js</automated>
    <automated>findstr /C:"#4ecdc4" graph-viewer\config.template.js</automated>
    <automated>findstr /C:"KnowledgeGraph" graph-viewer\index.html</automated>
  </verify>
  <acceptance_criteria>
    - config.template.js contains `KnowledgeNote: { label: "title" }`
    - config.template.js contains `KnowledgeTag: { label: "name" }`
    - config.template.js contains `KnowledgeSession: { label: "mode" }`
    - config.template.js contains `KnowledgeClass: { label: "label" }`
    - config.template.js contains `#4ecdc4` (KnowledgeNote teal)
    - config.template.js contains `#ffe66d` (KnowledgeTag yellow)
    - config.template.js contains `#a78bfa` (KnowledgeSession purple)
    - config.template.js contains `#f472b6` (KnowledgeClass pink)
    - index.html contains `buildCypher("KnowledgeGraph")` in the `activeTab === "specs-notes"` useEffect
    - Graph View dropdown (select with OntoGraph/MetaGraph) is NOT inside the `activeTab === "specs-notes"` block
  </acceptance_criteria>
  <done>NeoVis configuration verified: KnowledgeGraph node labels, colors, auto-switch behavior, and Graph View dropdown scoping all correct per D-12, D-13, D-14.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of Session History panel + NeoVis Knowledge View</name>
  <what-built>
Complete Session History collapsible panel in Specs&Notes sidebar with:
- Collapsible header showing "Session History (N)" count badge and mode filter dropdown
- Compact session rows with mode badges (INS/QRY/UPD), truncated prompts, relative dates, Restore buttons
- Accordion expand/collapse on row click showing full prompt, result, and timestamp
- Restore button that sets prompt textarea, response area, and mode dropdown from session data
- "Show more" button for paginated display (50 at a time)
- Empty states for no sessions and no filter matches
- NeoVis KnowledgeGraph nodes rendered with distinct colors (teal, yellow, purple, pink)
  </what-built>
  <how-to-verify>
**Prerequisites:** Rebuild and deploy the UI:
```bash
docker compose build --no-cache design-grammars && docker compose up -d design-grammars
```
Open http://localhost:8080 in incognito (Ctrl+Shift+N), log in, open an existing project.

**Session History Panel (D-01 through D-11):**
1. Click the "Specs&Notes" tab in the sidebar
2. Verify a collapsed "Session History" header bar appears below the mode-specific panels
3. Verify the header shows a count badge like "Session History (N)" and a mode filter dropdown
4. Click the header bar — verify the panel expands showing session entries
5. Verify each session row shows: colored mode badge (INS teal / QRY cyan / UPD amber), truncated prompt text, relative date (e.g., "2h ago"), and a "Restore" button
6. Click a session row — verify it expands inline showing full prompt, full result, and timestamp
7. Click the same row again — verify it collapses
8. Click the mode filter dropdown — select "Insert" — verify only insert sessions are shown
9. Select "All modes" — verify all sessions reappear
10. Click "Restore" on a session — verify the prompt textarea fills with the session's prompt, the response area fills with the session's result, and the mode dropdown switches to match

**NeoVis Knowledge View (D-12 through D-14):**
11. With Specs&Notes tab active, verify the graph viewport shows KnowledgeGraph nodes
12. Verify KnowledgeNote nodes are teal (#4ecdc4) and KnowledgeTag nodes are yellow (#ffe66d)
13. Switch to DesignRules tab — verify Graph View dropdown (OntoGraph/MetaGraph) appears
14. Switch back to Specs&Notes — verify Graph View dropdown is NOT visible

**Persistence (HSTY-03):**
15. Clear browser data (localStorage) and hard-refresh — verify session history still loads (data from Neo4j, not localStorage)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NeoVis → browser | Graph visualization renders node data from Neo4j |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | NeoVis graph view | accept | KnowledgeGraph data is project-scoped via Cypher `WHERE project = '{name}'`; same isolation as existing SWRL nodes |
</threat_model>

<verification>
1. All NeoVis config entries for KnowledgeGraph nodes present and correct
2. Auto-switch to KnowledgeGraph Cypher works on Specs&Notes tab activation
3. Graph View dropdown scoped to DesignRules tab only
4. Human verification of full Session History panel functionality
</verification>

<success_criteria>
NeoVis configuration verified. Human approves end-to-end Session History panel: collapsible header, compact rows, accordion expand, Restore, filter, empty states, "Show more", and KnowledgeGraph visual distinction.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.1-phases/07-ui-session-history-panel-neovis-knowledge-view/07-02-SUMMARY.md`
</output>
